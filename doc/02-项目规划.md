# 星序 Astra — 项目规划

> **文档梗概**：作为当前设计、任务原件、认领、版本分配、项目对接和状态的唯一权威来源；已完成事实进入 [`03-开发历史.md`](03-开发历史.md)。
>
> **具体规划法则**：
>
> 1. 所有开发、配置、文档、资源和验收工作必须先建立唯一任务卡；只有“已完成”使用 `[x]`，完成任务及时迁入 `03`。
> 2. 主开发集中分配小版本并独占 `主开发` 根工作区；`main` 只接收已收束的稳定集成，写入型长期小组只使用登记的隔离 worktree。
> 3. 当前完成目标是可一键启动、三角色协同、代表课程丰富且可复验的本地展示版；生产化能力保留为后置工程，不再成为 V8.0 展示版硬门槛。
> 4. 提交标题遵守 `.codex/AGENTS.md` 的 `Vx.y.z GROUP type(scope): 中文说明（TASK-ID）`；`review` 当前冻结。
>
> **最后一次更新时间**：2026-07-31
>
> **更新者**：PM组

---

## 0. 全局控制规则

- `main@V7.5.11` 是公开稳定线；`主开发` 当前集成线包含 V7.7.4 控制面、FE-012 首批共享证据与历次返修、V7.7.15 / CONTENT-001 六门代表课程内容契约、V7.8.0 / BE-007 三角色最小接口、V7.8.2 / BE-011 学生精确单元访问处置、V7.8.4 / BE-013 教师六课有限事实投影、V7.8.5 / FE-013 教师协同工作台、V7.9.4 / UI-003 管理员六领域治理、V7.9.7 / DATA-001 本地三角色演示初始化器、V7.9.12 / DATA-002 单课程教师演示补样、V7.9.19 / DATA-003 六课 producer profile 及 V7.7.25 / TOOLS-002 strict-readonly 启动能力。QA-014 的补充证据已把 Zoom 保持打开的移动→桌面 P2 退回 FE-012；V7.7.30 已由 `30f7f189` 精确修复并进入当前集成树。V7.7.33 的外部 Chrome 产品观察 finding=0，双向跨断点、尺寸 / DPR、44px / 焦点、三轮 DOM / Canvas 单例行为、非目标页和 console 均通过，但 reduced-motion 仿真与 main-realm owner 数字取证因 external extension 能力缺失而 mandatory NOT-RUN，故 A03a 未获 PASS、同类复验暂缓。QA-013 / V7.8.8 已对产品 `848c1ed` 运行态 RETURN FE-013，P0=0、P1=1、P2=0、P3=0：同一 accepted attachment edge 恰一次选择却稳定产生 4 组 progress + aggregate；双视口数据恢复、清场、hidden、键盘、console 与 SQLite 终账通过。四层路由、18 门 Future 课程页、Future A03b 与 FE-013 最小返修 / 复验均未完成，当前真实稳定实现只认 01。
- `astra-local.ps1 + SQLite + 127.0.0.1:9001` 是当前交付目标。真实 MySQL、公网域名/TLS、隔离 runner、压力测试、四周课堂试点和生产合规复核均为展示后工程。
- 登录、服务端权限、写入对账、迁移可恢复和学生源码不进入 API/宿主执行仍是基础正确性，不得以“展示优先”为由绕过。
- 原预留的 `V7.6.6 / FE-012`、`V7.6.7 / QA-014`、`V7.6.11 / FE-012` 与 `V7.6.12 / QA-014 A03` 均已取消且不得复用。V7.7 已用或唯一预留保持现状，`.31—33` 只属于 `QA-014 A03a`。V7.8.0 / BE-007、V7.8.1 / UX-003、V7.8.2 / BE-011、V7.8.3 / CONTENT-004、V7.8.4 / BE-013、V7.8.5 / FE-013、V7.8.6 / QA-013、V7.8.7 / FE-013 挂接竞态返修、V7.8.8 / QA-013 教师返修复验、V7.8.9 / FE-013 自主轮询返修与 V7.9.0—54 的既有分配或使用均不得复用。V7.9.30—33 与 V7.9.36—38 属于 Zoom QA 控制，V7.9.34—35、V7.9.39—43 属于教师 attachment / 请求风暴返修控制，V7.9.44 只属于 PM-016，V7.9.45—47 只属于 PM-017，V7.9.48 只属于 FE-017 最终产品提交，V7.9.49 只属于 PM-009 的 Future lane 计划登记，V7.9.50 只属于 PM-009 的 CONTENT-003 来源校准树回填，V7.9.51 已由 CONTENT-003 来源校准使用，V7.9.52 只属于 PM-009 的共享壳冻结、内容阻断与三 lane 实态登记，V7.9.53 只属于 PM-009 的组合复审返修边界校准，V7.9.54 只属于 PM-009 的数值返修与复审阻断记录。BE-007 私有候选 `69df127` 永久禁止集成；已集成专业候选清单保持不变。
- 根工作区同一时间只有主开发写入；历史 `qianduan` 实验只由 `archive/qianduan-wip-before-main-sync-20260727` 标签恢复，重复 worktree 已在 V7.6.9 删除。

---

## 目录

1. [当前周期](#1-当前周期)
2. [项目对接登记](#2-项目对接登记)
3. [正式任务原件](#3-正式任务原件)
4. [调度与依赖](#4-调度与依赖)
5. [验收与风险](#5-验收与风险)
6. [需要用户提供或确认](#6-需要用户提供或确认)

---

## 1. 当前周期

### 1.1 当前规划边界

1. `01` 只保留当前实现、真实入口、目录职责、操作与维护边界；规划能力只作明确标注并链接到 `02`，逐版本过程只链接到 `03`。
2. `02` 保留当前产品目标、未完成 / 暂缓任务、有效依赖、风险和需要用户确认的事项；完成任务从本文件清理。
3. `03` 保留全部已发生提交及 QA 后验结论；历史事实不得因范围重定而删除。
4. [`21-V7.5课程体验与教师编排设计.md`](02-子文档/21-V7.5课程体验与教师编排设计.md) 记录当前课程设计；[`22-V8最终完成路线与验收蓝图.md`](02-子文档/22-V8最终完成路线与验收蓝图.md) 定义展示版终态；[`23-V7.6产品定位与星序主路径契约.md`](02-子文档/23-V7.6产品定位与星序主路径契约.md) 冻结角色与唯一入口；[`24-V7.6全栈模块边界契约.md`](02-子文档/24-V7.6全栈模块边界契约.md) 冻结模块依赖；[`25-V7.7未来星系18课程内容与路由冻结矩阵.md`](02-子文档/25-V7.7未来星系18课程内容与路由冻结矩阵.md) 冻结六方向 / 18 课程的内容、事实和 canonical 路由；[`26-V7.7未来星系页面视觉规格.md`](02-子文档/26-V7.7未来星系页面视觉规格.md) 提出待确认的主页、目录、课程、导航、页脚与双视口视觉规格；[`27-V7.7三星系六门代表课程教学与证据冻结契约.md`](02-子文档/27-V7.7三星系六门代表课程教学与证据冻结契约.md) 冻结三星系六门代表课、事实边界、证据规则与最终演示顺序；[`28-V7.8教师自然工作流与证据回读冻结契约.md`](02-子文档/28-V7.8教师自然工作流与证据回读冻结契约.md) 冻结教师安排、证据核对、反馈与学生状态预览；[`29-V7.9本地演示数据与初始化冻结契约.md`](02-子文档/29-V7.9本地演示数据与初始化冻结契约.md) 冻结三角色账号、14 Course / 42 Unit、六课演示事实和幂等初始化；[`51-V7.9管理员治理工作流冻结契约.md`](05-子文档/51-V7.9管理员治理工作流冻结契约.md) 冻结管理员业务导航、课程状态治理、冲突对账与审计回读。

### 1.2 当前稳定基线

经用户确认，当前产品目标固定为：

1. 星序是可继续增加新学习星系的互动教学平台；当前学习单元只有工科试验室、代码空间和未来星系，`#planets` 是登录后的角色与任务总览，不是第四个学习星系。
2. 学生主路径是“目标 / 预测 → 操作 / 运行 → 反馈 → 修正 → 解释 / 提交”，不是被动参阅资料。只有满足版本化完成规则的服务端事实才可写成完成，目录数量和访问量都不代表教学质量。
3. 教师当前负责既有课程的班级范围、开放 / 锁定 / 隐藏、分块节奏、作业、进度、代码提交与反馈；通用低代码互动课程工厂不属于当前完成条件。
4. 管理员通过星序统一治理组织、人员、班级、课程状态和审计；可视化用于理解与受约束操作，不开放任意 SQL、原始表或无确认写库。
5. 产品借鉴综合教学平台的课程、班级和教学协作结构，但不宣称“全面替代”或“全面优于”某一成熟平台；当前差异化集中在可视化实验、代码追踪、跨学科互动和三角色对同一教学事实的闭环。
6. 学生 AI 助教、教师 AI 教学助手、AI 维护 / 管理助手属于展示后条件路线。当前无 AI provider、对话入口或写入链，运行能力继续标为 `unavailable`；恢复条件与人工确认边界见第 3.5 节。

| 范围 | 当前状态 | 下一缺口 |
| --- | --- | --- |
| 页面层级 | `#planets` 是星序总览；`#home` 是工科试验室首页；代码空间是 `codevis/` 子站；未来星系是主站课程目录。角色工作台都属于 `astra` 身份层 | FE-012 将角色任务和权威续学接入星序总览 |
| 后端学习事实 | Alembic 0051、版本化完成规则、幂等事件、投影、恢复、教师聚合和纠错原语已通过 QA-014 A02 | 三个学习单元客户端、课程规则实例与教师产品视图接线 |
| 三角色 | Cookie Session、越权拒绝、学生 / 教师 / 管理员工作台、课程发布、提交和受约束组织治理已存在 | 用一套本地演示数据把学生写入、教师查看和管理员治理串成完整脚本 |
| 课程体验 | 工科试验室 88 实验，代码空间和未来星系各 6 组 / 18 活动 | 优先精修每个星系至少两条代表路径，不把目录数量当课程质量 |
| 本地交付 | 9001 同源、SQLite、Alembic、一键启动和本地管理员引导已存在 | 固化演示数据 / 账号说明并完成全新目录复现 |
| AI 路线 | 当前没有 AI provider、AI 对话入口或 AI 写入链；未来星系的“数据科学与 AI”只是课程分类 | V8.0 展示候选完成后，按 AI-001—AI-003 的恢复条件逐项决定是否实施 |
| 后置工程 | MySQL 代码路径、目标发布门禁、runner 契约和运维工具保留 | 不阻塞展示版；恢复条件见第 3.5 节 |

### 1.3 V8 展示版完成路线

| 周期 | 交付重点 | 退出判断 |
| --- | --- | --- |
| V7.6 | 范围、产品构型与仓库瘦身已确认；继续三星系权威学习证据接入 | 文档 / 代码一致；代表路径可恢复、可对账；V7.6 独立 QA 通过 |
| V7.7 | 三星系代表课程内容与互动深化；未来星系恢复“方向目录 → 独立课程”层级 | 每星系至少两条完整的目标—预测—操作—反馈—解释路径；未来星系不再以方向页内横向标签代替课程目录 |
| V7.8 | 教师发布节奏、进度、提交和反馈协同 | 教师能从星序完成一次本地“安排—学习—查看—反馈”闭环 |
| V7.9 | 管理治理、演示数据、响应式与整体打磨 | 管理员能完成组织 / 人员 / 班级 / 课程调整并看到审计回读 |
| V8.0 | 一键启动、三角色串联、文档和候选封装 | 全新本地数据目录可复现完整演示；自动化、真实浏览器、SQLite 账本和 Git 一致 |

完整问题映射与证据包见 [`22-V8最终完成路线与验收蓝图.md`](02-子文档/22-V8最终完成路线与验收蓝图.md)。生产上线不是 V8.0 展示版的同义词。

用户要求从当前恢复点到 V8.0 约用四次更迭、总开发时间控制在约 8 小时。该时间是调度预算，不替代任务完成与 QA 证据；主开发按以下四轮合并原有版本路线，并通过非重叠责任域并行推进：

| 更迭 | 约束预算 | 并行主责 | 本轮可观察出口 |
| --- | --- | --- | --- |
| 第 1 轮 | 约 2 小时 | PM、CONTENT、FE 候选审查 | 恢复控制面；冻结六方向 / 18 课程矩阵、路由与完整视觉规格；FE-012 候选形成可提交或明确退回结论 |
| 第 2 轮 | 约 2 小时 | FE、CONTENT；QA 在精确候选后进入 | 完成未来星系四层结构、18 门独立深链课程、教学闭环和学习证据接线；V7.7 精确候选通过专项 QA |
| 第 3 轮 | 约 2 小时 | FE、DATA、UI；BE 仅在缺口被证明后进入 | 教师协同、管理治理、演示数据和三角色权威回读形成一条可重复本地脚本 |
| 第 4 轮 | 约 2 小时 | QA、REL、PM | 全新 SQLite 目录、9001 一键启动、桌面 / 390×844、自动化、浏览器、账本、文档和 Git 在 V8.0 唯一候选上闭环 |

### 1.4 已授权实施的未来星系重构契约

用户已于 2026-07-28 明确恢复开发并授权主开发依本节建立任务、恢复项目对接和调度工作组。产品代码仍须等待对应 CONTENT / FE 任务达到写入门禁，不得用本次授权绕过任务卡、视觉规格确认、版本令牌或 QA。

#### 1.4.1 当前问题

1. 当前方向页同时承担“方向目录”和“具体课程页”两种职责；以人文页为例，`语境地图 / 视角切换 / 主张审阅` 被横向标签压在同一页面内，学生无法形成清楚的方向—课程层级。
2. 当前具体课程页的标题区、目标区、活动切换和双栏学习区没有遵循工科试验室已经形成的模板化页面骨架，导致学习单元之间的课程进入方式和阅读顺序不一致。
3. 主要观察 / 操作区内容密度与布局约束不足；在用户本次观察的约 `1015×898` 视口中，柱形文本、关系节点和说明发生遮挡，视觉表现也过于简陋，已经妨碍课程判断而不只是影响装饰效果。
4. 以上问题适用于未来星系六个方向及其全部课程入口，不得只修复人文页或只用局部 CSS 遮掩。

#### 1.4.2 用户确认的目标层级

```text
未来星系主页面
└── 六个方向目录页
    └── 各方向的独立课程入口
        └── 具体课程页（目标、内容 / 实验、反馈与完成动作）
```

- 方向页只承担该方向的课程发现、课程状态和进入动作，不得继续把三门活动作为同一课程页内的二级标签。
- 每门实际课程必须使用独立路由 / 可恢复入口，并严格遵循工科试验室的模板化页面层级、主要区块和课程进入方式。
- 当前六方向 / 18 活动全部一对一保留并升级为 18 门独立课程；不得借重构合并、替换或下线其中任一门。
- 未来星系保留自身课程素材、色彩与主题表达；统一范围包括课程页面模块、导航逻辑和页脚逻辑，不直接复制工科试验室的视觉皮肤，也不得用主题差异改变信息架构、主次关系或可读性门禁。
- 主要展示与交互区必须在桌面和 `390×844` 下无文字遮挡、无根级横向溢出；图形、Canvas、Three 或动画只有在帮助观察、比较、判断和反馈时才使用。

#### 1.4.3 已冻结的实现选择

1. **课程保留**：六方向 / 18 活动全部一对一保留为独立课程。
2. **深链兼容**：由主开发选择兼容重定向。既有 `#<direction>/<activity-slug>` 深链继续可达，并规范化到新课程入口；旧方向空 hash 进入该方向目录，不再静默落到第一门课程。未知课程 slug 显示明确不可用 / 返回目录，不得静默替换为另一门课程。
3. **模板边界**：复用工科试验室的课程页面模块、进入节奏、导航逻辑和页脚逻辑；未来星系继续拥有独立品牌、色彩、内容视觉和 `fg-*` 命名空间。完整视觉规格与概念图须先由主开发确认，再允许 FE 写页面。
4. **FE-012 候选**：继续审查并择取整合，不归档放弃。受保护 detached 工作树永久保持不可写的保全来源；活动 FE 组只能只读审查，并把主开发批准的通用学习证据、角色首页和三星系接线选择性重放到独立 FE 工作树，再让未来星系结构任务消费稳定接口。不得在原候选上继续开发，也不得把 54 条脏路径整体冒充稳定实现。
5. **执行顺序**：CONTENT 冻结 18 课程矩阵与路由语义、FE 同期审查 FE-012 候选；结构 / 视觉规格确认后，FE 串行完成 FE-012 收束与未来星系实现，避免两个写入者同时修改 `frontier-learning.js`、Router、注册表和入口文件。
6. **展示口径**：不为 V7.7 单独准备用户演示；主开发按三个星系各至少两条代表路径选择内部验收样本，用户只验收最终 V8.0 整体成果。

V7.5 现有实现仍按 [`21-V7.5课程体验与教师编排设计.md`](02-子文档/21-V7.5课程体验与教师编排设计.md) 和 [`08-前端页面实现索引.md`](08-前端页面实现索引.md) 记载；本节是下一轮实现的权威契约，不能提前写入 `01` 作为稳定事实。

## 2. 项目对接登记

- **项目对接模式**：停用（长期工作组保持解散；当前只由主开发在根工作区单写推进）
- **当前协作状态**：用户已授权约四次更迭 / 8 小时并行调度，并要求主控在 26 号视觉规格下持续自主推进。CONTENT-003 / V7.9.51 已集成，Future 内容门禁解除。FE-017 的 R7 返修与 R8 数值精度微修已由原 FE 在 `f017@4ea1643` 完成，当前精确现场为 36 tracked modified + 22 untracked、0 staged；30 个 lane 文件零漂移，R8 只改两层 evidence 数值校验和既有组合合同。完整 npm、47/47 合同、22 个 untracked 语法、文档 / handoff / architecture / diff 均通过，候选仍未提交。R7 独立复审 B 已 `P0=P1=P2=P3=0`；R8 独立数值复审在返回结论前被平台误判为“possible cybersecurity risk”并终止。依 GLOBAL NO-APPROVAL 不重试、不换审查者或工具，复审→浏览器链暂缓；浏览器矩阵只读准备已完成，但服务与外部 Edge 均未启动。UI-003 / V7.9.60 已由原 UI owner 在 `u358` 形成 clean 专业提交 `3734a2f`，但正式服务启动被平台在 CreateProcess 前拒绝，故暂不集成且 Browser 未续跑。FE-013 / V7.8.9 的 fr89 两文件现场保持 2 modified、0 staged、0 untracked；新一轮只读门禁通过两份语法、目标动态合同、14 / 15 相关合同、218 JS、43 后端、docs / handoff / diff，并揭示唯一 P1：旧能力账本和冻结回归仍强制 4 秒轮询。V7.9.62 只扩展同一任务的能力真值与回归合同边界，原 FE owner 可在同一 fr89 续接；若平台阻断重现立即原地暂缓。V7.7.33 及其他暂停或关闭现场不变。
- **V7.9.66 平台写入冻结**：平台权限在 FE-013 第二轮返修与 UX-001 首轮复审后收紧为仅允许根工作区和临时目录写入；已登记在 `C:/Users/niu-h/.codex/worktrees/**` 下的专业 worktree 只能只读。主开发只读核实 `fr89@197bada` 为第 2.11 节精确 7 个 tracked modified、0 staged、0 untracked，`ux64@12aeef9` 为第 2.18 节 4 个 tracked modified + 1 个 untracked、0 staged；两者都没有提交。依 GLOBAL NO-APPROVAL 不申请权限、不重试、不换工具 / 目录、不移动工作树、不复制差异到根工作区；受影响写入、测试补强、暂存与提交立即暂缓。平台明确恢复原登记物理目录前，现有差异只能保全，不能写成稳定实现。
- **2026-07-31 暂停覆盖**：用户明确暂停长期目标，并要求先解散其他工作组。CONTENT、FE、QA、BE、UI、DATA 六个长期协作任务已取消置顶并归档；其余专业 owner 不再运行。自本条起，下表或正式任务原件中仍写“活跃 / 进行中 / 已认领”的未完成项统一解释为“用户暂停、只读保全”，直到用户再次明确恢复；不得据旧状态自动写入、测试、提交、启动服务或创建工作组。
- **2026-07-31 单任务恢复覆盖**：用户随后要求继续正常启动并按浏览器 13 条批注直接修改。只恢复主开发在根工作区对 `FE-020 / V7.9.68` 的单写权限，不恢复、重建或复用任何长期工作组、专业 worktree 或冻结候选；第 2.19 节和正式任务原件是本轮唯一写入边界。
- **2026-07-31 第二轮单任务覆盖**：FE-020 完成后，用户又以 10 条浏览器批注要求修复学生工作台弹性布局、可折叠侧栏、首要任务进度、授权导航 / 搜索、学科概览与 Hero 动画。只恢复主开发在根工作区对 `FE-021 / V7.9.69` 的单写；双范围测试班级与账号在 FE-021 提交后另建唯一 DATA 任务并通过正式 API 创建，不混入前端提交。
- **安全退场现场**：主开发只读确认根工作区仍 clean。首次对 clean 且已集成的 `c003` 执行无 `--force` 的 `git worktree remove` 时，物理目录已删除，但 Git 返回 `error: failed to delete '.git/worktrees/工科实验室25': Invalid argument`，registry 现标记 `prunable gitdir file points to non-existent location`；依 GLOBAL NO-APPROVAL 未重试、未 `prune`、未手工删除 `.git` 元数据，也未继续批量移除其他工作树。系统 TEMP 中仅按父目录和 `astra-*` 名称校验后尝试清理；成功删除 5 个旧目录，随后在 `C:/Users/niu-h/AppData/Local/Temp/astra-content-init-9vagh0s4` 遇到 `UnauthorizedAccessException` 并停止，剩余 158 个 `astra-*` 目录原样保留。受保护 `a02c`、所有脏候选与未验收候选均未触碰。
- **分支集成对账**：本地 32 条 `codex/*` 分支中 7 条已是 `主开发` 祖先，31 条的 tip 标题已经在 `主开发` 存在同标题稳定提交；这些专业任务已按既有语义集成记录收束，不再制造冗余 merge commit。唯一没有主开发同标题提交的是 `codex/ui-v7.9-admin-resource-generation @ 3734a2f`，它因正式 9001 / Browser 门禁未完成继续禁止集成。`f017*`、`fr89`、`ux64` 的未提交脏现场和受保护 `a02c` 同样不得合并、清理或冒充稳定实现。
- **主控任务标题**：星序 Astra｜主开发｜总控。
- **版本分配者**：主开发。
- **当前版本轨道**：V7.7.33 已按固定标题回执 `BLOCKED / NOT-RUN`；V7.8.8 已按固定标题回执 `RETURN FE-013`；V7.8.9 固定标题为 `V7.8.9 FE fix(teacher): 移除证据自主轮询（FE-013）`，当前未形成候选提交。V7.9.44—47 保持既有 PM 分配；V7.9.48 固定为 `V7.9.48 FE feat(frontier): 重构未来星系课程体验（FE-017）`；V7.9.49—57 与既有分配保持不变。V7.9.58 因真实 Browser 运行门禁失败永久留空且不得复用；V7.9.59 已用于入口扩展控制；V7.9.60 专业提交 `3734a2f` 已形成但因平台阻断暂不集成；V7.9.61 已记录该阻断；V7.9.62 已用于 FE-013 真值边界控制。V7.9.63 固定为 `V7.9.63 PM docs(ux): 登记星序焦点连续性返修包（PM-009）`，V7.9.64 只用于回填 UX worktree，V7.9.65 固定为 `V7.9.65 UX fix(focus): 保持星序范围选择焦点（UX-001）`。主开发在分配前已对本地 history / body、refs / tags、全部 worktree、tracked text与实时 origin refs / tags 查重，V7.9.63—65 均零占用。V7.8.0—9 与 V7.9.0—65 的已完成、分配、退回、阻断或预留版本均不得复用，`BE-012` 永久不分配。随后按 `V7.7 → V7.8 / V7.9 → V8.0` 的四轮出口推进。
- **V7.9.66 唯一用途**：本地 history / body、refs / tags、tracked text、全部 worktree及本轮权限切换前最后一次实时 origin refs / tags 均确认 V7.9.66 零占用；现唯一分配给 `V7.9.66 PM docs(platform): 记录专业工作树写入冻结（PM-009）`。该版本只同步 `.codex/AGENTS.md`、02、03，不修改产品 / 合同 / 专业文档，不完成或替代 V7.8.9、V7.9.48、V7.9.60、V7.9.65。
- **V7.9.67 唯一用途**：本地 history / body、refs / tags 与 tracked text 均确认 V7.9.67 零占用；现唯一分配给 `V7.9.67 PM chore(team): 暂停目标并停用协作组（PM-009）`。该版本只同步 `.codex/AGENTS.md`、02、03 与已发生的退场事实，不修改产品、测试、合同或专业文档，不完成、合并或替代任何未验收候选。
- **V7.9.68 唯一用途**：本地 history / body、refs / tags 与 tracked text 均确认 `FE-020` 和 V7.9.68 分配前零占用；现唯一分配给 `V7.9.68 FE feat(student-flow): 收束入班与课程发现体验（FE-020）`。该版本只消费当前 `主开发@d3772f1` 稳定树和用户 13 条浏览器批注，不进入、复制、提交、合并或清理 `f017*`、`fr89`、`ux64`、`u358`、其他冻结候选或受保护 `a02c`。
- **V7.9.69 唯一用途**：本地 history / body、refs / tags 与 tracked text确认 `FE-021` 和 V7.9.69 分配前零占用；现唯一分配给 `V7.9.69 FE fix(student-ui): 收束学生工作台与授权发现（FE-021）`。该版本只消费 `主开发@5dbea93` 和用户本轮 10 条批注，不修改 backend / 数据库，不恢复任何工作组或冻结候选。
- **集成分支**：`主开发`；`main` 保持稳定发布，`houduan` 保留下一轮后端开发起点，`review` 冻结。
- **提交责任组字段**：启用
- **长期团队策略**：主开发独占根工作区与共享 `02/03`；CONTENT 独占课程事实与冻结契约，FE 独占学生 / 教师 / Future 前端，QA 独立只读验收。CONTENT-003 已完成，c003 只读保留。Future 的共享 shell、三条 lane 与 R8 微修均已回到 f017 单一候选；f017a / f017b / f017c 全部冻结，不再并行写入。复审平台阻断期间，任何组都不得复制 f017 差异、换审查者或提前启动浏览器；标准 FE 保持唯一产品 owner，待阻断安全解除后继续同一 V7.9.48，不创建重复任务或版本。V7.8.9 继续由 fr89 的原 FE owner 独占，V7.9.62 后只开放第 2.11 节 7 文件；其他专业树与外部证据只读保留。仍不恢复 MySQL、生产发布、安全平台或通用课程系统。
- **离线协作阻断规则**：用户离线期间，全部当前及后续小组不再向用户发起权限 / 审批申请；既有任务授权与系统安全边界内直接推进。平台强制审批、权限不足或安全规则阻断时，立即停止受影响动作，不绕过、不反复申请、不扩大范围；保留精确错误、路径、日志、进度与现场，向主开发回报并把受影响任务标记为暂缓，不等待用户在线。该规则适用于 PM、CONTENT、FE、BE、DATA、UI、QA 与后续小组，不扩张原任务授权。

| 小组 | 代号 | 长期任务标题 | 责任边界 | Git 模式 | 分支/工作树 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- |
| 主开发 | PM | 星序 Astra｜主开发｜总控 | 产品、版本、共享文档、Git 集成与验收调度 | root | `主开发` / 根工作区 | 活跃 |
| 内容组 | CONTENT | 星序 Astra｜内容组｜长期对接 | 六方向 / 18 课程矩阵、教学闭环、事实、有限证据标签与权威依据；不写运行代码 | 独立 worktree | `C:/Users/niu-h/.codex/worktrees/c003/工科实验室` / `codex/content-v7.9-frontier-source-calibration @ 0ce127a5`；CONTENT-003 候选 clean、已集成 | 暂停 |
| 前端组 | FE | 星序 Astra｜前端组｜长期对接 | FE-017 唯一负责 Future；FE-013 原 owner 唯一负责教师自主轮询返修；共享 02/03 仍只由 PM 写 | 隔离 worktree | `f017@4ea1643`、`fr89@197bada` 与三 lane 只读保全；a02c 永久禁入 | 平台只允许根工作区写入，FE-013 / FE-017 均暂缓 |
| UX 组 | UX | 星序 Astra｜UX组｜焦点连续性 | UX-001 首片只负责星序范围选择 replacement focus 与动态合同；不写登录、课程、教师、管理员、Future 或共享 02/03 | 独立 worktree | `ux64@12aeef9` 保留 4 tracked modified + 1 untracked、0 staged 的 P2 返修现场 | 平台只允许根工作区写入，V7.9.65 暂缓 |
| QA 组 | QA | 星序 Astra｜QA组｜长期对接 | 对精确集成树执行只读自动化、正式 9001 / SQLite、外部浏览器、响应式、控制台与账本验收 | 只读 worktree | `qz33@73c034e` 已因两项 external extension 强制取证能力 BLOCKED 并冻结；`qt88@9861093` 已 RETURN FE-013 并冻结，两树均 clean、无 `.venv`；等待新精确候选 | 活跃 |
| 工具组 | TOOLS | 星序 Astra｜工具组｜长期对接 | TOOLS-002 已完成 astra-local 仓库外 Python / venv 选择、只读依赖验证与启动合同；不写产品前后端 | 独立 worktree | `C:/Users/niu-h/.codex/worktrees/6f2a/工科实验室` / `codex/tools-v7.7-readonly-local-start @ ba3d02c4` clean；最终独立复审 P0—P3=0，候选由本提交语义集成 | 暂停 |
| 后端组 | BE | 星序 Astra｜后端组｜长期对接 | BE-013 已完成教师六课有限事实安全投影；不修改 endpoint/schema/router/model/migration，不制造 producer | 独立 worktree | `C:/Users/niu-h/.codex/worktrees/be13/工科实验室` / `codex/be-v7.8-teacher-evidence-facts @ 47ac53a9` clean；候选已集成、写入关闭 | 暂停 |
| UI 组 | UI | 星序 Astra｜UI组｜长期对接 | UI-003 管理员六领域、课程治理、审计、懒加载、响应式和可访问性；不写后端、教师或 Future | 独立 worktree | 旧 `7a78@70c6380`、`u356@10250bbb` 只读保留；`u358 / codex/ui-v7.9-admin-resource-generation @ 3734a2f` clean，V7.9.60 独立代码复审 P0—P3=0 | 正式 9001 启动被平台策略拦截，V7.9.60 暂不集成；不重试、不换工具 / 目录、不清理 |
| 数据组 | DATA | 星序 Astra｜数据组｜长期对接 | DATA-003 已完成 physics / control-flow 精确 producer profile 与四课诚实 fallback；不写 `backend/app/**` 或前端 | 独立 worktree | `C:/Users/niu-h/.codex/worktrees/da03/工科实验室` / `codex/data-v7.9-teacher-evidence-producers @ 31722c8` clean；候选已集成，写入关闭 | 暂停 |

### 2.1 工作树登记

| 所有者 | 当前任务 | 分支 / 工作树 | 允许写入 | 当前状态 | 清理门禁 |
| --- | --- | --- | --- | --- | --- |
| 主开发 | `PM-009` V8 总控与 QA 调度 | `主开发` / 根工作区 | 独占共享 `.codex/AGENTS.md`、02、03、版本对账与专业候选集成 | 当前控制点包含 V7.8.7 集成、V7.7.33 平台能力阻断分类及 V7.8.8 P1 退回；根工作区保持单一写入者 | 每次提交后保持干净；专业组 / QA 使用登记的独立工作树 |
| 前端组 | `FE-017 / V7.9.48` 未来星系课程体验重构 | `C:/Users/niu-h/.codex/worktrees/f017/工科实验室` / `codex/fe-v7.9-future-galaxy @ 4ea1643` | 按第 2.14 节精确 allowlist；不得写共享 02/03、FE-013、backend、角色页、其他 worktree 或 a02c | R8 候选 36 tracked modified + 22 untracked、0 staged；tracked / untracked / combined 指纹为 `90a61c22… / 4ace8831… / 9198eb1c…`；全量静态门禁 PASS，未提交 | 独立数值复审被平台安全误判中止；依 GLOBAL NO-APPROVAL 暂缓复审→浏览器链，不重试、不换审查者、不 stage / commit / merge / push / rebase / 清理 |
| UX 组 | `UX-001 / V7.9.65` 星序范围焦点连续性 | `C:/Users/niu-h/.codex/worktrees/ux64/工科实验室` / `codex/ux-v7.9-shared-accessibility @ 12aeef96fae358eecc123a6a6d9f2285cb1cd5ff` | 只允许第 2.18 节 5 文件；共享 02/03、app-session、课程、教师、管理员、Future、backend 与其他 worktree 均冻结 | 首轮专业候选为 4 tracked modified + 1 untracked、0 staged；npm 218 JS / 46 contracts 与文档门禁通过，但独立复审 RETURN P2=2，要求补私有 detached DOM、student、destroy/remount、重叠 generation 与主动作合同；平台收紧时返修未完成 | 只读保全，不提交 / 集成 / 复制 / 移动；等待平台明确恢复原目录写入 |
| FE-017 课程 lane A | Cosmos + Engineering 正文 / owner | `C:/Users/niu-h/.codex/worktrees/f017a/工科实验室` / `codex/fe-v7.9-future-lane-cosmos-engineering @ 948b692` | 仅第 2.14 节登记的两方向 10 文件；不写 shared shell、manifest、合同、文档或其他方向 | R7 返修已逐字回收到 f017；本 lane 保留 4 tracked modified + 6 untracked、0 staged 的冻结现场 | 写入关闭；不提交、不分配版本、不 merge / push / rebase / 清理 |
| FE-017 课程 lane B | Data + Information 正文 / owner | `C:/Users/niu-h/.codex/worktrees/f017b/工科实验室` / `codex/fe-v7.9-future-lane-data-info @ 948b692` | 仅第 2.14 节登记的两方向 10 文件；不写 shared shell、manifest、合同、文档或其他方向 | 4 tracked modified + 6 untracked、0 staged；原 10 文件冻结指纹已回收到 f017；组合复审无 lane B 返修 | 保持冻结；不继续写入、提交、分配版本、merge / push / rebase / 清理 |
| FE-017 课程 lane C | Materials + Humanities 正文 / owner | `C:/Users/niu-h/.codex/worktrees/f017c/工科实验室` / `codex/fe-v7.9-future-lane-materials-humanities @ 948b692` | 仅第 2.14 节登记的两方向 10 文件；不写 shared shell、manifest、合同、文档或其他方向 | R7 返修已逐字回收到 f017；本 lane 保留 4 tracked modified + 6 untracked、0 staged 的冻结现场 | 写入关闭；不提交、不分配版本、不 merge / push / rebase / 清理 |
| 内容组 | `CONTENT-003 / V7.9.51` 十八课程来源校准 | `C:/Users/niu-h/.codex/worktrees/c003/工科实验室` / `codex/content-v7.9-frontier-source-calibration @ 0ce127a5cc9bccbc09731e51a65d8f5834e41c92` | 只修改 `doc/02-子文档/25-V7.7未来星系18课程内容与路由冻结矩阵.md`；未写代码、测试、01/02/03、其他子文档或其他工作树 | 标准内容组复核与独立事实复审均 `P0=P1=P2=P3=0`；文档 0 error、仅既有 doc/99 warning，handoff / diff PASS；专业树 clean | 本提交 no-commit 集成 25 号矩阵并同步共享 02/03；关闭内容写入，c003 只读保留 |
| 后端组 | `BE-011 / V7.8.2` 学生精确课程单元访问处置 | `C:/Users/niu-h/.codex/worktrees/0dce/工科实验室` / `codex/be-v7.8-unit-access @ 35fa21931ac02fa3a48d82eb2c1fc19be57d9db1` | 写入关闭；精确提交含独立 schema / service / endpoint、router 注册、专项测试及受影响 01/07/09 | clean；独立复审 P0=0、P1=0、P2=0、P3=0，候选已由主开发语义集成 | 不得自行 merge、push、rebase 或清理；不得触碰 `a02c`；工作树只在主开发完成安全清理门禁后处理 |
| 后端组 | `BE-013 / V7.8.4` 教师六课有限事实安全投影 | `C:/Users/niu-h/.codex/worktrees/be13/工科实验室` / `codex/be-v7.8-teacher-evidence-facts @ 47ac53a9e46e3cbb1e31b772aa1de06110d06016` | 写入关闭；候选精确包含任务卡登记的 2 个 service、2 个 pytest 与 4 份后端专业文档，共享 `.codex/02/03/27/28/29` 和组合测试由主开发闭环 | clean；独立复审 P0—P3=0，候选已按 DATA→BE 顺序由主开发语义集成 | 不得继续写入、merge、push、rebase 或清理；不得修改 endpoint/schema/router/model/migration、DATA/FE、其他 worktree 或 `a02c` |
| 内容组 | `CONTENT-004 / V7.8.3` 教师六课有限事实契约 | `C:/Users/niu-h/.codex/worktrees/c04e/工科实验室` / `codex/content-v7.8-teacher-evidence-contract @ 100a619b669745a3f84a4b7fd7ea79561c01c3b6` | 写入关闭；最终候选只修改 27 / 28 号冻结契约 | clean；首候选 `98b0450` 因 P1 / P2 退回且禁止集成，原 owner amend 为 `100a619b` 后第二轮独立复审 P0—P3=0，候选已由主开发语义集成 | 不得继续写入、merge、push、rebase 或清理；不得写 01/02/03/29、代码、测试、后端、DATA、FE、其他 worktree或 `a02c` |
| 旧内容预检树 | `PM-015` 初始恢复审计 | detached `C:/Users/niu-h/.codex/worktrees/b08d/工科实验室 @ 0eb3c1080f11be836b7fb66e8083742c294434e0` | 写入关闭；仅保留 Git registry 审计 | 只通过 worktree registry 核对，未进入目录；不作为 CONTENT 或 FE 活动树 | 不得复用、写入、移动或清理；后续仅由主开发按安全清理门禁处理 |
| FE-012 保全来源 | 只读候选 | detached `C:/Users/niu-h/.codex/worktrees/a02c/工科实验室` @ `e329e27` | 永久不允许写入 | 42 个已修改、12 个未跟踪、0 个已暂存文件；不是稳定候选 | 不得删除、覆盖、提交、清理或作为活动开发目录 |
| 前端组 | `FE-013 / V7.8.5` 教师自然工作流 | `C:/Users/niu-h/.codex/worktrees/fe13/工科实验室` / `codex/fe-v7.8-teacher-workflow @ b34f007f5fd91428beb92e6598d4f2b80e3a7f21` | 写入关闭；最终候选精确包含本文第 2.3 节登记的 20 个产品 / 合同 / 01 / 手册 / 08 文件 | clean；候选已集成；QA-013 / V7.8.6 因 attachment 异步边沿 P1 RETURN，失败转入新 V7.8.7 返修树 | 不得继续写入、merge、push、rebase、清理或改其他 worktree；不得触碰 `a02c`、Future、backend、DATA、共享 02/03 |
| 前端组 | `FE-012 / V7.7.30` Zoom 跨断点同步返修 | `C:/Users/niu-h/.codex/worktrees/fz30/工科实验室` / `codex/fe-v7.7-zoom-resize @ 30f7f189567e9dbd9d9aaef23e3a28af44d65044` | 写入关闭；专业候选精确包含第 2.5 节 6 文件 | clean；父 `876cb44`，独立复审 P0—P3=0，候选已由主开发语义集成；正式外部 Edge 尚待 QA | 不得继续写入、merge、push、rebase、清理、启动 / 干预其他服务或触碰 `a02c` |
| 前端组 | `FE-013 / V7.8.7` 课程挂接状态竞态返修 | `C:/Users/niu-h/.codex/worktrees/ft87/工科实验室` / `codex/fe-v7.8-teacher-attachment-race @ 15c0e831e7890f7dc3bfd7089e8eb607433b3a5f` | 写入关闭；候选精确为第 2.6 节 5 文件 | clean；独立复审 P0—P3=0，候选已由主开发语义集成 | 不得继续写入、merge、push、rebase、清理、连接其他组服务 / Browser / TEMP 或触碰 `a02c` |
| 前端组 | `FE-013 / V7.8.9` 教师证据自主轮询返修 | `C:/Users/niu-h/.codex/worktrees/fr89/工科实验室` / `codex/fe-v7.8-teacher-request-coalesce @ 197bada80e5b5468d135ae6011052a580639c41a` | V7.9.62 后仅原 owner 可写第 2.11 节 7 文件；共享 02/03、teacher.js、backend、Future 均冻结 | 首轮 7 文件候选移除 4 秒轮询但独立复审 RETURN P1=1（dialog / workflow focus 下 confirmed demand 丢失）；原 owner 已补 pending demand / focusout drain 与合同，目标 + regression PASS，平台收紧时完整门禁未完成。当前精确 7 tracked modified、0 staged、0 untracked，`.venv` 不存在 | 只读保全，不提交 / 集成 / 复制 / 移动；等待平台明确恢复原目录写入 |
| QA 组 | `QA-014 A03a / V7.7.33` Zoom 修正版精确复验 | detached `C:/Users/niu-h/.codex/worktrees/qz33/工科实验室 @ 73c034e87cdcbfa4c5b2c8a12b28a07fe45003ba` | 写入关闭；仓库与证据只读 | 可执行产品观察 finding=0；reduced-motion 仿真与 main-realm owner 数字取证 mandatory NOT-RUN，最终 BLOCKED / NOT-RUN、P0—P3=N/A。Browser / 服务已释放，qz33 clean、无 `.venv`、9001=0 | 同类发包暂缓；TEMP `astra-qa-v7733-06a79bb3584b4f0487558f2bcb55a4e5` 与 33 文件证据只读保留，不得复用、移动、清理或触碰 `a02c` |
| QA 组 | `QA-014 A03a / V7.7.32` Zoom 兼容 Python 续验 | detached `C:/Users/niu-h/.codex/worktrees/qz32/工科实验室 @ 88de17c6aa8e2a9e589d11d05d8f2415f47f6b88` | 写入关闭；仓库始终只读 | PRECHECK 已证明 Zoom 两份实现和一份专项合同逐字一致；三份专业文档仅含后续 `848c1ed` 合法演进。旧六文件逐字门禁过严，最终 BLOCKED / NOT-RUN、P0—P3=N/A、无产品判断，TEMP / 服务 / Browser 均未创建，9001=0 | qz32 及回执永久只读；不得复用、移动、清理或触碰 `a02c` |
| QA 组 | `QA-013 / V7.8.8` 教师挂接竞态精确复验 | detached `C:/Users/niu-h/.codex/worktrees/qt88/工科实验室 @ 98610934e8492a9687f6545b1f033d82d9a08f6d` | 写入关闭；仓库始终为 0，唯一 TEMP / SQLite / evidence 只读保留 | RETURN FE-013，P0=0/P1=1/P2=0/P3=0；最终 tracked / staged / untracked / ignored=0、`.venv` 不存在，Browser 已 finalize，9001=0 | 不得继续运行、复用 / 清理 TEMP、连接其他组服务或触碰 `a02c`；后续复验必须使用新登记的 detached 树与全新 TEMP |
| QA 组 | `QA-014 A03a / V7.7.31` Zoom 跨断点精确复验 | detached `C:/Users/niu-h/.codex/worktrees/qz31/工科实验室 @ 944855e54dcb020db6531c9da1417a5d33b8abc1` | 写入关闭；仓库始终只读 | BLOCKED / NOT-RUN、P0—P3=N/A；qz31 clean、无 `.venv`，data 空、SQLite / Browser 未创建、9001=0 | TEMP 与证据原样保留；不得复用、移动、清理或触碰 `a02c` |
| QA 组 | `QA-013 / V7.8.6` 教师协同独立运行验收 | detached `C:/Users/niu-h/.codex/worktrees/qe13/工科实验室 @ 990f4e8ff33d8c05968965136e147fdd7b103c29` | 写入关闭；仓库始终只读，隔离 SQLite / evidence 只在唯一 TEMP | RETURN FE-013，P0=0/P1=1/P2=0/P3=0；qe13 tracked/staged/untracked/ignored=0、无 `.venv`；Browser 释放、9001=0 | TEMP `astra-qa-v786-1f0ae4f658704c4fbbee933c8d17528b` 与 37 项证据原样保留；不得复用、清理或触碰 `a02c` |
| 前端组 | `FE-012 / V7.7.27` A03a 四项集中返修 | `C:/Users/niu-h/.codex/worktrees/f27a/工科实验室` / `codex/fe-v7.7-a03a-four-fixes @ b450e33b30f8151aae428b35db2e9aa41ed48d72` | 写入关闭；精确候选包含登记的 15 个前端、合同及受影响 01/08 文件 | clean；候选已由主开发集成；补充 QA 发现不在该任务四项内的 Zoom P2，已由 V7.7.30 修复 | 不得自行 merge、push、rebase 或清理；不得恢复该树或 d6b6 写入；不得触碰 `a02c` |
| QA 组 | `QA-014 A03a / V7.7.20` 最终回执 | detached `C:/Users/niu-h/.codex/worktrees/89e8/工科实验室 @ 46b27a1d60b58c0c4de153aad588abbb267f99fe` | 只读；外部 Edge / Chrome extension backend、自动化、全新 SQLite 隔离账本和文档证据 | P0=0、P1=0、P2=2、P3=1，RETURN；hidden 与 ordinary unknown warning 均含 slug，Zoom 打开态跨断点短暂沿用旧位图；其余既定回归通过 | 外部浏览器和 9001 已关闭；`.venv` 不存在，工作树 clean；本轮精确 TEMP 已安全删除且无 ACL 残留；不得触碰 `a02c` |
| QA 组 | `QA-014 A03a / V7.7.26` 最终回执 | detached `C:/Users/niu-h/.codex/worktrees/b56e/工科实验室 @ de3afdbf6934ec4e321bce75fa0595df0c67fed6` | 仓库写入为 0；证据只在唯一外部 TEMP | P0=0、P1=2、P2=2、P3=0，RETURN FE-012；浏览器 / 9001 已关闭，b56e clean、无 `.venv`，根 Python 指纹不变 | TEMP `astra-qa-v7726-db881ce314d546e9b760aca625544592` 原样保留；V7.7.28 必须使用全新 detached 树 / TEMP |
| QA 组 | `QA-014 A03a / V7.7.28` 保留现场补充回执 | detached `C:/Users/niu-h/.codex/worktrees/q28a/工科实验室 @ 2d6a065720fd6575729ef7d4298df28c5fc7dcfb` | 仓库写入为 0；证据只在唯一外部 TEMP | 初始 systemError 回执为 BLOCKED / NOT-RUN；后续同一保留现场补充完成 Zoom 移动→桌面保持打开探针，稳定复现 943px host 使用 863px backing store，最终 RETURN FE-012，P0=0/P1=0/P2=1/P3=0 | TEMP `astra-qa-v7728-ee8d104947b74b589f1bd3310f8f36cb` 原样保留；截图 / manifest 已登记，Browser / listener 均释放、9001=0；不得复用 |
| QA 组 | `QA-014 A03a / V7.7.29` runtime 续验 | detached `C:/Users/niu-h/.codex/worktrees/q28a/工科实验室 @ 2d6a065720fd6575729ef7d4298df28c5fc7dcfb` | 仓库写入为 0；证据只在唯一外部 TEMP | 当时既定矩阵 PASS；后续同一产品树的保留现场补充探针稳定发现 Zoom 移动→桌面保持打开 P2，因此该切片仍待 V7.7.30 运行复验 | V7.7.29 TEMP 与 38 文件证据继续原样保留；补充 P2 使用 V7.7.28 保留 TEMP 的截图 / manifest，Browser 与服务均已释放、9001=0 |
| 工具组 | `TOOLS-002 / V7.7.25` strict-readonly 本机启动 | `C:/Users/niu-h/.codex/worktrees/6f2a/工科实验室` / `codex/tools-v7.7-readonly-local-start @ ba3d02c4e8712e9ba10fd8499f99f5dc0bb43d6b` | 写入关闭；候选精确包含登记的 9 个启动器、合同与受影响入口文档文件 | clean；父 `0262b322401e644cafc8a31bcfa8b405ebb6df94`；首轮复审 RETURN，返修后最终独立复审 P0—P3=0，候选由本提交语义集成 | 不得继续写入、merge、push、rebase 或清理；不得写共享 02/03、QA TEMP、其他工作树或触碰 `a02c` |
| UI 组 | `UI-003 / V7.9.4` 管理员治理实现 | `C:/Users/niu-h/.codex/worktrees/7a78/工科实验室` / `codex/ui-v7.9-admin-governance @ 70c6380f760dd504291a97e4e1e58e15274fdf04` | 写入已关闭；提交精确包含 18 个登记实现 / 合同 / 01 / 08 文件 | clean；第七轮独立复审 P0—P3=0，候选由本提交语义集成；真实浏览器与 DATA 联调仍待精确 QA | 不得自行 merge、push、rebase 或清理；不得触碰 `a02c`；工作树只在主开发完成安全清理门禁后处理 |
| 数据组 | `DATA-002 / V7.9.12` 单课程教师演示补样 | `C:/Users/niu-h/.codex/worktrees/992d/工科实验室` / `codex/data-v7.9-teacher-demo-sample @ a920642d03f168594cf163c512fc759a1c1ac009` | 写入关闭；专业组提交精确包含 manifest、专项 pytest、Node 合同与受影响 01/04/07/09；通用初始化器保持基线 blob `a8a589db…` | clean；独立复审 P0=0、P1=0、P2=0、P3=0，候选已由主开发语义集成 | 不得自行 merge、push、rebase 或清理；不得触碰 `a02c`；工作树只在主开发完成安全清理门禁后处理 |
| 数据组 | `DATA-003 / V7.9.19` 六课演示证据 producer profile | `C:/Users/niu-h/.codex/worktrees/da03/工科实验室` / `codex/data-v7.9-teacher-evidence-producers @ 31722c8899ffc7f7a56d14951a308f11c22a05d8` | 写入关闭；候选精确包含 profile / manifest / initializer、DATA 专项 / Node 合同与 5 份专业文档 | clean；独立复审 P0—P3=0，候选已由主开发语义集成；11 个 ignored cache 因平台清理拒绝保留在该树、不进入提交 | 不得继续写入、merge、push、rebase 或清理；不得修改 `backend/app/**`、Alembic、FE、其他 worktree / TEMP 或 `a02c` |

### 2.2 FE-012 首批选择、集成与验收边界

`a02c@e329e27` 只提供只读素材，不能整体 apply、复制、cherry-pick、add、stash、format 或 commit。V7.7.2 只允许 FE 在 `codex/fe-v7.7-evidence` 中手工择取并校正以下首批能力：

1. **共享证据核心**：`shared/js/learning-evidence-{queue,client,loader,activity,status}.js`、`shared/js/learning-activity-catalog.js`、`shared/css/learning-evidence.css` 与 `tools/tests/learning-evidence-contract.cjs`。保留账号 / 角色隔离、7 天 / 256 条 / 1 MiB / 单批 50、有界重试、跨标签租约、敏感字段拒绝及客户端拒绝写 `completed/transferred`；Future catalog 只消费 25 号文档冻结的 18 个键。
2. **身份围栏**：只按必要 hunk 修改 `shared/js/app-session.js` 与 `shared/js/product-capabilities.js`，覆盖 logout、401、换号、换角色和跨标签 authority clear，不整文件覆盖壳层。
3. **非 Future 代表活动**：`shared/js/engineering-lab-publication-context.js`、工科 `physics.mechanics` 的最小事件接线，以及 `codevis/shared/js/course-manifest.js`、`codevis/shared/js/student-context.js`、`codevis/pages/course-challenge/course-challenge.js` 的真实交互事件。若触及 `shared/js/module-selector.js`，必须先修复“owner 释放成功前销毁证据 controller”的半状态事务顺序。
4. **角色回读**：`shared/js/role-home-client.js`、`shared/js/student-learning-evidence.js`、`shared/js/teacher-learning-evidence.js`、`pages/planets/planets.js` / `.css` 及 student / teacher 页面的最小替换 hunk。recovery 必须指向可恢复深链而不是只给 `#student`，积分不能由可能分页截断的 ledger 客户端求和冒充权威聚合。
5. **关联测试**：只更新被上述行为真正影响的现有前端合同，不通过弱化断言或缓存令牌变化制造“通过”。

本批明确禁止修改 `pages/frontier/**`、`shared/js/frontier-*.js`、`pages/engineering/bridge-truss.js` 的 Future 接线、Router、PageRegistry、experiment registry、`index.html`、`shared/js/main.js`、`sw.js` 和四份候选文档；这些文件分别等待 FE-017 或由主开发在最新集成点做最小 wiring。所有 `V7.6.6`、`20260727v766LearningEvidenceP0` 和纯缓存代际文案都不得重放。

V7.7.2 的实现组门禁为：相关 JS / CJS 语法、`learning-evidence-contract.cjs`、`npm test`、候选路径审计和 `git diff --check`；候选必须完全已跟踪、工作区干净并提供精确 SHA、父基线、文件清单、验证结果与 02 / 03 / 01 handoff 摘要。完整 API、离线、多标签、三角色、SQLite 和真实 Edge 仍由 QA-014 对集成 SHA 独立验收。

集成事实：FE 在 `codex/fe-v7.7-evidence` 形成 `ac200c8`（父 `7083e2c`）精确候选；主开发、core 与 surface 对 committed blob 的 P0—P2 结论均为 0。候选包含 transient marker + 非敏感持久 clear epoch、BroadcastChannel / storage 即时撤权、登录前清理、在线写入中止、租约释放、Code Space fatal latch、严格敏感键拒绝及对应 01/08 说明；未修改 Future、Router、PageRegistry、入口、SW、02 或 03。主开发集成后仍只关闭“首批实现”，FE-012 保持进行中；Future 接线完成后再执行 A03b。

A03a 退回事实：QA 在精确集成提交 `49bc1eaacc8df9190da56ea3958636e293f9dd68` 上以外部 Edge、隔离 9001 / SQLite 与只读 0051 账本执行 V7.7.5。209 个 JavaScript 语法检查、38 个前端合同、133 个后端用例通过，唯一跳过为范围外真实 MySQL 演练；双标签撤权、open / hidden、写权限、两条教学闭环、刷新、离线重放、幂等 / 乱序、教师聚合、管理员边界、两视口无根级溢出、应用 console 0 error/warn 和 21 条事件 / 2 条完成投影对账均通过。阻断为：B 班 `physics.mechanics=locked` 时学生工作台仍显示“可学习”与 `#physics/mechanics`，进入后 Canvas 可执行（P1）；清空 / 暂停 / 放大和 range 的命中区在桌面与 390×844 低于 44px（P2）。因此 A03a 结论为 FAIL，不能冒充完整 A03。

V7.7.6 只在原 `FE-012` 内返修：学生工作台必须按权威 `effective_release_state` 渲染 open / locked，locked 不得生成可执行 href；直接 locked 深链不得启动 Canvas、RAF、Observer 或交互监听，只显示明确状态和安全返回动作；hidden 继续不可发现。工科清空、暂停、放大与四个 range 的可命中区域在桌面和精确 390×844 均不得低于 44px，同时保持键盘方向键和视觉轨道。允许路径仅为 `pages/student/student.js`、`shared/js/{module-selector,learning-evidence-activity,engineering-lab-publication-context}.js`、`pages/physics/{physics.js,physics.css,physics-zoom.js}` 中必要文件、对应前端合同及受影响 01/08；不要求全部文件都修改，也不得借返修触碰 Future、Router、入口、SW、后端或共享 02/03。返修候选必须从 V7.7.5 集成点建立，主开发集成后才分配 A03a 复验版本。

V7.7.6 集成事实：FE 在 `codex/fe-v7.7-evidence-a03a` 形成最终候选 `4af3ba4e9d09d49962695521ca701b348af406ae`（父 `ad33f1f`）。首个候选经 core 复核发现生产切班未同步发布上下文后未被集成；最终候选新增 `switchClass()` 与真实 change-handler 动态合同，A→B / 空班会立即清除旧 authority、取消导航和在途 resolve，迟到 A 不得回写，B=locked 时零 owner 初始化。实现组、主开发与 core 对最终 committed blob 给出 P0—P2=0；提交态 210 个 JavaScript 语法检查、39 个前端合同、路径 / 文档 / 对接 / diff 门禁均通过，外部 Edge 实现组证据覆盖 A-open→B-locked→历史深链→空班与 console 0。该结果只允许集成，不替代独立 QA；V7.7.7 唯一分配给 QA-014 A03a 复验。

V7.7.7 复验事实：QA 在精确集成提交 `c6dc2cbac91a99d092bda07600915585b65ad33c` 上以全新系统临时 SQLite、`127.0.0.1:9001` 与外部 Edge / Chrome extension backend 独立执行 A03a。210 个 JavaScript 语法检查、39 个前端合同通过；A/open、B/locked 无 href、A→B / 空班 / 迟到响应、locked 零 owner、hidden 归一、open 教学 completed、桌面与移动端 44px 几何、键盘焦点、权限和只读 0051 账本均通过，应用 console error/warn 为 0。唯一阻断为精确 390×844 下评分卡先截获放大按钮中心，关闭评分卡后 `.fab-trigger` 仍与按钮重叠约 831px²，使标称 `52.25×44px` 的按钮不存在无遮挡的完整 `44×44px` 命中区；结论为 P0=0、P1=0、P2=1、A03a FAIL。V7.7.8 只允许原 FE-012 调整固定浮层与核心控件的移动端避让 / 层级及精确回归合同，不得改动已通过的发布 authority、学习证据、后端、Future、Router、入口或 SW；下一次 A03a 复验版本仅在返修精确候选集成后分配。

V7.7.8 集成事实：FE 在 `codex/fe-v7.7-evidence-a03a-touch` 形成最终候选 `572da35d6ab194f66bd75717b40c7af51c1f7645`（父 `1899d34ac8b08e0965e852090cdc9a0cae19478a`）。实现把力学评分卡移入 `.demo-section` 文档流、以 safe-area 感知的 44px 移动 dock 排列五个工具、让力学 scrim 保持非模态，并在侧栏打开时把整个浮动工具集置于 backdrop / sidebar / toggle 以下；减少动效同时关闭主 FAB、导览、收藏和返回顶部 ripple。初版固定卡片因顶部与滚动遮挡被 core 退回，侧栏末项与减少动效 ripple 缺口也在同一任务 amend 中关闭；最终 core 与 surface 对 committed blob 的 P0—P3 均为 0。提交态 `npm test` 通过 210 个 JavaScript 语法检查与 39 个前端合同，外部 Edge 在 1015×898 和精确 390×844 对评分卡、滚动、侧栏、七个核心控件、五个 dock、导出菜单、正常 / 减少动效执行矩形和 `elementFromPoint` 复核，结果为零遮挡、零根级溢出、应用 console error/warn 为 0；文档、handoff 与 diff 门禁通过。主开发已将候选作为 V7.7.8 集成；这只是待独立 QA 的 A03a 返修，不代表 FE-012、Future 或完整 A03 完成。Git、02 与 03 对账后，V7.7.9 唯一分配给 QA-014 A03a 精确复验。

V7.7.9 复验事实：QA 在精确集成提交 `1bc5af0beca47d1411d31a8e967d48fa874b09c1` 上使用全新系统临时 SQLite、`127.0.0.1:9001` 与外部 Edge / Chrome extension backend 独立执行。210 个 JavaScript 语法检查、39 个前端合同、评分卡文档流、两视口遮挡与 44px、侧栏、正常 ripple、locked 深链、权限、只读 0051 账本、根级溢出和应用 console 均通过；原 V7.7.7 浮层 P2 已关闭。新阻断为：真实点击“记录预测”并调整 range 后，隔离账本只有 5 条 `started`、3 条 `attempted`、0 条 `predicted/completed`，投影保持 `in_progress`（P1）；dock 折叠时 `.favorite-fab` 为 `opacity:0/pointer-events:none` 但仍在 Tab 顺序，移动与桌面均可稳定聚焦（P2）。结论为 P0=0、P1=1、P2=1、A03a FAIL；首次 owner 延迟未稳定复现，reduced-motion 浏览器仿真因外部后端无 CDP 记为 NOT-RUN、静态合同通过。服务、临时目录与浏览器已收束，9001 无监听，QA worktree clean，`a02c` 未触碰。失败项继续退回原 FE-012；Git、02、03 对账后 V7.7.10 唯一分配给这两个精确返修，下一次 QA 版本待集成 SHA 后另行分配。

V7.7.11 复验事实：QA 在精确集成提交 `7c83c34d5667c32504750bedeef75cf24bbac79d` 上使用全新系统临时 SQLite、`127.0.0.1:9001` 与外部 Edge / Chrome extension backend 独立执行。211 个跟踪 JavaScript 语法检查、40 个前端合同、handoff、`git diff/show --check` 与仓库 clean 通过；严格文档校验只命中候选外既存 `doc/99` 编号规则。真实 UI 已形成 `predicted=1 / attempted=2 / explained=1 / rule-completed=1`，唯一活动投影为 `completed`，同标签与第二标签恢复不丢失；折叠 / 展开、导出菜单关闭、快速 toggle、次级聚焦回主 FAB、动态 owner 属性和两视口 Tab 序列通过，V7.7.9 两项主失败均关闭。桌面 1015×898 的七个核心控件与五个 dock 均至少 44px、中心命中、零交叠、零根级溢出；locked / hidden / open、权限、非目标运行时、离页可见资源释放及应用 console error/warn=0 通过。独立阻断为精确 390×844 冷启动时力学内容内部仍保持约 896px 宽并被根级裁剪，暂停与放大中心落在视口外，标称零根溢出不能代替核心控制真实可达；结论为 P0=0、P1=1、P2=0、P3=0、A03a RETURN。服务与外部浏览器已停止，QA 与根工作树 clean，`a02c@e329e27` 未触碰。失败项退回原 FE-012，不建立重复任务；Git 全分支、标签、02 与 03 对账未发现既有 V7.7.12，故该版本唯一分配给移动端力学内容内宽、裁切和核心控制可达性返修。

V7.7.12 集成事实：FE 在 `codex/fe-v7.7-evidence-a03a-mobile-width` 形成最终候选 `e0d2412d5ebd103ffe1cb26eb9258e4b8c5df415`（父 `cb4053e60990980f2e641e2b37b66e4bb08119e5`）。候选精确包含三份力学实现、两份前端合同及受影响的两份 01 和 08，共 8 文件；没有修改 `module-selector`、Router、入口、SW、后端、Future 或共享 02/03。单列 mechanics grid 使用可收缩 track，Canvas owner 在窗口 resize 与 Zoom 归位后按父宽和 DPR 重建；Zoom 使用稳定 handler ref，覆盖聚焦、Tab / Shift+Tab、Escape / close / destroy 返回及幂等 attach / detach。独立只读复审为 P0—P3=0；Node 22.20.0 / npm 10.9.3 下 211 个语法检查、40/40 前端合同及定向生命周期合同通过，`git show --check` 通过。主开发从 `44f7757393aeda5724e5f9e7f42ee227cdde5f5a` 集成该候选；这只形成 V7.7.12 精确复验基线，不代表 A03a、完整 FE-012、Future 或完整 A03 完成。Git 全分支、标签、02 与 03 对账确认 `V7.7.16` 未占用，故该版本唯一分配给 QA-014 A03a。

V7.7.16 复验事实：QA 在精确根提交 `2e934e51350296d936cd48605982d181de9a6c3b` 上使用全新 SQLite、`127.0.0.1:9001` 与外部 Edge 150 独立执行。211 个 JavaScript 语法检查、40 个前端合同、七个目标后端测试文件、handoff、提交对象和 diff 门禁通过；strict 文档校验仅命中既有 `doc/99` 编号问题。390×844 与 1015×898 的根溢出为 0，力学 grid / Canvas 按 318px / 864px 父宽与 DPR 对齐，七个核心控件完整 44px 九点命中、Zoom 焦点与三轮重入释放、open / locked / hidden、角色权限、console 0/0 和第二学生 `started=1 / predicted=1 / attempted=2 / explained=1 / completed=1` 均通过。唯一阻断为次级“收藏实验”获焦后按 Escape：FAB owner 先折叠 dock，但同一事件继续进入模块级 Escape owner，URL 从 `#physics/mechanics` 变为 `#physics`，焦点落到 `body`；结论 P0=0、P1=1、P2=0、P3=0、A03a RETURN。失败项退回原 FE-012，不建立重复任务；V7.7.17 只允许在 `fab-trigger.js` 阻断已消费的 Escape，并以两个 document owner 同时注册的动态合同证明路由不变、模块仍 active、dock 折叠且焦点归还主 FAB。不得修改 `module-selector` 或扩大到 Future。

V7.7.17 集成事实：FE 在 `codex/fe-v7.7-evidence-a03a-escape` 形成候选 `b66c32180b1190d707b4e1c2c1cdba1fe600a5d4`（父 `c07bf60c86e331bb550a782ab6b9b109da8e5ae3`），精确修改 `fab-trigger.js`、一个动态合同及受影响的两份 01 和 08。实现把展开 dock 的 Escape 仲裁移到 document capture，消费普通 Escape 时执行 `preventDefault()`、`stopImmediatePropagation()` 与既有 `collapse()`；Zoom modal、实验指南和导出菜单持有 Escape 时主动让位，hide 以同一 capture 参数释放。动态合同真实加载 FabTrigger、ModuleSelector 与 PhysicsZoom，覆盖 `show → ModuleSelector init → hide → show`、首次 Escape 只折叠、第二次才关闭模块、三个高优先级 owner 及重复生命周期无泄漏。实现组与独立复审均为 P0—P3=0；211 个 JavaScript 语法检查、40/40 前端合同、文档 / handoff / diff 门禁通过。Git 全分支、标签、02 与 03 对账确认 V7.7.18 未占用，故该版本唯一分配给 QA-014 A03a 在本精确集成提交上执行真实 9001 / 外部 Edge 复验；这仍不代表 Future A03b、完整 FE-012 或完整 A03 完成。

V7.7.18 复验事实：QA 在精确根提交 `fdf5dafcd2839f743bea4b227cd83e3ff60d8803` 上使用全新 SQLite、`127.0.0.1:9001` 与外部 Edge 150 独立执行。首次 Escape 只折叠 dock、hash 保持 `#physics/mechanics`、active module 保持且焦点回主 FAB，第二次 Escape 才关闭模块；211 个 JavaScript 语法检查、40/40 前端合同、七个目标后端文件 136 passed / 1 skipped、1015×898 与 390×844、44px、open / locked / hidden、证据闭环、权限、非目标运行时和离页资源释放均通过。唯一 P2 是 B 班 hidden 冷启动 `#physics/hidden-qa-v7718` 正确失败关闭并归一到 `#physics` 时，应用 console 写入一次 `[ModuleSelector] refusing transition to unknown module: physics:hidden-qa-v7718`；因此结论为 P0=0、P1=0、P2=1、P3=0、A03a RETURN。失败继续退回原 FE-012，不创建重复任务；V7.7.19 只允许让 authoritative hidden / locked 的预期拒绝路径无应用告警，同时保留归一化、零 owner 与意外未知模块的 fail-closed 诊断，不得全局静默 console 或扩大到 Router、后端、Future、FAB / physics。

V7.7.20 最终复验事实：QA 在精确根提交 `46b27a1d60b58c0c4de153aad588abbb267f99fe` 上使用全新 SQLite、`127.0.0.1:9001` 与外部 Edge / Chrome extension backend 独立执行。hidden 深链 `#physics/hidden-qa-v7718` 2/2 冷启动均最终归一 `#physics`，module / Canvas / Zoom / evidence owner 为 0，但每次输出一条 `[ModuleSelector] refusing transition to unknown module: physics:hidden-qa-v7718` 应用 warning；ordinary unknown 虽失败关闭到 `#physics`，warning 仍回显 `physics:ordinary-unknown`。真实 `GET /api/courses/{course_id}/units` 会过滤 hidden，前端列表分类因此对 hidden 与不存在项都只能得到 `course_unit_missing`；V7.7.19 合同把 hidden 行放进学生列表，与真实接口语义不一致。Zoom 保持打开时从桌面切到移动视口，位图会短暂沿用旧尺寸，关闭后立即对齐。locked/open 的证据闭环 `predicted=1 / attempted=2 / explained=1 / completed=1`、双视口零根溢出、核心控件至少 44px、Escape / focus / lifecycle、角色权限、单一 0051 账本及应用 error/pageerror=0 均通过。最终结论为 P0=0、P1=0、P2=2、P3=1、A03a RETURN；浏览器和 9001 已关闭，QA 工作树 clean，本轮精确 TEMP 已安全删除且无 ACL 残留，受保护 `a02c` 未触碰。

BE-011 缺口与隐私边界：现有学生安全接口没有能力区分“精确 activity key 存在但 hidden”与“确实不存在”，不能由前端诚实推断，也不得通过教师 release plan、证据恢复或代码题接口旁路。最小方案固定为 `GET /api/courses/{course_id}/unit-access?class_id=...&activity_key=...`：先校验学生对 class / course 的既有授权，再按精确 key 返回 `available: true`，或只返回 `activity_hidden / activity_locked / course_unit_missing` 安全错误码；不返回单元 ID、标题、内容、计划、锁定原因或其他目录元数据。该接口只暴露调用者已经给出的精确键的有界存在性处置，是保留“hidden 静默归一、真正 missing 保留诊断”产品契约所需的最小取舍；不得扩展成目录发现 API。

BE-007 激活证据：`pages/teacher/teacher.js` 的 `loadCurriculumScope()` 已请求 `/api/progress/courses/{course_id}/classes/{class_id}/students`，但 `renderProgressMatrix()` 完全不消费 `state.data.courseProgress`，只显示 0051 aggregate 占位；`backend/app/api/endpoints/learning_evidence.py` 只有事件写入、已知 `event_id` 的纠错、学生 recovery、教师 aggregate 与 rebuild，没有教师按班级 / 课程 / 学生发现事件的 GET，因此现有纠错原语无法形成可演示入口。`pages/admin/admin.js` 的治理面板没有 courses，`backend/app/schemas/course.py` 只有 `CourseCreate/CourseRead`，`backend/app/api/endpoints/courses.py` 只有课程创建、挂班、协作者、单元、release-plan 和作业接口，没有课程状态 PATCH；管理员无法完成规划已经承诺的课程调整与审计回读。BE-007 只补两项最小合同：授权范围内的逐学生证据分页 / 筛选 / 事件 ID 回读，以及管理员课程 `draft/published/archived` 的 `expected_status + reason` 冲突保护、影响计数与审计写入。既有课程 / 聚合 / 提交接口继续复用，不新增迁移、通知、排课、任意 SQL、真实 MySQL、公网或通用内容平台。

BE-007 集成事实：最终候选 `d1fefcdc` 以 event type → exact leaf-path → scalar policy 投影教师证据，所有用户可控字符串只返回 `value_type / value_size` 且标记截断，未知 nested leaf、源码、JWT、API key、PEM 与 secret 均不回显；新增课程状态 schema、endpoint、service 并经 `/courses` 注册，冻结 `courses.py` 与 `schemas/course.py` 相对父提交逐字节不变。课程状态写入固定使用 `ADMIN_AUTHORITY_LOCK → school/course → lock_active_admin`，SQLite 同课程写串行化覆盖 `1×200 + 1×409` 与正式管理员撤权竞争。第三轮独立复审 P0—P3=0；后端全量 561 passed / 8 个真实 MySQL 条件跳过，仓库前端合同、架构、文档、对接和 diff 门禁通过。

BE-011 集成事实：专业组提交 `35fa21931ac02fa3a48d82eb2c1fc19be57d9db1`（父 `ac8268cf14c0b6c547eae45d64131b8b8a38c61f`）新增独立 `course_unit_access` schema / endpoint / service 和专项测试，仅修改 router 注册及受影响 01/07/09，共 9 文件；主开发逐 blob 集成 5 个实现 / 测试文件并语义合并当前文档。student-only 接口先校验精确班课授权，再复用既有发布算法，授权内 open / hidden / locked / missing 统一以 `available/error_code` 二字段返回；多匹配、目标 plan 缺失 / 关联不一致与未知态固定 `409`，无关 plan 漂移不阻断目标。独立复审 P0—P3=0；43 项受影响测试、后端全量 567 passed / 8 个真实 MySQL 条件跳过、214 个跟踪 JS / 41 个前端合同、架构、文档、对接和 diff 门禁均通过，冻结课程文件、models、Alembic、02/03 相对专业组父提交零差异。

V7.7.22 集成事实：Git 全分支、标签、02、03 与全仓引用查重确认该版本分配前未占用，随后由原 `FE-012` 唯一使用。专业组提交 `5db1112ea06256b9a4d15d595adf1383e1d15f2b`（父 `a2878e037fd0450f7b3262781832a6ea3e0e64f4`）精确修改 9 文件；三轮独立复审最终 P0—P3=0。前端解析顺序为学生 + 当前 class / physics course → 先读既有 units → exact open / locked 直接消费且零探针 → 零匹配才用同一 `AbortSignal` 请求 BE-011；hidden / 后端 locked 静默归一，真正 missing 与 ordinary unknown 只输出固定 `[ModuleSelector] refusing transition to unknown module`，不含 slug、异常正文或敏感文本。known / unknown pending 各有独立 controller，切换、close / reset / leave 同步 abort；unknown 新请求还以 controller identity + 新 transition generation 阻断 A→reset→同 slug B 后 A 迟到的 ABA 覆盖。列表未命中却返回 `available=true`、畸形 DTO、未知错误码或迟到旧 scope 均失败关闭且不启动 owner。Zoom 打开态只由 Zoom 协调、PhysicsSim 重建当前原容器 CSS 尺寸、DPR backing store、`W/H` 并立即重绘，不新增第二个 Canvas writer 或全局监听器。专业组定向合同和 `npm test` 通过 215 个跟踪 JavaScript / 42 个前端合同，文档 / handoff / diff 门禁通过；真实 9001 / SQLite / 外部 Edge 仍必须由 QA-014 复验。

V7.7.26 最终回执：QA 在 `de3afdbf6934ec4e321bce75fa0595df0c67fed6` 上完成正式脚本、SQLite、三角色 API 与外部 Edge 真实运行，结论 `RETURN FE-012`，P0=0、P1=2、P2=2、P3=0。P1：`physics.mechanics` 规则要求学生 `corrected`，但页面只产生 attempted，fresh learner 无法得到服务端 completed；P1：`physics.gas-laws` / `physics.thermodynamics` 的 unit-access 分别正确返回 locked / hidden，冷启动却仍保留目标 hash、激活模块与 Canvas，发布 gate 只覆盖 mechanics。P2：实验指南点击后约 100ms 内焦点已回主 FAB 而 overlay 尚未取得焦点，首个 Escape 无 owner；P2：同 scope 恢复已含 `first_started_at`，activity 初始化仍生成新 UUID 并再次写入 started。Zoom 旧位图采样发生在 resize 事件前，下一任务已对齐，产品 P3 撤销；外部只读执行面无法安装 resize-event / RAF probe，记 NOT-RUN 而非缺陷。QA 误把教师 correction 施加到 fresh physics attempt，只记程序性数据污染，不参与 P1 结论；物理缺口另有源码、规则与 in-progress 投影独立证明。启动后单一 `content_pages` 是确定性 local-preview seed，empty-baseline exit1 是 runbook assertion-stage mismatch，不退产品或工具。

V7.7.27 写入包：只复用原 `FE-012`，唯一版本 `V7.7.27`，新工作树 `C:/Users/niu-h/.codex/worktrees/f27a/工科实验室`，分支 `codex/fe-v7.7-a03a-four-fixes`，基线为本 V7.7.26 权威控制提交。四项责任不可拆成竞争任务：①把 physics mechanics 重构为固定落高、只改变恢复系数 `e=.40/.80` 的受控对照，精确形成 `started → predicted → attempted×2 → corrected → explained → rule-derived completed`；自由滑块 / 发射 / 暂停 / 清空不得制造证据，Canvas 必须有等价测量表、阶段焦点与结构化反馈；②所有 known physics activity 先消费权威 release gate，open 才能激活，listed/backend-only locked 与 hidden 均静默归一且零 module / Canvas / Zoom / evidence owner，ordinary unknown 仍只发固定无 slug 诊断；③实验指南建立 document-level、幂等、可释放的首个 Escape owner，保持 Zoom > Guide > export / dock / module 的仲裁，消费时必须阻止同事件下沉；④recovery 已含 exact `first_started_at` 时跳过 started POST，直接恢复状态并 drain pending commands，不改变真正新 scope 或 authority replacement 的 started。允许写入仅限 `index.html`、`pages/physics/physics.js`、`pages/physics/physics.css`、`shared/js/module-selector.js`、`shared/js/experiment-guide.js`、`shared/js/learning-evidence-activity.js`、必要时为 pending correction 状态最小修改 `shared/js/learning-evidence-client.js`、课程文案对应的 `shared/js/config.js`、`tools/tests/learning-evidence-contract.cjs`、`tools/tests/learning-evidence-v7710-contract.cjs`、`tools/tests/learning-evidence-a03a-contract.cjs`、`tools/tests/module-switch-lifecycle-contract.cjs`、`tools/tests/candidate-cleanup-contract.cjs`、可新增 `tools/tests/physics-mechanics-evidence-contract.cjs`，以及受影响的 `doc/01-开发者文档.md`、`doc/01-开发者手册.md`、`doc/08-前端页面实现索引.md`。禁止修改 shared 02/03、后端、模型 / 迁移、Router、PageRegistry、SW、Future、PhysicsZoom、数据初始化器与发布规则；不得通过放宽规则、增加第三次 attempt 或客户端写 completed 冒充完成。实现组须跑专项合同、完整 `npm test`、受影响后端只读回归、文档 / handoff / diff 门禁并提交固定标题 `V7.7.27 FE fix(evidence): 关闭 A03a 四项缺口（FE-012）`；独立复审 P0—P3=0 后才可集成，随后 V7.7.28 由同一 QA-014 使用全新 detached 树 / TEMP 复验。

V7.7.27 集成事实：原 FE 长期组在 `f27a` 形成精确候选 `b450e33b30f8151aae428b35db2e9aa41ed48d72`（父 `0e159932715d34d1e6cf15e242f67cb933c38c94`），固定标题与任务卡一致，精确修改 15 个登记文件。实现建立固定落高、仅比较 `e=.40/.80` 的两次受控试验和结构化 corrected 反馈；自由探索、owner reopen 与逻辑 resize 不制造学习事实。所有 known Physics 路由统一先过权威发布 gate，hidden / locked 静默归一且零 owner；Guide 取得 document capture Escape owner；同 scope recovery 已有 `first_started_at` 时跳过重复 started。首轮独立复审发现 reopen 的 DOM / runtime 状态分裂及 resize 仍用旧高度夹取，原 owner 在同一任务修复并增加动态合同；第二轮复审 P0—P3=0。主开发集成树六项专项、217 个跟踪 JavaScript / 44/44 前端合同与受影响后端 44 passed / 1 个显式 MySQL skip 通过；文档 compatibility 0 error（仅既有 doc/99 warning）、handoff 与 diff 门禁通过。实现组真实浏览器因运行前置未形成证据，故本集成不关闭 FE-012 或 A03a，`f27a` clean、写入关闭，下一步只允许 V7.7.28 独立复验。

V7.9.16 / V7.7.28 QA 运行包：V7.9.16 唯一属于 PM-009 控制面，产品对象固定为主开发 `2d6a065720fd6575729ef7d4298df28c5fc7dcfb`（父 `0e159932715d34d1e6cf15e242f67cb933c38c94`，标题精确为 V7.7.27）。主开发已创建 detached `C:/Users/niu-h/.codex/worktrees/q28a/工科实验室`；创建后 tracked / staged / untracked / ignored 为 0、仓库 `.venv` 不存在、9001 无监听。QA 必须显式以该路径运行，仓库写入为 0，只能在一次创建的全新系统 TEMP 中写相互隔离的 `scratch/data/evidence`。静态 / 自动化之后先在同一 data 以 `ASTRA_AUTO_CREATE_TABLES=false`、Alembic upgrade 且尚未 import / create_app 的阶段记录真正 migration-only 空基线；再以根工作区已验证的 `.venv/Scripts/python.exe` 作为 `-PythonExecutable`、配合 `-SkipDependencyInstall` 和正式 `astra-local.ps1` 启动。启动后允许且必须核对唯一公开 seed `physics/energy-conservation @ 2026.07-v6.6-sandbox-dom.1`（schema hash `d966381916a6eb4e7e52ef2e4c108b218cef16959757dc850f6d1c8e41cd524b`），其余业务表在 demo 初始化前为 0；不得把 post-start seed 当作空基线失败。浏览器只用可信 `C:/Users/niu-h/.codex/plugins/cache/openai-bundled/browser/26.721.81911/scripts/browser-client.mjs` 的 external extension backend，不切 in-app、不修改 trusted hash。

V7.7.28 的 fresh physics 顺序固定：先验证 owner reopen 不追加事实、Guide 打开后的首个 Escape、受控试验从 1000×560 resize 到 390×320 会取消并保持 attempted=0；随后重新完成 `.40` 与 `.80` 两次试验，得到 learner `started=1 / predicted=1 / attempted=2 / corrected=1 / explained=1` 与服务端唯一 completed。自由控件事件数保持不变，刷新与顺序 peer tab 后 started 仍为 1。另须覆盖 known open / locked / hidden / missing、角色权限、两视口 44px / 根宽 / focus / reduced motion、Zoom / Guide / dock 仲裁、console、owner / RAF / listener / Observer / Canvas 释放和三阶段只读 SQLite 终账。教师 correction 不得施加给 fresh learner；若程序性污染发生，不改库、不换库、不新增第三次 attempt 或第五个用户、不用第二数据库，只把受影响强制项记 invalid / NOT-RUN 并继续不依赖它的检查。旧 `89e8`、`c91f`、`b56e`、旧 TEMP、旧污染数据库与 `a02c` 永久不复用；沿用全局 no-approval 阻断规则。

V7.9.12 写入包：Git 全分支、标签、02、03 与全仓引用查重确认 `DATA-002`、`V7.9.11` 和 `V7.9.12` 均未占用；V7.9.11 只登记写入包，V7.9.12 唯一分配给 DATA-002。数据组复用唯一长期任务和 `992d`，从 V7.9.11 控制提交建立 `codex/data-v7.9-teacher-demo-sample`。只为 `control-flow.loop-boundary` 增加一份稳定、短小、合成的 submitted / pending 作业，使该课程同时具备五类证据、可纠正 attempt、待反馈作业和真实 `runner_unavailable`；既有 physics graded 与 humanities pending 样例必须保留。初始化器须继续只走正式 API，连续三次运行不增殖对象、提交或非登录审计；实现候选必须回读 `report.assignments["control-flow"].status == "submitted"` 并证明 pending 作业与代码题属于同一课程 / activity。禁止扩展模型、迁移、API、服务、路由、直接数据库写入或通用演示框架。

### 2.3 FE-013 教师自然工作流写入包

1. **唯一任务与版本**：复用标准长期任务“星序 Astra｜前端组｜长期对接”，未创建重名实现组。`V7.8.5` 唯一分配给 `FE-013`，固定实现标题为 `V7.8.5 FE feat(teacher): 收束教师协同工作台（FE-013）`；`V7.9.22` 只建立本写入包，`V7.9.23` 回填 `fe13 / codex/fe-v7.8-teacher-workflow @ 41e929a` 的真实 clean worktree、分支和精确 base。标准长期任务 systemError 后，主开发登记的临时执行者接续同一任务编号，最终专业候选为 `b34f007f5fd91428beb92e6598d4f2b80e3a7f21`，没有形成竞争任务。
2. **产品 allowlist**：只允许 `pages/teacher/teacher.js`、`pages/teacher/teacher-{foundation,workbench,curriculum}.css`、`shared/js/learning-evidence-client.js`、`shared/js/teacher-learning-evidence.js` 与 `shared/js/page-registry.js` 的 `TEACHER_RESOURCE_VERSION`。`teacher.js` 只保留非 evidence API 编排和安全 snapshot 窄桥，最终不得超过当前 2883 行，也不得修改架构 ceiling；新增进度、证据、预览、dialog、焦点和生命周期统一扩展现有 `AstraTeacherLearningEvidence`，不得建立第二个 page script 或全局 owner。
3. **合同与专业文档 allowlist**：允许新增 `tools/tests/teacher-natural-workflow-contract.cjs`；允许按真实行为修改 `teacher-{course-orchestration,confirmed-write,pagination,style-layer}-contract.cjs`、`learning-evidence-contract.cjs`、`learning-evidence-regression-contract.cjs`、`astra-workspace-hierarchy-contract.cjs`、`page-registry-contract.cjs`、`role-resource-pruning-contract.cjs`。三层 CSS 顺序和 SHA 门禁继续保留，不能删除断言制造通过。专业组只同步受影响的 `doc/01-开发者文档.md`、`doc/01-开发者手册.md` 与 `doc/08-前端页面实现索引.md`；02 / 03 由 PM 集中闭环。
4. **功能边界**：顶级导航固定为“教学总览 / 课程节奏 / 批改与学情”，原“作业发布 / 组织与课程”降为对应主区内次级动作，不删除既有能力。教师范围请求须使用合法身份口径，多班 / 多课不静默首选；切换范围立即 abort 并清空旧学生、证据、提交和详情。发布必须“预览本次安排 → 单次 PATCH → CAS / 409 / 不明结果权威回读”；桌面渲染语义表格，390×844 渲染学生 disclosure。共享 client 只新增 schema-validated 教师事件发现与追加纠正；事件 ID 仅保留在会话内 opaque map，不进入 UI、DOM 或 storage。页面只渲染服务端有限 facts / truncated，不自行重做 allowlist，不显示原始 JSON、开放文本或完整源码。
5. **禁止范围**：不得修改 `.codex/AGENTS.md`、共享 02 / 03、27 / 28 / 29 冻结契约、backend / model / migration / DATA、`tools/architecture/v76-module-boundaries.json`、index / main / Router / SW / app-session / evidence loader、queue、status、catalog、student / admin / planets / Future / Physics / codevis、Canvas / Three、其他 worktree / TEMP 或 `a02c`。不得实现教师 impersonation、加载学生 runtime、客户端写 completed / 聚合、把 browser pass 冒充 accepted、把证据或源码写 localStorage，hidden 预览不得泄漏标题、稳定键或历史。
6. **验证门禁**：最终专业候选的 10 个可写专项合同与 3 个只读合同全部通过；候选 `npm test` 为 217 个跟踪 JavaScript，主开发把新增合同纳入索引后的精确集成树复跑为 218 个跟踪 JavaScript，二者均为 45/45 前端合同通过；受影响后端 43/43 通过，文档 compatibility 为 0 error / 1 个既有 `doc/99` warning，handoff 与 diff / show 门禁通过。独立架构第三审与 UX 复审均 PASS、P0—P3=0；`teacher.js` 保持 2883 个逻辑行，PageRegistry 只变更教师资源版本。实现组的正式 9001 / SQLite / 外部 Edge 因平台在 CreateProcess 前拒绝启动命令而 NOT-RUN，未重试或绕过；1015×898、精确 390×844、真实网络 / 焦点 / console / 生命周期与账本必须由 QA-013 独立完成，本条不把静态合同冒充运行态 PASS。

### 2.4 QA-013 教师协同独立运行包

1. **唯一任务与版本**：复用标准长期任务“星序 Astra｜QA组｜长期对接”，不创建重名 QA 任务。`V7.8.6` 唯一分配给 `QA-013`，固定结果标题为 `V7.8.6 QA test(teacher): 验收教师协同工作台（QA-013）`；`V7.9.24` 只建立本运行包，`V7.9.25` 只回填新 detached QA 工作树实态。验收对象的产品提交为 `f47155d3b67976e6740452aed807a604c16bd55e`；新工作树已从 V7.9.24 控制提交建立为 `qe13@990f4e8ff33d8c05968965136e147fdd7b103c29`，产品文件须与 `f47155d` 逐字节一致。
2. **并行与现场前提**：现有 QA 正在独立现场占用 9001；QA-013 不得终止、复用、连接或干预该服务，只有其自然收尾且 listener=0 后才可启动。新 QA 必须使用全新 detached `qe13` 工作树和唯一新 TEMP；不得进入旧 `89e8 / c91f / b56e / q28a`、旧 TEMP / SQLite、fe13 或 `a02c`。若端口、平台审批、权限或安全规则阻断，按 GLOBAL NO-APPROVAL 立即停手并保留现场，不重试、绕过或扩大范围。
3. **只读环境与启动**：新 TEMP 初始只建立 `scratch/` 与 `evidence/`，`data/` 在正式启动前必须不存在；TEMP / TMP、Python bytecode、Node / npm / XDG cache 与 pytest basetemp 均分别映射到 scratch。只读核对根工作区外部 Python / `.venv`、`requirements.lock` 哈希、marker、`pip --isolated check` 和解释器版本后，使用官方 `astra-local.ps1 -Port 9001 -DataDirectory <TEMP>/data -PythonExecutable <已核对的仓库外/根线 Python> -SkipDependencyInstall -InitializeDemoData`；不得在 QA 树创建 `.venv`、直跑 uvicorn、改脚本或复制依赖。
4. **静态与服务门禁**：先运行 FE-013 的 10 个可写合同、3 个只读合同、完整 `npm test`、43 项受影响后端测试、文档 compatibility / handoff 与 Git clean / diff / show；再验证 local-preview baseline、迁移 0051、唯一公开 seed、3 个演示账号、正式初始化报告与 API 权威矩阵。QA 不修改仓库、脚本、数据库或产品；只通过公开 API 产生本轮必需的临时验收状态，所有写操作逐项记录请求、响应和后续权威 GET。
5. **教师权威脚本**：教师只能读取本校且 `mine=true` 的班级，管理员只能以 `school_id` 范围读取；多班 / 多课通过正式 API 建立且页面不得静默首选。发布覆盖首次成功、stale 409 和不明结果回读，均核对恰一次 PATCH 与最终 release 权威态；证据覆盖 physics precise、control-flow shallow、四课 unavailable、分页 / 筛选、opaque correction、作业反馈与 runner / browser 分栏。若需 correction，只对为本轮新建的第四次尝试写入，禁止修改 demo 原始 attempts 或用额外用户污染固定账本。
6. **外部浏览器矩阵**：只使用 trusted Browser `26.721.81911/scripts/browser-client.mjs`（SHA-256 `14E425736668BF21B5B39F2CC022EE8684728617FB5C49F35533C2E349F47193`）连接外部 Edge / Chrome extension backend，不切 in-app。覆盖精确 1015×898 与 390×844：三主导航、范围显式选择、桌面表格 / 移动 disclosure、44px、Tab / 方向键 / Escape / focus return、reduced-motion、hidden 零泄漏、应用 console error/warn=0、网络 base 与请求次数、反复进入 / 离页后的 timer / listener / owner 释放；扩展噪声单列。
7. **结论、证据与收尾**：最终以产品证据给 PASS 或 RETURN，并列 P0—P3；平台阻断且尚无产品缺陷时给 BLOCKED / NOT-RUN。每个程序性错误单列，不能污染产品判断或通过换库 / 改库 / 增加用户补证。保留 manifest、SHA-256、静态 / API / 浏览器 / SQLite / console / lifecycle 证据；使用原服务会话正常 Ctrl+C，确认 9001=0、工作树 clean、`.venv` 不存在。不得清理旧工作树 / TEMP、force kill、merge、push、commit 或触碰 `a02c`。
8. **最终回执**：QA-013 对 `qe13@990f4e8` / 产品 `f47155d` 给出 RETURN FE-013，P0=0、P1=1、P2=0、P3=0。已挂接 Control Flow 在教师选择 class 1 / course 4 后稳定显示 `course_not_attached`；权威 courses 与 release-plan 均为 200 且证明挂接，页面 courses / units / release-plan / code-submissions 正常，但 progress 与 aggregate 请求为 0。根因为 owner 的 0ms 刷新先消费 `curriculumAttached=false` 并缓存新 scope，异步回读变 true 后仅 class/course key 不变，故未再调度。13/13 专项、43/43 后端、文档 / handoff / Git 通过；完整 npm 在 `spawnSync git EPERM` 处按规则环境 NOT-RUN。双视口、44px、键盘 / 焦点、三轮生命周期、权限、六课 facts、hidden 零泄漏与应用 console 0 均通过；reduced-motion 动态项 NOT-RUN。QA correction 脚本误写既有 demo attempt 形成 event 58，精确 correction 项 INVALID / NOT-RUN，不是产品缺陷且未通过改库 / 换库 / 加用户补证。只读终账为 users=3、classes=2、courses=14、units=42、events=58、projections=6、audit=141；Browser 已释放、服务 Ctrl+C、9001=0，qe13 clean / detached / 无 `.venv`。37 项证据位于 `astra-qa-v786-1f0ae4f658704c4fbbee933c8d17528b`，manifest SHA-256 为 `13C3601DA51FC014D05604F70B738E9ED76C364F5D4DF70E8FB841BD839A60A2`。

### 2.5 FE-012 Zoom 跨断点同步返修包

1. **证据、任务与版本**：保留的 q28a / V7.7.28 运行现场对精确产品 `2d6a065720fd6575729ef7d4298df28c5fc7dcfb` 补充完成移动→桌面探针：在 390×844 打开 Zoom 并保持打开，切至 1015×898 后稳定得到 host `943×712.2`、Canvas CSS `943×528.1`、inline / bitmap 约 `863×483`、DPR 1，形成约 `1.09270` 自动放大；关闭重开与直接桌面打开均恢复 `863×483` 基准，正式 QA 回执判为可重复 P2 而非 CSS 预期。截图位于只读证据 `C:/Users/niu-h/AppData/Local/Temp/astra-qa-v7728-ee8d104947b74b589f1bd3310f8f36cb/evidence/browser-zoom-desktop-mismatch.png`，manifest SHA-256 为 `c581230e0da1ce4238da51e3b1ff2982d5ad2068dc82aeb2e8775fc67f82f00e`。失败归还既有 `FE-012`，不新建重复任务；`V7.7.30` 唯一分配，固定标题为 `V7.7.30 FE fix(evidence): 修复 Zoom 跨断点同步（FE-012）`。`V7.9.26` 只建立本写入包，`V7.9.27` 只在创建后回填新工作树。
2. **根因与唯一方案**：`PhysicsZoom._handleResize()` 会在 window resize 的过渡布局阶段按原父容器 863px 重建位图；弹层 host 随后稳定到 943px 时，`PhysicsSim` 已存在的 `ResizeObserver` 确实收到原父容器变化，却因 Canvas 正由 Zoom 持有而让 `resizeCanvas()` 直接返回，丢失最终通知。实现必须复用该 Observer：同一 Canvas 被 Zoom 持有时把父容器稳定通知转交 Zoom 的重新校准入口，仍由现有 `PhysicsSim.resizeForZoom()` 作为唯一 Canvas 尺寸 / DPR / `W/H` / render writer。自动 base scale 同时限制为 `Math.min(1, sx, sy)`，保留下缩但禁止过渡态低分辨率位图被自动放大；用户主动 pinch 放大语义不变。不得只做 scale clamp 冒充根修。
3. **精确 allowlist**：产品只允许 `pages/physics/physics.js`、`pages/physics/physics-zoom.js`；回归只允许 `tools/tests/learning-evidence-a03a-contract.cjs`；专业文档只允许按真实实现同步 `doc/01-开发者文档.md`、`doc/01-开发者手册.md`、`doc/08-前端页面实现索引.md`。共享 `.codex/AGENTS.md`、02、03 只由 PM 在集成提交集中闭环。没有需要提高的架构行数 ceiling，新增入口须窄小、幂等且不建立第二套 owner。
4. **禁止范围**：不得修改 `pages/physics/physics.css`、PageRegistry、ModuleSelector、ExperimentRegistry、index / main / Router / SW、后端、数据库、教师、管理员、DATA、Future、架构 ceiling或其他合同；不得新增 ResizeObserver、RAF、window / document listener 或第二个 Canvas writer；不得降低既有 host 小于 Canvas 时的下缩、焦点圈闭、Escape、close / destroy、DPR、render 与 listener / pinch owner 释放断言。不得进入、读取、复制、写入或清理 `a02c`，不得复用 f27a / d6b6 / fe13 或连接、终止、重启 QA-013 的 9001 / Browser / TEMP。
5. **动态合同**：在现有 `runMechanicsZoomRestoreContract()` 中真实模拟 390×844 打开 Zoom、window resize 先读取 863px 过渡宽度、随后不再派发 window resize 而仅让原父容器稳定到 943px 并触发现有 PhysicsSim ResizeObserver。必须断言过渡态不自动放大低分辨率位图；稳定后 CSS / inline 为约 `943×528.08`、bitmap 为 `943×528`（DPR 1），`PhysicsSim.W/H`、`Zoom.originalRect`、即时 render 与 base scale 1 一致；关闭重开与直接桌面打开等价。Observer 数量不得增加，close / destroy 后 Observer、window / document listener、pinch owner 与 Canvas ownership 全部释放；既有小 host 下缩断言继续通过。
6. **候选、集成与验证门禁**：专业组从 V7.9.26 精确控制提交 `876cb44df7f483774ca4c52ebbc291ba713e11d4` 在 `fz30 / codex/fe-v7.7-zoom-resize` 形成候选 `30f7f189567e9dbd9d9aaef23e3a28af44d65044`，精确修改上述 6 文件。实现复用既有 PhysicsSim ResizeObserver，在同一 Canvas + parent identity 且 Zoom 持有时转交稳定父宽；仍仅 `resizeForZoom()` 写 CSS / backing / DPR / W/H / render，base scale 使用 `Math.min(1,sx,sy)` 并保留用户 pinch。目标与 6 项相关合同、218 个跟踪 JS / 45/45 前端合同、专业文档、handoff、allowlist、diff / show 全部通过；独立复审 P0—P3=0，工作树提交后 clean，候选已由主开发语义集成。正式 9001 / 外部 Edge 的双向跨断点保持打开复验仍由独立 QA 在精确集成树另行执行，本条不把静态 / 代码复审冒充 runtime PASS。GLOBAL NO-APPROVAL 全程适用。

### 2.6 FE-013 课程挂接状态竞态返修包

1. **证据、任务与版本**：QA-013 / V7.8.6 在 detached `qe13@990f4e8` 对产品 `f47155d` 稳定复现：教师显式选择已挂接的 class 1 / course 4 后，权威 courses 与 release-plan 均为 200，但页面停在 `course_not_attached`，progress / aggregate 请求为 0。独立只读源码审计确认主页面先把 `curriculumAttached=false`，既有 owner 的 0ms 刷新先消费该状态并缓存新 scope，异步挂接回读变 true 后因 base / class / course key 未变而不再调度。失败归还原 `FE-013`，不创建重复任务；`V7.8.7` 唯一分配，固定标题为 `V7.8.7 FE fix(teacher): 修复课程挂接状态竞态（FE-013）`。`V7.9.28` 只建立本写入包，`V7.9.29` 只在创建后回填新工作树；V7.8.8 不在本提交分配。
2. **唯一方案与不变量**：只能扩展现有 `AstraTeacherLearningEvidence`：记录最近一次 `refreshSession` 实际消费的 attachment 布尔状态，并让既有 `evaluate()` 同时识别 scope key 变化与同 scope attachment 的双向边沿。状态必须在触发 render / observer 可见变化前更新，避免自触发调度环；仍复用既有单 timer、请求取消、epoch / ABA 与私有状态清理。不得把 attachment 拼入 correction scope fence，不得修改公共 scope key、强迫 `teacher.js` 重复 refresh、建立第二 owner / listener / Observer，或在 false 状态发出私有 progress / aggregate 请求。
3. **精确 allowlist**：产品只允许 `shared/js/teacher-learning-evidence.js`；回归只允许 `tools/tests/teacher-natural-workflow-contract.cjs`；专业文档只允许按真实实现同步 `doc/01-开发者文档.md`、`doc/01-开发者手册.md`、`doc/08-前端页面实现索引.md`。共享 `.codex/AGENTS.md`、02、03 只由 PM 在集成提交集中闭环。`pages/teacher/teacher.js`、CSS、PageRegistry、client / loader、backend、DATA、Future、Physics、入口 / Router / SW、架构 ceiling与其他合同全部冻结。
4. **动态合同**：必须在同一 base / class / course key 下驱动 `false→true→false`。首次 false 必须 fail-closed、零 progress / aggregate 请求且无旧私有数据；false→true 只能由既有单 timer 合并为恰一对精确 progress + aggregate 请求并恢复桌面表格 / 移动 disclosure 所需状态；true→false 不得新增请求，必须清除表格、disclosure、facts、selection、correction、feedback、loading / error 与所有旧私有引用。重复相同 attachment 状态不得产生请求风暴；切 scope、离页 / destroy、迟到响应与不明写入回读的既有断言不得弱化。
5. **工作树与并行边界**：主开发已从 V7.9.28 精确控制提交 `dc8e579dd39f56a0fee23e90dc2b70dc5f82a864` 建立 `C:/Users/niu-h/.codex/worktrees/ft87/工科实验室` / `codex/fe-v7.8-teacher-attachment-race @ dc8e579`；创建后 branch / HEAD / registry 一致，tracked / staged / untracked=0 且 `.venv` 不存在。全量 Git 历史 / body、local 与实时 origin refs / tags、tracked text、worktree registry、目标路径和分支查重均确认 V7.8.7、V7.9.28—29、ft87 与目标分支在分配前零占用。ft87 与 fz30 文件集合完全不相交，因此可在不同物理目录并行；各自仍只有一个写入者，不共享 9001、Browser 或 TEMP。旧 fe13 / qe13 只读保留，不进入、复制或恢复；`a02c` 永久禁入。
6. **候选、复审与集成**：专业候选 `15c0e831e7890f7dc3bfd7089e8eb607433b3a5f`（父 `dc8e579dd39f56a0fee23e90dc2b70dc5f82a864`）精确修改第 3 项五文件；独立只读复审 P0—P3=0。目标动态合同、13 项 FE-013 相关合同、完整 `npm test`（218 个 tracked JS、45/45 frontend contracts）、43 项受影响后端、文档 compatibility / handoff、allowlist 与 diff / show 门禁均通过；ft87 提交后 clean、写入关闭。主开发已语义集成产品、合同和专业文档，并在同一 V7.8.7 提交同步共享控制面。QA 仍须使用新 detached 工作树、全新 TEMP / SQLite 与正式 9001 / 外部 Edge 复验 attachment 边沿、桌面 / 移动、console、权限、生命周期及先前 INVALID / NOT-RUN 项；静态合同不得冒充 runtime PASS。GLOBAL NO-APPROVAL 全程适用。

### 2.7 QA-014 V7.7.31 Zoom 跨断点精确运行包

1. **唯一对象与版本**：复用标准长期任务“星序 Astra｜QA组｜长期对接”，不创建重名 QA 任务。`V7.7.31` 唯一分配给 `QA-014 A03a`，固定结果标题为 `V7.7.31 QA test(evidence): 复验 Zoom 跨断点同步（QA-014）`；`V7.9.30` 只登记本运行包，`V7.9.31` 只回填新 detached QA 工作树实态。产品提交为 `30f4dbd8d6fcdaf795e9bcb8f9b07085c7315263`；主开发已从 V7.9.30 控制提交建立 detached `qz31@944855e54dcb020db6531c9da1417a5d33b8abc1`，产品提交为祖先。QA 开始前还须证明 6 个 V7.7.30 专业文件与产品提交逐字节一致。V7.7.28 / 29 的旧证据只作失败基线，不可充当返修后 PASS。
2. **只读身份与隔离**：qz31 创建后为 `HEAD (no branch)`、tracked / staged / untracked / ignored=0、`.venv` 不存在、产品 ancestor gate=0，9001=0。完整 `[ASSIGN]` 后，QA 仍须重验这些前提和根工作区 clean / worktree registry 指纹；只使用一个全新系统 TEMP，顶层固定 `scratch / data / evidence`，所有 Node / npm / Python / pytest cache 与 basetemp 映射到 scratch，data 在正式启动前为空。不得进入、移动、复用或清理 q28a、qe13、旧 QA TEMP、fz30、ft87 或 `a02c`。
3. **静态门禁**：运行 `learning-evidence-a03a-contract.cjs`、相关 Physics / Zoom / module lifecycle / cleanup / registry 合同、完整 `npm test`、文档 compatibility / handoff、`git diff --check HEAD^ HEAD` 与 `git show --check HEAD`；记录 Node / npm / Python 版本和每项 exit code。完整 npm 若出现平台 `spawnSync git EPERM`，按 GLOBAL NO-APPROVAL 单列环境 NOT-RUN，不可用重复运行或缩减套件伪装通过；已有产品失败仍按产品结论优先。
4. **正式服务**：在预检后使用已集成 TOOLS-002 的公开参数与受控外部 Python，运行 `./astra-local.ps1 -Port 9001 -DataDirectory <TEMP>/data -PythonExecutable D:/Python/python.exe -SkipDependencyInstall`；不得在 QA 树创建 `.venv`、修改脚本、复制 / 链接环境或直跑 uvicorn。验证 health、本地预览 header、迁移 0051、唯一公开 seed 与正式 demo initializer；QA 只通过公开 API / UI 产生本切片必要状态，不修改 SQLite、脚本或仓库。启动 / 关闭命令、PID、端口、日志和最终账本写入 evidence。
5. **外部浏览器主断言**：只使用当前 trusted Browser `26.721.81911/scripts/browser-client.mjs` 的 external extension backend，不切 in-app。以学生公开演示路径进入 `physics.mechanics`，精确 390×844 打开 Zoom并保持；再切 1015×898，等待布局稳定且不再补发第二次 window resize，证明最终 Canvas CSS 与 `CSS×DPR` backing、PhysicsSim W/H、Zoom originalRect、即时 render 和 base scale 1 一致，禁止 863px backing 填充 943px host。随后覆盖 1015×898→390×844 反向切换、关闭重开、直接桌面打开和小 host 下缩，均不得自动放大低分辨率位图。
6. **必要非回归与生命周期**：两个精确视口均验证根级横向溢出 0、可见核心命中区 ≥44px、九点命中、Tab / Shift+Tab 圈闭、Escape 与关闭后的焦点返回、reduced-motion、应用 console error/warn=0。至少三轮打开→跨断点→关闭与离页重入，证明单一 PhysicsSim ResizeObserver、window / document listener、pinch owner、Canvas ownership、RAF / render owner 不增殖且 close / destroy 完全释放；非目标页面不加载 Physics Canvas / Zoom runtime。扩展噪声单列，不得污染应用结论。
7. **判定与收尾**：主 Zoom 断言或必要非回归出现产品失败即 `RETURN FE-012` 并列 P0—P3；全部 mandatory 实际运行且 P0—P3=0 才能 `PASS`。平台 / 权限 / 安全阻断且尚无产品失败时为 `BLOCKED / NOT-RUN`；不得申请审批、重试绕过、换 TEMP / 数据、直接改库或扩大到 Future / 教师。保留截图、尺寸 / DPR / owner / console / network、静态门禁、服务和只读 SQLite manifest / SHA-256；正常 Ctrl+C 停止原服务，释放 Browser，确认 9001=0、qz31 clean、无 `.venv`。QA 不 commit / merge / push / 清理任何工作树或旧 TEMP。

8. **最终回执**：V7.7.31 结论为 `BLOCKED / NOT-RUN`、P0—P3=N/A，未形成产品 PASS 或 RETURN。qz31 身份、祖先、六文件 blob、10 项定向合同、docs compatibility（0 error / 1 个既有 doc/99 warning）、handoff 与 Git diff / show 均通过；完整 npm 在首个 syntax gate 遇 `spawnSync git EPERM`。唯一官方启动使用第 4 项指定的 `D:/Python/python.exe`，在锁依赖 dry-run 因无法满足 `alembic==1.18.5` 于 `astra-local.ps1:351` 失败，且发生在 data 创建、salt、迁移、初始化和 Uvicorn 之前；因此 data 仍空、SQLite 不存在、9001=0、Browser 未创建。GLOBAL NO-APPROVAL 下没有重试、换解释器、联网安装或绕过。唯一 TEMP `C:/Users/niu-h/AppData/Local/Temp/astra-qa-v7731-ffd4c97cb3b542ec8eb9d14f0e4861df` 原样保留，manifest SHA-256 为 `BA9922AC7A96A10B2AA3C6007D47ACABD2B5B5227E5A6E40F39392314FE7950E`。

### 2.8 QA-014 V7.7.32 Zoom 兼容 Python 续验包

1. **唯一对象与版本**：复用同一标准 QA 长期任务和 `QA-014 A03a`，不创建重名任务。`V7.7.32` 唯一分配给同一 Zoom 产品切片，固定结果标题为 `V7.7.32 QA test(evidence): 复验 Zoom 跨断点同步（QA-014）`；`V7.9.32` 只记录 V7.7.31 阻断并登记本续验包，`V7.9.33` 只回填新 detached `C:/Users/niu-h/.codex/worktrees/qz32/工科实验室 @ 88de17c6aa8e2a9e589d11d05d8f2415f47f6b88` 实态。创建后为 `HEAD (no branch)`，tracked / staged / untracked / ignored=0、`.venv` 不存在、9001=0，产品 `30f4dbd8d6fcdaf795e9bcb8f9b07085c7315263` ancestor gate=0。产品基线仍只认该提交的六个 Zoom 专业文件；新 QA 控制树包含之后已集成的非 Physics 提交，QA 必须证明六文件与产品 blob 逐字节一致。完整 `[ASSIGN]` 到达前禁止运行。
2. **唯一解释器与冻结证据**：只允许官方 `astra-local.ps1 -PythonExecutable` 路径，显式解释器固定为 `D:/代码玩具测试/工科实验室/.venv/Scripts/python.exe`。主开发只读预检已证明 Python 3.12.12、可执行文件 SHA-256 `711DF14E4EF9F0890C5C84330FABA821839F3F6757DBE27CBF69AC3DE6852446`、`pip --isolated check` clean，`alembic=1.18.5 / fastapi=0.136.3 / sqlalchemy=2.0.51 / uvicorn=0.38.0`；requirements.lock SHA-256 为 `B69D8CBBF1D6B6E4447931797D90CBA78B840B5F629A8B23443E70B25B9B5F6D`，marker 内容一致且文件 SHA-256 为 `84994FC657D05A5C9DA0EE74453FAAE802A934C720FCD534B81465768D1CA477`。QA 须在启动前后重验这些值与根工作区 clean；所有 pycache、pip / Node / npm cache 和 pytest basetemp 映射到本轮 scratch，禁止安装、升级、写 marker 或改根 `.venv`。不得再调用失败的 D:/Python 3.14.2。
3. **隔离与静态证据**：从新 qz32 建立唯一全新系统 TEMP，顶层固定 scratch / data / evidence，正式启动前 data 为空。V7.7.31 的 `spawnSync git EPERM` 动作不得重试；本轮通过精确 commit / blob 身份继承 V7.8.7 集成提交已经实际通过的完整 npm 218 tracked JS / 45 contracts，并由 QA 重新执行 10 项 Zoom / Physics / lifecycle 定向合同、docs / handoff / Git 门禁。继承只覆盖不可变静态门禁，不可替代任何浏览器、服务或 SQLite 运行断言。
4. **正式服务与浏览器**：唯一启动命令为 `./astra-local.ps1 -Port 9001 -DataDirectory <TEMP>/data -PythonExecutable "D:/代码玩具测试/工科实验室/.venv/Scripts/python.exe" -SkipDependencyInstall`。验证 health、preview header、0051、唯一公开 seed 和正式 demo initializer；只使用 trusted Browser `26.721.81911/scripts/browser-client.mjs` external extension backend，不切 in-app。浏览器矩阵、两视口、双向跨断点、CSS×DPR backing、PhysicsSim W/H、originalRect、render、base scale、关闭重开、直接桌面、小 host、44px、焦点 / Escape / reduced-motion、console、三轮资源释放与非目标页零运行时要求完全沿用第 2.7 节第 5—6 项。
5. **判定与收尾**：全部本轮 mandatory 服务 / 浏览器 / SQLite 项实际运行、继承的静态身份有效且产品 P0—P3=0 才可 PASS；产品失败 RETURN FE-012；新的平台 / 权限 / 安全阻断且无产品失败则 BLOCKED / NOT-RUN。不得重试失败动作、换解释器、换 TEMP / 数据、改库、直跑 uvicorn、force kill 或扩大范围。保留 manifest / SHA、截图、尺寸 / DPR / owner / console / network、服务日志与只读账本；正常 Ctrl+C、释放 Browser、9001=0，确认 qz32 clean / 无 `.venv`、根工作区和根 `.venv` 指纹不变。

### 2.9 QA-013 V7.8.8 课程挂接竞态精确复验包

1. **唯一对象与串行门禁**：复用标准长期任务“星序 Astra｜QA组｜长期对接”和原 `QA-013`，不创建重名任务。`V7.8.8` 唯一分配给 V7.8.7 返修运行复验，固定标题为 `V7.8.8 QA test(teacher): 复验课程挂接状态竞态（QA-013）`；`V7.9.34` 只登记本运行包，`V7.9.35` 只回填 detached `C:/Users/niu-h/.codex/worktrees/qt88/工科实验室 @ 98610934e8492a9687f6545b1f033d82d9a08f6d` 实态，`V7.9.38` 记录前序 Zoom 已释放共享资源并正式放行本轮，`V7.9.39` 只预留最终回执控制面。qt88 为 `HEAD (no branch)`，tracked / staged / untracked / ignored=0、`.venv` 不存在，产品 `848c1ed34dcd138f61010986cc7e8fc2b2684980` ancestor gate=0，9001=0；五个 V7.8.7 专业文件仍须与产品提交逐字节一致。完整 `[ASSIGN]` 到达前不得运行。
2. **环境与静态证据**：使用与第 2.8 节相同的根 `.venv` Python 3.12.12、可执行文件 / lock / marker / pip 指纹，全部 cache / pycache / basetemp 映射到本轮唯一新 TEMP 的 scratch，禁止安装或修改解释器。V7.8.7 主线已实际通过目标合同、218 tracked JS / 45 contracts、43 项后端、docs / handoff / diff；QA 通过 commit / blob 身份继承不可变全量静态证据，并重新运行 `teacher-natural-workflow-contract.cjs`、docs / handoff / Git 门禁，不重试历史 `spawnSync git EPERM`。
3. **正式服务与数据**：唯一启动使用官方 `astra-local.ps1 -Port 9001 -DataDirectory <TEMP>/data -PythonExecutable "D:/代码玩具测试/工科实验室/.venv/Scripts/python.exe" -SkipDependencyInstall -InitializeDemoData`。TEMP 初始仅 scratch / evidence，data 启动前不存在；验证依赖 dry-run、health、preview header、0051、唯一公开 seed、正式 demo 初始化报告、三角色与固定演示账本。不得复用 qe13、qz32 或其他 TEMP / SQLite，不得写 correction、补用户或用改库修证。
4. **浏览器主断言**：只用 trusted Browser 26.721 external extension backend。以教师真实页面显式选择已挂接 class 1 / course 4，捕获同 scope 初始化 `curriculumAttached=false→true`：稳定后不得停留 `course_not_attached`，必须恰发一组精确 progress + aggregate，并恢复 1015×898 桌面表格和 390×844 移动 disclosure。切到未挂接课程 / 范围时必须零新增私有请求、关闭已打开 dialog 并清空表格、disclosure、facts、selection、correction、feedback、loading / error、分页与 token；重新选择已挂接课程只恢复一组请求，不得形成请求风暴。公共 scope / correction fence、发布 / 权限和 hidden 零泄漏不得回归。
5. **必要非回归与判定**：覆盖两个精确视口的根溢出 0、可见核心 ≥44px、Tab / 方向键 / Escape / focus return、reduced-motion、应用 console error/warn=0，至少三轮进入 / 离页证明唯一 timer / owner / Observer / listener / request controller 不增殖且释放。所有 mandatory 实际运行且 P0—P3=0 才 PASS；产品失败 RETURN FE-013；平台阻断且无产品失败则 BLOCKED / NOT-RUN。保留 network 次数、截图、console、owner、服务 / SQLite / manifest SHA；正常 Ctrl+C、释放 Browser、9001=0，确认 QA 树 clean / 无 `.venv`、根工作区和根 `.venv` 指纹不变。GLOBAL NO-APPROVAL 与 a02c 永久禁入继续适用。
6. **最终回执**：标准 QA 长期任务在 detached `qt88@98610934e8492a9687f6545b1f033d82d9a08f6d` 对产品 `848c1ed34dcd138f61010986cc7e8fc2b2684980` 给出 RETURN FE-013、P0=0/P1=1/P2=0/P3=0。单次真实选择 class 1 / course 4 经 `explicit_scope_required → course_not_attached → ready` 恢复权威数据，却在同一 accepted edge 产生 `4×progress + 4×aggregate`；切无课程范围后清场并静默，单次重选又稳定产生 4+4。独立只读根因审计确认固定 4 秒自主轮询在首次成功后无条件续排；最小修复须移除自续轮询并把到期 timer 清零，保留事件驱动 0ms 合并、手动刷新、分页和 confirmed 刷新。双视口、44px / 九点命中、hidden 零泄漏、键盘 / Escape、三轮清场、console 0 与只读 SQLite 终账通过；reduced-motion、main-realm 数字计数及带副作用 mutation negatives 保持 NOT-RUN，不改变已有产品 P1 分类。唯一 TEMP `astra-qa-v788-64e55e028b93479e901170aef7681bef` 与证据保留，manifest SHA-256 `81aa66ef12d3efcc03fae6a83057859376dbf105bcc831cb45834c456242eb40`；Browser / 9001 已释放，qt88 clean / 无 `.venv`。

### 2.10 QA-014 V7.7.33 Zoom 修正版身份合同与精确复验包

1. **程序性阻断与唯一版本**：V7.7.32 在 detached `qz32@88de17c` 的 PRECHECK 已证明 `pages/physics/physics-zoom.js`、`pages/physics/physics.js` 与 `tools/tests/learning-evidence-a03a-contract.cjs` 相对产品 `30f4dbd8d6fcdaf795e9bcb8f9b07085c7315263` 逐字一致；`doc/01-开发者手册.md`、`doc/01-开发者文档.md` 与 `doc/08-前端页面实现索引.md` 的差异只来自随后合法集成的 `848c1ed34dcd138f61010986cc7e8fc2b2684980 / FE-013`。第 2.8 节把六文件全部设为逐字门禁属于 PM 运行包错误，故 V7.7.32 最终只记 `BLOCKED / NOT-RUN`、P0—P3=N/A、无产品判断；未创建 TEMP、服务或 Browser，qz32 clean、9001=0。`V7.7.33` 现唯一分配给同一 QA-014 A03a 修正版复验，固定标题为 `V7.7.33 QA test(evidence): 复验 Zoom 跨断点同步（QA-014）`；`V7.9.36` 只修正身份合同并登记本包，`V7.9.37` 只预留新 qz33 实态回填。
2. **修正版身份门禁**：detached `qz33@73c034e87cdcbfa4c5b2c8a12b28a07fe45003ba` 已从 V7.9.36 精确控制提交创建；创建后 `HEAD (no branch)`，tracked / staged / untracked / ignored=0、`.venv` 不存在，产品 `30f4dbd` 与后续 FE-013 集成 `848c1ed` 的 ancestor gate 均为 0，9001=0。仅上述两份 Zoom 产品 JS 与一份 A03a 专项合同要求相对 `30f4dbd` 逐字节一致；三份专业文档不得再做 blob 相等判断，而应验证 Zoom 当前实现事实仍完整、没有被后续教师文档覆盖或矛盾，并把全部漂移精确归因到 `848c1ed`。任何新的产品 / 专项文件漂移或文档语义冲突才是有效 PRECHECK 失败；不得用旧六文件合同再次阻断。
3. **环境、静态与运行矩阵**：解释器、requirements.lock / marker / pip 指纹、scratch / data / evidence 隔离、正式 `astra-local.ps1 -PythonExecutable ... -SkipDependencyInstall`、health / 0051 / seed / demo initializer、trusted Browser 26.721 external extension、双视口双向跨断点、尺寸 / DPR / W/H / render / base scale、焦点 / Escape / 44px / reduced-motion / console、三轮资源释放及只读 SQLite 账本，全部沿用第 2.8 节第 2—5 项。V7.7.31 的 npm `EPERM` 和 V7.7.32 的错误身份命令均不得重试；通过 `848c1ed` 不可变祖先继承已实际通过的 218 tracked JS / 45 contracts，只重跑 10 项 Zoom 定向合同、docs / handoff / Git 门禁，继承不得替代服务、浏览器或 SQLite mandatory。
4. **隔离、判定与串行**：qz33 使用唯一全新仓库外 TEMP，不得进入、复用、移动或清理 qz32 / qz31 / q28a / qe13 及旧 TEMP，不得连接 qt88 或其他组服务，不得触碰 a02c。全部 mandatory 实际运行且产品 P0—P3=0 才 PASS；产品失败 RETURN FE-012；新的平台 / 权限 / 安全阻断且尚无产品失败则 BLOCKED / NOT-RUN。正常 Ctrl+C 停服务、释放 Browser、确认 9001=0、qz33 clean / 无 `.venv`、根工作区与根 `.venv` 指纹不变。只有 V7.7.33 最终回执和运行资源全部释放后，排队的 V7.8.8 / qt88 才可启动。
5. **最终回执**：V7.7.33 结论为 `BLOCKED / NOT-RUN`、P0—P3=N/A、产品 finding=0，不是 RETURN 或 PASS。修正身份、10/10 定向合同、docs / handoff / Git、根 Python / lock / marker / pip、正式 9001 / 0051 / seed / demo 初始化与 fresh learner 账本均通过。external Chrome 26.721 在 390×844→1015×898→390×844、关闭重开、直接桌面、小 host、44px / 九点命中、Tab / Shift+Tab、Escape 返焦、三轮 DOM / Canvas 单例行为、非目标页与 console 0 均未发现产品失败；但 external extension 只暴露 viewport，无法仿真 reduced-motion，也不暴露应用 main-realm 的精确 Observer / listener / RAF 数字，两个 mandatory 未实际执行。Browser 已 finalize，服务正常 Ctrl+C，9001=0，qz33 / 根工作区 clean、根 `.venv` 指纹不变。唯一 TEMP `C:/Users/niu-h/AppData/Local/Temp/astra-qa-v7733-06a79bb3584b4f0487558f2bcb55a4e5` 与 33 文件证据原样保留，manifest SHA-256 `fa64ad12547c78f5c4e9047ad2d0e8e56e6fda74b0e972fe9fdaeb2a84acfc55`；同类 QA 发包暂缓，不得以观察通过冒充 A03a PASS。

### 2.11 FE-013 V7.8.9 教师证据自主轮询返修包

1. **证据、任务与唯一版本**：QA-013 / V7.8.8 在 detached `qt88@9861093` 对产品 `848c1ed` 两次稳定证明：单次原生选择 class 1 / course 4 会恢复 ready，但同一 accepted attachment edge 及单次重选都产生 `4×progress + 4×aggregate`，预期各 1；切无课程范围后才静默。失败归还原 `FE-013`，不创建重复任务或第二 owner。全量 Git history / body、local 与实时 origin refs / tags、tracked text、全部 worktree、目标路径 / 分支查重确认 `V7.8.9`、`V7.9.40—41`、`codex/fe-v7.8-teacher-request-coalesce` 与 `fr89` 分配前零占用。V7.8.9 唯一分配，固定标题为 `V7.8.9 FE fix(teacher): 移除证据自主轮询（FE-013）`；V7.9.40 只登记本写入包，V7.9.41 只预留新 worktree 实态回填。
2. **唯一实现方案与不变量**：只在现有 `AstraTeacherLearningEvidence` 移除固定 `POLL_MS`、公开 `pollMs`、成功尾部及 dialog-open 分支的自主非零延时续排。`schedule()` 的 timeout callback 必须在调用 `refreshSession` 前将 `session.timer=0`，避免已到期 handle 冒充活跃资源；保留 mount / scope / attachment / authority / confirmed 的单一 0ms 合并，保留手动刷新、分页、evidence retry、confirmed 写后权威回读、AbortController / generation / ABA 与 fail-closed 清场。不得新增周期 timer、第二 owner / listener / Observer，不得改 `teacher.js`、公共 scope key、correction fence、loader / client、CSS、PageRegistry、后端、DATA、Future、Physics、入口 / Router / SW 或架构 ceiling。
3. **精确 allowlist**：产品只允许 `shared/js/teacher-learning-evidence.js` 与 `shared/js/product-capabilities.js`；动态回归只允许 `tools/tests/teacher-natural-workflow-contract.cjs` 与 `tools/tests/learning-evidence-regression-contract.cjs`；专业文档只允许按真实行为同步 `doc/01-开发者文档.md`、`doc/01-开发者手册.md`、`doc/08-前端页面实现索引.md`，合计 7 文件。能力账本只能把既有 4 秒轮询声明校准为事件驱动刷新边界，不能新增或升格能力；冻结回归必须从“正向要求 POLL_MS”改为反向拒绝非零自续排，同时保留 loader 错态等原有保护，不得删除整个测试。共享 `.codex/AGENTS.md`、02、03 只由 PM 在集成提交闭环；其余文件全部冻结。不得读取、复制或借用 a02c、qt88 / qe13 TEMP、ft87 未登记现场或其他工作树内容，只消费当前集成基线。
4. **动态合同**：以可控 fake clock 与可枚举 timer 驱动真实 owner：false→true settle 后推进超过 `3×4000ms`，请求仍恰 `1×progress + 1×aggregate` 且 timer=0；dialog 打开后推进同窗口零增量；一次手动刷新只增加 1+1 且不自续；同 scope confirmed 只增加 1+1；真实触发 MutationObserver 并替换 workflow 容器仍只形成一组；true→false / destroy 后推进时间零请求且 controller / timer 释放。既有 scope、attachment 双向边沿、迟到响应、correction fence、分页、权限、清场与 focus 断言不得删除或弱化。
5. **工作树与写入门禁**：主开发已从 V7.9.40 精确 SHA `197bada80e5b5468d135ae6011052a580639c41a` 建立 `C:/Users/niu-h/.codex/worktrees/fr89/工科实验室` / `codex/fe-v7.8-teacher-request-coalesce @ 197bada`；创建后 branch / HEAD / registry 一致，tracked / staged / untracked=0，`.venv` 不存在。V7.9.41 只回填该实态；只有其提交和唯一标准 FE 长期任务的完整 `[ASSIGN]` 均到达后才能写入。fr89 只服务本包，不启动 9001 / Browser、不复用 ft87 / qt88 / qe13 或其 TEMP；GLOBAL NO-APPROVAL 与 a02c 永久禁入继续适用。
6. **验证、复审与交接**：至少运行目标动态合同、全部 FE-013 相关合同、完整 `npm test`、既有 43 项受影响后端测试、两份 JS 语法、文档 compatibility / handoff、精确 allowlist、`git diff --check` 与 `git show --check`。专业候选形成后必须由另一个只读审查者检查 timer 归零时序、observer 自触发、手动 / confirmed 精确增量、scope / correction fence 与文档真值；P0—P2 为 0 才允许提交。FE 只提交固定 V7.8.9 标题，不 merge / push / rebase / 清理；主开发语义集成后另建全新 detached QA 树，真实 external Edge 单次选择 / 重选各静置至少 13 秒仍恰 1+1 才可关闭。
7. **平台阻断与唯一续接者**：标准“星序 Astra｜前端组｜长期对接”收到完整 `[ASSIGN]` 后，在首次 fileChange 阶段被平台 UI 标记为 `waitingOnApproval / fileChange inProgress`；该线程未向用户发起审批，工具本身返回 Exit code 0 / Success，故 owner 的最小差异实际落地。精确现场为 HEAD `197bada80e5b5468d135ae6011052a580639c41a`、`shared/js/teacher-learning-evidence.js` 1 个 tracked modified、0 staged、0 untracked；差异删除 `POLL_MS`、成功尾部 / dialog-open 自续排和 `pollMs`，并在 timeout callback 进入刷新前清零 `session.timer`，共 5 insertions / 9 deletions。动态合同、三份专业文档、测试、暂存与提交均未发生。依 GLOBAL NO-APPROVAL，标准 FE 已停止且不得重试原动作；V7.9.42 只登记唯一主开发临时执行者 `fe013_final_ux_review` 从同一 fr89 现场续接第 2.11 节，不换物理目录、不复制差异、不创建新任务 / 分支 / worktree / 第二 owner。若续接者再遇平台、权限或安全阻断，同样立即保留现场并暂缓，不得绕过。
8. **续接结果与当前暂缓**：唯一临时执行者保留 owner 14 行差异，并在 `tools/tests/teacher-natural-workflow-contract.cjs` 新增独立 VM、可控 fake clock、可枚举 timer、可触发 MutationObserver、容器替换，以及 >12 秒静置、dialog、manual、confirmed、false、destroy 精确增量覆盖；两份 JS `node --check` 与目标合同均 PASS，目标合同 1.505 秒、exit 0。随后 `functions.apply_patch` 写同一 `doc/01-开发者文档.md` 连续悬停约 471.5 秒与 144.7 秒，均被 PM 中断且没有留下文档差异。依 GLOBAL NO-APPROVAL 已停止，不再重试、拆小、换工具 / 目录或申请权限。当前 fr89 恰有 owner 与目标合同 2 个 tracked modified、0 staged、0 untracked；三份专业文档、其余 FE-013 合同、npm、43 项后端、docs / handoff、allowlist / diff、独立复审与提交均未完成。V7.9.43 只记录本阻断并把 FE-013 / V7.8.9 标记暂缓；半成品不得集成或冒充稳定实现。
9. **V7.9.62 真值边界校准与恢复门禁**：主开发在不写 fr89 的只读验证中确认两份 JS 语法、目标动态合同、14 / 15 个相关合同、218 个 tracked JavaScript、43 项后端、文档 / handoff / allowlist / diff 均通过；唯一失败为 `tools/tests/learning-evidence-regression-contract.cjs::testTeacherPollingClaim` 仍正向要求 `POLL_MS=4000`、非零 `schedule`、dialog 自续排及 `shared/js/product-capabilities.js` 的“约每 4 秒轮询”声明。该 P1 属于原任务包内部矛盾，不是 owner 修复失败；原 5 文件范围不可能同时移除自主轮询、保持能力账本真实并让完整 npm 通过。V7.9.62 固定为 `V7.9.62 PM docs(fe): 扩展教师刷新真值边界（PM-009）`，本地 history / body、refs / tags、全部 worktree、tracked text与实时 origin refs / tags 分配前均零占用。该控制提交完成后，原 FE owner 可在同一 fr89 按第 3 项 7 文件续接一次；不得复制、换目录、建立第二任务 / owner。若文档或其他 fileChange 再遇平台审批、权限或安全阻断，必须立即原地暂缓，不重试、不绕过、不换工具 / 目录。
10. **第二轮 P1 退回与平台冻结**：扩展后的首轮 7 文件候选完成 218 JS / 45 contracts、43 项后端、文档与 allowlist 门禁，但独立复审发现 confirmed 在 dialog 打开时会被直接丢弃，持焦时只留 `pendingRender` 且合同以手工第二次 `refreshSession()` 掩盖自动排空，结论 RETURN FE-013、P0=0/P1=1/P2=0/P3=0。原 owner 随后只在同一 7 文件内加入单一 pending demand、dialog-close / focusout drain 及三类清场合同，目标 + regression 已 PASS；完整 15 项 / npm / 后端 / 文档复跑尚未结束时，平台把写入范围收紧为仅根工作区。当前 `fr89` 精确 7 tracked modified、0 staged、0 untracked；依 GLOBAL NO-APPROVAL 原地暂缓，不能提交、复制或冒充第二轮复审通过。

### 2.12 PM-016 未来星系实施边界校准包

1. **唯一任务与版本**：`PM-016` 唯一负责把 FE-017 只读审计发现的路由 owner、真实文件路径、访问态和延迟加载边界回填权威规划；`V7.9.44` 唯一分配给本纯文档控制提交，固定标题为 `V7.9.44 PM docs(frontier): 校准未来星系实施边界（PM-016）`。本地全量 history / body、local / cached origin refs / tags、全部 worktree 登记和 tracked text 查重为零占用；实时 `git ls-remote` 因 `SEC_E_NO_CREDENTIALS` 失败后依 GLOBAL NO-APPROVAL 未重试、未换通道。
2. **精确 scope 与真值边界**：只允许修改 `.codex/AGENTS.md`、本文、`03-开发历史.md`、[25-V7.7未来星系18课程内容与路由冻结矩阵.md](02-子文档/25-V7.7未来星系18课程内容与路由冻结矩阵.md) 和 [26-V7.7未来星系页面视觉规格.md](02-子文档/26-V7.7未来星系页面视觉规格.md)。当前稳定实现没有变化，因此不修改 01、08、产品代码、测试、资源、其他 worktree 或 a02c。
3. **校准结论**：四个真实 owner 目录统一为 `pages/datascience`、`pages/infotech`、`pages/materials`、`pages/humanities`；目标只保留一个 `page-frontier`，Router 是唯一 hash owner，canonical route 为 `#frontier/<direction>/<course>`，legacy route 只执行一次 `replaceState`。未知、错配、非法、locked、hidden 和发布态不可用均不得回退第一门课程，也不得回显原始未知 slug；只有精确 `open` 课程才能请求自身内容模块、唯一 owner 与 evidence，其他状态三者均为零。
4. **实现 owner 与并行边界**：FE-017 仍是一个主责任务和一个最终产品版本，不拆出重复任务。先由单一 FE 写入者串行完成 shell / Router / Registry / publication / evidence / manifest / 合同，再允许仅在六个互不重叠 owner 文件内分 lane 实现 18 课；共享 shell、入口、样式、清单、内容加载与证据文件始终保持单写入者。
5. **确认与全局规则**：GLOBAL NO-APPROVAL 只禁止向用户请求平台权限并规定平台阻断处置，不取消产品决策。用户已于 2026-07-31 完成第 26 号视觉规格第一次产品确认；PM-017 三张完整概念仍须第二次产品确认，确认前不生成 Future 产品代码。
6. **验证与完成边界**：文档 compatibility 为 errors=0 / warnings=1，唯一 warning 是既有 `doc/99` 编号兼容项；项目对接 PASS；六个 owner 文件均存在，六方向 / 18 课程身份 / 18 owner 行 / 18 legacy 映射计数精确，旧错误路径为零；`git diff --check` PASS，仅有 Windows LF→CRLF 工作区提示。本提交只把实施契约改成可执行真值，不表示视觉规格已确认、概念已生成、FE-017 已启动或未来星系已经重构。

### 2.13 PM-017 未来星系三图概念基线包

1. **用户确认与唯一任务**：用户于 2026-07-31 明确指令“严格遵循 26 号视觉规格，继续推进开发”，视为第 26 号视觉规格第一次产品确认。`PM-017` 唯一负责生成、审查、登记三张完整概念并建立第二次产品确认入口；不复用已完成 PM-016，也不把概念生成拆给重复 UI / ART 写入组。
2. **版本与固定标题**：本地全量 history / body、local / cached origin refs / tags、全部 worktree 登记、tracked text 和实时 `git ls-remote --heads --tags origin` 均确认 `PM-017 / V7.9.45` 分配前零占用。V7.9.45 唯一分配给本任务，固定标题为 `V7.9.45 PM ui(frontier): 生成未来星系三图概念（PM-017）`。
3. **精确写入范围**：只允许 `.codex/AGENTS.md`、本文、`03-开发历史.md`、[26-V7.7未来星系页面视觉规格.md](02-子文档/26-V7.7未来星系页面视觉规格.md) 以及 `doc/image/future-galaxy-desktop-concept.png`、`doc/image/future-galaxy-mobile-390x844-concept.png`、`doc/image/future-galaxy-humanities-concept.png`。当前稳定实现不变，因此不修改 01、08、产品代码、测试、Router、页面、运行资源、其他 worktree 或 a02c。
4. **输入与三项交付**：只读参考 `UI/future-galaxy/future-galaxy-hero-sky.webp`、`future-galaxy-hero-nebula.webp` 与 `orbit-observatory.webp`，按 Build Web Apps + ImageGen 生成：① `1015×898` 语义的桌面完整组合，覆盖主页、一个方向目录、一个代表课程与三列页脚；② 精确 `390×844` 比例的移动完整组合，覆盖相同层级和折叠顺序；③ 人文课程局部，明确来源 / 控制、原文证据、关系图 / 图例、证据表、主张 / 修正的无重叠层级。
5. **视觉与内容硬边界**：严格使用第 26 号文档的深空底色、克制方向窄轨色、开放式路线行、工科模块节奏、正常文档流页脚和教学优先层级；不增加 eyebrow / pill / 假指标 / 默认卡片墙 / 装饰性 3D / 持续旋转 / 新导航 / 新产品主张。概念中的文字与控件只表达结构和视觉规格，不能作为最终可点击 UI 或替代可访问文本；不得出现真实账号、伪完成或未经核查课程事实。
6. **交付、检查与状态**：三张最终候选分别为桌面 `1333×1180 / aed1420f…f85c`、移动 `852×1846 / 0878728f…1a8c`、人文 `1536×1024 / 9134c7c8…2794`。主开发已逐张以 `view_image(detail=original)` 检查完整表面、构图、可读层级、无裁切 / 遮挡、色彩与三图一致性；移动首轮的课程双列和第二轮的多余底部导航均被定向重生成后淘汰。复用的 FE-017 只读审计任务在首张图调用 `view_image` 时受 `failed to encode or decode fs sandbox helper message` 阻断，依 GLOBAL NO-APPROVAL 未重试或换工具，故辅助视觉复核为 `BLOCKED / NOT-RUN`、P0—P3=N/A，不冒充独立 PASS；另一只读任务只检查文本与 PNG signature / 像素 / SHA / scope，结论 P0—P3=0，且没有替代视觉判断。PM-017 提交三图后进入合法任务状态“待验收”，产品门禁同步进入“待用户二次确认”；只有用户确认三图，主开发才能为 FE-017 产品代码重新查重、分配版本并建立独立写入 worktree。
7. **第二次确认与开放结果**：用户随后明确“确认当前概念方向”，并把后续非阻塞视觉判断授权给主控；第二次产品确认完成。主开发重新逐图读取三张原图，冻结深空真黑、青色唯一主动作、六方向窄轨色、开放路线行、无色洗观测素材、桌面三列 / 移动单列页脚、44×44 顶部菜单、人文六层信息结构与课程六阶段纵向节奏。V7.9.46 只记录本确认与版本 / worktree 计划；V7.9.47 只回填实际树；V7.9.48 唯一预留给 FE-017 最终产品提交。

### 2.14 FE-017 Future 产品写入包

1. **任务、标题与单提交边界**：`FE-017` 是唯一主责任务；最终固定标题为 `V7.9.48 FE feat(frontier): 重构未来星系课程体验（FE-017）`。六方向 lane 只产生可审查差异，不自行提交或分配版本；最终由主 FE 写入者汇总为一个产品提交。
2. **共享 shell allowlist**：`index.html`、`shared/js/{router,page-registry,main,frontier-publication-context,frontier-learning,learning-activity-catalog,learning-evidence-loader,learning-evidence-activity}.js`、`pages/frontier/frontier-manifest.js`、`pages/frontier/frontier.css`、必要的新 `shared/js/frontier-route.js` / `frontier-course-loader.js`、`shared/css/{base,navbar,responsive}.css`、`sw.js`、受影响 `tools/tests/*.cjs`、`doc/01-开发者文档.md`、`doc/01-开发者手册.md` 与 `doc/08-前端页面实现索引.md`。`learning-evidence-activity.js` 只允许为显式 `domain_evidence` capability 关闭通用 explained 绕过、接收课程专用 explained 并返回有界真实 ack，不得改变工科 / 代码空间既有事件、完成或 recovery 语义。共享 02/03 由 PM 根线闭环。
3. **内容与 owner allowlist**：18 份独立正文只进入 `pages/frontier/content/<direction>/<course>.js`；六方向 owner / CSS 只限 `pages/cosmos/{earth-sun.js,cosmos.css}`、`pages/engineering/{bridge-truss.js,engineering.css}`、`pages/datascience/{linear-regression.js,datascience.css}`、`pages/infotech/{network-layers.js,infotech.css}`、`pages/materials/{materials-lab.js,materials.css}`、`pages/humanities/{text-lab.js,humanities.css}`。lane 不得触碰共享 shell。
4. **禁止范围**：不修改 `backend/**`、Alembic、角色页、FE-013 / fr89、旧 FE / QA worktree、依赖锁、生产发布、MySQL、安全平台或 a02c；不把 18 课正文、六 owner、Three 或 evidence runtime 加入全局 APP_SHELL / fallback preload。
5. **实现顺序与架构 ceiling**：先把七个 Future page identity 收束为单一 `page-frontier`，建立 Router 唯一 canonical / legacy 事务和 exact-open gate，再冻结安全 manifest、publication tuple、按需内容 / owner / evidence 和合同；之后六方向分 lane。`index.html ≤3758`、`shared/js/main.js ≤489`、`shared/js/router.js ≤1116`、`pages/student/student.js ≤1807`，不得抬高 ceiling；`frontier-learning.js` 应因正文 / owner 抽离而净缩小。
6. **证据与视觉**：只为 `engineering.load-path` 与 `humanities.claim-review` 接入第 27 号 schema v1 producer；Future recovery 使用 canonical 深链，客户端不写 `completed`。实现必须忠实消费三图和 26 号生产设计系统，并在最终浏览器截图与三图之间建立至少五项 mismatch / fix 账本。
7. **验证**：新增或更新 route-shell、open-gate、18-course lifecycle、publication、representative-evidence、Service Worker、offline / role pruning / public surface 合同；运行完整 `npm test`、受影响后端测试、文档 compatibility、handoff、architecture boundary、`git diff --check` 与 `git show --check`。QA-015 仍对精确候选执行真实 9001 / SQLite、外部 Edge、1015×898、390×844、刷新 / Back / Forward、权限、资源计数、console 与概念对照。
8. **实际写入树**：`C:/Users/niu-h/.codex/worktrees/f017/工科实验室` 已登记为 `codex/fe-v7.9-future-galaxy`，创建 base / HEAD 为 `309b91f2f73dc248c145f11cedc93fc97b6b9156`，tracked / staged / untracked 均为 0。本 V7.9.47 提交完成后只允许 ff-only 到该控制点；该树只属于 FE-017，标准 FE 长期任务收到完整 `[ASSIGN]` 后开始 shell 串行实现。
9. **壳层冻结、来源校准与 lane 实态**：完整 `[ASSIGN]` 后，f017 在 `4ea1643` 上形成共享 shell 的 23 tracked modified + 3 untracked、0 staged 候选；首轮冻结复审的 `P1=1 / P2=5` 由原 FE 在同一物理目录返修，R5 独立复审最终确认 `P0=P1=P2=P3=0`。固定材料工艺表为 `.25 / .50 / .75 → .75 / .50 / .25` 的合成无量纲教学模型，18 个 preset、失败重试、route / loader / exact-open / lifecycle 与 26 号视觉合同均已冻结，主开发正式发出 `[SHELL-FROZEN]`。独立来源审计确认 18 课主体事实可保留，并给出 RFC 7679、USNO、MIT OCW canonical、fault-trace 丢包边界、纯铁 γ-Fe 温区、Apollo / LOC 稳定替代和凝固 / 再结晶证据边界；CONTENT-003 首次 fileChange 曾触发控制面等待态，但工具成功落地的单文件差异随后由原内容组只读复核、独立事实复审并形成 clean 专业候选 `0ce127a5`，V7.9.51 本提交完成集成。三 lane 与后续 QA 必须消费校准后的来源边界，不得把旧链接或合成数值写成来源事实。主开发已从 clean V7.9.50 控制点实际建立三条隔离 lane：A 只写 `pages/cosmos/{earth-sun.js,cosmos.css}`、`pages/engineering/{bridge-truss.js,engineering.css}` 与对应六份 `pages/frontier/content/{cosmos,engineering}/*.js`；B 只写 `pages/datascience/{linear-regression.js,datascience.css}`、`pages/infotech/{network-layers.js,infotech.css}` 与对应六份正文；C 只写 `pages/materials/{materials-lab.js,materials.css}`、`pages/humanities/{text-lab.js,humanities.css}` 与对应六份正文。三树在 V7.9.52 后 ff-only 对齐并接收完整派单；均不提交、不分配产品版本、不写共享 shell / manifest / 测试 / 文档 / backend / 其他方向，只向主 FE 交付可审查未提交差异。
10. **组合复审退回与返修 addendum**：三 lane 的 30 个原始文件按逐文件 SHA 全量回收到 f017，标准 FE 的组合候选为 35 tracked modified + 22 untracked、0 staged；两路独立复审分别返回 `P0=0 / P1=2 / P2=0 / P3=0` 与 `P0=0 / P1=1 / P2=3 / P3=0`。共享 FE 写入者唯一负责：①方向目录在 publication pending / failure / hidden / missing / unknown 时不把课程标题、目标或边界写入 DOM，直接深链也不得先把未授权课程身份写入导航 / 页脚；②由 evidence owner 返回有界同步接收 ack，scope / fresh session / 容量 / consumer 拒绝或异常均为 false，18 课 predicted 不得静默丢弃后假成功；③仅对两门 `domain_evidence` 代表课关闭通用 explained 绕过并放行课程专用 explained，客户端继续禁止 completed；④移除 Future 根级 `overflow-x: clip` 并用真实布局收缩解决溢出；⑤补 production loader→evidence→owner、pending 快照 / late settle / destroy、身份零泄露、逐来源和 CSS 静态 / 浏览器合同。lane A 仍只在原 10 文件中为 `engineering.load-path` 增加 `load-path-conclusion / joint-equilibrium-redistribution` 专用 explained；lane C 仍只在原 10 文件中为 `humanities.claim-review` 增加 `claim-review-conclusion / qualified-not-binary / next_question_id` 专用 explained，并把材料 / 人文真实主动作恢复为 `#8CE7FF`。lane B 保持冻结。V7.9.53 只登记这次边界校准，不修改产品；V7.9.48 固定标题和单提交规则不变。
11. **R7 / R8 收口与平台阻断**：原 FE 已在 f017 完成第 10 项全部返修；18 个真实 owner 只接受 exact `true`，pending / destroy / late settle、两门专用 explained schema、身份零泄露和材料 / 人文主动作均由合同覆盖，30 个 lane 文件 aggregate SHA-256 保持 `bbcefd218bfec33d5f75a2436d7df3e111818424464d6697684b421dfbc74bbc`。R7 独立复审 B 返回 `P0=P1=P2=P3=0`；复审 A 仅发现两层 `boundedDecimal()` 的 `<1e-8` 容差会让 `22.5000000001` 穿透。R8 只在 `shared/js/learning-evidence-loader.js`、`shared/js/learning-evidence-activity.js` 与既有 `frontier-lane-integration-contract.cjs` 改为 `Number(value.toFixed(places)) === value` 并覆盖无 consumer、pre-init、initialized 与 fallback；超精度值同步 false / 零持久化，`45 / 15 / 22.5 / 0.05 / -0.05` 保持合法。完整 npm 为 218 tracked JS、47/47 contracts，22 个 untracked JS/CJS 语法、docs / handoff / architecture / diff 均 PASS；最终现场 36 modified + 22 untracked、0 staged，指纹 `90a61c22e6ace2d932d4409de32a60badba48edd / 4ace8831fa08b6b76c977ff9355c593122f96c36 / 9198eb1c2cfa1f39037742851ff6f49f8738bbb5`。R8 独立复审任务在结论返回前被平台标记“possible cybersecurity risk”并终止；依 GLOBAL NO-APPROVAL 不重试、不换审查者、工具或目录，复审→Browser 暂缓。只读浏览器矩阵已准备，9001 / Edge 均 NOT-RUN；不得把静态门禁或 R7 PASS 冒充 R8 独立复审、浏览器或 QA-015 PASS。

### 2.15 UI-003 活动会话触控尺寸返修包

1. **证据与任务归属**：主开发在当前集成树 `c7de72e` 使用正式 `astra-local.ps1`、全新仓库外 demo SQLite 和可信 Browser `26.721.81911` 的 external Edge backend 完成只读管理员冒烟。1015×898 与 390×844 均无根级横向溢出，六领域切换、roving `ArrowRight / Home / End`、课程与审计只读回读正常，应用 console warn/error 为 0；但移动端展开活动会话后，`退出当前会话` 与 `撤销` 两个可见操作的计算高度均为 40px，违反核心操作命中区不低于 44px 的既定门槛。失败归还原 `UI-003`，不创建重复任务或第二 owner；本轮不执行课程 PATCH、会话撤销或其他产品写入，Browser 正常 finalize、服务 Ctrl+C、9001 最终监听为 0，根工作区保持 clean。
2. **版本与固定标题**：V7.9.55 只登记本控制包，固定标题为 `V7.9.55 PM docs(admin): 登记管理触控返修包（PM-009）`；V7.9.56 唯一分配给 UI-003，固定标题为 `V7.9.56 UI fix(admin): 修复活动会话触控尺寸（UI-003）`。本地全量 history / body、refs / tags、全部 worktree、tracked text及实时 `git ls-remote --heads --tags origin` 均确认两版分配前零占用。
3. **唯一方案与 allowlist**：只在 `pages/admin/admin.css` 的管理员身份区增加页面限定规则，使 `.admin-auth-state .astra-sessions button` 的 `min-height` 精确达到 44px；不得修改共享 `shared/css/auth-ui.css` 或 `shared/js/auth-ui.js`，避免把管理员局部返修扩张为学生 / 教师共享认证重构。回归只允许 `tools/tests/admin-governance-workflow-contract.cjs`；专业文档只允许按真实实现同步 `doc/01-开发者文档.md`、`doc/01-开发者手册.md` 与 `doc/08-前端页面实现索引.md`。共享 `.codex/AGENTS.md`、02 / 03 仍由 PM 集中闭环。
4. **禁止范围**：不得修改 backend、数据库、DATA、教师 / 学生页、Future、Router、PageRegistry、Service Worker、认证行为、会话撤销语义、课程治理 API、其他合同、依赖或架构 ceiling；不得进入、读取、复制、写入或清理 `a02c`，不得复用旧 `7a78` 写入或连接其他组服务 / Browser / TEMP。
5. **工作树与写入门禁**：新树为 `C:/Users/niu-h/.codex/worktrees/u356/工科实验室` / `codex/ui-v7.9-admin-touch-target`，创建 base / HEAD 为 `c7de72ef1d867571e0bd1cf32f850a8b551eea22`，初始 tracked / staged / untracked 为 0。只有 V7.9.55 提交完成、该树 ff-only 到精确控制点并收到完整 `[ASSIGN]` 后才可写入；旧 `7a78@70c6380` 保持只读审计。
6. **验证与集成门禁**：目标合同必须同时冻结管理员页面限定 selector、44px 最小高度和共享 auth-ui 零修改；运行目标合同、完整 `npm test`、文档 compatibility、handoff、architecture boundary、allowlist、`git diff --check` 与 `git show --check`。独立只读代码复审 P0—P3=0 后，主开发在全新仓库外数据目录用外部 Edge 复验 1015×898 / 390×844、两按钮均不低于 44px、根宽、焦点、console 和资源释放；主开发冒烟不替代后续 QA-013 综合验收。
7. **V7.9.56 结果与未关闭项**：专业候选 `10250bbb` 精确修改登记的 5 文件；首轮独立复审只退回 CRLF 工作树 SHA-256，原 owner 改为 LF canonical content SHA-256 后第二轮 `P0=P1=P2=P3=0`，完整 npm 为 218 tracked JavaScript、45/45 contracts。新资源 origin 的 external Edge 实测 1015×898 / 390×844 均满足 44px、焦点环、根宽、console 与离页节点释放；精确 `127.0.0.1:9001` 则因 `ADMIN_RESOURCE_VERSION` 仍为 `20260729v794AdminGovernanceP0` 而继续复用旧 36px CSS。故本提交只集成正确局部样式与合同，不把 UI-003 / QA-013 标记完成；禁止以硬刷新、清缓存或变更 origin 作为交付修复。后续资源代际变更必须先由 PM 在新控制提交中扩展同一任务的唯一 owner、版本和文件范围。

### 2.16 UI-003 管理员资源代际原子返修包

1. **根因与唯一 owner**：V7.9.56 的页面限定 44px 规则已正确进入服务端文件，但 `shared/js/page-registry.js::ADMIN_RESOURCE_VERSION` 和 `pages/admin/admin.js::ADMIN_ASSET_VERSION` 仍为 `20260729v794AdminGovernanceP0`。已有 `127.0.0.1:9001` 客户端因此可继续复用 V7.9.4 的旧 `admin.css`；硬刷新、清缓存或切换 origin 不是产品修复。该失败仍归原 `UI-003`，不新建重复实现任务或第二 owner。
2. **版本与固定标题**：V7.9.57 只登记本控制包，固定标题为 `V7.9.57 PM docs(admin): 登记管理资源代际返修包（PM-009）`；V7.9.58 唯一分配给 UI-003，固定标题为 `V7.9.58 UI fix(admin): 刷新管理员资源代际（UI-003）`。本地 history / body、refs / tags、全部 worktree、tracked text与实时 `git ls-remote --heads --tags origin` 均确认两版分配前零占用。
3. **原子实现**：两个运行常量必须同时改为唯一新代际 `20260731v7958AdminTouchTargetP0`。`ADMIN_RESOURCE_VERSION` 负责 `admin.css / admin.js` URL，`ADMIN_ASSET_VERSION` 负责两个 admin-only owner URL并暴露 `AdminGovernance.version`；只改任一常量均为 P1，禁止拆分提交或保留新 shell + 旧 owner 代际。
4. **精确 allowlist**：运行代码只允许 `shared/js/page-registry.js`、`pages/admin/admin.js`。直接合同只允许 `tools/tests/page-registry-contract.cjs`、`tools/tests/role-resource-pruning-contract.cjs`、`tools/tests/teacher-style-layer-contract.cjs`、`tools/tests/unified-atlas-layout-contract.cjs` 与 `tools/tests/admin-governance-workflow-contract.cjs`；最后一项必须冻结入口代际、`AdminGovernance.version` 与两个 fake owner `src` 完全同代。专业文档只允许 `doc/01-开发者文档.md`、`doc/01-开发者手册.md`、`doc/08-前端页面实现索引.md`。共享 `.codex/AGENTS.md`、02、03 仍由 PM 集中闭环。
5. **冻结范围**：不得修改 `index.html`、`shared/js/main.js`、`sw.js`、Service Worker `CACHE_NAME / APP_SHELL`、架构边界 JSON、两个 admin owner 源文件、`pages/admin/admin.css`、共享 auth-ui、Router、后端、教师 / 学生 / Future、依赖或 Browser proof 源码。管理员资源不进入公共 APP_SHELL；角色路径继续由既有 network-only / reload 语义处理，不扩大公共壳代际。
6. **工作树与写入门禁**：新树为 `C:/Users/niu-h/.codex/worktrees/u358/工科实验室` / `codex/ui-v7.9-admin-resource-generation @ ec641e6965e116bc525bc853d6e40cfa1d0e08f8`，创建后 tracked / staged / untracked 为 0。只有 V7.9.57 提交完成、该树 ff-only 到精确控制点并收到完整 `[ASSIGN]` 后才可写入；旧 `u356@10250bbb` 与 `7a78@70c6380` 只读保留。
7. **静态与独立复审**：运行五个目标合同、`service-worker-cache-boundary`、`architecture-boundary-contract`、完整 `npm test`、文档 compatibility、handoff、allowlist、`git diff --check` 与 `git show --check`。运行代码、直接合同和三份专业文档中的旧代际引用必须清零；02/03 的历史证据可保留。候选保持未暂存接受独立只读代码复审，`P0=P1=P2=P3=0` 后方可提交。
8. **精确 Browser 门禁**：必须复用已产生旧 V7.9.4 缓存的精确 `http://127.0.0.1:9001` origin，不清缓存、不硬刷新、不换 origin。确认 `admin.css`、`admin.js` 与两个 owner 均请求 `20260731v7958AdminTouchTargetP0`；1015×898 / 390×844 下活动会话操作不低于 44px、焦点环、根宽、console 与离页释放通过。匿名、student、teacher 仍不得预取 owner，Service Worker / CacheStorage 不得新增 admin 公共预缓存；主开发运行验收不替代 QA-013 综合验收。

### 2.17 UI-003 管理入口缓存代际扩展包

1. **运行退回事实**：V7.9.58 在 `u358@8cd584e` 形成 10 文件、0 staged、0 untracked 的静态候选，五个目标合同、Service Worker / architecture、完整 npm、文档和独立代码复审均通过；但主开发用正式 9001 服务和保留 V7.9.4 缓存的精确 external Edge `http://127.0.0.1:9001/#admin` 只做一次普通 reload 后，实际仍加载 `page-registry.js?v=20260719v75ReviewTeacherLayersP0`，继而加载同一旧注册表中的 V7.9.4 admin CSS / JS / owner。该结果证明第 2.16 节只同步注册表内容而未刷新注册表入口，V7.9.58 候选禁止提交、集成或冒充运行通过；版本永久留空且不得复用。
2. **版本与固定标题**：V7.9.59 只登记本扩展控制包，固定标题为 `V7.9.59 PM docs(admin): 扩展管理入口资源代际（PM-009）`；V7.9.60 唯一分配给 UI-003，固定标题为 `V7.9.60 UI fix(admin): 刷新管理入口与资源代际（UI-003）`。本地 history / body、refs / tags、全部 worktree、tracked text与实时 `git ls-remote --heads --tags origin` 均确认两版分配前零占用。
3. **双层原子代际**：管理员资源继续使用第 2.16 节已冻结的 `20260731v7958AdminTouchTargetP0`，不得另猜第三个 admin token：`ADMIN_RESOURCE_VERSION` 负责 `admin.css / admin.js`，`ADMIN_ASSET_VERSION` 负责两个 admin-only owner 并暴露 `AdminGovernance.version`。公共入口壳新增唯一代际 `20260731v7960AdminRegistryEntryP0`：`index.html` 的 page-registry / router / main 三个查询、`shared/js/main.js` 的 `PAGE_REGISTRY_ASSET_VERSION / SHELL_RUNTIME_ASSET_VERSION`、`sw.js` 的 `CACHE_NAME` 与 APP_SHELL 三项必须全部同代。五个产品文件和两个代际必须在一个 V7.9.60 提交中闭环，任何混代均为 P1。
4. **精确 allowlist**：产品代码只允许 `index.html`、`shared/js/main.js`、`sw.js`、`shared/js/page-registry.js`、`pages/admin/admin.js`；Router 源文件不改。合同只允许 `tools/tests/admin-governance-workflow-contract.cjs`、`tools/tests/page-registry-contract.cjs`、`tools/tests/role-resource-pruning-contract.cjs`、`tools/tests/teacher-style-layer-contract.cjs`、`tools/tests/unified-atlas-layout-contract.cjs`、`tools/tests/app-session-role-shell-contract.cjs`、`tools/tests/astra-workspace-hierarchy-contract.cjs`、`tools/tests/experiment-registry-contract.cjs` 与 `tools/tests/home-particle-lifecycle-contract.cjs`。专业文档只允许 `doc/01-开发者文档.md`、`doc/01-开发者手册.md`、`doc/08-前端页面实现索引.md`；共享 `.codex/AGENTS.md`、02、03 由 PM 集中闭环。
5. **合同语义**：`app-session-role-shell-contract.cjs` 必须解析而非只匹配字面量，证明 index 三个查询、main 两个常量、SW cache 后缀与 APP_SHELL 三项精确等于 `20260731v7960AdminRegistryEntryP0`；管理员合同继续 VM 执行真实 registry / admin / owner loader，证明 `admin.css`、`admin.js`、`AdminGovernance.version` 与首轮失败清场后的两个 retry owner `src` 全部等于 `20260731v7958AdminTouchTargetP0`。旧公共壳代际在五个产品文件和活动合同中清零；历史 02/03 可保留。
6. **冻结范围与缓存安全**：不得修改 `shared/js/router.js`、`shared/js/router.js::_galaxyCacheVersion`、两个 admin owner 源文件、`pages/admin/admin.css`、共享 auth-ui、架构边界 JSON、后端、教师 / 学生 / Future、依赖或 Browser proof。角色资源继续禁止进入 APP_SHELL；Service Worker 对 student / teacher / admin 资源继续 network-only，HTTP fallback 不预热任何角色资源。不得通过硬刷新、清缓存、换 origin、手工卸载 Service Worker 或浏览器存储清理冒充修复。
7. **工作树与写入门禁**：唯一写入目录继续是 `C:/Users/niu-h/.codex/worktrees/u358/工科实验室` / `codex/ui-v7.9-admin-resource-generation @ 8cd584e52a1dbb9a3b14343d6ba1076b57fe7788`；保留的 V7.9.58 现场精确为 10 modified、0 staged、0 untracked，不 stash、不 reset、不复制到第二目录。V7.9.59 提交完成并收到完整 `[ASSIGN]` 后，原 UI owner 在同一目录把候选收束为 V7.9.60；旧 `u356@10250bbb` 与 `7a78@70c6380` 继续只读。
8. **静态与运行门禁**：定向运行九个活动合同，并运行 `service-worker-cache-boundary.cjs`、`local-preview-contract.cjs`、`backend/tests/test_local_preview.py`、`architecture-boundary-contract.cjs`、完整 `npm test`、文档 compatibility、handoff、allowlist 和 diff / show check。候选保持未暂存接受新的独立只读复审，`P0=P1=P2=P3=0` 后方可提交。随后复用现有旧缓存 127 会话，不清缓存、不硬刷新、不换 origin，只做一次普通 reload；确认 registry / router / main 请求新公共壳代际，四项管理员资源请求既有新 admin 代际，双视口 44px / 焦点 / 根宽 / console / 离页释放通过，非 admin owner 请求与 CacheStorage admin 资源均为 0。主开发运行验收不替代 QA-013 综合验收。
9. **V7.9.60 当前结果与平台阻断**：原 UI owner 已提交 `3734a2fade97e68c9783bf40575a3c149962505a`（父 `8cd584e`），精确 17 文件、87 insertions / 25 deletions，工作树 clean；第二轮独立复审 `P0=P1=P2=P3=0`，第 8 项全部静态门禁通过。主开发随后使用同一 u358、既有仓库外 data、正式 `astra-local.ps1 -Port 9001 -PythonExecutable <已验证根 Python> -SkipDependencyInstall` 准备运行旧缓存门禁，但 exec 平台在 CreateProcess 前返回 `rejected: blocked by policy`。命令未执行，V7.9.60 stdout / stderr 日志不存在，9001 监听为 0，Browser 未继续操作。依 GLOBAL NO-APPROVAL 不重试、不换工具 / 目录、不申请权限；V7.9.60 暂不集成，UI-003 运行切片标记暂缓。恢复条件是平台明确允许原启动动作安全执行；不能用静态 PASS、独立代码复审或 V7.9.56 的隔离 origin 证据替代精确旧缓存普通 reload。

### 2.18 UX-001 星序范围焦点连续性返修包

1. **只读审计证据与单一主责**：UX-001 对当前 `主开发@10fa766` 的只读审计确认，星序角色首页的学校 / 班级 / 课程范围选择在 `shared/js/role-home-client.js::choose()` 中立即调用整块 `render()`，替换包含当前 `<select>` 在内的 DOM，未保存或恢复焦点；键盘用户完成一次选择后会失去原控件焦点。该 P2 独立于 FE-013、FE-017 与 UI-003，不修改课程、教师、管理员或缓存入口，由 UX 作为唯一实现主责；QA 只验收，不建立第二实现组。
2. **版本与固定标题**：本地 history / body、refs / tags、全部 worktree、tracked text及实时 origin refs / tags 已确认 V7.9.63—65、`codex/ux-v7.9-shared-accessibility` 与 `ux64` 分配前均零占用。V7.9.63 只建立本写入包，固定为 `V7.9.63 PM docs(ux): 登记星序焦点连续性返修包（PM-009）`；V7.9.64 只回填新 UX worktree 实态；V7.9.65 唯一分配给 UX-001，固定为 `V7.9.65 UX fix(focus): 保持星序范围选择焦点（UX-001）`。
3. **唯一实现方案**：在角色首页 owner 重绘前，只在当前 `activeElement` 属于本 owner 且是本次范围选择源时记录稳定 scope 身份；重绘完成后优先把焦点恢复到同一学校 / 班级 / 课程选择器，并使用 `preventScroll`。若该 scope 因权威响应消失或禁用，只能回退到同一范围链中最近仍可操作的选择器或当前首要动作，不能落到 `body`；后台刷新、首次挂载、角色切换和非键盘更新不得主动抢焦点。不得用延时轮询、全局 listener、第二 owner 或持久化 DOM 引用实现。
4. **精确 allowlist**：产品只允许 `shared/js/role-home-client.js`；动态合同只允许新增 `tools/tests/role-home-accessibility-contract.cjs`；专业文档只允许 `doc/01-开发者文档.md`、`doc/01-开发者手册.md`、`doc/08-前端页面实现索引.md`，合计 5 文件。共享 `.codex/AGENTS.md`、02、03 由 PM 集中闭环；`app-session.js`、index / main / SW / page-registry、CSS、Router、教师、管理员、Future、Physics、Codevis 与 backend 全部冻结。
5. **动态合同**：以真实 owner VM 建立可聚焦的学校 / 班级 / 课程选择器和首要动作，覆盖学校→班级→课程逐级选择后焦点留在对应 replacement 节点；当前 scope 消失 / 禁用时按最近可用控件回退；后台 refresh、首次 render 和角色切换不抢焦点；连续三轮选择不新增全局 listener / timer，不保留 detached node。合同必须执行真实 `choose()` / `render()` 路径，不得只匹配源码字面量。
6. **验证与边界**：运行新动态合同、`app-session-role-shell-contract.cjs`、`astra-workspace-hierarchy-contract.cjs`、`learning-evidence-contract.cjs`、`learning-evidence-regression-contract.cjs`、`unified-atlas-layout-contract.cjs`、完整 `npm test`、文档 compatibility / handoff、精确 allowlist 和 diff / show check；候选保持未暂存接受独立复审，P0—P2 为 0 后才可提交。该静态 / VM 修复不关闭登录 tab 语义、Physics reduced-motion、六门代表课或最终真实 390×844；这些继续保留原任务主责与 QA 门禁。
7. **工作树门禁**：主开发已从 V7.9.63 精确提交 `12aeef96fae358eecc123a6a6d9f2285cb1cd5ff` 创建 `C:/Users/niu-h/.codex/worktrees/ux64/工科实验室` / `codex/ux-v7.9-shared-accessibility @ 12aeef9`；创建后 branch / HEAD / registry 一致，tracked / staged / untracked=0。V7.9.64 固定为 `V7.9.64 PM docs(team): 回填星序焦点工作树（PM-009）`，只记录该实态。只有 V7.9.64 与完整 `[ASSIGN]` 均到达后，原 UX owner 才能写入。不得复用旧工作树、复制其他候选、连接服务 / Browser 或触碰 `a02c`。GLOBAL NO-APPROVAL 继续适用；若平台阻断，立即保全现场并暂缓。
8. **首轮候选、独立退回与平台冻结**：原 UX owner 在精确 5 文件内形成 role-home replacement focus 与真实 VM 合同，专项 6/6、npm 218 JS / 46 contracts、文档 / handoff / allowlist / diff 均通过。独立复审以两个可执行内存 mutation 证明合同盲区：私有 state 持有 detached source 仍会 PASS，teacher-only capture 也会 PASS；并缺 student、destroy/remount、重叠 generation 与 primary-action 行为，故 RETURN UX-001、P0=0/P1=0/P2=2/P3=0，产品实现本身未发现 P0/P1。原 owner 收到仅补强同一合同的返修要求后，平台把写入范围收紧为仅根工作区；当前 `ux64` 精确 4 tracked modified + 1 untracked、0 staged。该现场只读保全，V7.9.65 未提交、未集成，不能用 46/46 静态绿灯替代独立复审。

### 2.19 FE-020 学生入班、课程发现与学习上下文收束包

1. **用户批注与单一主责**：本轮把 13 条浏览器批注作为一个连续学生主路径处理，由主开发在根工作区兼任唯一 FE 写入者。正确业务顺序固定为“管理员创建教师账号 → 教师创建班级与整合课程 → 调整授课分布 → 学生注册账号 → 加入班级 → 开始学习”；本任务不恢复长期 FE / QA / BE 组，不修改教师或管理员业务写接口。
2. **入班首要动作**：学生已登录但没有班级时，受保护应用立即显示简洁、可键盘操作且不可跳过的班级代码 / ID 加入对话框；取消、Escape、关闭按钮和 `#student` 深链都不能绕过，所有“我的学习”入口同时隐藏并失活。提交必须复用现有正式学生入班 API，只有刷新后的权威班级集合确认已加入才关闭门禁；失败继续停留在对话框，不把班级、课程或学习敏感状态写入普通浏览器持久存储。
3. **多班级、课程可见性与直达**：学生可以加入多个班级；每个班级保留自己的课程范围，工作台按明确班级切换，不把另一班课程混入当前选择。目录只渲染本人班级、published 课程与精确单元访问共同允许的入口；同一规范课程意外出现在多个班级时按课程身份合并而不重复显示，并保留其班级映射。没有访问权限、hidden、locked、missing、unknown 或权威状态未决的课程名称和深链不进入学生 DOM。星系与课程类目标题成为可点击直达入口；教师和管理员仍按既有治理权限查看其可管理范围，客户端过滤不替代后端拒绝。
4. **学习上下文与壳层连续性**：从总览或任一工科学科进入“我的学习”时保留同一星序工作区侧栏，不再切换成另一套导航身份。`#student/<subject>` 提供当前学科的学习进度、比例、评分概况和临时笔记板；临时笔记只保存在本次页面运行内存，离开 / 登出即清理。没有已发布课程时不渲染空的课程选择控件。
5. **工科学科目录清理**：数学、物理、化学、算法、生物五个目录统一删除“学习方法建议”和“学习说明”两块；权威参考资料移到课程目录之后、工程页脚之前。该调整只改变目录信息层级，不改课程事实、实验 owner、证据或完成规则。
6. **未来星系主页增量规格**：在 26 号视觉语言、颜色、可访问性和按需加载边界内，主页改为单视口、无页面纵向滚动的弹性深空观测图；六方向以星图节点嵌入同一画面并可键盘直达方向目录。动态仅限低频星光 / 视差线索，`prefers-reduced-motion` 下静止；节点和文字仍为 DOM，不能把图片热点冒充交互。方向页和课程页继续正常文档流，不受主页锁屏规则影响。
7. **版本、范围与门禁**：V7.9.68 固定标题为 `V7.9.68 FE feat(student-flow): 收束入班与课程发现体验（FE-020）`。允许修改 `index.html`、`sw.js`、`shared/js/{main,router,page-registry,module-selector,role-home-client,frontier-learning}.js`、`pages/{planets,student,frontier}/**`、直接受影响的前端合同与 `tools/browser/role-workflows-proof.cjs`、`.codex/AGENTS.md`、01、02、03、08 与 26 号规格；如审计证明无需修改某文件则保持零差异。为遵守架构行数门禁，活动学生 owner 迁到 `pages/student/student-workbench.js`，旧 `pages/student/student.js` 必须恢复与基线逐字节一致且不再由 PageRegistry 加载。禁止修改 backend、数据库、迁移、教师 / 管理员 owner、18 课内容 / 证据 owner、代码空间、依赖或任何其他 worktree。
8. **验证**：直接 FE-020 合同、architecture boundary、220 个受跟踪 JS 语法与 46/46 前端合同已通过；本机 9002 的零班学生实测 `#student → #planets`、不可取消入班对话框、Escape 不关闭及全部“我的学习”入口隐藏 / inert，9003 演示库实测正式入班后的授权课程目录、同侧栏学生工作台、学科上下文与追加班级入口。Edge headless 在精确 390×844 验证学生页根宽 390、主动作 46px，Future 为 `docHeight=innerHeight=844`、六个 DOM 星图节点、根零溢出；桌面与 `#planets/#mathematics/#student/mathematics/#frontier` console error/warn、pageerror 均为 0。文档 compatibility / handoff 与 Git 门禁仍须在提交前完成。该主开发浏览器验收只关闭本任务，不冒充已暂停的 QA-015 或 V8.0 最终 QA。

### 2.20 FE-021 学生工作台弹性布局与授权发现返修包

1. **用户批注与主责**：本任务由主开发兼任唯一 FE 写入者，一次关闭 10 条批注中的前端缺陷；不恢复 FE / UI / QA 长期组，不修改教师、管理员或后端业务写接口。
2. **工作台布局**：学生页主舞台必须使用 `minmax(0, 1fr)` 和容器可用宽度，不再用 `100vw` 重算导致侧栏二次占宽。header 控件允许换行且不裁切；内容区默认上下堆叠，只有主舞台实际宽度足够时才切左右布局。侧栏增加 44px、可键盘操作、带 `aria-expanded` 的收缩按钮；收缩后保留图标导航和恢复入口。身份摘要固定在侧栏可视区底部，随 rail 而非页面正文移动，移动端回到正常流。
3. **首要任务与进度**：`#planets` 与 `#student` 的班级 / 课程控件使用同一组件密度和状态语义。当前班级存在多门课程时不得以 `course_selection_required` 清空已存在的活动进度；优先保持用户已选课程，若无显式选择则显示课程选择提示和跨课程权威进度摘要，不能把已有 projection 误报为丢失。
4. **授权导航与搜索**：student 的工科顶栏、学科卡片、全局搜索结果和键盘选择都只消费 `AstraStudentCourseCatalogue` 当前快照。未挂班、非 published、非 exact-open 的页面 / activity 不进入可操作 DOM；权限变化后打开的搜索立即重新过滤，教师和管理员维持既有可管理范围。
5. **学科概览与 Hero**：数学、物理、化学、算法、生物同步删除“学习方式 / 互动观察”和“练习入口 / 小测验”两项，只保留有实际数据价值的实验数量。五个 Hero Canvas 在目标学科进入时必须有唯一 owner 运行，离页停止；减少动效允许静态首帧，普通模式不得留下空白 Canvas。
6. **范围**：允许修改 `index.html`、`sw.js`、`pages/student/{student-workbench.js,student.css}`、`pages/planets/{planets.js,planets.css}`、`shared/js/{role-home-client,global-search,module-selector,scroll-animations,router,page-registry,main}.js`、`shared/css/{module-selector,navbar,page-layout,global-search}.css`、直接受影响的前端合同、`.codex/AGENTS.md`、01、02、03、08；审计证明无需修改的文件保持零差异。`pages/student/student.js`、backend、模型 / 迁移、teacher / admin、Future 18 课 owner、代码空间和其他 worktree 全部冻结。
7. **验证**：FE-021 动态合同、完整 `npm test`、architecture boundary 与 Git diff 门禁已通过；9003 的部分开放学生在 1002×898、1249×898、390×844 下完成 rail 收缩 / 恢复、header 无裁切、默认上下 / 宽屏左右、身份摘要底部、课程选择记忆、无权 nav / search 零结果、学科概览删项与 Hero 普通模式复验。文档 compatibility / handoff 与提交后 `git show --check` 仍须在固定提交形成前完成。双范围账号由后续独立 DATA 任务通过正式 API 创建，不混入 FE-021。主开发运行验收只关闭 FE-021，不替代暂停的最终 QA。

## 3. 正式任务原件

### 3.1 V7.6 产品与学习事实

> **暂停状态覆盖（2026-07-31）**：用户已暂停 V8.0 长期目标并解散协作组。以下所有尚未勾选完成的任务，无论原行写“进行中”“已认领”或“未认领”，当前权威状态均为“暂缓 / 无活动责任组”；原任务编号、版本令牌、依赖与验收标准继续保留，只有用户明确恢复后才重新调度。

- [x] PM-015｜状态：已完成｜目标版本：V7.6｜模块：V8 四轮恢复控制面｜主责：PM｜协作：CONTENT、FE、QA｜任务：记录用户六项决策，冻结约四次更迭 / 8 小时调度、兼容路由、候选整合策略和最小并行团队；查重并恢复长期任务，登记唯一写入工作区和版本门禁｜前置：PM-014；用户已明确恢复 V8.0 开发｜关联文档：本文第 1.3—2.1 节、[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md)｜关联版本：V7.6.15（决策 / 控制面）、V7.6.16（团队实态回填）｜验证：Git / 02 / 03 / worktree / 长期任务标题一致；文档与项目对接校验及 `git diff --check` 通过
- [ ] PM-009｜状态：进行中｜目标版本：V7.6—V8.0｜模块：星序角色主路径与协作总控｜主责：PM｜协作：CONTENT、UX、BE、FE、DATA、UI、QA、TOOLS｜任务：以本地展示脚本而非四周课堂为验收线，把 `#planets` 落成角色任务总览；学生看到下一项学习与续学，教师看到发布 / 进度 / 提交，管理员看到组织 / 人员 / 课程治理；能力文案必须与实际路由和 API 一致。用户离线期间统一执行“不申请审批；受平台 / 权限 / 安全门禁阻断即停手、保留现场、回报总控、标记暂缓”的协作规则｜前置：BE-006 已完成；不增加新角色或伪入口，不扩张既有授权｜关联文档：[23-V7.6产品定位与星序主路径契约.md](02-子文档/23-V7.6产品定位与星序主路径契约.md)、本文第 2 节｜关联版本：既有控制面保持，V7.9.34—35 负责教师返修 QA 包 / 树，V7.9.36—38 负责 Zoom 修正版 QA，V7.9.39—43 负责教师复验与平台阻断控制，V7.9.49 登记 FE-017 三条课程 lane 计划，V7.9.50 登记 CONTENT-003 来源校准树，V7.9.52 冻结共享课程壳并回填内容阻断和三 lane 实态，V7.9.53 校准组合复审返修边界并扩展共享 evidence 精确 allowlist，V7.9.54 记录 R8 数值返修和独立复审平台阻断，V7.9.55 登记 UI-003 活动会话触控返修包｜运行收口随 FE-012 / DATA-002 / TOOLS-002 / FE-013 / UI-003 / FE-017｜验证：三角色从登录到首要任务无需开发者解释；角色 / 路由 / API / 能力账本逐项一致；所有派单携带同一阻断处理规则
- [ ] FE-012｜状态：进行中｜目标版本：V7.6—V7.7｜模块：星序首页、三单元学习证据与 Physics Zoom｜主责：FE｜协作：CONTENT、UX、QA；BE 只提供已集成的 BE-011 冻结合同｜任务：V7.7.27 已关闭既定四项 A03a 缺口；后续同一产品 SHA 的补充运行证据稳定复现 Zoom 在 390×844 打开并保持、切至 1015×898 后以 863px backing store 填充 943px host 的 P2。V7.7.30 已按第 2.5 节复用既有 ResizeObserver / Canvas writer 同步最终父宽，并禁止自动放大过渡态低分辨率位图；专业候选 `30f7f189` 通过独立复审并已集成，不恢复旧 FE 树、不扩 Future｜前置：BE-011、V7.7.22、TOOLS-002、V7.7.27 已闭环；fz30 提交后 clean、写入关闭，受保护 a02c 永久禁入｜关联文档：本文第 2.2、2.5 节、[24-V7.6全栈模块边界契约.md](02-子文档/24-V7.6全栈模块边界契约.md)、[27-V7.7三星系六门代表课程教学与证据冻结契约.md](02-子文档/27-V7.7三星系六门代表课程教学与证据冻结契约.md)、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：既有 FE-012 版本保持原分配；V7.7.30 唯一用于本次 Zoom 返修｜验证：静态动态合同、完整 npm、文档、diff 与独立代码复审已通过；仍须由 QA 在精确集成树用外部 Edge 双向复验后才可关闭 FE-012 A03a 切片
- [ ] QA-014｜状态：暂缓｜目标版本：V7.6—V7.7｜模块：V7.6 独立验收｜主责：QA｜协作：PM、ARCH、FE、TOOLS；BE 仅答复冻结合同｜任务：A01 / A02 已完成；V7.7.30 已修复 Zoom 补充 P2。V7.7.31 因解释器锁依赖、V7.7.32 因 PM 过严身份合同、V7.7.33 因 external extension 缺少 reduced-motion 仿真与 main-realm 数字 owner 取证而依次 BLOCKED / NOT-RUN。V7.7.33 的全部可执行产品观察 finding=0，但不得冒充 A03a PASS；同类发包暂缓，A03b 仍在 Future 接线后补齐第三单元并关闭完整 A03｜前置：qz31 / qz32 / qz33 与各自 TEMP 只读冻结、Browser / 9001 已释放；恢复 A03a 需出现真实新增取证能力而非重复同一后端，A03b 等待 FE-017｜关联文档：本文第 2.5、2.7—2.8、2.10 节、[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md)、[24-V7.6全栈模块边界契约.md](02-子文档/24-V7.6全栈模块边界契约.md)｜关联版本：历次 A03a 版本保持原分配；V7.7.30 为 FE 返修，V7.7.31—33 为三轮精确 QA，V7.9.30—33 / 36—38 只属于对应控制面｜验证：V7.7.33 已实测双向跨断点、CSS×DPR/backing、base scale、关闭重开 / 直接桌面 / 小 host、44px / 焦点 / Escape、三轮行为生命周期与 console 0；仍缺 reduced-motion 仿真和 main-realm 直接数字 owner 证据｜暂缓原因：可信 external extension 当前不提供两项强制取证能力，重复同一后端只能再次得到程序性阻断｜暂缓开始：2026-07-30｜下次复查：2026-08-01（或 Browser 能力新增 / V8.0 综合 QA 启动时，以先到者为准）

### 3.2 V7.7 代表课程与教师协同

- [x] PM-017｜状态：已完成｜目标版本：V7.9.45—47｜模块：未来星系三图视觉概念与实施开放｜主责：PM｜协作：FE、CONTENT、QA｜任务：严格按 26 号规格生成桌面、精确 390×844 移动及人文三图；取得两次产品确认，提取生产设计系统，完成版本 / worktree / shell 与六 lane 边界登记，不修改产品实现｜前置：PM-016、CONTENT-003、V7.7.3、两次产品确认｜关联文档：本文第 2.13—2.14 节、[25-V7.7未来星系18课程内容与路由冻结矩阵.md](02-子文档/25-V7.7未来星系18课程内容与路由冻结矩阵.md)、[26-V7.7未来星系页面视觉规格.md](02-子文档/26-V7.7未来星系页面视觉规格.md)｜关联版本：V7.9.45（三图）、V7.9.46（确认与开放）、V7.9.47（工作树实态回填）｜验证：两次产品确认、逐图 `view_image`、尺寸 / SHA-256、设计 inventory、版本查重、实际 `f017` 路径 / branch / base / clean、文档 compatibility、handoff 与 diff 门禁通过；三图现为 FE-017 唯一视觉基线
- [x] PM-016｜状态：已完成｜目标版本：V7.9.44｜模块：未来星系实施边界校准｜主责：PM｜协作：CONTENT、FE、QA｜任务：把 FE-017 最新只读审计中的真实 owner 路径、Router 单 owner、单一 `page-frontier`、canonical / legacy 状态机、open-only 内容 / owner / evidence 延迟加载和 shell / 六 owner 串行并行边界回填 25 / 26 与共享控制文档；不修改当前实现｜前置：CONTENT-003、V7.7.3、FE-012 已集成基线、`f8de6ea` 只读审计｜关联文档：本文第 2.12 节、[25-V7.7未来星系18课程内容与路由冻结矩阵.md](02-子文档/25-V7.7未来星系18课程内容与路由冻结矩阵.md)、[26-V7.7未来星系页面视觉规格.md](02-子文档/26-V7.7未来星系页面视觉规格.md)｜关联版本：V7.9.44｜验证：四个纠正后的 owner 路径均存在；六方向 / 18 课 / legacy 映射无增减；非 open 状态内容 / owner / evidence 请求为零；文档 compatibility、handoff 与 diff 门禁通过
- [x] CONTENT-003｜状态：已完成｜目标版本：V7.7、V7.9.51｜模块：未来星系六方向 / 18 课程内容与信息架构（来源审计返修）｜主责：CONTENT｜协作：PM、FE、QA｜任务：原 18 课程身份、教学闭环和代表课选择保持冻结；本轮仅在 25 号矩阵校准来源审计证明的 7 项：RFC 2679→7679、NOAA 计算器维护状态与 USNO 替代、MIT OCW canonical 链接、fault-trace 丢包不能区分根因、纯铁 γ-Fe 约 911—1392°C 与相态条件、Apollo / LOC 稳定深链、再结晶晶粒页面不得直接支撑凝固冷却；同时逐项标明 60/130 kN、3/7 跳、1200-byte MSS、20/45/92、.25/.50/.75 等为合成教学参数而非来源事实，不修改运行代码或其他课程决策｜前置：PM-015；2026-07-31 独立 18 课来源审计｜关联文档：[25-V7.7未来星系18课程内容与路由冻结矩阵.md](02-子文档/25-V7.7未来星系18课程内容与路由冻结矩阵.md)、本文第 1.4、2.1、2.14 节、[06-实验体验与信度审查报告-20260606.md](06-实验体验与信度审查报告-20260606.md)｜关联版本：V7.7.0（原冻结）、V7.9.51（来源校准已使用）｜完成结果：原 c003 专业候选 `0ce127a5` 只修改 25 号矩阵，18 门课程 / stable key / route / 闭环零增减；标准内容组复核与独立事实复审均 `P0=P1=P2=P3=0`，RFC Editor、NOAA / USNO、MIT OCW、NIST、Cambridge、NASA / LOC 等权威边界逐项通过；本提交同步 25 / 02 / 03 完成主线闭环｜验证：文档 compatibility `errors=0`、仅既有 doc/99 warning；handoff、`git diff --check` 与专业候选 `git show --check` 通过
- [ ] FE-017｜状态：暂缓｜目标版本：V7.9.48｜模块：未来星系四层路由与课程模板｜主责：FE｜协作：CONTENT、PM、QA｜任务：按“未来星系主页面 → 六方向目录 → 独立课程入口 → 具体课程页”实现 18 门课程；旧方向 / 课程 hash 兼容规范化，统一工科课程页面模块、导航与页脚逻辑但保留未来星系视觉身份；重做人文观察区的信息层级，按 exact-open 权限加载并释放内容、Canvas / Three 与 evidence；接入工程 / 人文两门代表证据。R7 已关闭原组合复审的身份、ack、两门 explained、overflow 和主动作问题；R8 又关闭十进制精度穿透，f017 当前为 36 tracked modified + 22 untracked、0 staged，完整静态门禁通过但未提交｜前置：两次视觉确认、FE-012 / BE-011 / BE-013、CONTENT-003 / V7.9.51 已集成；R5 共享 shell 与三 lane 原始字节已冻结｜关联文档：本文第 1.4、2.14 节、[25-V7.7未来星系18课程内容与路由冻结矩阵.md](02-子文档/25-V7.7未来星系18课程内容与路由冻结矩阵.md)、[26-V7.7未来星系页面视觉规格.md](02-子文档/26-V7.7未来星系页面视觉规格.md)、[27-V7.7三星系六门代表课程教学与证据冻结契约.md](02-子文档/27-V7.7三星系六门代表课程教学与证据冻结契约.md)、[05-UI规范模板.md](05-UI规范模板.md)、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：V7.7.3（规格）、V7.9.48（最终产品）、V7.9.53（返修边界控制）、V7.9.54（R8 / 平台阻断控制）｜验证：R7 独立复审 B `P0=P1=P2=P3=0`；R8 自测覆盖真实链、无 consumer、pre-init、initialized、fallback；npm 218 JS + 47/47 contracts，22 个 untracked 语法及 docs / handoff / architecture / diff PASS｜暂缓原因：R8 独立数值复审在结论返回前被平台误判为可能的网络安全风险并终止；按 GLOBAL NO-APPROVAL 不重试、不换审查者 / 工具 / 目录，Browser / QA-015 尚未运行｜暂缓开始：2026-07-31｜下次复查：2026-08-01（仅在平台状态已明确解除时）｜恢复门禁：平台状态允许同一独立复审安全续接；复审 PASS 后才启动既定 9001 / 外部 Edge 矩阵并形成 V7.9.48，不能用静态门禁或 R7 PASS 替代
- [x] FE-020｜状态：已完成｜目标版本：V7.9.68｜模块：学生入班、课程发现、学习上下文与未来星系主页｜主责：FE（主开发兼任唯一写入者）｜协作：无活动工作组｜任务：已落实用户 13 条浏览器批注及追加班级规则：零班级学生无法进入个人页且必须经正式 API 入班；学生可加入多个班级并按班级隔离课程，目录只呈现 published + exact-open 内容且课程身份不重复；类目可直达；我的学习保留同一侧栏并提供学科上下文、进度 / 比例 / 评分 / 临时笔记；空课程控制不渲染；五学科移除方法 / 说明并下移参考资料；未来星系主页改为单视口动态星图与六方向快捷节点｜前置：V7.9.67 已记录停组；只消费 `主开发@d3772f1`，未消费冻结候选｜关联文档：本文第 2.19 节、[23-V7.6产品定位与星序主路径契约.md](02-子文档/23-V7.6产品定位与星序主路径契约.md)、[26-V7.7未来星系页面视觉规格.md](02-子文档/26-V7.7未来星系页面视觉规格.md)、[05-UI规范模板.md](05-UI规范模板.md)、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：V7.9.68｜完成结果：活动学生 owner 迁入 `student-workbench.js`，PageRegistry、Router、星序目录、工科目录与 Future 首页在同一资源代际闭环；旧 `student.js` 与 backend / DB / teacher / admin / FE-017 冻结候选保持零差异｜验证：FE-020 动态合同、220 JS 语法、46/46 前端合同、architecture boundary、9002 零班硬门禁、9003 已入班多班入口、390×844 与桌面页面 / console 门禁通过；文档 compatibility / handoff 与 Git 最终检查通过
- [x] FE-021｜状态：已完成｜目标版本：V7.9.69｜模块：学生工作台响应式、首要任务与授权发现｜主责：FE（主开发兼任唯一写入者）｜协作：无活动工作组｜任务：已修复工作台因 rail 二次占宽造成的文字不均 / 控件裁切，增加可折叠 rail 与底部身份摘要，内容默认上下且宽屏才左右；planets / student 共享本人内存班课选择并自动恢复首门课程；按当前班级开放范围隐藏工科顶栏与全局搜索结果；五学科只保留实验数量并恢复普通模式 Hero 动画｜前置：FE-020 / V7.9.68 已完成，只消费 `主开发@5dbea93`｜关联文档：本文第 2.20 节、[05-UI规范模板.md](05-UI规范模板.md)、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：V7.9.69｜完成结果：学生 stage 以自身 inline-size 决定单 / 双栏，rail 232px↔76px 且身份摘要贴底；学生搜索、顶栏和卡片共同消费授权目录，未开放内容不进入交互结果；Router 保持 1116 行冻结上限，Hero 自启动回归动画 owner｜验证：220 个跟踪 JS 语法与 47/47 前端合同、architecture boundary、9003 的 1002×898 / 1249×898 / 390×844、侧栏 44px、根零溢出、课程记忆、授权 nav / search、Canvas 初始化及 console/pageerror 0 通过；文档 compatibility / handoff 与 Git 最终门禁通过
- [ ] QA-015｜状态：已认领｜目标版本：V7.7｜模块：未来星系 18 课程独立验收｜主责：QA｜协作：PM、CONTENT、FE｜任务：只对 FE-017 精确候选验证六方向 / 18 课程层级、旧深链兼容、hidden / locked / open、教学闭环、事实依据、桌面 / 移动 / 键盘、Canvas / Three 按需加载与销毁；对照已确认视觉概念建立至少五项忠实度账本｜前置：FE-017 精确提交；CONTENT-003 冻结矩阵｜关联文档：[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md)、[06-实验体验与信度审查报告-20260606.md](06-实验体验与信度审查报告-20260606.md)｜关联版本：待精确候选后分配｜验证：真实外部 Edge、1015×898、390×844、刷新 / 前进 / 后退、角色状态、console、资源计数、截图与概念对照均有证据，P0—P2 为 0
- [x] CONTENT-001｜状态：已完成｜目标版本：V7.7｜模块：三星系代表课程｜主责：CONTENT｜协作：PM、FE、UX、QA；BE 仅答复冻结合同｜任务：由主开发为内部验收选择每个星系至少两条代表路径，补齐学习目标、预测、教学型互动、常见错误、反馈、再次尝试 / 解释和权威完成规则；删去实现栈说明与无教学关联文案，不扩大课程数量；不要求用户验收中间 V7.7 展示｜前置：CONTENT-003；FE-012 稳定活动键与证据接入｜关联文档：[27-V7.7三星系六门代表课程教学与证据冻结契约.md](02-子文档/27-V7.7三星系六门代表课程教学与证据冻结契约.md)、[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md) 第 4 节、[06-实验体验与信度审查报告-20260606.md](06-实验体验与信度审查报告-20260606.md)｜关联版本：V7.7.15｜验证：六条代表路径均已冻结可操作变量、错误反馈、重试 / 解释、权威完成规则、教师回读和事实依据；实现与真实浏览器验收仍由 FE / BE / QA 后续任务完成
- [x] CONTENT-004｜状态：已完成｜目标版本：V7.8｜模块：教师六课有限事实与安全回读契约｜主责：CONTENT｜协作：PM、BE、DATA、FE、QA｜任务：已为六门代表课冻结 `(activity_key,event_schema_version,event_type,exact leaf path)`、有限字符串枚举、数值范围 / 精度、跨叶组合、中文标签、权威依据和固定降级；只把 physics.mechanics 与 control-flow.loop-boundary 标为现有真实 producer，其余四课明确等待 FE / DATA，不让后端或初始化器合成不存在的教学事实；开放理由、解释、源码、主张与下一问题正文继续不回显。力学 `e²` 只作教学解释，不作为拒绝离散实测的后端条件；control-flow runtime error / runner_unavailable 不产生 `reported_correct` 原值，也不冒充公开样例不匹配｜前置：CONTENT-001、UX-003、BE-007、DATA-002；主开发只读缺口审查与六课矩阵预审已完成｜关联文档：[27-V7.7三星系六门代表课程教学与证据冻结契约.md](02-子文档/27-V7.7三星系六门代表课程教学与证据冻结契约.md)、[28-V7.8教师自然工作流与证据回读冻结契约.md](02-子文档/28-V7.8教师自然工作流与证据回读冻结契约.md)｜关联版本：V7.8.3｜验证：最终候选 `100a619b`，第二轮独立复审 P0—P3=0；六课矩阵字段 / 标签 / producer 状态唯一；OpenStax、ECMAScript、NASA、Library of Congress、工程静力学与 delta debugging 等权威依据已核查；未知 activity / schema / event / path / enum、越界或过精度一律省略并 `truncated=true`，每事件最多 12 facts；三项专项合同、文档、handoff 与 diff 门禁通过
- [x] UX-003｜状态：已完成｜目标版本：V7.8｜模块：教师自然工作流｜主责：UX｜协作：CONTENT、BE、FE、QA｜任务：把教师路径收束为班级 / 课程选择、开放 / 锁定 / 隐藏、进度、提交、反馈与学生视角；隐藏底层 schema、租约和运维术语｜前置：CONTENT-001、BE-006｜关联文档：[28-V7.8教师自然工作流与证据回读冻结契约.md](02-子文档/28-V7.8教师自然工作流与证据回读冻结契约.md)、[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md)、[05-UI规范模板.md](05-UI规范模板.md)｜关联版本：V7.8.1｜验证：已冻结明确范围—安排—进度—证据—反馈—学生状态预览的七分三十秒演示、逐屏主动作、API 映射、状态文案、双视口 / 键盘门禁与 FE-013 切片；实现和真实演示仍由 BE-007 / FE-013 / QA-013 完成
- [x] BE-007｜状态：已完成｜目标版本：V7.8｜模块：教师与管理展示闭环最小服务｜主责：BE｜协作：UX、FE、CONTENT、DATA、UI、QA｜任务：已新增教师在有权班级 / 课程内按学生、活动与事件类型分页发现 0051 学习证据及 event_id→既有 correction 回读；安全摘要使用 exact leaf-path policy，用户可控字符串只返回结构元数据。已新增管理员课程 `draft/published/archived` 的 `expected_status + status + reason` 受约束变更、影响计数、审计、冲突和权威锁序；未新增迁移、通知、排课、任意 SQL、生产发布或通用内容平台｜前置：PM-009 缺口证明与 V7.9.2 返修 owner 已完成；`69df127` 保留为禁止集成历史，最终候选 `d1fefcdc` 三轮独立复审通过｜关联文档：[07-后端优化与设计.md](07-后端优化与设计.md)、[09-后端阶段收束小版本开发安排.md](09-后端阶段收束小版本开发安排.md)｜关联版本：V7.8.0｜验证：6 项专项、后端全量 561 passed / 8 个真实 MySQL 条件跳过、211 个跟踪 JS 语法、40/40 前端合同、架构、文档、对接与 diff 门禁通过；冻结课程文件相对父提交逐字节一致，第三轮独立复审 P0—P3=0
- [x] BE-011｜状态：已完成｜目标版本：V7.8｜模块：学生精确课程单元访问处置｜主责：BE｜协作：FE、QA；PM 负责合同与集成｜任务：已新增独立 schema / service / endpoint，在学生已有 class / course 授权内按精确 `activity_key` 返回 `available: true` 或 `activity_hidden / activity_locked / course_unit_missing`；不返回 unit ID、标题、内容、release plan、lock reason 或目录数据，不修改现有学生 units 过滤语义｜前置：QA-014 / V7.7.20 已证明 hidden / missing 能力缺口；V7.8.2、0dce 独立 worktree 与精确 `[ASSIGN]` 均已闭环｜关联文档：本文 V7.7.20 复验事实、[24-V7.6全栈模块边界契约.md](02-子文档/24-V7.6全栈模块边界契约.md)、[07-后端优化与设计.md](07-后端优化与设计.md)、[09-后端阶段收束小版本开发安排.md](09-后端阶段收束小版本开发安排.md)｜关联版本：V7.8.2｜验证：专业组提交 `35fa219`；独立复审 P0—P3=0；43 项受影响测试通过，后端全量 567 passed / 8 个真实 MySQL 条件跳过，专业组 214 个跟踪 JS / 41 个前端合同、主线 215 个跟踪 JS / 42 个前端合同、架构、文档、对接与 diff 门禁通过；`courses.py`、`schemas/course.py`、models、Alembic 相对专业组父提交零差异
- [x] BE-013｜状态：已完成｜目标版本：V7.8｜模块：教师六课有限事实安全投影｜主责：BE｜协作：CONTENT、DATA、FE、QA；PM 负责合同与集成｜任务：已在不改变既有教师 evidence endpoint、DTO、班课 / subject 授权、稳定分页与 event_id→correction 语义的前提下接入独立纯投影，严格按 `(activity_key,event_schema_version,event_type,exact leaf path)` 消费 27 / 28 号契约；首批只投影 `physics.mechanics` 与当前浅层 `control-flow.loop-boundary`，另外四门、未知 activity / schema / event 固定为空 facts 且 `truncated=true`，不再保留会被误当课程事实的通用 `value_type / value_size` 元数据｜前置：CONTENT-004、DATA-003 已完成；未扩 endpoint/schema/router/model/migration｜关联文档：[27-V7.7三星系六门代表课程教学与证据冻结契约.md](02-子文档/27-V7.7三星系六门代表课程教学与证据冻结契约.md)、[28-V7.8教师自然工作流与证据回读冻结契约.md](02-子文档/28-V7.8教师自然工作流与证据回读冻结契约.md)｜关联版本：V7.8.4｜验证：专业组候选 `47ac53a9`，独立复审 P0—P3=0；根线组合测试直接消费 DATA profile，三文件组合套件 90 passed / 1 个显式 MySQL skip，后端全量 619 passed / 8 个显式 MySQL release drill skip；力学只校验 trial↔e、固定 preset 与 `h/H↔h/200` 内部一致，不以理论 `e²` 拒绝离散实测；循环 runtime error 不输出公开样例未匹配，future trace / accepted / completed 不合成；exact enum、数值类型 / 范围 / 精度、组合原子性、未知 / 敏感 / nested raw、12 facts 上限、四课降级、授权 / 分页 / correction 均覆盖
- [ ] FE-013｜状态：进行中｜目标版本：V7.8.5→V7.8.7→V7.8.9｜模块：教师协同工作台｜主责：FE｜协作：UX、CONTENT、QA；BE 只答复已集成的 BE-007 / BE-011 / BE-013 合同｜任务：V7.8.5 已集成教师自然工作流；V7.8.7 已修复 attachment false→true 的 ready 恢复，但 QA-013 / V7.8.8 真实运行发现固定 4 秒自主轮询使单次选择与单次重选都产生 4 对 progress + aggregate，P1 再次退回原任务。V7.8.9 已在 fr89 保留 owner 最小修复与目标动态合同；只读门禁进一步证明旧能力账本与回归合同仍正向要求 4 秒轮询，使原 5 文件范围无法合法闭环。V7.9.62 现把同一任务扩展为 7 文件，仍由原 owner 在同一 fr89 续接，不创建第二任务或工作树｜前置：V7.8.8 精确 network / UI / SQLite 证据与独立只读根因复审均为 P1；V7.9.40—43 保留两轮平台阻断事实；V7.9.62 校准能力真值与回归合同边界｜关联文档：本文第 2.3—2.4、2.6、2.9、2.11 节、[28-V7.8教师自然工作流与证据回读冻结契约.md](02-子文档/28-V7.8教师自然工作流与证据回读冻结契约.md)、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：V7.8.5（原实现）、V7.8.6（首轮 QA RETURN）、V7.8.7（attachment 返修）、V7.8.8（二轮 QA RETURN）、V7.8.9（唯一返修提交）、V7.9.40—43（写入包 / 树 / 两轮平台阻断控制）、V7.9.62（真值边界控制）｜验证：两份 JS 语法、目标动态合同、14 / 15 相关合同、218 JS、43 后端及 docs / handoff / diff 已通过；扩展后须使全部相关合同与完整 npm 通过，再完成独立复审与真实 QA｜阻断恢复规则：仅恢复同一 fr89 的登记 7 文件；若任何 fileChange 再遇平台审批、权限或安全阻断，立即原地暂缓，不重试、绕过或换工具 / 目录

> **V7.9.66 状态覆盖**：FE-013 当前为**暂缓**。第二轮返修保留在 `fr89` 的 7 个未提交文件中；平台只允许根工作区写入，故完整门禁、独立复审、暂存和 V7.8.9 提交均暂停。恢复条件是平台明确恢复原登记 worktree，而不是复制差异或改换目录。

### 3.3 V7.9 管理治理与展示收口

- [x] DATA-001｜状态：已完成｜目标版本：V7.9｜模块：本地演示数据与领域治理｜主责：DATA｜协作：UI、BE、QA｜任务：已通过既有正式 API 提供可重复初始化的三角色、组织、班级、14 Course / 42 Unit、发布、六门代表课程证据、pending / graded 提交、反馈、runner_unavailable、状态冲突和审计回读；未新增模型、迁移、endpoint、service、router 或直接数据库写入｜前置：A01、BE-007、唯一 DATA 长期任务、`992d` worktree、允许文件与版本均已登记并完成｜关联文档：[29-V7.9本地演示数据与初始化冻结契约.md](02-子文档/29-V7.9本地演示数据与初始化冻结契约.md)、[07-后端优化与设计.md](07-后端优化与设计.md)、[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md)｜关联版本：V7.9.0（A01）、V7.9.5（契约对账）、V7.9.6（团队实态）、V7.9.7（实现）｜验证：专业组提交 `93d9205`；第五轮独立复审 P0—P3=0；DATA 专项 11 passed，连续三次运行不增殖业务对象或非登录审计，混合既有/fresh 漂移在 catalog 创建前失败，PowerShell / UNC、凭据、敏感回显、npm、文档、handoff 与 diff 门禁通过
- [x] DATA-002｜状态：已完成｜目标版本：V7.9｜模块：单课程教师演示补样｜主责：DATA｜协作：FE、UX、QA；PM 负责合同与集成｜任务：已在既有 DATA-001 manifest 中为 `control-flow.loop-boundary` 增加一份 submitted / pending 合成作业，使同一课程连续展示五类证据、纠正、待反馈作业与 `runner_unavailable`；保留 physics graded 与 humanities pending 锚点，未扩展数据模型、API 或初始化框架｜前置：DATA-001、UX-003、V7.9.11 控制提交、`992d` 工作树与精确派单均已闭环｜关联文档：[28-V7.8教师自然工作流与证据回读冻结契约.md](02-子文档/28-V7.8教师自然工作流与证据回读冻结契约.md)、[29-V7.9本地演示数据与初始化冻结契约.md](02-子文档/29-V7.9本地演示数据与初始化冻结契约.md)｜关联版本：V7.9.11（控制面）、V7.9.12（实现）｜验证：专业组提交 `a920642`；独立复审 P0—P3=0；DATA 专项 11 passed；正式 API 证明作业、代码题与 `control-flow.loop-boundary` 同 course / unit；三次 assignment / submission / code ID 和非 `auth.*` 审计不增殖；215 个跟踪 JS / 42 个前端合同、文档、handoff 与 diff 门禁通过，初始化器 blob 保持 `a8a589db…`
- [x] DATA-003｜状态：已完成｜目标版本：V7.9｜模块：六课演示证据 producer profile 对齐｜主责：DATA｜协作：CONTENT、BE、FE、QA；PM 负责共享文档与集成｜任务：已把六门同形通用 payload 收束为 profile 单一来源：`physics.mechanics` 使用受控预测 / 两档观测 / 修正的 precise fixture，`control-flow.loop-boundary` 使用浏览器预检 / 正式提交 / 修订的 shallow fixture，数学、调试、工程、人文固定为 generic lifecycle / unavailable；manifest 与报告显式给出 `producer_mode / course_fact_status / fixture_provenance`，不从源码、输出、solver 或自然语言猜测课程事实｜前置：CONTENT-004 与 BE-013 均已完成；producer→projector 组合合同已形成｜关联文档：[27-V7.7三星系六门代表课程教学与证据冻结契约.md](02-子文档/27-V7.7三星系六门代表课程教学与证据冻结契约.md)、[28-V7.8教师自然工作流与证据回读冻结契约.md](02-子文档/28-V7.8教师自然工作流与证据回读冻结契约.md)、[29-V7.9本地演示数据与初始化冻结契约.md](02-子文档/29-V7.9本地演示数据与初始化冻结契约.md)｜关联版本：V7.9.19｜验证：专业组候选 `31722c8`；独立复审 P0—P3=0；DATA 专项 12 passed，后端全量 579 passed / 8 个显式 MySQL release drill skip，217 个跟踪 JS / 44 个前端合同、Node DATA 合同、AST、文档、handoff 与 diff 门禁通过；恰 3 演示账号、14 课程、42 单元、6 代表课、3 作业、1 代码题，`minimum_attempts+1`、每课一条教师 correction、服务端 completed、公开 ASGI API-only 与 fresh + 两次重跑幂等均由专项覆盖
- [ ] UI-003｜状态：暂缓｜目标版本：V7.9｜模块：管理治理工作台｜主责：UI｜协作：DATA、BE、UX、QA｜任务：V7.9.4 与 V7.9.56 已集成；V7.9.58 因旧 registry loader 运行失败永久留空。V7.9.60 专业提交 `3734a2f` 已在 u358 原子同步公共壳与管理员代际，17 文件静态门禁和第二轮独立复审 P0—P3=0；但精确旧缓存运行门禁在正式服务启动的 CreateProcess 前被平台 `rejected: blocked by policy`，命令未执行，故候选暂不集成且不能冒充 Browser PASS。课程状态单次 CAS、影响回执、unknown-result 写锁、精确审计回读及最终完整数据联调仍须后续 QA｜前置：A01、BE-007、V7.9.4、DATA-002、V7.9.56、V7.9.59 已集成；u358 专业树 clean｜关联文档：本文第 2.15—2.17 节、[51-V7.9管理员治理工作流冻结契约.md](05-子文档/51-V7.9管理员治理工作流冻结契约.md)、[05-UI规范模板.md](05-UI规范模板.md)、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：V7.9.1（A01 合同）、V7.9.4（实现）、V7.9.56（触控返修）、V7.9.57（首轮资源控制）、V7.9.58（运行退回 / 永久留空）、V7.9.59（入口扩展控制）、V7.9.60（专业候选）、V7.9.61（平台阻断记录）｜验证：静态 / 独立复审已通过；恢复后仍必须在精确旧缓存 127.0.0.1 external Edge 一次普通 reload 后确认公共 registry / router / main 同代、四项管理员资源同代、按钮至少 44px、根宽、焦点、console、离页释放、其他角色零 owner 预取和 CacheStorage 零 admin 资源；主开发冒烟不替代 QA-013 综合验收｜暂缓原因：正式服务启动被 exec 平台在 CreateProcess 前策略拒绝；依 GLOBAL NO-APPROVAL 不允许重试、绕过或换工具 / 目录｜暂缓开始：2026-07-31｜下次复查：2026-08-01（只核对平台状态；平台未明确解除前不得主动重试启动）
- [ ] QA-013｜状态：进行中｜目标版本：V7.8.6→V7.8.8→V8.0｜模块：本地展示脚本与候选验收｜主责：QA｜协作：PM、CONTENT、UX、BE、FE、DATA、UI｜任务：V7.8.6 在 qe13 对 `f47155d` RETURN FE-013；V7.8.8 在 qt88 对 `848c1ed` 再次 RETURN FE-013，均为 P0=0/P1=1/P2=0/P3=0。第二轮已证明 ready 与桌面 / 移动数据恢复，但同一 accepted attachment edge 产生 4+4 请求；最终阶段仍须在后续精确返修和复验通过后，以全新数据目录执行完整三角色候选脚本｜前置：qe13 / qt88 现场与各自 TEMP 只读冻结；Browser / 9001 已释放；后续复验等待 FE-013 新候选、全新 detached 树与新运行包｜关联文档：本文第 2.4、2.6、2.9 节、[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md)、[04-部署指南.md](04-部署指南.md)｜关联版本：V7.8.6（首轮 RETURN）、V7.8.7（返修）、V7.8.8（二轮 RETURN）、V7.9.34—35（QA 包 / 树）、V7.9.38（放行）、V7.9.39（退回记录）；下一复验版本待分配｜验证：单次选择与重选后各静置至少 13 秒仍只恰一组 progress + aggregate；切未挂接范围零私有请求且清空状态，手动刷新恰一组，三轮进出后离页零请求；双视口、权限、hidden、console 与生命周期保持通过
- [ ] REL-008｜状态：未认领｜目标版本：V8.0｜模块：展示版最终收口｜主责：REL｜协作：PM、CONTENT、UX、ARCH、BE、FE、DATA、UI、QA｜任务：生成唯一展示版候选，统一一键启动、演示数据 / 账号说明、当前能力、已知边界、目录结构、规划与历史；不以本地候选宣称 production｜前置：QA-013 PASS｜关联文档：[01-开发者文档.md](01-开发者文档.md)、[03-开发历史.md](03-开发历史.md)、[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md)｜关联版本：待产生｜验证：代码、文档、浏览器、SQLite 账本和 Git 指向同一 revision，工作区无临时产物

### 3.4 当前必要质量任务

- [ ] UX-001｜状态：进行中｜目标版本：V7.9.65→V8.0 随功能片关闭｜模块：关键路径可访问性｜主责：UX｜协作：FE、CONTENT、UI、QA｜任务：范围保持登录、星序、六条以上代表课程、教师和管理主路径的键盘、焦点、减少动效、核心 44px 触达与 390×844；首个独立切片只修复星序学校 / 班级 / 课程范围选择重绘后的焦点连续性，不碰 UI-003 公共缓存代际、教师、Future 或课程 owner。登录 tab 语义、Physics reduced-motion、六门课程与最终整体验收继续按第 2.18 节依赖分开推进；全站逐路由读屏认证后置｜前置：当前主开发静态审计已确认 `role-home-client.js::choose()` 重绘替换 active select；V7.9.63 写入包与 V7.9.64 worktree 实态登记｜关联文档：本文第 2.18 节、[05-UI规范模板.md](05-UI规范模板.md)、[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md)｜关联版本：V7.9.63（任务控制）、V7.9.64（worktree 登记）、V7.9.65（星序焦点实现）｜验证：真实 owner VM 证明选择后焦点落在 replacement scope、后台刷新不抢焦点、三轮无 listener / timer / detached node 增殖；完整 npm、文档 / handoff / diff、独立代码复审通过，最终仍由 QA-014 / QA-013 在同一候选补真实浏览器证据

> **V7.9.66 状态覆盖**：UX-001 的 V7.9.65 写入片当前为**暂缓**。首轮候选因 P2 合同盲区退回，返修现场保留为 4 tracked modified + 1 untracked、0 staged；平台恢复 `ux64` 原目录写入前不得提交或集成。登录 tabs 继续等待 UI-003，Physics reduced-motion 只允许只读审计。
- [x] TOOLS-001｜状态：已完成｜目标版本：V7.7｜模块：展示环境与验收工具可复现｜主责：TOOLS｜协作：BE、FE、QA｜任务：只修阻断 `astra-local.ps1`、锁依赖、`npm test`、后端全量或本地展示验收的确定性环境 / 工具问题；本轮唯一写入包把只读 SQLite QA 账本及其合同从已失效的 0050 期望对齐至当前权威 head 0051，不修改迁移、业务表、API 或生产发布工具｜前置：QA-014 预检已证明账本仍硬编码 `20260719_0050`，与当前 `20260727_0051` 冲突｜关联文档：[01-开发者文档.md](01-开发者文档.md)、[04-部署指南.md](04-部署指南.md)｜关联版本：V7.7.4｜验证：合同测试、全新 SQLite `upgrade head` 后的只读空基线账本、`npm test`、文档 / 对接校验与 `git diff --check` 通过
- [x] TOOLS-002｜状态：已完成｜目标版本：V7.7.25｜模块：strict-readonly 本机启动 Python 选择｜主责：TOOLS｜协作：PM、QA｜任务：保持 `astra-local.ps1` 默认仓库 `.venv` 一键行为不变，已新增互斥的显式 `-PythonExecutable` 与仓库外 `-VirtualEnvironmentPath`；显式 Python 必须配合 `-SkipDependencyInstall`，只做严格 `pip install --dry-run --no-index --require-hashes` 与 `pip check`，在创建 data / salt、迁移、初始化和 Uvicorn 前失败关闭，禁止向调用方 Python 安装依赖或写 marker。外部 venv 在首个 Python 执行前及安装 / marker 边界重验路径，拒绝 device namespace、repo / target 路径链 reparse 与 `sys.prefix` 不匹配；所有后续 Python 子进程统一使用解析后的 runtime，不新增 junction、symlink、复制环境、`uvicorn.exe`、`Start-Process` 或直跑绕过｜允许写入：候选精确修改 `astra-local.ps1`、`tools/tests/local-preview-contract.cjs`、`tools/tests/demo-data-initializer-contract.cjs`、`tools/tests/local-preview-python-selection-contract.cjs`、`README.md`、`backend/README.md`、`doc/01-开发者文档.md`、`doc/01-开发者手册.md` 与 `doc/04-部署指南.md` 共 9 文件｜前置：QA-014 / V7.7.24 已证明 c91f 无 `.venv` 且启动器必写仓库，P0—P3=N/A；V7.9.14 控制面登记的唯一 owner / 版本 / worktree 已闭环｜关联文档：[01-开发者文档.md](01-开发者文档.md)、[01-开发者手册.md](01-开发者手册.md)、[04-部署指南.md](04-部署指南.md)｜关联版本：V7.7.25（工具实现）；V7.7.26 仅属于后续 QA-014｜验证：专业组候选 `ba3d02c4e8712e9ba10fd8499f99f5dc0bb43d6b`（父 `0262b322401e644cafc8a31bcfa8b405ebb6df94`）；首轮独立复审 P1=2、P2=1、P3=1 后由原 owner 返修，最终复审 P0—P3=0。PowerShell 5.1 解析、三项定向合同、主开发精确集成树 216 个跟踪 JS / 43/43 前端合同、文档 compatibility 0 error（仅既有 `doc/99` warning）、handoff 与 diff 门禁通过；工具组未启动 9001、浏览器或真实安装，候选已由本提交集成并关闭写入

### 3.5 展示后暂缓工程

| 任务 | 暂缓范围 | 恢复条件 |
| --- | --- | --- |
| `OPS-004 / BE-008 / FE-014` | 隔离 runner、隐藏测试、资源计量、部分得分、重判 | 用户把真实 OJ 明确恢复为交付目标并提供隔离运行环境 |
| `BE-009 / FE-015` | 通用互动内容 schema 与教师低代码课程工作室 | 既有课程发布 / 隐藏 / 进度闭环稳定后仍有明确展示价值 |
| `SEC-001` | 未成年人数据政策、导出 / 删除全流程和专业法律复核 | 确认真实用户、司法辖区和责任 owner |
| `CONTENT-002` | 全目录逐课迁入 `validated / demonstration-only / retired` | 代表课程质量模板稳定且项目进入真实教学运营 |
| `FE-016 / BE-010` | 全量巨型文件拆分、88 实验 legacy 清零、高基数证据重构 | 出现明确维护阻塞或进入长期运营 |
| `QA-012 / PERF-001` | 学期级数据、真实并发、MySQL EXPLAIN、容量与压力 | 确认目标规模、真实访问分布和目标环境 |
| `OPS-001` | MySQL、域名、TLS、secret、服务、监控、备份恢复和生产回滚 | 用户明确恢复公网发布并提供 staging / production owner |
| `REL-001` | Webhook、GitHub issue、audit anchor 等外部 provider | 存在明确业务价值、provider、权限与审批 |

#### 3.5.1 AI 路线任务原件

- [ ] AI-001｜状态：暂缓｜目标版本：V8.1+ 条件项｜模块：学生 AI 助教｜主责：AI（恢复时建立唯一实现组）｜协作：CONTENT、BE、FE、QA｜任务：只围绕学生当前已发布课程、操作证据和明确知识来源提供分层提示、追问与解释；不代替学生作答，不根据聊天或点击自动写入掌握 / 完成｜前置：CONTENT-001 与 FE-012 完成；用户明确 provider、预算、可发送数据和未成年人使用边界｜关联文档：[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md) 第 7 节｜关联版本：待恢复后分配｜验证：来源可追溯、提示可拒绝、失败可降级；无 provider 时不展示可用入口，AI 输出不能直接写权威学习事实｜暂缓原因：不阻塞 V8.0 展示闭环，当前未选择 provider 与数据边界｜暂缓开始：2026-07-28｜下次复查：2026-09-30（或 V8.0 候选完成 / 用户明确恢复时，以先到者为准）
- [ ] AI-002｜状态：暂缓｜目标版本：V8.1+ 条件项｜模块：教师 AI 教学助手｜主责：AI（恢复时建立唯一实现组）｜协作：CONTENT、UX、BE、FE、QA｜任务：基于教师有权读取的课程与班级事实生成课程节奏、反馈或题目草案，并清楚标记依据与不确定性；教师确认前不得发布、评分、反馈学生或改变课程状态｜前置：FE-013 教师协同闭环稳定；AI-001 的共享 provider / 数据边界可复用或另行审批｜关联文档：[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md) 第 7 节｜关联版本：待恢复后分配｜验证：班级 / 课程授权隔离、人工确认、版本与审计回读通过；不得把生成内容冒充教师决定｜暂缓原因：先稳定非 AI 教学工作流，避免用生成界面掩盖基础流程缺口｜暂缓开始：2026-07-28｜下次复查：2026-09-30（或 V8.0 候选完成 / 用户明确恢复时，以先到者为准）
- [ ] AI-003｜状态：暂缓｜目标版本：V8.1+ 条件项｜模块：AI 维护与管理助手｜主责：AI（恢复时建立唯一实现组）｜协作：ARCH、BE、UI、SEC、QA｜任务：先提供只读的运行诊断、数据关系解释和治理建议；任何配置、组织、权限、课程或数据变更都必须显示影响范围，经管理员显式确认后调用既有领域 API 并进入审计链，禁止直连数据库、执行任意命令或绕过角色权限｜前置：UI-003、审计和领域治理闭环稳定；用户明确模型、工具 allowlist、执行 owner 与回滚责任｜关联文档：[22-V8最终完成路线与验收蓝图.md](02-子文档/22-V8最终完成路线与验收蓝图.md) 第 7 节｜关联版本：待恢复后分配｜验证：默认只读、最小权限、确认 / 取消 / 冲突 / 回读 / 审计和错误降级均通过；建议不得伪装成已执行｜暂缓原因：当前展示价值低于三角色基础治理，且工具调用边界尚未批准｜暂缓开始：2026-07-28｜下次复查：2026-09-30（或 V8.0 候选完成 / 用户明确恢复时，以先到者为准）

暂缓任务不是当前缺陷，也不能在能力文案中写成已完成。既有代码、测试和部署工具保留，除非 ARCH-002 能证明其既无调用、测试、文档或恢复价值。

## 4. 调度与依赖

```text
PM-014 + 用户六项决策 ──> PM-015 恢复控制面
PM-015 ──┬─> CONTENT-003（只读课程 / 路由矩阵）
         ├─> FE-012 ──> QA-014 / V7.7.20 RETURN ──> BE-011 ──> FE-012 最小接线 ──> QA-014 补充 Zoom P2 ──> FE-012 / V7.7.30 ──> 精确 QA
         └─> QA 长期组恢复（等待精确候选）

PM-016 + CONTENT-003 初始冻结 + 视觉规格首次确认 ──> PM-017 三图概念 ──> 用户二次确认 + FE-012 共享接口冻结 ──> FE-017 共享壳冻结 ──> 三条互斥课程 lane ──> f017 单一集成 ──> QA-015 / V7.7
CONTENT-003 来源校准（V7.9.51 已完成） ─────────────────────────────────────────────────────────────────────┘
CONTENT-003 + FE-012 ──> CONTENT-001 ──> UX-003
PM-009 源码对账 ──已证明两项缺口──> BE-007（教师证据发现 + 管理课程状态）
BE-007 ──┬─> UI-003（已集成） ──> FE-013 / V7.8.5
         └─> DATA-001 ──> DATA-002
CONTENT-004 ──> BE-013 + DATA-003（已集成） ──> FE-013 / V7.8.5
CONTENT-001 + UX-003 + FE-012 + BE-007 + BE-011 ─────> FE-013 / V7.8.5
CONTENT-001 + DATA-002 + FE-013 / V7.8.5 ──> QA-013 / V7.8.6 RETURN ──> FE-013 / V7.8.7 ──> QA-013 / V7.8.8 RETURN ──> FE-013 / V7.8.9 ──> QA-013 复验
FE-013 复验 + UI-003 + QA-015 ──> QA-013 最终候选 ──> REL-008 / V8.0

V8.0 展示候选 + 明确 provider / 数据 / 责任边界 ──> AI-001 / AI-002 / AI-003（分别恢复）
```

当前依赖链已经恢复，但写入按门禁分波次开放。CONTENT-004、DATA-003 与 BE-013 已按合同→producer→投影顺序集成并关闭写入，教师事实后端与演示数据合同形成稳定基线。FE-013 / V7.8.9 已按 V7.9.62 只向原 fr89 owner 恢复 7 文件写入；QA-014 旧运行包继续冻结。CONTENT-003 / V7.9.51 已完成 25 号矩阵来源校准并解除 Future 内容门禁；FE-017 与 QA-015 必须消费该精确矩阵。FE-017 已完成视觉双确认、共享接口、独立树、R5 壳层冻结与三条课程 lane 实态登记，但 R8 独立复审平台阻断解除前不得继续。BE-007、BE-011、BE-013、UI-003 的已集成基础切片、DATA-001、DATA-002、DATA-003、TOOLS-002 与 CONTENT-004 均已进入主线；UI-003 / V7.9.60 仍未集成。最终演示联调仍由 QA-013 / QA-015 汇合。保全工作树本身不构成写入授权。

## 5. 验收与风险

- 每个版本至少通过任务专项、受影响全量自动化、`git diff --check`、文档链接 / 对接门禁；用户可见路径增加真实浏览器证据。
- 展示版核心门禁是登录、角色隔离、三单元代表课、教师协同、管理治理、SQLite 对账和 9001 一键启动，不再要求生产环境证据。
- V7.7.28 唯一 TEMP `C:/Users/niu-h/AppData/Local/Temp/astra-qa-v7728-ee8d104947b74b589f1bd3310f8f36cb` 与 V7.7.29 唯一 TEMP `C:/Users/niu-h/AppData/Local/Temp/astra-qa-v7729-8fa82b10499d41e7aea9be9aa8c5b9d9` 均保持原样。V7.7.29 的 38 文件证据继续证明当时已执行矩阵；V7.7.28 保留现场的补充截图 / manifest 证明此前未覆盖的 Zoom 移动→桌面 P2，两者不得互相覆盖或复用为返修后 PASS。Browser 均已释放、9001=0；下一轮必须在精确集成树与全新 runtime 中独立复验。
- 风险：四轮 / 8 小时预算被误当作降低验收标准；物理课程为满足计数而虚增第三次 attempt、把教师 correction 或客户端 completed 冒充学生完成；教师 evidence owner 只比较 class/course 而忽略同 scope attachment 双向变化，或把 attachment 塞进 correction scope fence、强制主页面重复刷新制造重复请求；教师事实策略用理想 `e²` 误杀真实离散观测，或把 runtime error / runner_unavailable 误写成公开样例不匹配；known locked / hidden gate 只修单一 slug 而继续绕过发布态；started 去重误伤真正换身份 / 换 scope；Escape owner 与 Zoom / dock / module 仲裁冲突；Zoom 只用 CSS 或 scale clamp 掩盖 backing store / DPR / W/H 漂移、或新增第二个 Observer / Canvas writer；18 门课程只换路由而没有教学闭环；未来星系方向页继续混用目录与课程职责并出现内容遮挡；BE-011 精确键处置被扩成可枚举目录或泄露 hidden 元数据；FE-012 与 FE-017 同时写共享入口；detached a02c 被误删、整体提交或冒充稳定实现；诚实降级被包装成成功；课程名称或规则摘要被误宣传为已具备 AI。
- 不做：新增课程数量、装饰性 3D、任意 SQL / 表编辑、宿主执行学生源码、伪造 accepted / 完成 / AI / 生产状态、为了压测或公网部署推迟当前展示主路径。

## 6. 需要用户提供或确认

当前开发已恢复。课程保留、深链兼容、模板边界、FE-012 候选、执行顺序和最终演示口径均已在第 1.4.3 节冻结；后续只在以下节点请求必要输入：

1. FE-017 写页面前，用户先确认 [`26-V7.7未来星系页面视觉规格.md`](02-子文档/26-V7.7未来星系页面视觉规格.md)；主开发随后按 Build Web Apps 流程生成完整桌面 / 移动概念图并再次取得确认，确认后的规格与图共同成为实现和 QA 的忠实度基线。
2. `DATA-001 A01` 已依既定裁决选择“稳定无个人信息用户名 + 每个全新目录动态生成并仅显示一次的强密码”；账号、14 Course / 42 Unit、幂等与安全重置合同见 29 号子文档。后续无需再次请求账号方案，只有用户明确改变凭据策略时才重开决策。
3. 只有用户恢复生产化、真实课堂或真实 OJ 时，才请求域名、目标环境、MySQL、runner、真实参与者和责任 owner。
4. 只有用户恢复 AI-001—AI-003 时，才请求 provider、预算、可发送数据、模型 / 工具 allowlist、未成年人使用边界和最终执行责任人；在此之前不要求配置 API key。
