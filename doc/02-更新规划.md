# 星序 Astra — 更新规划

> **文档梗概**：校内课程资源、分叉同步、学校审核、教师发布和学习结果管理的完整实施方案；本文是业务规则、正式任务、依赖与验收条件的唯一原件。
>
> **具体规划法则**：
>
> 1. 以项目负责人在本轮讨论中确认的规则为准。实验端强调广度、递进、适配和解释性；管理平台完成主要后端能力及必要页面。
> 2. 架构图和对象关系见 [01 第 13 章](01-开发者手册.md#13-目标架构与设计待实现)。其中新增设计均为待实现，不能覆盖 01 中已经核实的当前实现。
> 3. 本次 `DOC-006` 只写文档；下方产品开发任务均未实施，不自动启动代码、数据库、部署或后台工作组。
> 4. 当前代码基线与未提交工作区改动分别记录。不分配未获安排的发布号，不虚构 Git 提交、完成状态或新测试结果。
>
> **当前提交基线**：`main`；本次收尾前的内容提交为 `b9bac97`，包含 V8.6.6、ARCH-011、BE-031、BE-032 与 DOC-006。REL-009 只做清理、记录及 main 集成，未新增产品发布号。
>
> **当前规划周期**：校内课程平台完善；本次完成设计落档，研发截止日期与正式版本尚未安排。
>
> **最后一次更新时间**：2026-09-06
>
> **更新者**：主开发组

---

## 0. 全局总览与硬规则

星序面向单校使用：管理端由学校管理，教师制作与组织教学，学生按授权学习。本校实例保留系统预设资源，不建设跨校公共平台或跨校资源交流。

“系统资源”与“教师授课课程”是不同对象。系统资源可以在登录后自由探索；教师发布到学校资源库的课程，未加入的学生只能看介绍，加入后才能读取正文。资源可重复引用，课程、教学分叉和学生记录仍有各自的身份。

教师修订公共内容时，只同步到本人名下、具有明确同源关系且确有相应内容的课程。学校既可一次批复，也可逐门管理。审核通过不等于已经发布；教师只能发布学校审过的快照。旧学习记录与成绩保留，是否补做由教师明确选择。

本轮实验端交付扩课与统一改进计划，不以少数精品课程或 3D 代表实验为实施主线。管理平台的教师、学校和学生页面属于后端业务闭环的必要交付，不以仅有 API 代替可操作流程。

## 目录

0. [全局总览与硬规则](#0-全局总览与硬规则)
1. [当前基线与交付目标](#1-当前基线与交付目标)
2. [完整实施方案](#2-完整实施方案)
3. [当前任务状态](#3-当前任务状态)
4. [实施批次与依赖](#4-实施批次与依赖)
5. [验收与性能目标](#5-验收与性能目标)
6. [已确认决策与默认处理](#6-已确认决策与默认处理)
7. [长期方向与暂缓任务](#7-长期方向与暂缓任务)
8. [已完成内容迁移账本](#8-已完成内容迁移账本)

---

## 1. 当前基线与交付目标

### 1.1 已有事实

现有系统具备三角色、学校与班级、教师申请、建课审核与选课、多人共享草稿、不可变发布、三种完成预设、学习证据和工作台。正式目录为 `90 / 18 / 19 = 127` 个活动身份；它不是新分叉课程的数量，也不代表所有活动具备相同记录能力。

当前仅有 `/api` 与 `/api/v1` 主业务入口；课程信息审核与教师内容发布分离。课程家族、跨分叉同步、逐候选内容审核和本文所列 `/api/v2` 尚未实现。不能把已存在的内容版本或页面预演等同于本计划的新版本管理系统。

上一轮审查与工作区修正的真实结果见 [03 工作区交付与规划归档](03-发布历史.md#6-规划替换与工作区交付归档)。`ARCH-011 / BE-031 / BE-032` 的范围和验证已迁出活动任务区，不作为本轮研发已完成的证据。

### 1.2 本轮研发完成后的可观察结果

1. 教师能从系统资源、已发布且有权取用的校内课程或空白模板建立自己的课程，配置讲解、题目及开放的互动设置。
2. 同一套内容可以派生为高一版、高二版等并行课程，各有名称、编号、设置、学生关系与发布历史。
3. 教师一次提交公共内容修订，学校能看到来源课程、影响版本和逐项差异，并批量或逐门处理。
4. 教师发布已通过的候选后，学生按所属课程和版本学习；历史成绩、补做和旧结果认定都可追溯。
5. 资源目录、课程查询、统计、后台任务与故障诊断形成一致的后端服务；必要页面能操作完整流程。
6. 实验端形成按课程覆盖、前置关系和共用品质标准组织的扩展与修改清单。

## 2. 完整实施方案

### 2.1 术语、身份与模型边界

| 对象 | 目标职责 | 身份与约束 |
| --- | --- | --- |
| 系统资源 / 资源版本 | 本校实例预置的学习内容、来源、运行引用与能力说明 | 保留稳定资源键；新版本不覆盖旧版本；教师不能写入系统原件 |
| 模板 / 模板版本 | 可编辑字段、参数、渲染方式、验证和记录能力 | 参数受模板约束，运行代码与配置分离 |
| 课程家族 | 表达有证据的共同来源 | 由取用或派生过程建立，不按标题、学科或相似正文猜测 |
| 教学分叉 | 面向不同教学对象的独立课程 | 以课程 ID 区分，昵称和学科用于展示与分类；高一版、高二版各有名单 |
| 单元 / 内容块实例 | 一门课内的具体学习安排 | 实例 ID 与资源来源 ID 分离；重复引用同一实验不会共用完成结果 |
| 草稿 / 候选修订 | 可编辑工作内容与送审快照 | 草稿有并发代际；候选冻结内容、设置、资源引用和补做策略 |
| 修改批次 / 审核项 | 一次提交与每门受影响课程的审核结果 | 一个批次有多个审核项，审核状态以各项为准 |
| 发布包 | 实际对学生生效的课程版本 | 绑定已通过候选和完整版本依据，追加产生，不原位改写 |
| 学习尝试 / 评分历史 / 结果认定 | 行为、答案、评价与版本之间的来源关系 | 补做产生新尝试，历史不覆盖；认定引用既有结果并保存依据 |

现有 `Course` 作为教学分叉的迁移起点，增加谱系和版本关系；不把“高二版”仅做成高一课程页面的一个名称开关。课程内的修订序号与教学分叉编号分别展示。

新接口明确使用课程、单元、发布版本与学习上下文。旧 `course_cohort` 由兼容服务解析；自主探索不创建虚假班级。新旧存储之间有明确映射，不建立两份长期并行的完成真值。目标关系图见 [01 第 13.2—13.3 节](01-开发者手册.md#132-系统资源模板与课程分叉关系)。

### 2.2 系统资源、模板与教师创作

接入现有资源目录，记录标题、学科、来源、适用信息、内容版本、运行入口及真实支持的配置和记录能力。保留既有稳定键与必要兼容路由，模板和教师分叉不混入“127 项系统活动”的历史计数。

教师可以取用、重组资源或从空白模板开始，保留原作者和来源信息。初期复用现有导入、讲解、学习任务、媒体、活动引用、检查点与来源块，并补齐函数参数图像、数据图表两类基础配置模板，服务多门课程的制作。

模板编辑采用表单与即时预览。只开放模板声明的公式、参数、图表和情境；不开放任意脚本执行或自由拼装交互规则。可计算公式通过受控表达式解析与白名单运算求值，前后端使用一致的表达语义；未知字段、非法公式、缺失资源和越界配置明确拒绝。

资源能力必须如实展示。旧实验没有可靠操作记录能力时，教师采用检查点或作业作为完成方式，不能发布“会自动记录操作完成”的虚假配置。新增模板保留版本化运行制品；无法补齐的旧运行环境标为兼容模式，不宣称已经恢复全部历史渲染字节。

### 2.3 教学分叉与独立草稿

从同源课程取用或派生时，新建独立课程 ID、单元和内容块实例，保留家族与来源关系。派生不复制学生名单、提交、成绩、积分或正式完成状态；老师重新配置授课对象与准入条件。

每个分叉有自己的昵称、设置、草稿和发布历史。单元顺序、增删、年级设置等允许不同。同一课程的共同教师继续共享草稿，通过版本条件处理并发编辑；不能用最后一次保存静默覆盖他人的修改。

直接从空白模板建立的课程默认独立。仅有相同模板、标题或学科不足以判定同源；没有证据的旧课程也不自动合并到一个家族。

### 2.4 公共内容同步与冲突处理

教师选中“同步其他版本”后，先由后端形成只读预览，不能立即改变其他课程或学生正在使用的发布包：

1. 校验发起人拥有源课程，按同校、本人名下、同源关系筛选可同步目标；共同教师的单课编辑权不自动成为跨课同步权。
2. 计算本次编辑前后的实际差异，按内容来源 ID 匹配目标实例，不能按标题或数组下标替换。
3. 只传播已有公共内容的允许字段。课程昵称、授课对象、名单、章节选择、增删和排序不自动传播。
4. 没有相应内容时标记“无对应内容，跳过”，不补插章节；目标已经具有新值时标记“无需修改”。
5. 目标仍是本次修改前值时可同步；目标已有不同修改时标记冲突，教师在预览中选择保留或替换，不默认覆盖。
6. 预览按课程列出修改、跳过、冲突和学习结果影响。教师确认后，一次提交生成修改批次及各课程的候选快照。
7. 提交时重新检查源、目标代际与归属。预览过期、权限变化或未处理冲突必须返回明确结果，不悄悄部分应用。

**标准场景**：高一数学包含向量、三角、复数，高二数学包含复数、微积分。修正高一版复数与三角的公式时，高二版只预览复数的对应修改，不新增三角内容；另一位教师的课程不参与同步。

同步流程和分支判断见 [01 第 13.4 节](01-开发者手册.md#134-跨分叉同步与冲突判定)。

### 2.5 学校审核：批量操作与逐门结果

审核工作台展示修改来源、源版本、受影响课程、具体内容差异、配置差异与补做策略。管理员可选择一批审核项统一通过或退回，也可展开逐门处理。

每门课程独立记录审核结论、审核人、意见与时间；批次状态由条目聚合，不强迫所有课程取得相同结论。高一通过、高二退回时，允许高一进入发布准备，高二继续使用旧版。

送审候选不可修改。教师可以继续编辑后续草稿，但同一课程同时只允许一个待审候选；需要替换待审内容时，先撤回再提交。已有待审目标必须在同步预览中明确处理，不隐式叠加新的审核请求。

审核写入检查条目状态与版本条件。一次批量操作的选中范围保持事务一致；过期或不合法的请求不留下不明的部分审核结果。管理员随后仍可针对各项分别操作。权限按学校和实际角色校验，不能由客户端自行声明“已审核”。

### 2.6 教师发布、回滚与版本生效

教师仅能发布已通过的候选快照，不能发布审核后又改动的草稿。发布前重验权限、当前发布基础、资源有效性及候选完整性。

可一次发布选中的已通过课程；该次选中集合在事务内完成发布包、规则、绑定、审计和后台任务入队，失败整体回滚。其他未选课程和被退回课程保持各自现状。重复发布请求按幂等标识回读既有回执，不生成重复发布包或任务。

发布后，后续进入课程的学生读取新版本。已开始的尝试按注册时的版本收尾，结果按新版本认定策略决定是否计入当前状态；失去课程权限后不能继续写入。

回滚从历史版本建立新修订，重新审核后发布。历史编号、发布内容、审核意见与学生记录均不覆盖。旧的直接发布或回滚写入口同样接入新检查，不能借兼容接口绕过学校审核。状态与时序见 [01 第 13.5—13.6 节](01-开发者手册.md#135-审核项与发布状态)。

### 2.7 学习上下文、成绩留档与补做

学习上下文分为系统自主探索、正式授课学习、教师预览。身份、课程范围和版本由服务端确认；学生自报动作、服务端判分、教师评价分别记录来源。教师预览不计入学生结果，自主探索不自动折算为某门教师课程的成绩。

| 变化或场景 | 目标处理 |
| --- | --- |
| 纯展示性修改 | 默认沿用旧结果，保留原始依据 |
| 题目、完成要求或影响学习行为的配置变化 | 预览列出影响，由教师明确选择沿用或补做；选择随候选快照送审 |
| 同课其他单元发生变化 | 不能仅因课程整体规则换版就让未受影响单元失去完成状态 |
| 学生补做或教师重新评分 | 建立新尝试与评分历史，不覆盖旧提交和旧分数 |
| 新版认定旧结果 | 追加认定记录，指向旧结果、目标版本、策略和责任人；不改写旧事实 |
| 当前版本成绩与进度 | 只引用本版有效或明确认可的结果，与历史版本分开回看 |
| 重复请求、重放或任务重试 | 不能重复提交、认定、计分或发放积分 |

不把已有 `transferred` 状态改成“跨版本继承”的新含义。完成、正确性、教师反馈和掌握判断分别表达；不能因访问页面或任意客户端声明就产生权威成绩。

统一成绩、完成投影与积分计算入口，保留原有账本的可追溯性。版本绑定与认定图见 [01 第 13.7 节](01-开发者手册.md#137-学生访问版本绑定与结果认定)。

### 2.8 两种学习入口与权限

| 内容或操作 | 学生 | 教师 | 学校管理员 |
| --- | --- | --- | --- |
| 系统预设资源 | 登录后可自由探索 | 可取用、按模板制作自己的课程 | 维护本校系统资源入口与配置治理 |
| 已发布校本课程介绍 | 可查看；符合条件时申请加入 | 在授权范围查看、取用或派生 | 查看与管理本校课程 |
| 校本课程正文与历史版本 | 加入且具有有效权限后读取 | 自己或获授权协作的课程可编辑；取用不等于修改他人原件 | 为本校审核与治理读取 |
| 草稿与送审候选 | 不可读取 | 根据归属、协作权和候选状态操作 | 为审核读取，不能替教师绕过发布步骤 |
| 跨分叉同步 | 不可发起 | 仅本人名下的源与目标课程 | 审核影响结果，不能伪造教师发起身份 |
| 学习记录与成绩 | 仅本人及授权范围 | 仅自己有教学权限的范围 | 本校有明确管理目的的授权范围 |

小红未加入小明老师的高二数学课时，只能查看介绍，不能通过正文、版本、启动接口或缓存绕过准入。课程权限失败时，页面不能悄悄换成系统实验并继续显示教师课程身份。

### 2.9 目标接口与兼容方式

以下为待实现的接口分组，不代表当前已经提供。新增完整能力统一进入 `/api/v2`；身份与权限仍复用现有会话基础。

| 接口组 | 最低能力与输入输出要求 |
| --- | --- |
| 资源与模板 | 分页目录、资源/模板版本、允许配置、校验与预览；返回真实能力和来源 |
| 课程创作 | 新建、取用、派生、草稿读写；区分课程 ID、修订代际、资源来源和实例 ID |
| 修改批次 | 同步预览、一次提交、逐项/批量审核、候选回读；返回修改、跳过、冲突和意见 |
| 发布与历史 | 发布选中的已通过候选、发布回执、分页版本摘要和单版本详情、回滚修订 |
| 学习 | 启动上下文、尝试、过程记录、结果、补做、旧结果认定与历史回读 |
| 管理与建议 | 校内检索、审核待办、通知、统计、导出任务、课程关联与解释性建议 |

写请求携带版本条件与幂等标识；身份、归属、来源与审核状态由服务端维护。冲突、拒绝、未知写入结果和可重读回执使用稳定错误/结果语义，前端显示明确中文操作结果。

现有 `/api`、`/api/v1` 保留适用的兼容读取，不能产生第二套业务真值。旧客户端不能表达新能力时明确提示升级，不返回缺失正文、丢失参数或伪造成功。新旧发布写入口均由同一审批与事务服务约束。

### 2.10 分层、统计、关联建议与后台任务

后端按身份与组织、资源模板、课程创作、分叉同步、审核发布、学习评价、查询统计、后台任务与审计划分职责。HTTP 适配不继续堆入统计和持久化细节；模型字段归属明确，兼容只通过显式适配。

统计统一人群、学校/课程范围、时间与发布版本。发布历史改为分页摘要，选中后才取正文；课程汇总使用批量查询，清除逐课程重复读取。复用请求编号和耗时记录，贯通修改批次、审核项、发布和任务，能够定位慢查询与失败来源。

课程关联区分必需先修、推荐先学和相关拓展，校验循环关系与失效引用。自主探索只提供建议；正式课程先修限制由教师明确配置。建议根据有效事实输出下一步和原因，不自动选课、改分或虚构掌握程度。实验端的学习网络展示按扩课计划后续推进。

复用现有 DB-backed worker 支持站内通知、汇总重算和导出。业务写入与任务入队一致，执行有重试、幂等、失败诊断和权限复验。外部投递通道不因增加站内任务而自动开启。

### 2.11 渐进迁移与维护边界

以现有工作区为起点，先建立对象映射与迁移校验，再逐步接入新服务和页面。保留旧身份、真实发布、学习记录和上一轮修正，不通过重置数据获得干净的新模型。

能够核实的来源才补入谱系；未知的旧课程保持独立。历史发布保留其当时身份，不能补写虚假的学校审核记录。未完整迁移的旧能力明确使用兼容读取，不扩大可见范围或绕过新发布要求。

采用有目标映射的 Alembic 迁移，验证空库、已有数据、升级、回退与再次升级。先使读取和旧数据映射成立，再切换创作、审核发布及学习写入；移除旧通道前完成消费者与回归迁移。

### 2.12 实验端课程拓展与统一改进计划

本轮对实验端交付初步计划，内容实施依后续任务推进。规划按学科盘点现有课程的覆盖、重复、前置关系、互动与解释缺口；课程编号、数量和标题以实际目录核对，不把教学分叉重复算成新增系统实验。

数学优先补充启发性、趣味性与生活决策方向。下表是候选规划主题，尚未新增为正式课程，也不承诺每个主题对应一个新运行器。

| 候选方向 | 学习组织重点 | 与既有内容的连接 / 复用检查 |
| --- | --- | --- |
| 入门博弈论 | 选择、收益、合作与竞争，逐步建立策略直觉 | 评估与概率、表格、参数图表模板的连接 |
| 随机现象与模拟 | 随机漫步、抽样与多次观察的差异 | 优先复用概率统计的观测与图表能力 |
| 优化与决策 | 约束、目标与方案取舍 | 对照不等式、导数应用、建模内容，避免重复命名 |
| 网络与数学结构 | 关系、路径与结构的可视化理解 | 对照现有算法图实验，优先跨学科组织与解释复用 |
| 迭代与规律探索 | 简单规则产生的变化与图形 | 对照数列、函数、建模内容，明确真正新增的观察价值 |

每项候选必须记录目标对象、学习目标、建议前置、后续关联、互动形式、解释方式、模板复用、工作量与验收方式。下一轮扩充顺序依据覆盖缺口和复用收益，不以少数精品课程提高个别深度。

共同质量基线包括：观察对象清楚、交互真实有效、参数和结果可解释、失败有反馈、模型边界有依据、键盘/触控和移动 Web 可用。课程难度可以递进，讲解与交互品质保持共同标准。既有课程按共性问题成组改进，3D 作为后续适用内容的可复用表达能力评估。

### 2.13 独立前端原型（2026-09-06）

用户指定在 `qianduan/` 独立制作新 Web 前端，先设计效果和交互，以演示数据替代后端数据。一级页面关系为登录、星序总览、管理、工科实验室、代码空间、未来星系；本次主做登录与星序总览，其余一级入口保留可返回的页面占位，不接入既有产品或冒充新后端能力。

- 首次进入先播放“众星归位”，随后展示可拖拽、随鼠标跟随的真实三维星云。封面、入场和登录共用星云轮播；移除右上角球体。用户明确要求以参考图指导程序化绘制，不采用固定星空图片配合视差。封面保留“星序 / ASTRA / 点击以探索星序”，副标语留空。
- 点击探索后检查浏览器内的演示会话；未登录进入左侧星空、右侧表单的登录页，已有演示会话进入总览。模拟登录不保存密码，不视为正式账号认证。
- 总览使用顶栏、可收缩侧栏和内容区；根据后续批注将侧栏改为更轻的透明导航，减少圆角矩形。顶栏左侧品牌紧邻当前页简称，右侧搜索和“输入邀请码”；主要空间收进品牌菜单。侧栏仍依次为“星序总览、课程、班级、笔记”，底部为“账号信息”。默认内容保持最多三条提醒、左课表和右日历的倒“品”字布局。
- 课程视图从右下角展示约四分之一星系；恒星、尘埃与旋臂连续排列，课程使用不同形态与色调，移除彩色行星围绕单个小太阳的旧样式。提供点选、拖拽、键盘和前后按钮；左侧无厚卡片的课程信息与选中对象保持一致。课程定位曲线仅为导航辅助，不把它包装成天体轨道。
- 使用 Three.js、TypeScript 与独立构建入口，分开场景渲染、交互状态、视图和数据服务。资源按生命周期释放；处理窗口/侧栏尺寸变化、页面隐藏、减少动态效果、WebGL 不可用时的静态可操作降级。体素场景与真实后端按后续任务扩展，不虚构 WebGPU 或体素引擎完成状态。
- 用户随后明确：本轮以桌面主流程和整体视觉为交付点，不继续细磨手机适配与边角效果；已有基础适配保留，真实后端接入后再统一调整。
- 已确认并由 FE-052 实施的多轨方案：以 A 为基底融合 C，圆弧朝左，多条星系轨迹并排，一轨一课、两门为主、边缘循环补位。星云主带、定位轨迹和标记使用同一三维坐标；加入近处间距较大、远处间距较小的倾斜视角。向内浏览时外轨淡出、内轨向前展开；反向浏览时内轨隐去、外轨返回。左侧说明固定并在课程就位后更新；末尾与开头连续衔接，不建立轨道深浅与课程难度的隐式绑定。

## 3. 当前任务状态

REL-010 与 BE-042 已完成并归档至 [03 第 8 节](03-发布历史.md#8-2026-09-07-正式门户接入与前后端封装)。前端、部署与交叉审查继续按以下任务执行。

- [ ] FE-053｜正式门户与师生教学流程接入
  - 改动：新前端成为正式门户；补齐教师课程、版本、班级、作业批改，学生作业与反馈和必要管理员审核；保留三星系课程页面。静态演示与真实 API 明确分开。
  - 验收：学生、教师、管理员真实会话下完成课程审核、发布、加入、作业提交和批改；演示包同一套 UI 可独立浏览。
  - 追踪：状态：进行中｜目标版本：暂无｜模块：正式前端｜主责：FE组｜协作：无（单线）｜前置：REL-010｜关联文档：本文｜关联版本：暂无

- [ ] REL-011｜前后端封装、GitHub Pages 与分支收束
  - 改动：main 保存完整项目，qianduan 发布可独立运行的静态前端；用户已确认 Pages 使用演示数据，本机使用真实后端；部署完成后核对并清理旧分支。
  - 验收：本机封装可重复运行、静态子路径与 Pages 线上验证通过、真实 Git 提交与分支清理证据可追溯。
  - 追踪：状态：已认领｜目标版本：暂无｜模块：发布与仓库｜主责：REL组｜协作：无（单线）｜前置：FE-053、BE-042｜关联文档：本文｜关联版本：暂无

- [ ] ARCH-013｜跨前后端与课程体系交叉审查
  - 改动：核对架构、数据和权限、课程广度/递进/交互/深度/表现；修复本轮范围内问题，并将版本管理等后续目标与真实实现分开更新到现有文档。
  - 验收：问题有证据、修复有回归、未实现目标与遗留风险明确，01/02/03 职责和进度一致。
  - 追踪：状态：进行中｜目标版本：暂无｜模块：架构与内容审查｜主责：ARCH组｜协作：无（单线）｜前置：REL-010｜关联文档：本文｜关联版本：暂无

- [x] FE-052｜状态：已完成｜执行阶段：桌面透视多轨轮盘交付｜目标版本：未分配｜模块：qianduan 课程视图｜任务：按确认的 A+C 左向多轨方案实现近疏远密的统一三维透视、局部循环课程与整轨换层；停稳后更新课程说明｜主责：前端组｜协作：无（主开发单线）｜前置：UI-019 及用户明确要求按当前方案实施｜关联文档：[原型说明](01-开发者手册.md#54-qianduan-独立前端原型)、[验证记录](03-子文档/39-跨版本综述与规划迁移附录.md#c9-fe-052-透视多轨课程轮盘实现)｜关联版本：无（未提交工作区交付）｜验证：11 项测试与构建通过；1277×898 Edge 实测内外切换、首尾循环、拖拽、键盘、直接点选、搜索定位、减少动态效果、静态降级与资源释放；投影量测近处间距约 225px、远处约 184px，不扩展手机精调

- [x] UI-019｜状态：已完成｜执行阶段：多轨方案确认并转入 FE-052｜目标版本：未分配｜模块：qianduan 课程视觉｜任务：生成 A/B/C 与合并示意，明确左向多轨、局部课程及内外循环换层｜主责：UI组｜协作：无（主开发单线）｜前置：用户 A+C 合并、手绘与近远透视指引｜关联文档：[合并效果](image/ui019-multi-orbit-approved.png)、[切换过程](image/ui019-orbit-sequence.png)、[选型记录](03-子文档/39-跨版本综述与规划迁移附录.md#c8-ui-019-课程局部循环拨盘示意选型)｜关联版本：无｜验证：用户明确要求按当前方案实施，实时三维要求保持；实现归 FE-052

- [x] FE-051｜状态：已完成｜执行阶段：桌面三维原型交付｜目标版本：未分配｜模块：qianduan 星云与导航｜任务：移除球体和运行时星空图片，以三维密度场、体积积分和恒星几何实现开屏/登录轮播及右下角课程星系；重做透明侧栏与品牌/当前页/搜索/邀请码顶栏｜主责：前端组｜协作：无（主开发单线）｜前置：FE-050、用户页面批注及对真实三维绘制的追加要求｜关联文档：[原型实现](01-开发者手册.md#54-qianduan-独立前端原型)、[本轮记录](03-子文档/39-跨版本综述与规划迁移附录.md#c7-fe-051-实时三维星云与导航重构)｜关联版本：无（未提交工作区成果）｜验证：类型/构建与 4 项日期测试通过；1277×898 Edge 验证相机拖拽、星云切换、入场/登录、课程切换与预览、搜索、邀请码、侧栏、静态动效与离页释放；未声明移动端或跨设备稳定帧率通过

任务只在本节维护勾选原件。责任组名称表示职责，当前仍由主开发单线承担，不代表创建了长期工作组。DOC-006 与 REL-009 已完成；2026-09-06 用户暂停 Figma 工作，FE-050—FE-052 已在 `qianduan/` 交付桌面前端、实时体积星云和透视多轨课程轮盘，进一步细化随用户后续指引展开。原有 17 项产品研发任务仍为“未认领”、执行阶段为“待实施”；责任分工与依赖不构成自动开工。

- [x] FE-050｜状态：已完成｜执行阶段：桌面原型交付｜目标版本：未分配｜模块：qianduan 开屏、登录、总览与课程星盘｜任务：按 2.13 实现独立可运行前端、3D 星空与课程拨盘、演示状态和基础响应式布局｜主责：前端组｜协作：无（主开发单线）｜前置：FE-049 与用户本轮页面/技术/封面指引｜关联文档：[原型规则](#213-独立前端原型2026-09-06)、[实现说明](01-开发者手册.md#54-qianduan-独立前端原型)｜关联版本：无（未提交工作区交付）｜验证：类型检查与构建通过、4 项日期边界测试通过；Edge 实测开屏/访客进入/会话恢复、三块总览、任务完成、跨月日程、星盘按钮/拖拽/键盘、课程预览与离页释放；390px 基础布局无横向溢出。按用户要求停止进一步手机精调；辅助路径未测范围在 39 号 C.6 明确记录

- [ ] UI-018｜状态：暂缓｜执行阶段：Figma 布局绘制未完成｜目标版本：未分配｜模块：通用 Web 页面设计稿｜任务：核对当前页面并在用户已打开的 Figma 文件中绘制可编辑通用布局，重复实验仅取一个代表，保留后续交互调整空间｜主责：UI组｜协作：无（主开发单线）｜前置：用户明确要求在 Figma 绘制当前通用页面布局｜关联文档：[当前页面结构](01-开发者手册.md#5-前端页面路由与可见交互)、[UI 规范](05-UI规范模板.md)｜关联版本：无｜验证：已完成部分导入与网页对照，整体尚未验收，不标为交付完成｜暂缓原因：用户明确要求先不处理 Figma，转向新前端目录｜暂缓开始：2026-09-06｜预计恢复：待用户明确恢复｜下次复查日期：未安排，由用户恢复指令触发

- [x] FE-049｜状态：已完成｜执行阶段：独立目录准备｜目标版本：未分配｜模块：新前端工作区｜任务：创建 `qianduan/` 独立目录，以 `.gitkeep` 保留空目录，后续页面与交互按用户逐步指引实施｜主责：前端组｜协作：无（主开发单线）｜前置：2026-09-06 用户明确要求创建 qianduan 并开始新前端设计｜关联文档：本文、[新前端目录](../qianduan/)｜关联版本：无（工作区变更，未提交）｜验证：目录已建立，现有前端与已有用户文件保留；本任务不代表新前端页面已经实现

- [x] REL-009｜状态：已完成｜执行阶段：工作区收尾｜目标版本：未分配（普通开发提交）｜模块：工作区清理与 main 集成｜任务：备份全部当前改动和 Git 引用，归档旧配置、缓存与截图，按任务提交既有成果并统一 main 基线｜主责：主开发组｜协作：无｜前置：DOC-006 完成及用户授权清理、向 main 提交｜关联文档：[03 收尾记录](03-发布历史.md#7-2026-09-05-开发提交与工作区收尾)、[协作约定](../AGENTS.md)｜关联版本：无｜验证：31 个目录、677 个文件归档并逐文件校验；Git bundle 验证通过；后端定向 42 项、前端语法 281 项和契约 82 项通过；文档引用、快照和 diff 检查通过，main 连续且工作区干净

- [x] DOC-006｜状态：已完成｜执行阶段：文档落地｜目标版本：未分配｜模块：01/02/03 与归档｜任务：写入已确认方案、七组目标图，迁移旧规划与未提交记录并验证引用｜主责：文档组｜协作：无（当前主开发单线）｜前置：用户已确认的文档落地方案｜关联文档：本文、[01 目标架构](01-开发者手册.md#13-目标架构与设计待实现)、[03 归档](03-发布历史.md#6-规划替换与工作区交付归档)｜关联版本：无｜验证：三份主文档结构与任务检查 0 问题、191 处链接有效；1 号全部 13 张 Mermaid 成功渲染，其中 7 组为新增目标图；旧 02 快照及既有历史保全，纯 Markdown 变更与 diff 检查通过

- [ ] ARCH-012｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：领域与协议边界｜任务：整理模型职责、权限与事务入口，明确资源/分叉/候选/学习上下文及新旧协议映射｜主责：架构组｜协作：无（当前主开发单线）｜前置：DOC-006｜关联文档：[2.1 模型边界](#21-术语身份与模型边界)、[01 分层图](01-开发者手册.md#131-系统分层与模块职责)｜关联版本：无｜验证：现状与目标逐项对应，接口/依赖方向可验证，兼容边界清楚

- [ ] DATA-011｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：模型与迁移｜任务：建立资源版本、课程谱系、候选审核及学习历史所需的数据映射和可回退迁移｜主责：数据组｜协作：无（当前主开发单线）｜前置：ARCH-012｜关联文档：[2.11 迁移](#211-渐进迁移与维护边界)、[01 数据关系](01-开发者手册.md#133-草稿候选审核项与发布数据关系)｜关联版本：无｜验证：空库与已有库升级、回退、再升级通过，既有事实可对账，未知来源不补造

- [ ] BE-033｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：资源与模板｜任务：提供资源/模板目录、版本、配置校验和受控预览，声明真实记录能力｜主责：后端组｜协作：无（当前主开发单线）｜前置：DATA-011｜关联文档：[2.2 资源模板](#22-系统资源模板与教师创作)｜关联版本：无｜验证：预设引用可回读，非法配置/缺失资源拒绝，版本与能力一致

- [ ] BE-034｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：课程派生与草稿｜任务：提供取用、派生、独立单元/内容实例及并发草稿能力，不复制学生记录｜主责：后端组｜协作：无（当前主开发单线）｜前置：BE-033｜关联文档：[2.3 分叉](#23-教学分叉与独立草稿)｜关联版本：无｜验证：高一/高二并存，同资源重复引用不串用，过期草稿保存不覆盖他人修改

- [ ] BE-035｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：跨分叉同步｜任务：实现本人同源内容匹配、三值比较、冲突预览、一次提交与冻结候选｜主责：后端组｜协作：无（当前主开发单线）｜前置：BE-034｜关联文档：[2.4 同步](#24-公共内容同步与冲突处理)｜关联版本：无｜验证：标准复数/三角案例、缺失跳过、他人排除、重排匹配、过期与冲突处理通过

- [ ] BE-036｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：学校审核｜任务：提供修改来源与影响明细、逐项/批量批复和可追溯审核意见｜主责：后端组｜协作：无（当前主开发单线）｜前置：BE-035｜关联文档：[2.5 审核](#25-学校审核批量操作与逐门结果)｜关联版本：无｜验证：整批与逐门可操作，混合结果正确，快照不可变，审核条件写入可靠

- [ ] BE-037｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：发布与回滚｜任务：统一新旧发布门禁，发布已通过候选，维护原子绑定、幂等回执和回滚新修订｜主责：后端组｜协作：无（当前主开发单线）｜前置：BE-036｜关联文档：[2.6 发布](#26-教师发布回滚与版本生效)｜关联版本：无｜验证：未审拒绝、通过后改草稿不夹带、选中集合失败回滚、重复与回滚流程通过

- [ ] BE-038｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：学习结果与成绩｜任务：统一上下文、尝试与评分历史、补做策略和旧结果认定，迁移当前投影读取｜主责：后端组｜协作：无（当前主开发单线）｜前置：BE-037｜关联文档：[2.7 结果](#27-学习上下文成绩留档与补做)、[2.8 权限](#28-两种学习入口与权限)｜关联版本：无｜验证：自由探索/预览隔离，版本和撤权检查有效，历史不覆盖、不重复计分

- [ ] BE-039｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：课程关联与解释建议｜任务：提供关联类型、循环/失效校验以及依据有效事实生成的下一步说明｜主责：后端组｜协作：无（当前主开发单线）｜前置：BE-038｜关联文档：[2.10 关联服务](#210-分层统计关联建议与后台任务)｜关联版本：无｜验证：建议有可回读依据，不自动选课或改分，先修门槛只按显式课程配置生效

- [ ] BE-040｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：统计与查询｜任务：统一统计口径、分页版本摘要、按需正文和批量课程汇总｜主责：后端组｜协作：无（当前主开发单线）｜前置：BE-037、BE-038｜关联文档：[2.10 查询](#210-分层统计关联建议与后台任务)、[5.2 性能](#52-性能与运行证据)｜关联版本：无｜验证：人群/版本/时间范围一致，历史列表不携带全正文，查询数不随列表大小线性增长

- [ ] BE-041｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：后台任务与追踪｜任务：接入站内通知、汇总与导出，贯通事务入队、幂等、重试和审计追踪｜主责：后端组｜协作：无（当前主开发单线）｜前置：BE-037、BE-040｜关联文档：[2.10 后台任务](#210-分层统计关联建议与后台任务)｜关联版本：无｜验证：提交与入队一致，重试不重复产生结果，失败和权限变化有明确回执

- [ ] FE-046｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：教师创作管理页面｜任务：完成模板编课、派生、同步预览、冲突处置及已通过候选发布页面｜主责：前端组｜协作：无（当前主开发单线）｜前置：BE-033、BE-034；同步/发布部分依赖 BE-035、BE-037｜关联文档：[2.2—2.6 教师流程](#22-系统资源模板与教师创作)｜关联版本：无｜验证：真实 API 下保存、预览、同步、重读与发布可用，桌面/移动 Web 及错误状态通过

- [ ] FE-047｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：学校审核管理页面｜任务：展示源版本与影响列表，支持批量/逐门审核、意见和校内资源治理｜主责：前端组｜协作：无（当前主开发单线）｜前置：BE-036、BE-037｜关联文档：[2.5 学校流程](#25-学校审核批量操作与逐门结果)｜关联版本：无｜验证：混合审核结果可理解，页面不直接伪造发布，权限与双视口操作通过

- [ ] FE-048｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：学生入口与历史回读｜任务：实现系统探索、校本介绍/加入/正文、版本提示、补做与成绩历史视图｜主责：前端组｜协作：无（当前主开发单线）｜前置：BE-038、BE-039｜关联文档：[2.7—2.8 学生流程](#27-学习上下文成绩留档与补做)｜关联版本：无｜验证：未加入无法取正文，探索不计课程成绩，当前与历史版本解释准确

- [ ] PERF-002｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：性能与并发验证｜任务：建立固定隔离数据集与采样，验证新查询、同步发布和任务路径的耗时及 SQL 行为｜主责：性能组｜协作：无（当前主开发单线）｜前置：BE-035—BE-041｜关联文档：[5.2 性能验收](#52-性能与运行证据)｜关联版本：无｜验证：报告数据量、硬件、耗时和查询数，发现的超预算与并发问题完成整改

- [ ] CONTENT-011｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：实验端扩课与统一改进计划｜任务：细化课程覆盖/重复/前置关系盘点，形成数学启发性候选和可复用改进批次｜主责：内容组｜协作：无（当前主开发单线）｜前置：DOC-006｜关联文档：[2.12 实验端计划](#212-实验端课程拓展与统一改进计划)｜关联版本：无｜验证：每项候选有目标、前置、互动、解释、复用、工作量和验收；不以精品课为主线

- [ ] QA-035｜状态：未认领｜执行阶段：待实施｜目标版本：未分配｜模块：校内平台整体收口｜任务：完成迁移、权限、并发、角色业务、前端与后台任务的全链路验收并同步实现文档｜主责：测试组｜协作：无（当前主开发单线）｜前置：BE-033—BE-041、FE-046—FE-048、PERF-002、CONTENT-011｜关联文档：[第 5 节验收](#5-验收与性能目标)｜关联版本：无｜验证：全部必验场景有真实证据，失败修复，跳过/未测明确记录，不用目标图冒充实现

## 4. 实施批次与依赖

| 批次 | 任务范围 | 上游条件 | 退出结果 |
| --- | --- | --- | --- |
| A：基线与模型 | ARCH-012、DATA-011 | DOC-006 完成 | 领域与协议明确，旧数据映射及迁移可验证 |
| B：资源与创作 | BE-033、BE-034、FE-046 的创作部分 | A | 可取用、配置、派生、保存并预览独立课程 |
| C：同步与审核发布 | BE-035—BE-037、FE-046 的同步部分、FE-047 | B | 一次提交、逐项审核和受控发布完整可操作 |
| D：学生与结果 | BE-038、BE-039、FE-048 | C 的发布契约稳定 | 两种入口、版本绑定、补做与结果回读一致 |
| E：查询与任务 | BE-040、BE-041、PERF-002 | C/D 的数据语义稳定 | 统一统计、分页读取、后台任务和性能证据 |
| F：整体收口 | QA-035；CONTENT-011 完成前端计划细化 | B—E 联调完成 | 校内三角色业务闭环通过验收，前端扩课计划可供下一阶段实施 |

CONTENT-011 可在 DOC-006 完成后独立推进规划细化，不依赖新增实验代码，也不恢复并行工作组。本轮没有指定工时上限或截止日期，不能用历史 20 小时预算替代当前范围。

## 5. 验收与性能目标

以下是计划验收场景，不代表已经通过的新测试。每项结果必须能够对应真实请求、数据状态或可观察页面行为。

| 场景 | 必须观察到的结果 |
| --- | --- |
| 高一/高二公共内容纠错 | 复数对应字段同步，缺失的三角跳过，不插入章节 |
| 他人课程、无关同名课程 | 不出现在可修改目标内，不被同步修改 |
| 内容重排和重复引用 | 来源匹配不依赖下标；实例和学习结果互不串用 |
| 目标已有不同修改 | 预览出现冲突，未作明确处理时不覆盖 |
| 预览后发生编辑或归属变化 | 提交明确失败或要求刷新，旧工作内容保留 |
| 整批与逐项审核 | 支持两种操作，审核结论和意见按课程独立留档 |
| 部分通过、部分退回 | 只发布选中的已通过候选，其他课程继续旧版 |
| 通过后继续修改草稿 | 实际发布内容仍严格等于通过的候选快照 |
| 发布失败、重复请求和回滚 | 无半套结果或重复包；回滚追加修订并经过审核 |
| 学生未加入校本课程 | 正文、历史版本、启动入口和缓存均不能绕过准入 |
| 自主探索与教师预览 | 不污染正式课程名单、成绩或学生完成投影 |
| 沿用、补做与再次评分 | 历史保留，当前版结果有有效来源，不重复计分 |
| 运行中换版或撤权 | 在途尝试使用已绑定版本；撤权后停止写入 |
| 非法模板配置或缺失资源 | 明确阻止，不出现空白发布或假成功 |
| 后台任务重试与恢复 | 不重复通知、认定或记账，失败能定位与恢复 |
| 迁移与旧客户端 | 已有记录可对账，未知来源不猜测，不能通过旧入口绕审 |

### 5.1 工程与页面验收

验证迁移往返、权限正反向、并发编辑、真实 API、前端契约和三角色桌面/移动 Web 操作。主要操作覆盖正常、加载、空态、失败、冲突、重试及离页重入；验证键盘、触控、可读解释和数据一致性。

原有学习证据、幂等、事务、接口公开面与实验保护检查继续提供回归保障。对过时的源码形状或行数约束，应依据新架构修正其合同并保留真实行为断言，不能删掉能发现问题的测试。

### 5.2 性能与运行证据

沿用当前配置中的常规接口 500ms、管理聚合 1000ms、导出 5000ms 预算，在固定隔离数据集与明确硬件上记录耗时分布、请求次数和 SQL 数量。导出改为任务时，受理耗时与生成耗时分开报告，不混成一个通过值。

课程和发布列表有界分页，发布摘要不携带所有历史正文；列表与汇总的查询次数不得随返回课程数量线性增加。预览、审核及发布除耗时外还要验证事务和幂等正确性。

本地 SQLite 是本轮完整运行验收边界。保留 MySQL 迁移和查询兼容；真实 MySQL 并发、长期运行、公网部署与灾难恢复按准备部署的版本另作专项，不将兼容检查写成实机证明。

## 6. 已确认决策与默认处理

| 来源 | 决策 | 本文落位 |
| --- | --- | --- |
| 用户明确确认 | 前端增加广度、递进与共同质量，本轮先出修改与拓展计划 | 2.12、CONTENT-011 |
| 用户明确确认 | 系统资源预置，教师可调整并从模板简单创作 | 2.2、BE-033 |
| 用户明确确认 | 模板提供基本交互，教师配置开放项 | 2.2；不扩展为任意交互编程 |
| 用户明确确认 | 高一/高二教学版本并存，有独立昵称、设置、编号与学生关系 | 2.1、2.3 |
| 用户明确确认 | 同步仅限本人名下，同源公共部分缺失时不修改 | 2.4 |
| 用户明确确认 | 学校了解修改来源与影响，可整批也可逐门审核 | 2.5 |
| 用户明确确认 | 旧记录与成绩保留，补做由教师决定 | 2.7 |
| 用户明确确认 | 校本课程未加入只能看介绍；系统内容可自由探索 | 2.8 |
| 用户明确确认 | 不建设跨校交流；后端主要能力应完善落地 | 0、1.2、7 |
| 已采纳的工程默认 | 身份和来源由服务端维护，候选不可变，所有发布入口统一检查 | 2.4—2.9 |
| 已采纳的工程默认 | 冲突先展示，结构与授课设置不随公共内容自动传播 | 2.4 |
| 已采纳的工程默认 | 不分配新发布号，历史兼容与未提交状态明确记录 | 1.1、2.11、8 |

以上决议来自本轮对话及已确认实施方案。71 号文件保留首轮审查背景，不再是当前执行方案原件。

## 7. 长期方向与暂缓任务

本节为范围边界，不是正式任务状态流水。当前不实施：微信小程序、跨校公共平台、跨校课程交流、批量实验重绘、少数精品课深度专项、外部 AI、隔离 OJ 集群、公网部署和真实课堂实证。

3D、学习网络的学生端展示及批量课程扩充在前端规划基础上后续推进；它们不能反向阻止本轮资源、审核发布与后端调优。已有 MySQL、部署、审计和外部适配工具保留，不因本轮范围收束而删除。

## 8. 已完成内容迁移账本

| 原内容 | 处理 | 权威位置 |
| --- | --- | --- |
| V8.3—V8.6 已完成周期与重复摘要 | 移出当前规划正文 | [03 发布历史](03-发布历史.md#4-v8-当前阶段摘要) 与 35 号分卷 |
| 首轮审查中的样板课程、3D 代表实验优先等路线 | 归档为已撤换规划，不作为当前实施主线 | [39 号 C.1](03-子文档/39-跨版本综述与规划迁移附录.md#c1-已撤换规划及原因) |
| ARCH-011、BE-031、BE-032 及其验证回执 | 保留当时未提交交付；后续实际提交见 03 第 7 节 | [39 号 C.2](03-子文档/39-跨版本综述与规划迁移附录.md#c2-未提交工作区交付记录) |
| 本次重写前的 02 正文 | 保存完整历史快照与校验摘要 | [39 号 C.3](03-子文档/39-跨版本综述与规划迁移附录.md#c3-重写前02完整快照) |
| 新方案的架构图与关系展开 | 仅维护一份图示原件 | [01 第 13 章](01-开发者手册.md#13-目标架构与设计待实现) |
| 当前业务规则、任务与验收 | 本文完整维护 | 本文第 0—7 节 |

21—29 号历史契约、06/09/99 历史材料原位保留，仍可用于追溯。当前规则发生冲突时，以本轮明确确认的方案为准；当前实现仍须核对 01 和实际代码。

本次文档维护不创建产品发布记录。未来完成研发任务时，同步当前实现、任务状态和实际 Git 提交，不把目标图或旧测试结果当成新实现完成证明。
