# 星序 Astra — V7.7 三星系六门代表课程教学与证据冻结契约

> 该文档隶属于[02-项目规划 第 3.2 节](../02-项目规划.md#32-v77-代表课程与教师协同)，冻结 `CONTENT-001` 选定的三星系六门代表课程、教学闭环、学习证据、教师回读、事实边界与演示顺序，供 `FE-012 / FE-017 / FE-013`、`BE-007`、`QA-014 / QA-015 / QA-013` 消费。
>
> **权威关系**：任务、状态和版本仍以 [`02-项目规划.md`](../02-项目规划.md) 为准；当前真实稳定实现仍以 [`01-开发者文档.md`](../01-开发者文档.md) 和 [`08-前端页面实现索引.md`](../08-前端页面实现索引.md) 为准。本文是内容与证据契约，不表示六课页面、Future canonical 路由、FE-013 教师工作台或 V8.0 已经实现。
>
> **形成方式**：CONTENT-001 对当前路由、owner、课程内容、学习证据接线和事实表达完成只读审计，主开发于 2026-07-29 接受其推荐场景并统一冻结。稳定事实与目标契约必须分栏理解；不得把建议契约回写为当前实现。
>
> **CONTENT-004 / BE-013 增量**：V7.8.3 冻结教师有限事实的课程级消费合同，V7.8.4 已按该矩阵接入活动级纯投影。当前只有 `physics.mechanics` 与 `control-flow.loop-boundary` 具有真实交互 producer；其余四课仍是待 FE producer 的未来合同。活动级 allowlist 不授权通用演示样例冒充课程真实观察。

## 1. 冻结选择与共同门禁

| 学习星系 | 代表课程 | 当前稳定入口 | 目标入口 | 当前证据状态 |
| --- | --- | --- | --- | --- |
| 工科试验室 | `physics.mechanics` | `#physics/mechanics` | 保持稳定 | 已有真实受控对照、修正与共享学习证据 producer；教师课程级有限事实投影已接入 |
| 工科试验室 | `mathematics.derivative-application` | `#mathematics/derivative-application` | 保持稳定 | 未接共享学习证据 |
| 代码空间 | `control-flow.loop-boundary` | `codevis/#challenge?activity=control-flow.loop-boundary` | 保持稳定 | 已有真实浏览器预检、修正、正式提交动作与共享学习证据 producer；教师投影已将旧浏览器错误编码与正式 runner 分栏 |
| 代码空间 | `debugging-testing.minimal-case` | `codevis/#challenge?activity=debugging-testing.minimal-case` | 保持稳定 | 未接共享学习证据；现有场景与提示必须整体重写 |
| 未来星系 | `engineering.load-path` | `#engineering/load-path` | `#frontier/engineering/load-path` | Future canonical 与共享学习证据均待实现；旧入口须兼容规范化 |
| 未来星系 | `humanities.claim-review` | `#humanities/claim-review` | `#frontier/humanities/claim-review` | Future canonical 与共享学习证据均待实现；旧入口须兼容规范化 |

六课共同遵守：

1. 页面顺序固定为“学习目标 → 预测 → 操作 / 观察 → 反馈 → 修正 → 解释 / 完成”；答案不得与预测同屏首显。
2. 每课只突出一个主变量，其余量锁定或清楚列为常量。Canvas、图表、轨迹或关系图只承担一种教学职责，并提供等价文本或表格。
3. 访问、播放、点击、拖动、节点数量、公开样例通过和客户端自写 `completed` 均不构成完成。
4. 六课统一要求 `started / predicted / attempted / corrected / explained` 五类学习事实。第一次判断正确时，`corrected` 记录补充适用条件、模型限制或证据强度修订，不能用空事件凑数。
5. 服务端规则只依据版本化事件类型与次数派生完成；当前服务端不能独立判断事件顺序、开放文本质量或学科正确性，页面与 QA 必须保留相应阶段门和结构化证据。
6. 教师视图只展示预测、尝试序列、修订、解释与必要参数摘要；不展示完整源码、长开放文本、敏感材料或底层事件实现术语。

## 2. 工科试验室代表课程

### 2.1 `physics.mechanics`：恢复系数与第一次反弹高度

- **稳定事实与缺口**：当前 owner 为 `pages/physics/physics.js`，已经具有固定落高、`e=0.40 / 0.80` 两次受控竖直落球、第一次峰值测量、课程专用修正与共享学习证据；自由探索不写领域事实。教师发现接口已按本课有限事实矩阵解释参数，FE-013 尚未把它渲染进教师工作台。
- **学习目标（3—4 分钟）**：在落高、重力、半径、初速度和阻尼相同条件下，比较 `e=0.4` 与 `e=0.8` 的第一次反弹高度，并用恢复系数与理想模型限制解释差异。
- **预测**：操作前选择哪次反弹更高，并判断 `e` 加倍后高度约为 2 倍、4 倍还是无法仅凭该表述判断；必须写出理由。
- **变量、常量与观察**：只操纵 `e`；固定竖直释放、初始高度、重力、半径、水平初速度为 0、阻尼为 0；记录 `H`、第一次峰值 `h`、`h/H` 和两条轨迹。
- **典型错误**：把 `e` 当重力或能量来源；认为高度与 `e` 线性变化；把半径当质量；把教学阻尼当真实接触摩擦系数。
- **分层反馈**：先提示“只比较第一次峰值，不看尾迹长度”；再说明恢复系数是碰撞法向相对速度之比，而由速度到高度还经过平方关系；修正仍由学生完成。
- **重试与完成**：恢复统一落球预设，只改 `e` 各运行一次，提交两行测量表，并以“观察—关系—理想条件”解释。只说“更高”、同时改变多个参数或只拖球不算完成。
- **证据与教师回读**：`minimum_attempts=2`；尝试记录 `e/H/h/预设`，教师只按第 8 节有限事实回读预测、两次受控参数与 `h/H`、修订和模型限制；预测理由正文不回显。
- **事实门禁**：风险 **P1/C**。把“牵引力”改为“初速度”，“摩擦 μ”改为“阻尼 / 教学耗散参数”，“能量”改为“能量代理”。依据：[OpenStax College Physics 2e §8.5](https://openstax.org/books/college-physics-2e/pages/8-5-inelastic-collisions-in-one-dimension)。

### 2.2 `mathematics.derivative-application`：由导数符号判断单调性与极值

- **稳定事实与缺口**：当前 owner 为 `pages/mathematics/derivative-application.js`，已有函数曲线、切线、导函数、单调区间与极值标注，未接共享学习证据。当前答案过早显露，且“`f′=0` 处即极值点”是错误的一般化。
- **学习目标（4—5 分钟）**：以 `f(x)=x³−3x` 为高中主线，由切线方向判断导数符号，再由临界点两侧符号变化判断单调性和局部极值。
- **预测**：隐藏答案图层后，预测 `x=0`、`x=2` 处切线方向，并判断 `x=-1`、`x=1` 的极值类型及所需证据。
- **变量、常量与观察**：只操纵切点 `x₀`；固定函数和坐标范围；分阶段观察切线方向、`f′(x₀)`、三个区间的导数符号和临界点两侧符号变化。
- **典型错误**：把函数值当导数，或把 `f′>0` 理解为图像位于 x 轴上方；把 `f′=0` 当极值充分条件；混淆局部极值和闭区间绝对最值。
- **分层反馈**：先要求描述切线从左到右上升还是下降；再说明导数符号描述局部增减，临界点仍须比较左右符号；学生补全符号表后再重判。
- **重试与完成**：隐藏导函数后重新选择两个切点，提交两处斜率符号、完整符号表，并解释为什么不能只凭 `f′=0` 判断极值。只拖滑块或打开答案不算完成。
- **证据与教师回读**：`minimum_attempts=2`；producer 落地后，教师只按第 8 节有限事实回读切点、初始判断、符号表、是否出现“驻点即极值”错误及结论 ID，不回显解释正文。
- **事实门禁**：风险 **P1/B**。删除“`f′=0` 即极值”，高中主线不压入泰勒展开。依据：[教育部课程标准说明](https://jsj.moe.gov.cn/n2/1/1/1507.shtml)、[OpenStax Calculus §3.1](https://openstax.org/books/calculus-volume-1/pages/3-1-defining-the-derivative)、[OpenStax Calculus §4.5](https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph)。

## 3. 代码空间代表课程

### 3.1 `control-flow.loop-boundary`：`<` 与 `<=` 的循环边界

- **稳定事实与缺口**：现有 manifest、浏览器 JavaScript runner、执行轨迹、正式提交 adapter 和共享学习证据均已存在。当前本地运行错误可能与正式 `runner_unavailable` 混码，轨迹未突出第一次为假的最终条件检查。
- **学习目标（3—4 分钟）**：在初值、步长、边界和循环体不变时，解释 `<3` 与 `<=3` 的循环体执行次数、最终输出和第一次 false 时的状态。
- **预测**：`count=0`、每轮加 1、边界为 3 时，分别填写两种运算符的执行次数、输出和第一次 false 时的 `count`。
- **变量、常量与观察**：只改关系运算符；固定初值 0、步长 1、边界 3 和循环体；记录每次条件值、状态更新、循环体次数、最后 false、浏览器输出和正式 runner 精确状态。
- **典型错误**：认为 `<3` 最终停在 2；把条件检查次数当循环体次数；把浏览器通过当正式 accepted；把 `runner_unavailable` 当答案错误。
- **分层反馈**：先把条件检查和循环体执行分成两列；再说明 `while` 先检查再执行，第一次 false 仍是轨迹证据但不会进入循环体。
- **重试与完成**：恢复初始代码，重填两条轨迹，再做浏览器预检；runner 可用时才正式提交。解释两种运算符为何相差一轮。只看输出或浏览器 pass 不算完成。
- **证据与教师回读**：`minimum_attempts=2`；浏览器运行和正式提交动作分开记录，正式 accepted 不是课程完成硬条件。教师回读有限预测、浏览器预检和修订；正式 accepted / runner 状态只能从 code-submissions / attempt 回读，不能由学习证据中的 `cursor.judge` 推断。
- **事实门禁**：风险 **P1/B**。必须区分 `browser_runtime_error` 与 `runner_unavailable`，轨迹不承担正式判题。依据：[ECMAScript 2025 While Statement](https://tc39.es/ecma262/2025/multipage/ecmascript-language-statements-and-declarations.html#sec-while-statement)。

### 3.2 `debugging-testing.minimal-case`：缩减、复现、修复与回归

- **稳定事实与缺口**：现有 manifest 与共用 challenge / C Worker 外壳，未接共享学习证据。当前代码按“非零元素计数”实际正确，提示把 `!=0` 改成 `==0` 会改变需求；没有失败输入、缩减、复现和回归，不能作为最小反例课程。
- **学习目标（5—6 分钟）**：从四元素失败输入缩到仍保持同一失败的单元素输入，定位谓词错误，修正后同时重跑最小输入和原输入。
- **冻结场景与预测**：规格为“统计严格大于 0 的元素”；缺陷谓词为 `!=0`；输入 `[4,0,-2,7]` 期望 2、实际 3。学生先预测负数、零或正数哪类能单独保留失败。
- **变量、常量与观察**：先只缩输入，复现 `[-2]` 的期望 0 / 实际 1 后才解锁源码；固定规格、expected、runner 和失败签名；观察规模 `4→1`、逐步 `count`、修复后最小与原输入双通过。
- **典型错误**：未复现就改代码；修改 expected；把“最小”理解为数值最小；硬编码当前输入；只测最小输入而不回归原输入。
- **分层反馈**：先提示每次删除后确认仍是同一种 expected / actual 差异；再说明最小反例是继续删除就不再复现；学生填写负数、零、正数的谓词真假表。
- **重试与完成**：顺序固定为“原失败 → 缩减 → 最小失败 → 修源码 → 最小通过 → 原输入回归通过”；提交缩减链和一般性修复解释。跳步、改 expected 或只运行一次不算完成。
- **证据与教师回读**：`minimum_attempts=4`，至少覆盖原失败、最小失败、修后最小、原输入回归；不回显 revision hash 或完整源码。producer 落地后，教师按第 8 节四个离散阶段回读规模、失败签名、修复先后和两次回归。
- **事实门禁**：风险 **P0/C**。starter、规格、测试、提示和全过程必须整体重写。依据：Zeller & Hildebrandt, [“Simplifying and Isolating Failure-Inducing Input”](https://www.cs.purdue.edu/homes/xyzhang/spring07/Papers/delta-debugging.pdf), IEEE TSE 28(2), 2002, DOI `10.1109/32.988498`。

## 4. 未来星系代表课程

### 4.1 `engineering.load-path`：理想桁架的荷载重分配

- **稳定事实与缺口**：当前兼容 owner 为 `pages/engineering/bridge-truss.js`，有理想平面桁架求解、支反力和杆件轴力着色，未接 Future canonical 或共享学习证据。“哪几根杆先变红”与青色拉 / 橙色压图例冲突；固定容量没有材料、截面或屈曲依据。
- **学习目标（4—5 分钟）**：在理想桁架近似下，把同一 60 kN 载荷由 B 移到 C，用支反力、代表杆轴力和一个节点平衡解释重分配；D 工况用于修正后的镜像检查。
- **预测**：预测 B→C 后左右支反力会更接近还是更悬殊，GH / CD 轴力绝对值如何变化，并给出一个节点平衡理由。
- **变量、常量与观察**：只操纵载荷节点 B / C / D；固定 60 kN、15 杆、几何、支座和理想铰接，隐藏拆杆与安全系数；观察 `Ay/Ey`、GH / CD 拉压与大小、节点 `ΣFx/ΣFy` 残差。
- **典型错误**：把力当作沿唯一道路流动；把红色当压杆或危险；把近零杆理解为所有工况恒为零；把理想模型和合成容量当真实桥梁设计结论。
- **分层反馈**：先比较左右反力，再看杆件；随后说明理想桁架由每个节点二维平衡求轴力，移动载荷会同时改变多个节点方程。
- **重试与完成**：按 B→C→D 运行，其他量锁定；提交三工况表、一个节点平衡解释和模型未覆盖项。只点节点、只看颜色或宣称完成真实设计不算完成。
- **证据与教师回读**：`minimum_attempts=3`；producer 落地后，教师按第 8 节回读是否锁定工况、三组反力 / 杆力、节点残差、修订与模型限制。
- **事实门禁**：风险 **P0/B**。首屏和解释区常驻“理想二维桁架教学近似”；修正颜色问题，隐藏或标注“合成教学容量”。依据：[Engineering Statics: Trusses](https://engineeringstatics.org/Chapter_06-trusses.html)、[Method of Joints](https://engineeringstatics.org/method-of-joints.html)、[MIT truss experiment](https://ocw.mit.edu/courses/1-105-solid-mechanics-laboratory-fall-2003/resources/exp4_03/)。

### 4.2 `humanities.claim-review`：双来源证据与主张修订

- **稳定事实与缺口**：当前兼容 owner 为 `pages/humanities/text-lab.js`；`pages/humanities-futures/text-lab.js` 并不存在。现状是自编文本的词频 / 共现观察，未接 Future canonical 或共享学习证据，也没有“材料—证据—主张—反例—修订”流程。
- **学习目标（5—6 分钟）**：使用两份带来源元数据的 Apollo 11 一手材料，判断“着陆阶段的计算机警报几乎导致任务失败”应被支持、限定还是暂不判断，并据支持与限制证据修订主张。
- **预测**：材料展示前选择初始分类，并说明什么证据会证伪或迫使自己缩小“几乎导致失败”的范围。
- **变量、常量与观察**：依次切换实时通信记录和事后技术复盘；初始主张在修订前锁定；记录来源 ID、时间、角色、实时 / 回顾语境，以及证据属于支持、挑战或不确定。词频阈值仅作副工具。
- **典型错误**：用词频证明主张；用单一一手材料证明宽泛因果；把未提及当反证；把不同视角判成一方虚假；把节点点击当完成。
- **分层反馈**：先核查谁、何时、在什么语境产生材料；再说明实时记录与事后复盘回答不同问题，交叉核查往往只支持更窄的陈述。
- **重试与完成**：加入第二来源、调整证据角色或缩小时间 / 因果范围；完成需要两来源、支持与限制证据各一、修订主张和待查问题。只看词频、点节点或贴真假标签不算完成。
- **证据与教师回读**：`minimum_attempts=2`；节点点击不计事件。producer 落地后，教师只回读初始分类、来源 / 证据角色、修订 ID 和下一问题 ID；原主张、修订主张与问题正文均不回显。
- **事实门禁**：风险 **P0/C**。保留页面壳和共现副工具，替换 corpus、通用答案和完成逻辑；冻结示例结论为“需限定”，不得截取材料预设真 / 假。依据：[NASA 实时通信记录](https://ntrs.nasa.gov/citations/20160014392)、[NASA Apollo 11 Technical Crew Debriefing](https://www.nasa.gov/wp-content/uploads/static/history/alsj/a11/a11tcdb.html)、[Library of Congress 一手资料分析指南](https://www.loc.gov/programs/teachers/getting-started-with-primary-sources/guides/?reload=no)。

## 5. 实现优先级与责任边界

### 5.1 P0

1. 为导数、最小反例、荷载路径与主张核查补共享学习证据接线；所有页面只发五类 learner facts。
2. 落地两个 Future canonical 路由并保留旧 hash 兼容规范化；落地前不得宣称 canonical 已稳定。
3. 六课统一实施预测先于操作的阶段门。
4. 整体重写最小反例流程与双来源主张核查流程。
5. 荷载路径加入理想模型边界，修正颜色文案并从主流程隐藏安全系数 / 拆杆。
6. 力学加入受控落球对照、测量表和解释门禁。
7. 教师逐学生安全证据投影使用 `BE-007` 的最小接口；当前 aggregate 不能冒充上述明细。

### 5.2 P1—P3

- **P1**：力学峰值表、导数符号表、循环完整轨迹与 runner 状态、最小反例缩减链、荷载路径节点残差、主张核查来源证据表。
- **P2**：精确 390×844、键盘焦点、44px、Canvas 等价表、减少动效和教师可说出口反馈。
- **P3**：网络图、轨迹动画、视觉润色和大学拓展；不得抢占事实、流程与证据门禁。

FE 是六课页面和前端证据接线的唯一实现主责；CONTENT 只维护内容与事实；BE 只提供已证明缺口的安全投影；QA 独立验收。任何 P0 失败退回原主责，不另建重复实现组。

## 6. 八分钟最终演示顺序

| 时间 | 演示动作 |
| --- | --- |
| `0:00—1:00` | 力学：预测 → `e=0.4/0.8` 对照 → 模型限定 |
| `1:00—2:00` | 导数：切线 → 符号 → 极值 |
| `2:00—3:00` | 循环：`<` / `<=` 轨迹 → runner 诚实状态 |
| `3:00—4:20` | 最小反例：`4→1` → 修复 → 双重回归 |
| `4:20—5:30` | 荷载路径：B / C / D → 节点平衡 → 理想近似 |
| `5:30—7:00` | 主张核查：双来源 → 限制证据 → 修订主张 |
| `7:00—8:00` | 教师回读：预测、尝试、修正、解释与降级状态 |

该顺序使用预置演示初始状态，不代表新学生能在八分钟内自然完成六门课，也不授权预置伪造 `completed`。若 SQLite 账本没有对应事件见证，页面只能标为“课程流程预览”。

## 7. QA 验收附加项

1. 六课分别证明目标、预测、操作 / 观察、反馈、修正和解释 / 完成六阶段可达，答案不会在预测前泄露。
2. 精确核对 `minimum_attempts`：力学 2、导数 2、循环 2、最小反例 4、荷载路径 3、主张核查 2；客户端不直接发送 completed。
3. 教师回读与学生事实同源；runner、模型近似、合成容量和课程流程预览的降级标签不可被 UI 隐藏。
4. 两个 Future 课程的旧路由规范化、刷新恢复、浏览历史和发布态正确；方向页不得挂载课程 runtime。
5. 1015×898 与 390×844 均无内容遮挡或根级横向溢出；核心命中区不低于 44px；键盘、方向键、减少动效和等价表格可用。
6. 非目标页不加载课程 Canvas / Three / runner；页面离开后释放 RAF、事件、Observer、Canvas 和课程 owner。

## 8. 教师有限事实 v1 契约

### 8.1 全局投影门禁

教师课程事实只按四元组查表：

```text
(activity_key, event_schema_version, event_type, exact leaf path)
```

当前唯一支持的 `event_schema_version` 为整数 `1`。它由服务端事件行参与查表，不要求前端根据响应自行猜测版本。投影必须遵守：

1. 只有下列六个精确 `activity_key`、版本 `1`、已登记 `event_type` 和精确叶路径可以进入 `evidence_summary.facts`。未知 activity、schema、event、路径或多余嵌套叶一律省略，并令 `truncated=true`。
2. 字符串只允许矩阵中列出的协议枚举原值；枚举由批准控件产生并映射为固定中文标签，不是开放文本。未知枚举省略并令 `truncated=true`。
3. 数值必须是 JSON number、不得是布尔值、必须有限、位于登记范围内并满足最大小数位。精度按解析后的数值校验：`abs(value − round(value,N)) ≤ 1e-9`，不依赖 JSON 原文末尾零。`NaN`、无穷、越界、超精度或类型漂移一律省略并令 `truncated=true`。
4. 标为“组合”的叶必须整组出现并满足同行映射。缺叶、交叉不一致或只通过部分校验时，整组课程事实不显示，并令 `truncated=true`，不得拼出误导性半条观察。
5. 每个事件最多 12 个 facts；按本文表格自上而下稳定取值。即使未来 producer 新增字段，也不能挤掉既有字段；第 13 项及以后省略并令 `truncated=true`。
6. `started`、服务端派生 `completed / transferred` 和 administrative correction 只显示外层事件类型、发生时间与纠正链状态，不提供课程专属 facts。服务端完成表示规则满足，不等于教师判定“已掌握”。
7. 预测理由、解释、反馈、教师纠正原因、源码、revision / request hash、`source_ref`、原主张、修订主张、下一问题正文、token / Cookie / 密钥、任意 `artifact.ref` 及未知 nested raw 均不在课程矩阵中；活动级投影必须省略并 `truncated=true`。BE-013 的教师课程摘要已丢弃旧通用 `value_type / value_size` 元数据，正文及可辨识片段始终不得进入 facts 或中文标签。
8. facts 是“学生记录 / 页面观测”的有限转述，不是服务端重新判科目正确性。中文标签不得把 `reported_correct` 写成“答案正确”，不得把结构化解释写成“解释合格”。

外层事件类型的固定教师标签为：`started` → `开始学习`、`predicted` → `提交预测`、`attempted` → `完成一次尝试`、`corrected` → `学生修订`、`explained` → `提交解释`、`completed` → `系统确认完成`、`transferred` → `历史规则迁移`。teacher correction 只显示为 `教师纠正记录`，不得与 learner `corrected` 混写。

以下表格中的“整数”要求无小数部分；“≤N 位”指十进制小数位上限。所有中文标签均为教师端固定文案。

### 8.2 `physics.mechanics` / schema v1

**producer 与教师动作**：当前真实 producer 为 `pages/physics/physics.js`；它发出预测、`e=0.40 / 0.80` 两次受控观察和修正，共享 evidence owner 发出结构化解释。该课属于第一批 BE 可实现活动级投影。教师动作是比较两次固定预设、核对预测与修正，不判定开放理由质量。

| event_type | 精确叶路径 | 类型、允许值与固定中文标签 |
| --- | --- | --- |
| `predicted` | `prediction.expects_higher_080` | bool；`true` → `预测 e=0.80 反弹更高`；`false` → `未选择 e=0.80 反弹更高（不代表选择 e=0.40）` |
| `predicted` | `prediction.expected_height_multiplier` | 整数 `{0,2,4}`；`0` → `预测无法仅凭题述判断倍数`；`2` → `预测约 2 倍`；`4` → `预测约 4 倍` |
| `predicted` | `prediction.reason_size` | 整数 `[4,160]`；标签 `已写预测理由（{n} 字）`，只显示长度 |
| `predicted` | `cursor.stage` | enum：`prediction-recorded` → `预测已记录` |
| `attempted` | `operation` | enum：`restitution_adjustment` → `运行恢复系数受控试验` |
| `attempted` | `cursor.stage` | enum：`after-observation` → `第一次反弹观察已记录` |
| `attempted` | `cursor.trial` | 整数 `{40,80}`；分别显示 `试验 e=0.40`、`试验 e=0.80` |
| `attempted` | `cursor.preset.restitution` | number `{0.40,0.80}`，≤2 位；标签 `恢复系数 e` |
| `attempted` | `cursor.preset.drop_height_px` | 整数且精确为 `200`；标签 `固定落高 H=200 px` |
| `attempted` | `cursor.preset.gravity_px_s2` | 整数且精确为 `980`；标签 `固定重力 980 px/s²` |
| `attempted` | `cursor.preset.radius_px` | 整数且精确为 `16`；标签 `固定半径 16 px` |
| `attempted` | `cursor.preset.horizontal_velocity_px_s` | 整数且精确为 `0`；标签 `固定水平初速度 0 px/s` |
| `attempted` | `cursor.preset.damping` | number 且精确为 `0`；标签 `固定教学阻尼 0` |
| `attempted` | `cursor.observation.first_rebound_height_px` | number `[0,200]`，≤1 位；标签 `第一次反弹高度 h` |
| `attempted` | `cursor.observation.height_ratio` | number `[0,1]`，≤2 位；标签 `h/H` |
| `corrected` | `correction.height_follows_e_squared` | bool 且仅允许 `true`；标签 `已修正为 h/H≈e²` |
| `corrected` | `correction.model_limit_acknowledged` | bool 且仅允许 `true`；标签 `已确认理想受控条件` |
| `corrected` | `correction.ratio_040` | number `[0,1]`，≤2 位；标签 `e=0.40 时 h/H` |
| `corrected` | `correction.ratio_080` | number `[0,1]`，≤2 位；标签 `e=0.80 时 h/H` |
| `corrected` | `cursor.stage` | enum：`after-repair` → `修正已记录` |
| `explained` | `artifact.kind` | enum：`claim-evidence-link` → `已提交结构化解释` |
| `explained` | `artifact.value` | enum：`claim-supported` → `学生选择：证据支持判断`；`claim-needs-review` → `学生选择：判断仍需复核` |
| `explained` | `cursor.stage` | enum：`explained` → `解释已记录` |

必要组合：

- `attempted` 的 11 个叶组成一个组合：`trial=40 ↔ restitution=0.40`，`trial=80 ↔ restitution=0.80`；六个 preset 必须保持表中定值；页面观测只要求内部一致，即 `abs(height_ratio − round(first_rebound_height_px / 200, 2)) ≤ 0.01`。
- `corrected` 五叶组成一个组合：两个布尔值必须为 `true`，两个 ratio 分别通过表中 `[0,1]` 与两位小数门禁，并且 `ratio_080 > ratio_040`。它证明学生提交了本页规定的修正，不把数值近似误写成现实材料的普适定律。

`h/H≈e²` 是学生需要解释的理想模型目标，不是 BE 用来否定当前 RAF 可变步长、离散积分页面观测的服务器重判规则。服务端不得用理论值或固定容差替换、拒绝已经通过内部一致性校验的真实页面测量。

### 8.3 `mathematics.derivative-application` / schema v1

**producer 与教师动作**：当前没有共享学习证据 producer；以下是未来 FE / DATA producer 合同，不是现有支持。教师只核对符号、临界点判断及“驻点不足以判极值”的修正。

| event_type | 精确叶路径 | 类型、允许值与固定中文标签 |
| --- | --- | --- |
| `predicted` | `prediction.slope_x0_id` | enum：`positive` → `x=0：正斜率`；`zero` → `x=0：零斜率`；`negative` → `x=0：负斜率` |
| `predicted` | `prediction.slope_x2_id` | enum：`positive` → `x=2：正斜率`；`zero` → `x=2：零斜率`；`negative` → `x=2：负斜率` |
| `predicted` | `prediction.critical_neg1_id` | enum：`local-max` → `x=-1：局部极大`；`local-min` → `x=-1：局部极小`；`neither` → `x=-1：都不是`；`insufficient` → `x=-1：证据不足` |
| `predicted` | `prediction.critical_pos1_id` | enum：`local-max` → `x=1：局部极大`；`local-min` → `x=1：局部极小`；`neither` → `x=1：都不是`；`insufficient` → `x=1：证据不足` |
| `predicted` | `prediction.criterion_id` | enum：`stationary-only` → `依据：只看 f′=0`；`left-right-sign-change` → `依据：比较左右导数符号`；`insufficient` → `依据：尚不确定` |
| `attempted` | `operation` | enum：`inspect-fixed-tangent` → `观察固定函数切线` |
| `attempted` | `cursor.point_id` | enum：`x0` → `切点 x=0`；`x2` → `切点 x=2` |
| `attempted` | `cursor.x` | 整数 `{0,2}`；标签 `切点 x` |
| `attempted` | `cursor.derivative` | 整数 `{-3,9}`；标签 `f′(x)` |
| `attempted` | `cursor.sign_id` | enum：`negative` → `导数为负`；`positive` → `导数为正` |
| `corrected` | `correction.interval_left_sign_id` | enum 且精确为 `positive` → `(-∞,-1)：正` |
| `corrected` | `correction.interval_middle_sign_id` | enum 且精确为 `negative` → `(-1,1)：负` |
| `corrected` | `correction.interval_right_sign_id` | enum 且精确为 `positive` → `(1,+∞)：正` |
| `corrected` | `correction.critical_neg1_id` | enum 且精确为 `local-max` → `x=-1：局部极大` |
| `corrected` | `correction.critical_pos1_id` | enum 且精确为 `local-min` → `x=1：局部极小` |
| `corrected` | `correction.stationary_alone_sufficient` | bool 且仅允许 `false`；标签 `已确认 f′=0 不是极值充分条件` |
| `corrected` | `cursor.stage` | enum：`after-repair` → `符号表修正已记录` |
| `explained` | `artifact.kind` | enum：`derivative-sign-conclusion` → `导数符号结论` |
| `explained` | `artifact.conclusion_id` | enum：`critical-point-needs-side-signs` → `临界点需比较左右导数符号` |
| `explained` | `cursor.stage` | enum：`explained` → `解释已记录` |

必要组合：两个 `attempted` 组合只允许 `x0 / 0 / -3 / negative` 或 `x2 / 2 / 9 / positive`；`corrected` 七叶必须整组出现。预测允许学生选择错误枚举，教师端只如实显示，不自动改写为正确答案。

### 8.4 `control-flow.loop-boundary` / schema v1

**producer 与教师动作**：当前真实 producer 为 `codevis/pages/course-challenge/course-challenge.js`，属于第一批 BE 可实现活动级投影，但它只产出“预测存在”、浏览器预检 / 正式提交动作、浅层 runner / judge、代码修订和共享解释。它不产出运算符、循环体次数、输出或第一次 false。教师先消费下表浅层 facts；详细轨迹必须等待 FE 课程 producer 按后续表发出。正式提交结果、accepted 与 runner 状态必须另读 code-submissions API。

| event_type | 精确叶路径 | 类型、允许值与固定中文标签 |
| --- | --- | --- |
| `predicted` | `prediction.choice` | enum：`prediction-recorded` → `已记录预测（正文不可见）` |
| `predicted` | `cursor.stage` | enum：`before-browser-precheck` → `浏览器预检前` |
| `attempted` | `operation` | enum：`browser_precheck` → `运行浏览器预检`；`formal_oj_submission` → `发起正式提交` |
| `attempted` | `reported_correct` | bool；仅当 `cursor.runner=browser_precheck_finished` 时输出课程 fact：`true` → `浏览器公开样例匹配`；`false` → `浏览器公开样例未匹配`。运行失败组合中的 `false` 只校验既有 producer 形状，不输出该 bool fact |
| `attempted` | `cursor.runner` | enum：`browser_precheck_finished` → `浏览器预检已完成`；`browser_runtime_error` → `浏览器练习运行失败`；`runner_unavailable` → `浏览器练习运行失败（旧 producer 编码，不是正式 runner 状态）` |
| `attempted` | `cursor.judge` | enum：`judge_result_received` → `正式提交响应已收到（结论另读）`；`judge_result_unconfirmed` → `正式提交结果尚未确认` |
| `corrected` | `correction.kind` | enum：`code-revision` → `学生修改了代码` |
| `corrected` | `correction.result` | enum：`public-check-pass` → `修订后浏览器公开样例匹配`；`public-check-needs-review` → `修订后浏览器公开样例仍需检查` |
| `corrected` | `cursor.stage` | enum：`after-repair` → `修订后` |
| `explained` | `artifact.kind` | enum：`claim-evidence-link` → `已提交结构化解释` |
| `explained` | `artifact.value` | enum：`claim-supported` → `学生选择：证据支持判断`；`claim-needs-review` → `学生选择：判断仍需复核` |
| `explained` | `cursor.stage` | enum：`explained` → `解释已记录` |

必要组合：

- `operation=browser_precheck` 时必须有 bool `reported_correct`，并且 `cursor.runner` 只能是三个浏览器枚举之一；不得出现 `cursor.judge`。只有 `cursor.runner=browser_precheck_finished` 才输出 `reported_correct` 的匹配 / 未匹配 fact。
- `cursor.runner=browser_runtime_error` 或旧 `runner_unavailable` 时，既有形状必须带 `reported_correct=false`，但该 bool 仅参与组合校验并从课程 facts 省略；教师只看到“浏览器练习运行失败 / 未形成公开样例结论”。错误组合若带 `true` 则整组省略并 `truncated=true`。
- `operation=formal_oj_submission` 时必须没有 `reported_correct / cursor.runner`，并且 `cursor.judge` 必须出现。`judge_result_received` 只说明收到响应，不得映射为 accepted、得分或 runner 可用。
- 当前 producer 在浏览器运行失败时实际发出旧编码 `runner_unavailable`；`browser_runtime_error` 是 FE 纠正编码后允许使用的目标枚举。两者都只显示为浏览器练习失败，正式 OJ 状态唯一来源是授权的 code-submissions / attempt 读取。

**未来 FE producer 增量（仍为 schema v1，当前不得显示为已接入）**：

| event_type | 精确叶路径 | 类型、允许值与固定中文标签 |
| --- | --- | --- |
| `predicted` | `prediction.lt.body_execution_count` | 整数 `[0,5]`；标签 `预测 <3 的循环体次数` |
| `predicted` | `prediction.lt.first_false_count` | 整数 `[0,5]`；标签 `预测 <3 第一次 false 时 count` |
| `predicted` | `prediction.lt.output_id` | enum：`zero-one-two` → `预测输出 0,1,2`；`zero-one-two-three` → `预测输出 0,1,2,3`；`other` → `预测其他输出`；`insufficient` → `尚不能判断输出` |
| `predicted` | `prediction.lte.body_execution_count` | 整数 `[0,5]`；标签 `预测 <=3 的循环体次数` |
| `predicted` | `prediction.lte.first_false_count` | 整数 `[0,5]`；标签 `预测 <=3 第一次 false 时 count` |
| `predicted` | `prediction.lte.output_id` | enum 同上，标签前缀改为 `预测 <=3` |
| `attempted` | `cursor.trace.operator_id` | enum：`lt` → `运算符 <`；`lte` → `运算符 <=` |
| `attempted` | `cursor.trace.initial_count` | 整数且精确为 `0`；标签 `初值 count=0` |
| `attempted` | `cursor.trace.step` | 整数且精确为 `1`；标签 `步长 +1` |
| `attempted` | `cursor.trace.bound` | 整数且精确为 `3`；标签 `边界 3` |
| `attempted` | `cursor.trace.body_execution_count` | 整数 `{3,4}`；标签 `循环体执行次数` |
| `attempted` | `cursor.trace.first_false_count` | 整数 `{3,4}`；标签 `第一次 false 时 count` |
| `attempted` | `cursor.trace.output_id` | enum：`zero-one-two` → `输出 0,1,2`；`zero-one-two-three` → `输出 0,1,2,3` |

未来预测六叶保留学生原判，可独立显示错误值；不得由浏览器运行结果反填预测。未来观察七叶必须整组出现，并且只允许：

| operator_id | body_execution_count | first_false_count | output_id |
| --- | --- | --- | --- |
| `lt` | `3` | `3` | `zero-one-two` |
| `lte` | `4` | `4` | `zero-one-two-three` |

BE 只负责在收到这些精确 FE producer 叶后校验与投影；BE / DATA 不得解析源码、浏览器输出、通用演示字符串或 `reported_correct` 来补造运算符、次数、输出与第一次 false。结构化轨迹组缺失时固定标为“课程 producer 尚未提交结构化轨迹”，不能把浏览器 public check 当正式 OJ accepted、课程正确性或 completed。

### 8.5 `debugging-testing.minimal-case` / schema v1

**producer 与教师动作**：当前没有共享学习证据 producer，且现有课程场景仍不满足冻结题意；以下是未来 FE / DATA producer 合同。教师核对“先复现、再缩减、再修复、最后双重回归”四个离散阶段；源码和 revision hash 均不可见。

| event_type | 精确叶路径 | 类型、允许值与固定中文标签 |
| --- | --- | --- |
| `predicted` | `prediction.failure_class_id` | enum：`negative` → `预测负数保留失败`；`zero` → `预测零保留失败`；`positive` → `预测正数保留失败` |
| `predicted` | `prediction.minimal_case_id` | enum：`minimal-neg2` → `预测最小样例 [-2]`；`minimal-zero` → `预测最小样例 [0]`；`minimal-pos4` → `预测最小样例 [4]` |
| `attempted` | `operation` | enum：`run-original-failure` → `复现原失败`；`run-minimal-failure` → `复现最小失败`；`run-minimal-after-fix` → `修复后重跑最小样例`；`run-original-regression` → `修复后回归原输入` |
| `attempted` | `reported_correct` | bool；`false` → `本阶段仍复现失败`；`true` → `本阶段检查通过` |
| `attempted` | `cursor.case_id` | enum：`original-4-0-neg2-7` → `原输入 [4,0,-2,7]`；`minimal-neg2` → `最小输入 [-2]` |
| `attempted` | `cursor.input_size` | 整数 `{1,4}`；标签 `输入规模` |
| `attempted` | `cursor.expected_positive_count` | 整数 `{0,2}`；标签 `期望正数计数` |
| `attempted` | `cursor.actual_positive_count` | 整数 `{0,1,2,3}`；标签 `实际正数计数` |
| `attempted` | `cursor.predicate_id` | enum：`not-equal-zero` → `谓词 !=0`；`greater-than-zero` → `谓词 >0` |
| `attempted` | `cursor.failure_signature_id` | enum：`positive-count-mismatch` → `失败签名：正数计数不一致`；`none` → `无失败签名` |
| `corrected` | `correction.fix_id` | enum：`predicate-greater-than-zero` → `修复为 >0` |
| `corrected` | `correction.minimal_case_passed` | bool 且仅允许 `true`；标签 `最小样例回归通过` |
| `corrected` | `correction.original_case_passed` | bool 且仅允许 `true`；标签 `原输入回归通过` |
| `corrected` | `cursor.stage` | enum：`after-repair` → `修复与回归已记录` |
| `explained` | `artifact.kind` | enum：`minimal-case-conclusion` → `最小反例结论` |
| `explained` | `artifact.conclusion_id` | enum：`reproduce-shrink-fix-regress` → `先复现、缩减，再修复并回归` |
| `explained` | `cursor.stage` | enum：`explained` → `解释已记录` |

预测组合为 `negative ↔ minimal-neg2`、`zero ↔ minimal-zero` 或 `positive ↔ minimal-pos4`，三组都可如实展示。四个 `attempted` 只接受以下完整组合：

| operation | reported_correct | case_id / size | expected / actual | predicate / signature |
| --- | --- | --- | --- | --- |
| `run-original-failure` | `false` | `original-4-0-neg2-7 / 4` | `2 / 3` | `not-equal-zero / positive-count-mismatch` |
| `run-minimal-failure` | `false` | `minimal-neg2 / 1` | `0 / 1` | `not-equal-zero / positive-count-mismatch` |
| `run-minimal-after-fix` | `true` | `minimal-neg2 / 1` | `0 / 0` | `greater-than-zero / none` |
| `run-original-regression` | `true` | `original-4-0-neg2-7 / 4` | `2 / 2` | `greater-than-zero / none` |

`corrected` 的四叶必须整组出现，且两个回归 bool 都为 `true`；否则不显示“修复与回归已完成”。

### 8.6 `engineering.load-path` / schema v1

**producer 与教师动作**：当前页面有理想桁架 solver，但没有共享学习证据 producer；以下是未来 FE / DATA producer 合同。教师只核对固定 60 kN 的 B / C / D 三工况、节点平衡和模型边界；数值是本页 15 杆理想模型基准，不是现实桥梁安全结论。

| event_type | 精确叶路径 | 类型、允许值与固定中文标签 |
| --- | --- | --- |
| `predicted` | `prediction.reaction_balance_id` | enum：`more-balanced` → `预测左右反力更接近`；`more-unbalanced` → `预测左右反力更悬殊`；`no-change` → `预测反力不变`；`insufficient` → `预测证据不足` |
| `predicted` | `prediction.gh_change_id` | enum：`absolute-increase` → `预测 \|GH\| 增大`；`absolute-decrease` → `预测 \|GH\| 减小`；`no-change` → `预测 \|GH\| 不变`；`insufficient` → `预测证据不足` |
| `predicted` | `prediction.cd_change_id` | enum：`absolute-increase` → `预测 \|CD\| 增大`；`absolute-decrease` → `预测 \|CD\| 减小`；`no-change` → `预测 \|CD\| 不变`；`insufficient` → `预测证据不足` |
| `attempted` | `operation` | enum：`run-fixed-load-case` → `运行固定荷载工况` |
| `attempted` | `cursor.load_node_id` | enum：`B`、`C`、`D`；固定标签 `荷载节点 B/C/D` |
| `attempted` | `cursor.load_kn` | 整数且精确为 `60`；标签 `荷载 60 kN` |
| `attempted` | `cursor.reaction_ay_kn` | number `[0,60]`，≤1 位；标签 `左支座 Ay` |
| `attempted` | `cursor.reaction_ey_kn` | number `[0,60]`，≤1 位；标签 `右支座 Ey` |
| `attempted` | `cursor.member_gh_kn` | number `[-120,120]`，≤1 位；标签 `GH 轴力` |
| `attempted` | `cursor.member_cd_kn` | number `[-120,120]`，≤1 位；标签 `CD 轴力` |
| `attempted` | `cursor.member_gh_type_id` | enum：`tension` → `GH 受拉`；`compression` → `GH 受压`；`zero` → `GH 近似零力` |
| `attempted` | `cursor.member_cd_type_id` | enum：`tension` → `CD 受拉`；`compression` → `CD 受压`；`zero` → `CD 近似零力` |
| `attempted` | `cursor.residual_fx_kn` | number `[-0.05,0.05]`，≤2 位；标签 `节点 ΣFx 残差` |
| `attempted` | `cursor.residual_fy_kn` | number `[-0.05,0.05]`，≤2 位；标签 `节点 ΣFy 残差` |
| `corrected` | `correction.conclusion_id` | enum：`load-redistributes-by-equilibrium` → `荷载移动会按节点平衡重分配反力与杆力` |
| `corrected` | `correction.mirror_check_passed` | bool 且仅允许 `true`；标签 `B / D 支反力镜像检查已完成` |
| `corrected` | `correction.model_id` | enum：`ideal-2d-pin-jointed-truss` → `理想二维铰接桁架` |
| `corrected` | `correction.model_limit_acknowledged` | bool 且仅允许 `true`；标签 `已确认不是现实桥梁安全结论` |
| `corrected` | `cursor.stage` | enum：`after-repair` → `修正已记录` |
| `explained` | `artifact.kind` | enum：`load-path-conclusion` → `荷载路径结论` |
| `explained` | `artifact.conclusion_id` | enum：`joint-equilibrium-redistribution` → `由节点二维平衡解释重分配` |
| `explained` | `cursor.stage` | enum：`explained` → `解释已记录` |

每个 `attempted` 的 11 叶必须整组出现；`Ay + Ey = 60±0.1 kN`，杆力正值对应 `tension`、负值对应 `compression`、绝对值小于 `0.05` 才可为 `zero`。反力与两根杆力按一位小数舍入后必须等于对应节点行；残差只需满足表中范围。当前 solver 冻结组合为：

| 节点 | Ay / Ey (kN) | GH / 类型 (kN) | CD / 类型 (kN) |
| --- | --- | --- | --- |
| `B` | `45.0 / 15.0` | `-30.0 / compression` | `22.5 / tension` |
| `C` | `30.0 / 30.0` | `-60.0 / compression` | `45.0 / tension` |
| `D` | `15.0 / 45.0` | `-30.0 / compression` | `37.5 / tension` |

`corrected` 的五叶必须整组出现；模型 ID、支反力镜像检查和模型边界确认缺一不可。

### 8.7 `humanities.claim-review` / schema v1

**producer 与教师动作**：当前页面没有批准的一手资料 corpus，也没有共享学习证据 producer；以下是未来 FE / DATA producer 合同。教师核对来源身份、语境、证据角色和限定动作，不读取学生开放主张正文，也不把来源节点点击算尝试。

| event_type | 精确叶路径 | 类型、允许值与固定中文标签 |
| --- | --- | --- |
| `predicted` | `prediction.claim_id` | enum 且精确为 `apollo11-alarm-nearly-caused-failure-v1` → `初始主张：警报几乎导致任务失败（冻结题目）` |
| `predicted` | `prediction.classification_id` | enum：`supported` → `初判：支持`；`qualified` → `初判：需限定`；`undetermined` → `初判：暂不判断` |
| `attempted` | `operation` | enum：`review-source` → `核查一份来源` |
| `attempted` | `cursor.source_id` | enum：`nasa-ntrs-20160014392` → `NASA 实时通信转录`；`nasa-a11-technical-debrief-section-9` → `NASA 事后技术复盘` |
| `attempted` | `cursor.source_role_id` | enum：`realtime-communications` → `实时通信记录`；`postflight-technical-debrief` → `事后技术复盘` |
| `attempted` | `cursor.context_id` | enum：`powered-descent-live` → `动力下降实时语境`；`postflight-retrospective` → `任务后回顾语境` |
| `attempted` | `cursor.evidence_item_id` | enum：`air-ground-1202-reading-and-go` → `记录 1202 报警与地面继续指令`；`debrief-alarm-attention-and-continuation` → `复盘警报对注意与继续任务的影响` |
| `attempted` | `cursor.evidence_role_id` | enum：`supports` → `学生标为支持证据`；`limits` → `学生标为限制证据`；`uncertain` → `学生标为尚不确定` |
| `corrected` | `correction.classification_id` | enum 且精确为 `qualified` → `修订为：需限定` |
| `corrected` | `correction.revision_id` | enum：`qualify-causal-scope-v1` → `已缩小因果与时间范围` |
| `corrected` | `correction.source_count` | 整数且精确为 `2`；标签 `已核查两类来源` |
| `corrected` | `correction.has_support_evidence` | bool 且仅允许 `true`；标签 `已保留支持证据` |
| `corrected` | `correction.has_limit_evidence` | bool 且仅允许 `true`；标签 `已保留限制证据` |
| `corrected` | `correction.causal_scope_narrowed` | bool 且仅允许 `true`；标签 `已限定因果范围` |
| `corrected` | `cursor.stage` | enum：`after-repair` → `主张修订已记录` |
| `explained` | `artifact.kind` | enum：`claim-review-conclusion` → `主张核查结论` |
| `explained` | `artifact.conclusion_id` | enum：`qualified-not-binary` → `结论需限定，不作简单真假判定` |
| `explained` | `artifact.next_question_id` | enum：`alarm-go-criteria` → `待查：报警继续标准`；`computer-restart-preserved-tasks` → `待查：重启保留任务`；`attention-diversion-effect` → `待查：注意分散影响` |
| `explained` | `cursor.stage` | enum：`explained` → `解释已记录` |

`attempted` 的来源身份四叶必须按下列两组匹配；`evidence_role_id` 保留学生判断，可为三个允许值之一：

| source_id | source_role_id | context_id | evidence_item_id |
| --- | --- | --- | --- |
| `nasa-ntrs-20160014392` | `realtime-communications` | `powered-descent-live` | `air-ground-1202-reading-and-go` |
| `nasa-a11-technical-debrief-section-9` | `postflight-technical-debrief` | `postflight-retrospective` | `debrief-alarm-attention-and-continuation` |

`predicted` 两叶、`corrected` 七叶和 `explained` 四叶分别构成完整组合；缺少第二来源、支持或限制证据时，不显示“已限定主张”。

### 8.8 producer 状态、降级与实现顺序

| 课程 | 当前 producer 实态 | 教师投影顺序 | 未接入时固定行为 |
| --- | --- | --- | --- |
| `physics.mechanics` | 真实交互 producer 已存在 | 第一批 BE 活动级 allowlist | 若活动级合同未部署，显示固定降级，不回退到通用猜测 |
| `control-flow.loop-boundary` | 真实 producer 仅有浅层动作，详细结构化轨迹待 FE；浏览器错误有旧编码 | 第一批 BE 先投影浅层 allowlist；未来轨迹须等 FE producer；正式状态另读 code-submissions | 若活动级合同未部署，显示固定降级；轨迹缺失不补造，旧编码不冒充正式 runner |
| `mathematics.derivative-application` | 待 FE / DATA producer | 后续合同 | `课程专属事实尚未接入；当前只能确认事件类型与时间。` |
| `debugging-testing.minimal-case` | 待整体重写及 FE / DATA producer | 后续合同 | 同上 |
| `engineering.load-path` | 有 solver、无证据 producer | 后续合同 | 同上 |
| `humanities.claim-review` | 待批准 corpus 与 FE / DATA producer | 后续合同 | 同上 |

DATA-003 已把初始化器从六课同形通用 payload 收束为显式 profile：`physics.mechanics` 发出当前受控力学 producer 的 precise 形状，`control-flow.loop-boundary` 发出当前浅层动作的 shallow 形状；数学、最小反例、工程与人文继续写 generic lifecycle fallback，并明确 `course_fact_status=unavailable`。这些确定性 fixture 只用于复验产品闭环，不是浏览器实测；教师端仍不得根据 activity 名称、自然语言片段或字符串相似度补造符号表、未来轨迹、solver 数值、杆力或历史证据。未匹配本节精确矩阵的叶必须省略并 `truncated=true`。

### 8.9 权威依据与支持边界

| 课程 | 权威来源 | 本合同只据此支持 |
| --- | --- | --- |
| 力学 | [OpenStax College Physics 2e §8.5](https://openstax.org/books/college-physics-2e/pages/8-5-inelastic-collisions-in-one-dimension) | 球在固定表面竖直反弹的理想关系 `e=√(h/H)`，所以固定条件下 `h/H≈e²`；不外推现实材料、旋转或复杂耗散 |
| 导数 | [OpenStax Calculus Volume 1 §4.5](https://openstax.org/books/calculus-volume-1/pages/4-5-derivatives-and-the-shape-of-a-graph) | 导数正 / 负对应区间增 / 减；临界点不是极值充分条件，需比较左右导数符号 |
| 循环 | [ECMAScript 2025 §14.7.3.2](https://tc39.es/ecma262/2025/multipage/ecmascript-language-statements-and-declarations.html#sec-while-statement) | `while` 每轮先求条件；条件转为 false 时返回，不执行本轮循环体 |
| 最小反例 | Zeller & Hildebrandt, [Simplifying and Isolating Failure-Inducing Input](https://www.cs.purdue.edu/homes/xyzhang/spring07/Papers/delta-debugging.pdf) | 1-minimal 表示删除任一单个变化都会使失败消失；本课只把该思想用于固定四元素到单元素场景 |
| 荷载路径 | [Engineering Statics — Method of Joints](https://engineeringstatics.org/method-of-joints.html)、[MIT OCW Solid Mechanics Laboratory — Truss Experiment](https://ocw.mit.edu/courses/1-105-solid-mechanics-laboratory-fall-2003/resources/exp4_03/) | 理想平面桁架节点满足 `ΣFx=0、ΣFy=0`，未知杆力可先按受拉假设，负号表示受压；教学模型需与实验 / 现实差异分栏 |
| 主张核查 | [NASA NTRS Apollo 11 Technical Air-to-Ground Voice Transcription](https://ntrs.nasa.gov/citations/20160014392)、[NASA Apollo 11 Landing Journal](https://www.nasa.gov/wp-content/uploads/static/history/alsj/a11/a11.landing.html)、[NASA Apollo 11 Technical Crew Debriefing](https://www.nasa.gov/wp-content/uploads/static/history/alsj/a11/a11tcdb.html)、[Library of Congress Primary Source Analysis Tool](https://www.loc.gov/programs/teachers/getting-started-with-primary-sources/guides/?reload=no) | 实时转录可核对 1201 / 1202 与地面继续指令，事后复盘提供另一语境；一手资料分析须观察、反思、提问并继续调查，不能由单一材料直接证明宽泛因果 |

上述链接于 CONTENT-004 审计时可访问或可由其权威站点检索到；若后续来源内容或 URL 变化，CONTENT 必须先更新本文依据，再变更 producer 的批准枚举和中文标签。
