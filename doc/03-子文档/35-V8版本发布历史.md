# 星序 Astra — V8 版本发布历史

> 子文档编号：35
> 上级文档：[03-发布历史](../03-发布历史.md)
> 文档状态：已从原始主文档迁移，保留原有技术事实
> 最近更新：2026-08-28
> 更新者：PM 组（主开发单线兼任）

V8 聚焦活动保护、通用课程闭环、三角色工作台和可解释管理展示，并在 V8.0.37 重组核心文档。

---

## 本卷范围

- **版本范围**：V8.0.0 至 V9.0.0 之前
- **迁移口径**：从原 03 主文档按版本标题机械抽离；记录顺序沿用原文出现顺序，不重写提交事实。
- **收录记录块**：74 个。

## 详细发布记录
## 1.0.126 V8.0.0 ARCH chore(architecture): 冻结活动契约与候选处置（ARCH-003）

- **提交日期**：2026-08-09
- **任务编号**：`ARCH-003`
- **提交标题**：`V8.0.0 ARCH chore(architecture): 冻结活动契约与候选处置（ARCH-003）`
- **架构冻结**：24 号契约补齐当前实际态、目标态和学习证据链路图，冻结 `LearningActivity v1` manifest、`restore/predict/observe/assess/emitEvidence/dispose` 六方法生命周期、七个运行状态、四个投影状态和七类端口；这是一份供 FE/BE/CONTENT/QA 共同消费的领域契约，不以更换前端框架冒充架构改造。
- **候选处置**：对 `f017/f017a/f017b/f017c/fr89/ux64/u358/c003` 建立逐候选“直接吸收/重新实现/保留参考/不可用”矩阵；除 `c003` 的 blob 已与主线等价外，其余脏候选均只允许重新实现、保留参考或判定不可用，禁止整体复制旧工作树。
- **资产保护**：集成前后候选指纹保持 `f017=36/22/0`、三个 lane 均为 `4/6/0`、`fr89=7/0/0`、`ux64=4/1/0`，`u358` 保持 clean；本任务未进入、修改、清理或提交任何遗留候选。
- **机器门禁**：`tools/architecture/v76-module-boundaries.json` 与 `tools/tests/architecture-boundary-contract.cjs` 同步扩展，架构契约测试通过；全组验证为 222 个 JavaScript 语法检查通过、48/48 前端合同通过、文档 0 error（仅既有 99 号命名 warning）和项目对接通过。
- **集成方式**：架构组原交付为 `f753bbea`；主开发在 `3ed9d87` 上使用同补丁无提交集成，并在同一版本提交中补齐共享 02/03。两者以补丁等价而非提交哈希相等对账；架构组长期工作树保持 clean。

---

## 1.0.127 V8.0.1 QA test(critical-journeys): 固化关键教学旅程失败基线（QA-016）

- **提交日期**：2026-08-09
- **任务编号**：`QA-016`
- **提交标题**：`V8.0.1 QA test(critical-journeys): 固化关键教学旅程失败基线（QA-016）`
- **六项红线**：在 `aec0587f6431` 产品基线上动态复现 FUTURE-01（零变量变化/观察仍可判断）、FUTURE-02（本地成功但权威事件为 0）、TEACH-01（Physics 请求遗漏 `course_id` 并混入 Humanities/Control Flow）、CODE-01（新 client ID 与新源码仍返回 409）、MECH-01（未测 e=0.40 即可键盘启动 e=0.80）和 DEMO-01（新演示库预置 6 个 completed 投影与 57 个事件）。V8.0.0 只新增架构文档和合同门禁，未改变这些产品语义，因此该失败基线可作为后续修复的比较锚点。
- **诚实测试语义**：`baseline_assertion=PASS` 只表示当前缺陷被真实观察，`desired_gate=FAIL` 才表示目标语义尚未达标；基线模式六项均 PASS、独立目标 gate 退出 1。默认后端合同以精确 409 或“种子完成 + 投影完成 + 事件存在”三证齐全后才 xfail，避免把初始化、权限或基础设施异常误吞成预期失败；`--runxfail` 仍得到两个真实失败。
- **浏览器与移动端**：外部 Edge/Chrome 在独立临时 SQLite 上复现 Future、力学和教师课程作用域问题；390×844 下根级无横向溢出，关键按钮/选择器为 44—45 px，反馈保留 `aria-live=polite`，但教师表格容器为 354 px、内容宽 610 px，仍依赖局部横向滚动；应用源 console warning/error 为 0。
- **数据后续**：根据 DEMO-01 在 02 新建 `DATA-005`，要求把教师诊断展示样例与现场演示学生账本分离；默认现场学生必须 `not_started` 且零事件，同时保留明确隔离的教师样例，不以删除答辩数据掩盖可信度问题。
- **验证结论**：probe 正反向自检、222 个 JavaScript 语法检查、49 个前端合同、相关后端整组、文档兼容和项目交接均通过；完整 `backend/tests` 在 424 秒硬上限触发 TIMEOUT，未取得完成摘要，本文明确记为 TIMEOUT 而非通过。相关后端首轮一次并发证据用例瞬时出现 `School not found`，隔离与整组复跑均通过，但继续作为时序风险保留。
- **资源与集成**：QA 仅提交 7 个授权测试/证据文件，服务已停止，5 个任务临时资源已验证删除；QA 原交付为 `541896d5`。主开发在 `5ddbdc5` 上使用同补丁无提交集成并补齐共享 02/03；两者以补丁等价而非哈希相等对账，QA 长期工作树保持 clean。

---

## 1.0.128 V8.0.2 FE fix(teacher-scope): 隔离教师课程作用域（FE-023）

- **提交日期**：2026-08-09
- **任务编号**：`FE-023`
- **提交标题**：`V8.0.2 FE fix(teacher-scope): 隔离教师课程作用域（FE-023）`
- **课程范围修复**：教师 pending submission 与代码提交分页现在显式携带当前 `class_id + course_id`，并以一次捕获的 generation / 班级 / 课程范围决定响应是否仍可写入。课程切换会中止旧 generation；迟到的旧课程响应、任一异课程行或父 submission 不属于当前代码列表时均不得进入 state 或 DOM。真正的班级成员、活跃学生和未选课程时的班级知识汇总继续保持班级级语义。
- **分页与详情门禁**：pending、代码列表和 attempts 要求返回 limit/offset、唯一正整数 ID、范围字段及 `next_offset` 与 `offset + items.length` 精确一致；提前 null、重叠游标、未消费完却返回空页、错误父 submission 或源码回指不一致都会整页失败关闭。首个候选 `8959120e` 因独立 QA 发现三类游标反例而作废，FE 在原任务内 amend 为 `493de2c6` 并补齐 pending/code/attempt 共九项动态反例。
- **独立验收**：QA 对精确候选 `493de2c67d12` 复核父提交、标题、四文件 allowlist 与 clean，九项恶意分页反例、空课程、403、重试、切课迟到、代码父级范围及班级级数据边界全部通过，P0—P3=0并允许集成。QA-016 TEACH probe 的请求包含 `course_id=101`，混合 Control Flow 响应未进入 state，foreign DOM 为 false；基线 probe 的 `actual` 仍是 V8.0.1 固定失败叙述，修复判定以布尔值和状态证据为准，报告器动态叙述留待 QA-017 收束。
- **浏览器与状态边界**：外部 Edge 连接可用，但项目官方启动在服务副作用前因 caller-managed Python 的锁依赖 dry-run 无法解析 `alembic==1.18.5` 而终止；桌面与精确 390×844、console 和真实网络矩阵为 NOT-RUN，未以 VM 或静态合同替代。因此运行实现进入主线，`FE-023` 在 02 保持“待验收”，FE-024/025 仍按组内串行门禁等待补证。
- **集成与验证**：前端组原交付为 `493de2c6`；主开发在 `21bb2f5` 上使用同补丁无提交集成，并在同一版本提交中同步 01/02/03/08。主开发与 QA 均复跑定向合同和 QA probe；完整 `npm test` 为 224 个 JavaScript 语法检查、49/49 前端合同通过，文档兼容为 0 error / 1 个既有 99 号命名 warning，项目对接与 `git diff --check` 通过；前端组工作树保持 clean。

---

## 1.0.129 V8.0.3 BE feat(code-revisions): 支持学生多次代码修订（BE-014）

- **提交日期**：2026-08-09
- **任务编号**：`BE-014`
- **提交标题**：`V8.0.3 BE feat(code-revisions): 支持学生多次代码修订（BE-014）`
- **领域语义**：`CodeSubmission` 新增 `client_submission_id`，唯一范围改为 `student + problem + class + client_submission_id`。同键且 `language/source/stdin` 原始 UTF-8 摘要一致时返回原 submission、不会新增 attempt；同键异载荷固定 409，包括 active version 已切换且异载荷同时违反新旧策略的情况；不同键产生独立源码修订和各自 attempt。缺省键只提供同版本同内容的兼容重放，显式客户端要表达相同内容的新修订时仍须使用新键。
- **读取与原始字节**：列表保持 `created_at DESC, id DESC`，并返回 `is_latest_revision` 与按状态优先级、时间和 ID 决胜的 `is_best_revision`；当前 best 是 status-best，不是数值最高分。源码、stdin、NFD 组合字符和 CRLF 均按收到的 UTF-8 字节保存、摘要、运行和回读，不用“规范化”改写学生作品。
- **迁移与并发**：Alembic `20260809_0052` 先增加 nullable 客户端键、按批把旧行回填为 `legacy:{id}`，再替换旧单修订唯一约束并收紧 non-null；既有 submission、源码、快照和 judge attempt 保留。降级在 DDL 前检查旧唯一范围，多修订会失败关闭。SQLite 用单进程锁保护本地双击；MySQL 设计使用行锁、唯一约束、嵌套 savepoint 和 winner 重读，但真实 MySQL 并发/锁等待/非事务 DDL 演练未运行。
- **动态迁移账本返工**：首个后端候选 `76230a5` 虽通过代码修订合同，却遗漏 `sqlite-qa-ledger.py` 仍写死 0051，会把正确 0052 数据库误判为漂移，因此在集成前作废。最终候选 `78aede090e29` 改为以标准库静态解析 `backend/alembic/versions` revision 图，检查重复/缺失引用、环路、连通性和唯一 head，并输出 `expectedAlembicHeads`；数据库 revision 必须精确相等才允许 empty baseline。只读 `mode=ro`、数据库/硬链接覆盖保护、fsync 和原子替换保持不变。合同实跑确认动态 0052 通过、动态父 revision 0051 与多 head 失败关闭。
- **复审与剩余风险**：ARCH 独立复审为 PASS，P0/P1=0并允许集成；P2 指出 200 行列表当前可能形成每行两个相关子查询，SQLite best 计划使用临时 B-tree且缺少 `student + problem + class + created_at + id` 等价索引，转入 `BE-016`。真实 MySQL 0052 迁移、并发双击、死锁/锁等待、savepoint 重读和 EXPLAIN 为 NOT-RUN，转入 `OPS-005`；旧 target-release 工具仍冻结 0050，不代表当前候选可生产发布。
- **验证与集成**：后端组最终候选为 clean 的 8 文件 allowlist；专业组与主开发复跑的 `npm test` 均为 224 个 JavaScript 语法检查、49/49 合同通过，主开发 BE 定向 49.8 秒得到 10 passed、1 个真实 MySQL 条件跳过、1 个 DEMO-01 xfailed，CODE-01 已通过；初版完整后端在 484 秒硬上限 TIMEOUT、无失败摘要。主开发在 `d288280` 上使用最终候选同补丁无提交集成，并同步 README、01/02/03/04/07；条件跳过、TIMEOUT 和 NOT-RUN 均未写成通过。

---

## 1.0.136 V8.0.4 FE feat(future-evidence): 建立观察门禁与学习证据闭环（FE-024）

- **提交日期**：2026-08-09
- **任务编号**：`FE-024`
- **提交标题**：`V8.0.4 FE feat(future-evidence): 建立观察门禁与学习证据闭环（FE-024）`
- **候选与集成**：前端组从授权父提交 `384e2e28df72` 在 `codex/astra-v8-frontend-v2` 形成 clean 候选 `348a332e368521599ac8b4979a0c3da129440ce7`，共修改 28 个登记文件、增加 2040 行、删除 94 行；主开发在相同父线上按原 SHA `--ff-only` 集成，没有复制、改写或重提候选，也没有触碰 backend/schema/数据库、教师/Physics owner 或共享 02/03。
- **教学状态机**：`engineering.load-path` 成为 18 个 Future activity key 中唯一的本轮代表活动。固定理想二维 15 杆桁架使用 60 kN B/C/D 三个受控载荷位置，学生必须依次完成预测→B 观察→C 观察→判断→D 反例→纠正→解释；领域专用 `domainOnly` 禁止通用 predicted/explained/free-text 捷径，未操作、乱序或观察不足时不能提交。
- **权威证据**：预测、三次 attempted、纠正和解释通过现有学习证据客户端进入真实服务；每次写入都重新校验 authority、identity、generation、open、route 和 scope。网络重放复用 `client_event_id`，显式重做生成新 ID；客户端不写 `completed`，完成只由 BE-004 规则生产者派生。`manual intervention`、证据 provider 失败或 activity 尚未 mount 时，真实控件同时写入 `disabled/aria-disabled` 并保持零控制器、零 API 写入，不用 Canvas 遮罩冒充失败关闭。
- **恢复与缓存边界**：当前 `/me/recovery` 不能证明完整 B→C→D 组合，普通刷新因此诚实回退到预测，不拼接离散事件伪造步骤或完成；稳定 run/group、顺序/schema、服务端/离线历史合并、原子恢复和 manual-intervention 解除转入 ARCH-004。V804 代际已同步 index、Router、app-session loader、学习证据子依赖、main fallback、SW 注册、`CACHE_NAME` 和 `APP_SHELL`，关闭初审发现的缓存链漏接。
- **独立审查**：首候选因“mount 前 manual-intervention 未禁用 14 个控件”和“V804 cache generation 不完整”被 QA/ARCH 各以 P1 退回；同一任务原地修正后，QA 与 ARCH 对精确最终 SHA 复审均 PASS，P0/P1=0。ARCH 保留两项不阻断 P2：四个 Future/证据文件责任集中，以及后续需建立真实旧 SW 升级动态夹具；QA 保留 `qa016.future` 探针漂移和既有 DEMO-01 预完成种子两项 P2。
- **真实浏览器证据**：QA 使用官方 `astra-local.ps1`、仓库外全新数据和真实 external Edge，在桌面与精确 390×844 完成 B/C/D、错误→修正→解释、键盘提交、服务写入和完成投影；应用来源 console/pageerror/network failure 为 0。SQLite 只读对账确认两轮各只有 `predicted + attempted×3 + corrected + explained`，`completed` 仅由规则生产者生成。另在同一隔离 origin 先安装并控制父版旧 SW，再普通 reload 到候选，证明 V804 接管、旧 cache 自动删除和新 entry/router/main/evidence 子资源加载；自建 registration/cache、三次服务和四个端口均在结束后清理，候选、父线与 QA 工作树 clean。
- **集成后复验**：主工作区 `npm test` 在 122.3 秒内退出 0，226 个受跟踪 JavaScript 语法与 51 个前端合同全绿；课程学习闭环、代码判题、学习证据和教师事实四组后端定向测试在 111.6 秒内退出 0，为 94 passed / 2 skipped；`git diff --check` 通过。

---

## 1.0.138 V8.0.5 FE fix(mechanics-sequence): 强制力学受控实验顺序（FE-025）

- **提交日期**：2026-08-09
- **任务编号**：`FE-025`
- **提交标题**：`V8.0.5 FE fix(mechanics-sequence): 强制力学受控实验顺序（FE-025）`
- **候选与集成**：前端组从授权父提交 `0e57356393d611f7974bc1a3c20e118f9956a640` 在 `codex/astra-v8-frontend-mechanics` 形成单提交 clean 候选 `95571d7822dc4988a400893ed0c4261852105d46`，修改 24 个登记文件、增加 3826 行、删除 665 行。QA 与 ARCH 对同一最终 SHA 独立 PASS 后，主开发按原 SHA `--ff-only` 快进，没有复制、改写或重提候选。
- **受控教学状态机**：`physics.mechanics` 由 P0—P8 与持久化响应共同控制真实 `disabled/aria-disabled`。预测取得一致权威终态后只开放固定 `e=0.40`，第一份完整快照及 `attempted` 确认后才开放 `e=0.80`，第二份快照确认后才开放比较、模型边界修正和结构化解释；最后只等待服务端完成投影。越序、重复档位、自由探索、拖拽、暂停、清除、resize 和无法证明完整历史的刷新都不能伪造课程推进。
- **权威记录协议**：共享 activity 为 Mechanics 显式 opt-in 建立首次授权前 identity fence、session/authority/caller 合并取消、严格 result DTO/ID/type/终态矩阵和一次性私有 receipt。pre-authorize、initial transport、peer-terminal-pending 三阶段都拒绝同 ID/type 重入；`confirmed/reconciled/manual` 的矛盾组合、错身份、伪回调、迟到结果、撤权、destroy、caller abort 与 queue-loss 均失败关闭。初始传输期间的无身份 removed/expired 只锁存 loss、不提前删除围栏；queued 到达后才安全释放并保留原 ID 重试。客户端从未获得写 `completed` 的能力。
- **返工与审查价值**：`1bf425eb` 因 plain confirmed/queued 可推进被退回；`c7fa593` 因覆盖 pending Promise、DTO 身份未核对、矛盾 manual/confirmed 和无身份清理悬挂被退回；`fb843413` 因 destroy 不能取消首次 transport 被退回；`d7f3d1aa` 因 pre-authorize 生命周期与 queue-loss 围栏过早释放被退回；`77f239c7` 因权威通知在首笔 outer 结束前释放 Map、允许同栈二次 transport 被退回。最终 `95571d7` 把围栏释放唯一归属首笔 outer 的 success/error/finally，并用同 emit 栈反例冻结该不变量；五轮退回均未以“自动化全绿”掩盖竞态。
- **自动化与独立复审**：候选元数据、`git diff --check`、11 组新增定向矩阵、227 个受跟踪 JavaScript 语法、52 个前端合同、39 项教师 facts 和 QA-016 MECH 全部退出 0。ARCH 独立复跑 pre-authorize/initial × confirmed/manual/invalid 同栈重入、Future/CodeVis/generic、缓存与专业文档，P0/P1=0；只保留 `learning-evidence-activity.js=1708`、`physics.js=1647` 等文件规模 P2 给 ARCH-005。
- **真实浏览器与账本**：QA 使用官方 `astra-local.ps1`、仓库外全新 SQLite 和 trusted external Edge，在 1015×898 与 390×844 完成 P0→P8。桌面双击 `e=0.80` 只产生一笔 POST；P7 的无 permit、错 ID、错类型和伪 confirmed receipt 均返回 false、零 POST。移动端首笔 evidence POST 被阻断时保持 P0/busy、两档禁用、IndexedDB local-pending、SQLite 0 行；解除阻断后所有重放复用同一 `client_event_id`，SQLite 只落一行并只推进 P1。两视口各精确新增 `predicted=1 / attempted=2 / corrected=1 / explained=1`，全库无重复 ID，客户端新增 completed=0，所有 completed 行均为 `producer_type=rule`；移动端根溢出 0，主要控件 44px。Future 预测后只开放 B，CodeVis 公开预检 stdout=3 且未冒充正式判题。
- **缓存与清理**：全新 origin 安装后由 `sw.js?v=20260809v805MechanicsSequenceP0` 控制且只保留 V805 cache；另一全新 origin 先由 V804 真实控制，在不注销注册、不清 CacheStorage 的条件下切换候选服务，一次普通 reload 即由 V805 接管、删除旧 cache，并缓存 V805 Physics、module-selector 和 learning-evidence activity/client/loader/queue/status 字节。无应用 pageerror/unhandled/严重 console error；全部测试标签、viewport、两个 origin 的 SW/cache/IndexedDB/cookie、四个端口、服务和仓库外 TEMP 对称清理，工作树 clean。
- **集成后复验与边界**：主工作区 `npm test` 在 95.2 秒内退出 0，为 227/227 语法、52/52 前端合同；课程闭环、代码判题、学习证据和教师事实四组后端为 94 passed / 2 skipped。该版本修复 MECH-01，但不提供稳定 run/group 或最近完整运行组恢复，不关闭 ARCH-004/005、其余课程适配、教师诊断摘要、DEMO-01、真实 MySQL 或用户研究。

---

## 1.0.141 V8.0.6 BE perf(code-revisions): 收束修订投影查询（BE-016）

- **提交日期**：2026-08-09
- **任务编号**：`BE-016`
- **提交标题**：`V8.0.6 BE perf(code-revisions): 收束修订投影查询（BE-016）`
- **最终候选与集成**：后端组把既有五文件补丁先从旧父 `56b9457` 重放为 `df1688fc24e2`，再因 V7.9.85 父线测试治理提交重放为最终 `29446a6727bf3d680831574b0c24a1b17b39a0cb`；精确父为 `e7ecc135f49f3bc97c632cc3fe028d83f5827981`。三代有效候选 stable patch-id 均为 `27e2a2e2a565cc2fee4534ac544e49ba628ccfd1`，最终仍只有 endpoint/schema/service/code-judge test/doc07 五文件、+643/-58。QA/ARCH 对最终 SHA 快速签收后，主开发按原 SHA `--ff-only` 集成，没有复制、改写或新增 merge commit。
- **查询与投影语义**：列表先按既有权限、过滤、排序和分页取当前 page，再由 page ID 恢复并去重 `student + problem + class` scope；第三条查询只读取这些 scope 的完整历史最小六列元数据。service 以 `(created_at,id)` 折叠 latest，以冻结状态优先级折叠 status-best；accepted score=1 仍胜过 runtime_error score=999，明确 best 不是数值最高分。projection scope 缺失时失败关闭，endpoint/schema/service 分层与 OpenAPI 说明同步，不新增 0053 或数据库表。
- **查询形状证据**：2,500 条合法 SQLite 覆盖 200 scope/1,000 candidate 与 40 scope/200 candidate、深 offset、空页 2 条/非空页 3 条数据 SQL、student/teacher/admin、跨班 403、course/activity/status 过滤和稳定 `created_at DESC,id DESC`。SQLite EXPLAIN 不再含两个相关 scalar subquery/status-best 临时排序，并复用既有 actor-scope 唯一索引前三列；MySQL 8 CTE 方言离线编译通过。多轮本机计时互有快慢且幅度很小，测试不设性能阈值，不能据此宣称普遍提速。
- **父线回归与处置**：QA 首次复审 `df1688fc` 时发现父线 `fe024-cache-generation-contract` 仍期待 Browser NOT-RUN，与已更新稳定文档冲突。该问题由 V7.9.85 在父线修复，不塞入 BE 五文件；最终候选再重放后，定向缓存合同与 227/52 均恢复。QA 因此既没有把父线失败误判为 BE 产品缺陷，也没有以“与候选无关”为由绕过全仓门禁。
- **独立审查**：ARCH 对 scope、状态优先级、2/3 SQL、分层与文档边界给出 PASS，P0/P1=0；QA 对 2,500 行、深分页、权限/过滤、OpenAPI、0051 历史迁移、全新 0052 账本、QA-016 CODE/DEMO 和 51 个未被旧父线失败掩盖的前端合同给出 PASS。最终 SHA 的五个变更 blob 与深审 SHA 逐一相同，V7.9.85 与 BE 文件交集为 0，故 code-judge 10 passed / 1 MySQL skip 和规模证据可由相同补丁继承；最终快速复验又独立确认 227/52、diff-check 与 clean。
- **主线复验与环境噪声**：主开发首次把 npm 与 code-judge 并行运行时，local-preview 合同的固定端口 62102 被另一轮审查占用，形成“port already occupied”假失败；端口随后只剩 TIME_WAIT、无 owner 进程。改为串行后 code-judge 为 10 passed / 1 个真实 MySQL 条件 skip，`npm test` 在 98.9 秒内为 227/227、52/52。完整后端已在 stable patch-id 相同的 `df1688fc` 上收集 634 项并运行到 100%、退出 0；旧 0051 current-head 假失败不再存在。
- **剩余边界**：非空页从两次变为三次往返；projection、网络回传和驱动缓冲仍为完整历史 `O(H)`，单 scope 无修订上限，page 仍加载 source/stdin/快照宽行，page 与 projection 两语句只具最终一致。真实 MySQL EXPLAIN/网络/驱动内存/并发可见性均 NOT-RUN；这些容量债转入 BE-017/OPS-005。本版本只收束语义和查询形状，不关闭正式隔离 OJ、真实 MySQL 或普遍性能治理。

---

## 1.0.143 V8.0.7 QA test(future-current): 建立当前代表活动动态探针（QA-019）

- **提交日期**：2026-08-10
- **任务编号**：`QA-019`
- **提交标题**：`V8.0.7 QA test(future-current): 建立当前代表活动动态探针（QA-019）`
- **最终候选与集成**：QA 最终提交为 `76dc64f80ad952734d2bac5c795bf50bb7a5ce51`，精确父为 `7c6abcab7a7836cd7cfaefedb60e18ebbdc3ee25`；主开发按原 SHA 快进集成。提交只新增 `tools/qa/README-QA-019.md`、`tools/qa/qa019-future-probe.cjs`、`tools/qa/qa019_future_backend_probe.py`、`tools/tests/qa019-future-probe-contract.cjs` 四文件，共 +2037 行；QA-016 历史证据、产品代码、共享文档和数据库迁移均未被改写。
- **当前产品链**：Node 探针直接从当前 `learning-activity-catalog`、`frontier-manifest`、`bridge-truss` 状态机/求解器/恢复 normalizer 和 `sameLearningEvidenceAuthority` 取事实，实际形成 `predicted + attempted×3 + corrected + explained`；错误判断为 `single-load-path`，客户端完成态只到 `waiting-server`，无操作和所有身份/班课/单元/活动/代际/访问/路由/manual-intervention 失效均保持零敏感写入、零客户端 `completed`。刷新只断言安全回退到 `prediction`，没有把 ARCH-004 尚未实现的精确恢复写成通过。
- **真实服务与账本**：同一批六条前端事件经 stdin 进入 Python，使用真实 `create_app()`、FastAPI TestClient、`/api/learning-evidence/events` 和仓库外全新 SQLite；数据库迁移到由 Alembic 图动态解析的唯一 head。handler 前 503 后数据库为零行，同一 ID 恢复得到 201，再次重放得到 200 duplicate 且账本仅一行；学习者六事件、唯一规则 `completed`、完成 witness 来源 ID、教师事件 ID/主体/类型/producer/activity、投影和聚合完全对账，外键检查为零，临时数据库清理完成。
- **两轮独立复审**：首候选 `61c5e797…` 因两项 P1 假阳性被 ARCH 退回：child exit=1/cleanup refused 仍可能汇总 PASS，witness/teacher ID 不一致仍可能汇总 PASS。QA amend 为最终 SHA 后，以统一 `evaluateBackendSuccess` 驱动检查、报告与 CLI；38 个变异包含 1 个产品回调、14 个结构化报告和 23 个后端结果变异，全部使目标探针失败。ARCH 复审 P0/P1=0，并额外确认 28/28 后端变异均令总结果失败。
- **验证与诚实边界**：实探针为 10 PASS / 0 FAIL / 0 SKIP / 1 `NOT-RUN`（Browser），定向后端 3 passed，`npm test` 为 229 个受跟踪 JavaScript 文件语法和 53 组前端合同。TestClient 中 handler 前 ASGI 503 不证明真实 socket/offline 或 commit 后响应丢失；38 个变异也不证明通用产品正确性，且 1122/610/201 行探针/后端/合同与源码 marker 耦合较重。当前 desktop/390×844 Browser 明确未执行，不继承 V8.0.4 历史人工证据；这些债分别转入 QA-017 与 ARCH-005。

---

## 1.0.146 V8.0.8 QA fix(teach-report): 对齐教师探针观察语义（QA-020）

- **提交日期**：2026-08-10
- **任务编号**：`QA-020`
- **提交标题**：`V8.0.8 QA fix(teach-report): 对齐教师探针观察语义（QA-020）`
- **最终候选与集成**：QA 首个已审候选为 `57320fa6e48a774a0bdb9b45433fc9123adecdcf`，精确父为 V7.9.87 `382cede2f1e4`；因主线期间只新增 V7.9.88 共享文档调度提交，QA 获 PM 单次授权后无冲突重放为最终 `e3a4f8395d77882a99e1a1848b238e476abf8647`，精确父为 `1c9b81457e605d9a5699354e317eb563fc9464f7`。old/new 三文件 blob 和 stable patch-id `5a8a064a004b7565ba87814ccd4efe7d3c385a65` 完全相同，主开发按最终 SHA 快进集成。
- **范围与历史保护**：提交只修改 `tools/qa/README-QA-016.md`、`tools/qa/qa016-critical-journeys.cjs`、`tools/tests/qa016-probe-selftest-contract.cjs`，共 +494/-47；教师产品代码、后端 schema/迁移、共享 02/03、QA-016 两份历史 evidence blob 和 QA-019 四文件均零变化。QA-016 schema/task/version 与 `aec0587…` baseline revision 保持原值，没有用更新快照改写已经发生的失败证据。
- **事实与历史语义分栏**：runner 从当前教师产品 VM 捕获 pending request 的 class/course 参数、进入 state 的课程 ID 和 DOM foreign row。正式 `run()` 只走 `historical_combination`，严格保留旧组合布尔：缺少 course_id AND state 含 202 AND DOM foreign row；单缺参和单混入只在显式 `controlled_positive` 正控中为真，并同时公开 historical=false，二者组合才恢复 historical=true。当前构建实际报告请求携带 `class_id=11/course_id=101`、混课页整页拒绝、零行进入 state、DOM 无 submission/foreign row，不再借空集合暗示正常 Physics 行已呈现。
- **统一报告链**：issues→summary→overall→execution→human_summary→进程 exit 从同一观察事实派生并在输出前做一致性校验；对 defect、actual、evaluation、summary、overall、status、exit、human 以及两组多层联合篡改共 10/10 拒绝，统一 exit 3。正式 baseline/gate 在最终 SHA 上分别诚实 exit 2/1，TEACH 均为 false，stderr 与 human_summary 完全一致，两次后端临时数据均 `verified-removed`；后端定向为 1 passed / 1 xfailed，xfail 仍是既有 DEMO-01。
- **两轮预审与边界**：主开发在提交前退回两项问题：初版把旧 AND 静默改成 OR，并用“所有行均在 Physics”描述零行；QA 分栏修订后才提交。ARCH 终审 PASS、P0/P1=0；同时登记 P2：一致性校验信任 canonical `observation_facts`，若成套篡改 facts 和全部派生层而不对账原始 request/state/DOM，仍可自洽 PASS；源码字符串 marker 也有维护成本。P3 是 README “当前基线应退出 0”与当前 exit 2 的历史/当前措辞歧义，后续统一转入 QA-021。
- **主线验证**：定向合同与 self-test 退出 0，`git diff --check` 通过，主线 `npm test` 为 229 个受跟踪 JavaScript 文件语法和 53 组前端合同；Browser 对 QA-020 为 `NOT-RUN` 且非验收要求，没有冒充产品 Browser PASS。

---

## 1.0.152 V8.0.10 QA test(raw-provenance): 建立原始证据独立重算门禁（QA-021）

- **提交日期**：2026-08-10
- **任务编号**：`QA-021`
- **提交标题**：`V8.0.10 QA test(raw-provenance): 建立原始证据独立重算门禁（QA-021）`
- **候选与集成**：QA 候选为 `e47f84b267483d393e3a0779614b294c168f14bb`，精确父为 `f505353d14286d1c03d079d999521a14900fa8fa`；主开发在 V7.9.93 控制提交之后无冲突重放为 `b8691e4b8069427b7cec202a7cb6505e02eec896`。四个文件的候选/主线 blob 完全一致，stable patch-id 均为 `2a9dc306465d2b51376ee900a7df1f5ae61ed455`。
- **实现结果**：新增独立 `qa016-provenance-verifier.cjs`；其 recompute 形状精确只允许 `schema_version`、`envelope_id` 和 `raw_records`，额外传入 canonical facts、issue、summary、overall、execution 或 human 字段均失败。runner 从 Future/Teacher/Mechanics VM 捕获点和 backend probe 的真实 API/SQLite 返回整理 request/response/state/DOM records；frontend 为 15 条、正式 backend 为 22 条，均重算六项 canonical facts，再由同一结果派生 issue、summary、overall、execution、human summary 与进程 exit。
- **负向与信任边界**：fact-only、raw-only、单派生层、全部派生层、facts+全部派生但 raw 不变、缺/重复来源、重复 raw ID、坏时间戳和坏 verifier identity 共 13 类不一致反例均 FAIL，`exitCodeForReport` 与独立子进程均退出 3。协调修改 raw、facts 与全部派生层仍可自洽 PASS，因此 assurance 固定为 `consistency-only`；该版本不证明来源真实性、签名防篡改、独立传输、当前 Browser 或真实网络。
- **历史保护与措辞**：两份 QA-016 历史 evidence blob 保持 `65fe54c95f83ae9df2f1812b4654d279bb6cba95` / `bc195e8cf0c79fefe1ba6740a38a21da040ad151`；产品、后端、QA-019 与共享 01/02/03 在候选中零差异。README 已把“原始 `aec0587f6431` baseline 退出 0”和“当前修复态 baseline 退出 2”分开，未改写已发生历史。
- **双重复审与主线验证**：主开发和 ARCH 独立复审均 PASS，P0/P1=0，唯一非阻断 P2 为上述 `consistency-only` 上限；Browser=`NOT-RUN`。主线定向变异合同退出 0，正式 baseline/gate 分别真实退出 2/1，临时 SQLite verified-removed；`npm test` 为 232 个受跟踪 JavaScript 文件语法和 54 组合同，`git diff HEAD^ HEAD --check` 通过。

---

## 1.0.155 V8.0.11 ARCH fix(activity-runtime): 分离学习者序号与服务端顺序（ARCH-005）

- **提交日期**：2026-08-10
- **任务编号**：`ARCH-005`
- **提交标题**：`V8.0.11 ARCH fix(activity-runtime): 分离学习者序号与服务端顺序（ARCH-005）`
- **候选与集成**：最终 ARCH 候选为 `690688f9082bbded582461f3f95545055cb9c46b`，精确父为 `b8691e4b8069427b7cec202a7cb6505e02eec896`；主开发无冲突重放为产品提交 `080028afb88f1cd53087e7f826a186678cf31525`。提交精确修改 7 个授权文件，`+1855/-496`，标题保持不变；ARCH 工作树在最终候选上 clean。
- **三轮退回价值**：首候选 `04a8cd4` 因 recovery sidecar 可把 token/projection/identity 权威字段带到 adapter，以及新 learner event 可接受已占用旧 `server_sequence` 而被 P1 退回；第二候选 `499bd0c` 已关闭上述 P1，但嵌套 recovery identity / subject identity 的未知字段仍会被 normalizer 静默丢弃，与“frozen v2 每层精确”矛盾，再以 P2 退回。最终候选把 outer envelope、identity、subject identity 和原始规范值全部纳入 exactness，未知、额外、敏感或仅 trim 后相等的字段均在 adapter 前失败关闭。
- **双游标与重放结果**：`run.sequence` 保持 learner-only；权威 receipt/历史/完成 witness 另用 `learner_sequence + server_sequence + server_last_sequence`。新事件首个权威 receipt 必须追加在调用前已知 server last 之后；confirmed/reconciled 同 ID 重放必须匹配已经锁定的位置，位置漂移或新事件占旧位均失败。首次 EvidencePort 调用冻结 command 与 post-event canonical domain snapshot 为不可变 sidecar，同 ID 重试逐字复用；多条 local-pending 只有 aggregate cursor 而无逐事件权威位置时安全进入 manual，不伪造自动恢复。
- **模块与容量**：新增 473/480 行纯 `learning-activity-recovery.js`，主 `learning-activity.js` 从 979 行收缩到 849/900；运行时合同为 1276/1300，恢复合同为 619/620。恢复输入的敏感字段策略由主 owner 注入并递归执行，污染 recovery 在 adapter/evidence 前归一为 `recovery_schema_incompatible` / manual；课程、DOM/Canvas、后端 DTO 和 IndexedDB 实现均未进入内核。
- **独立复审与验证**：QA 对最终 SHA 给出 `[REVIEW] PASS`，P0/P1/P2=0；21 组 identity exactness、敏感 sidecar、confirmed position、旧位置复用、server/local gap、sidecar 漂移和 aggregate-cursor 多 pending 边界均通过。主开发独立复跑 `learning-activity-recovery-contract`、`learning-activity-runtime-contract`、`architecture-boundary-contract` 与完整 `npm test`，最终为 234 个受跟踪 JavaScript 文件语法和 55 组合同、退出 0；主线重放后同套验证再次退出 0。
- **诚实边界**：本提交只完成纯 JavaScript 消费合同和容量治理。真实课程页仍走 `LearningEvidenceActivity`，真实 IndexedDB 单事务、FastAPI v2 半包、0053 数据库约束、HTTP 提交/响应丢失与乱序、Browser 和六课适配均为 `NOT-RUN` / 未完成。若服务端违反 learner 写入顺序，JS 合同不能替数据库证明正确；该责任明确交给 BE-018。

---

## 1.0.161 V8.0.12 BE feat(activity-recovery): 提供双游标权威恢复服务包（BE-018）

- **提交日期**：2026-08-12
- **任务编号**：`BE-018`
- **提交标题**：`V8.0.12 BE feat(activity-recovery): 提供双游标权威恢复服务包（BE-018）`
- **候选与集成**：干净来源候选 `40bf80a7900c` 精确父为 `83742ac2c806`，主开发在 `V7.9.100 / 820a522` 后无冲突重放为 `f831b03f3a1b`。提交仍精确修改 13 个文件、`+4657/-173`；13/13 文件 blob 零差异，stable patch-id 同为 `13e9046fda634b984aa554a3068054eb4ee12f01`，没有在集成时解决冲突或改变后端字节。
- **服务器半包**：新增专用 `learning_activity_runtime.py`、0053 运行身份/双游标约束及 Authority、Release、events、recovery 四个端口。learner cursor 与 server cursor 独立连续；derived completion 只占 server cursor；重放、位置、authority/release revision、learner subject、run/group 和完整版本身份失败关闭。恢复响应只证明连续服务器 ledger、绑定 learner cursor 的 snapshot、projection 和 witness，`client_pending_status=unknown`，不冒充 IndexedDB 完整 pending 或领域 snapshot 正确性。
- **独立验收**：QA-022 对 `40bf80a` 给出 PASS，候选特定 P0/P1/P2=0/0/0。定向 BE-018 为 14 passed；完整 learning evidence 为 53 passed/1 真实 MySQL skip；0053 SQLite 往返/约束、唯一 head `20260810_0053` 与 MySQL offline DDL compile 全部通过；`npm test` 为 234 个 tracked JS 语法/55 组合同；完整后端为 639 passed/8 skipped/1 known xfailed/0 failed。8 skip 均是未提供显式真实 MySQL，唯一 xfail 是既有 QA-016 DEMO-01，不属于本候选回归。
- **保全与退出**：旧 `5779@f505353` 仍有 13 个 dirty 路径，tracked patch hash 和两个 untracked SHA-256 在 QA 前后不变。集成并完成字节对账后，仅以非强制 `git worktree remove` 退出 clean `b812` 并 prune；来源分支 `codex/astra-v8-backend-v812-replay@40bf80a` 继续保留，没有使用 `-D` 或覆盖提交。
- **能力边界**：真实课程页仍未消费 V8.0.11 内核和 V8.0.12 服务器半包，真实 IndexedDB 单事务 outbox、网络丢包后恢复、页面精确续学、真实 MySQL 与 Browser 均不能由本后端 PASS 外推。当前 Goal 只把 V8.0.12 作为最小可信后端底线，页面作品化由 UI-004/FE-029 继续完成。

---

## 1.0.163 V8.0.13 UI ui(role-workspaces): 打磨师生高频工作台体验（UI-004）

- **提交日期**：2026-08-12
- **任务编号**：`UI-004`
- **提交标题**：`V8.0.13 UI ui(role-workspaces): 打磨师生高频工作台体验（UI-004）`
- **候选与集成**：来源 `38b502ad8137` 直接以 `86c2477757be` 为父并保持 clean；主开发在已含 V8.0.12 与 FE-029 的根线无冲突重放为 `8cc6b9d`。精确 5 个授权文件、`+786/-144`，5/5 文件 blob 一致，stable patch-id 同为 `95910464fa1c9c98dcdc1deb69bf04b769d352ce`。
- **作品化结果**：学生首屏形成当前班课、唯一继续课程/作业动作、下一事项和进度回执；教师首屏形成当前班课、唯一下一教学动作、发布轨道和同步回执。两端统一深海军蓝、细边框、冷青/琥珀状态语言、低圆角与 390×844 层级，没有新增渐变、API、伪指标或伪 tab。
- **语义保全**：API/fetch 字面量集合零变化；既有师生 `data-*` owner 无删除，只新增两个 focus stage 容器。课程发布导航、作业操作、当前 `class + course`、stale generation、教师 evidence owner 和写入不确定锁均保持原实现。
- **独立复审**：QA 对精确候选给出 PASS，P0/P1/P2=0/0/0；10 项师生定向与完整 `npm test` 234 JS/55 contracts 通过。教师三层 CSS 规范化哈希更新为 `02048f69134e…34b9`，合同只同步精确指纹与 V8.0.13 标签，没有弱化注册顺序、admin 复用、44px、响应式或 reduced-motion 断言。实现组 Edge 自测不替代 V8.0.15 同 revision 终验。

---

## 1.0.164 V8.0.14 FE feat(flagship-motion): 强化工程与力学教学动画（FE-029）

- **提交日期**：2026-08-12
- **任务编号**：`FE-029`
- **提交标题**：`V8.0.14 FE feat(flagship-motion): 强化工程与力学教学动画（FE-029）`
- **候选与集成**：最终来源 `4fb963a69e6d` 直接以 `86c2477757be` 为父并保持 clean；旧 `8a0b663` 因试验结束后 Canvas `aria-label` 残留旧峰值的 P2 明确作废。主开发无冲突重放最终候选为 `d284b9e`；精确 5 个授权文件、`+1055/-20`，5/5 blob 一致，stable patch-id 同为 `1426c84e72900a7923eae93a4ab2381f4cb57cb5`。
- **工程课程**：预测阶段只显示中性 15 杆与固定 60 kN 工况，不提前泄露反力、杆力数值或答案色；B→C 用 exact solve 的表现副本插值载荷、反力和杆力，最终 B/C/D 表格、三项 delta、判断、模型边界纠正与解释仍消费精确结果。唯一 Canvas writer、单 RAF、resize/destroy 取消与 B→C→判断→D 顺序保持不变。
- **力学课程**：受控实验只改变 `e=0.40→0.80`，H/g/r/vx/damping 固定；真实 update loop 产生下降、首次碰撞、回弹上升与第一峰值，DOM/HUD/双峰对照同步。reduced-motion 继续内部求值，但受控画布只在阶段事件绘制关键帧；无 trial 时会把 Canvas 标签恢复为当前自然语言阶段，不再读出旧峰值。
- **证据边界与验证**：动画帧不进入 evidence，authority/permit/receipt/offline/P0—P8 和服务端 `completed` owner 未改；新增渐变与客户端 completed 均为 0。工程/力学冻结合同及最终候选 `npm test` 234 JS/55 contracts 通过；旧候选独立复审的唯一 P2 经 4 行窄修、逐态探针与主开发差异复核关闭。完整应用 Browser 仍交 QA-022 在 V8.0.15 统一 revision 终验。

---

## 1.0.165 V8.0.15 PM chore(showcase): 统一作品资源代际与集成（PM-021）

- **提交日期**：2026-08-12
- **任务编号**：`PM-021`
- **提交标题**：`V8.0.15 PM chore(showcase): 统一作品资源代际与集成（PM-021）`
- **资源接线**：page registry 对学生/教师使用 `20260812v813RoleWorkspacesP0`，对 Future runtime/CSS 使用 `20260812v814FlagshipMotionP0`；experiment registry 以 V814 加载 Physics，frontier owner 以 V814 加载工程 Canvas。`index.html`、app-session、experiment/page registry、Router、main、Service Worker cache/APP_SHELL 统一为 `20260812v815ShowcaseP0`，完成 V805→V815 普通刷新接管准备；未改字节的 planets、manifest 与 publication context 保留原代际。
- **展示语言**：`student-learning-evidence.js` 只把“0051 RECOVERY / 权威学习投影 / SERVER PROJECTION ONLY”等研发术语改为“学习进度 / 进度同步 / 学习活动记录 / 课程记录”，错误态和同步状态也改为面向学生的自然中文；API、作用域、恢复算法、pending 语义和完成判定零变化。
- **静态门禁**：新增/更新的精确合同同时断言 V813 角色、V814 工程/力学、V815 主壳的真实加载 URL、旧缓存删除前提与用户可见语言；角色资源仍 network-only，`/api` 仍完全旁路 Service Worker。定向资源、页面注册、角色裁剪、Future、Physics、学习证据与教师样式合同全部通过；完整 `npm test` 为 234 个 JavaScript 语法检查、55/55 前端合同，当前根线 BE-018 定向回归为 14 passed（只保留 2 条既有 SQLite datetime adapter 弃用 warning）。文档兼容为 0 error / 1 个既有 99 号命名 warning，项目对接和 `git diff --check` 通过。
- **诚实边界**：本提交形成统一展示候选，但 QA-022 的同 revision 桌面/390×844、键盘、reduced-motion、console/pageerror/Network、刷新安全回退和教师回读仍待执行；实现组历史自测不被继承为最终 PASS，Goal 也不在本提交时提前关闭。

---

## 1.0.166 V8.0.16 FE fix(physics-reload): 修复力学刷新发布上下文竞态（FE-030）

- **提交日期**：2026-08-12
- **任务编号**：`FE-030`
- **提交标题**：`V8.0.16 FE fix(physics-reload): 修复力学刷新发布上下文竞态（FE-030）`
- **失败基线**：QA-022 在精确 `V8.0.15 / 3b01ab2`、fresh student 与 canonical `#physics/mechanics` 全页加载中稳定复现：session 有效但 `/api/classes?mine=true` 被竞态中止，10 秒后 `evidenceReady=false`，四个受控动作全禁用，必须离开再进入才能恢复。V815 因此以 P0=0、P1=1、P2=2 被 RETURN，不能发布。
- **候选与集成**：来源 `2e621737b1d5` 精确父为 `3b01ab2e27ee`，只修改 `module-selector.js` 与 Physics binding 合同、`+479/-28`；主开发无冲突重放为 `104e526`。根因是 publication context 的同身份冷启动请求互相中止后，one-shot evidence mount 把暂态 unavailable 当作永久失败并销毁 controller，而 catalogue-ready 只重绘目录、不重新 mount。
- **窄修边界**：只对 `publication_context_unavailable` 复用同一 controller 至多一次；ready snapshot 防丢事件，loading 监听 ready，120ms 退避、2.5 秒上限；identity、精确 hash、transition generation、独立 AbortSignal、route/release/reset 均可取消迟到。manual/identity/hidden/locked/cancelled 不重试，永久失败第二次后仍 block。修复不改 Physics owner、API、证据类型、authority/permit/receipt 或服务端 `completed`。
- **验证证据**：真实 publication-context VM 合同触发首次 classes AbortError→暂态 unavailable→hash 不变→同 controller `evidenceReady=true`，`mount=1/retry=1/evidence POST=0`；A03A、frontier publication、module lifecycle、Physics evidence 定向及候选完整 `npm test` 234 JS/55 contracts 通过。真实 9001 复验继续交 QA-022，不把合同外推为 Browser PASS。

---

## 1.0.167 V8.0.17 UI fix(role-feedback): 对齐课程入口与工作台忙碌语义（UI-005）

- **提交日期**：2026-08-12
- **任务编号**：`UI-005`
- **提交标题**：`V8.0.17 UI fix(role-feedback): 对齐课程入口与工作台忙碌语义（UI-005）`
- **失败基线**：同一 V815 旅程中，Engineering Systems 的 open Load Path 在星序→工程应用→受力路径自然链可进入，但学生工作台因 Future 单元 `content_slug=null` 显示“待绑定/等待教师开放”；师生刷新虽进入真实 disabled，却均无 `aria-busy`，学生还短暂显示“当前没有任务”的伪空态。这三项均为可观察 P2。
- **候选与集成**：来源 `25519b3244ad` 精确父为 `3b01ab2e27ee`，只修改学生/教师工作台与新增 UI-005 合同、`+375/-10`；主开发无冲突重放为 `8726c84`。学生只在后端 exact-open、当前 student/class/course、LearningActivityCatalog `resolve/recoveryHref`、目录 ready 的当前 class record 与 `allowsActivity` 全部精确一致时装饰 Future href；跨班、锁定、跨课程和目录拒绝均失败关闭，不硬编码 Load Path，也不改变 Physics 原 content_slug/data owner。
- **忙碌反馈**：学生/教师稳定工作台根在真实 busy/loadingScope 时设置 `aria-busy=true`，settled/failed 后移除；学生解析下一动作期间显示“正在读取/同步学习任务”，真实禁用且不渲染旧 scope target/link。API、shared、CSS、publication authority 和数据 owner 均未改变。
- **验证证据**：候选 12 项定向、主开发独立 8 项和完整 `npm test` 234 JS/56 contracts 通过；无新增 API、客户端 `completed` 或渐变。Browser backend 列表为空后按主控停止等待，真实 9001/390/键盘/aria 由 QA-022 在统一 revision 复验。

---

## 1.0.168 V8.0.18 PM chore(showcase): 统一修复资源代际与终验候选（PM-022）

- **提交日期**：2026-08-12
- **任务编号**：`PM-022`
- **提交标题**：`V8.0.18 PM chore(showcase): 统一修复资源代际与终验候选（PM-022）`
- **资源接线**：ModuleSelector 使用 `20260812v816PhysicsReloadP0`，学生工作台使用 `20260812v817RoleFeedbackP0`，教师工作台继续 V813、Physics/Future 继续 V814；`index.html`、app-session、experiment/page registry、Router、main、Service Worker cache/APP_SHELL 使用 `20260812v818ShowcaseRecoveryP0`，完成 V815→V818。app-session 继续把 V818 传给 loader、queue、client、status、catalog、publication context 与 activity；角色资源保持 network-only，`/api` 完全旁路。
- **架构门禁修正**：首次全量回归以 `architecture-boundary-contract` 拒绝新增 `MODULE_SELECTOR_ASSET_VERSION` 常量；主开发没有放宽测试，而是删除新版本表项，在既有 main fallback 清单中使用唯一 V816 字面量，并同步 Router 与精确合同。修正后架构、资源链、页面注册、角色裁剪、Physics reload 和 Engineering entry 定向全部通过。
- **自动化证据**：最终完整 `npm test` 为 235 个 tracked JavaScript 语法检查、56/56 前端合同，BE-018 定向为 14 passed，仅保留 2 条既有 Python 3.12 SQLite datetime adapter 弃用 warning；文档兼容、项目对接与 `git diff --check` 同步作为提交门禁。后端产品字节自 V8.0.12 独立 639 项 PASS 后未变化，因此不重复完整后端。
- **终验边界**：本提交只形成新的统一候选，不继承 V815 的 Browser 结果，也不提前关闭 Goal。QA-022 必须在保留的 fresh 数据上重启同 SHA 运行快照，先复验 canonical Physics 冷加载、Engineering 工作台入口、学生/教师 busy/loading，再抽查无重复 started、scope、教师回读、390、键盘、reduced-motion、console/pageerror/Network。External extension 与 IAB 若仍不可用，只能明确记录 Playwright Edge headless 降级，不能称 external Edge PASS。
- **QA 结果**：QA-022 在精确 `aa24fbf`、复用原 fresh 数据且 HTTP 13 项资源逐字节一致的环境复验；Engineering 工作台真实主动作与师生 `aria-busy`/loading 均 PASS，但两个全新 canonical Physics context 首次加载 10.1 秒后仍 `evidenceReady=false`、`publication_context_unavailable`、`classes?mine=true ERR_ABORTED`，同页 reload 才约 0.92 秒恢复。原 ledger 保持 6 事件、`started=1`、POST=0；V818 结论 P0=0、P1=1，RETURN。

---

## 1.0.169 V8.0.19 FE fix(physics-first-load): 修复力学首次冷加载发布权威竞态（FE-031）

- **提交日期**：2026-08-12
- **任务编号**：`FE-031`
- **提交标题**：`V8.0.19 FE fix(physics-first-load): 修复力学首次冷加载发布权威竞态（FE-031）`
- **根因修正**：V816 的 catalogue-ready 事件可能在 ModuleSelector 初始化前已经发出；恢复逻辑又把 catalogue ready 误当成 publication authority ready，在固定 120ms 过早消费唯一一次 `controller.ready()`，随后 publication provider 即使于 320ms 得到同身份班级权威也无法恢复。红灯探针稳定得到第二次 ready 已在 250ms 被提前调用，而 provider 仍为 `{class_id:null, classes:[], authority_generation:4}`。
- **实现边界**：只读轮询 `AstraEngineeringLabPublicationContext.snapshot()` 的 `class_id/classes/identity_id/authority_generation`，同身份、同班级、同 authority generation 连续稳定 120ms 后才执行唯一一次 ready retry；40ms 采样、2.5s 严格上限。等待期间不调用 `prepare/resolve/controller.ready`，identity、精确 hash、transition generation、AbortSignal、catalogue allowlist、route/release/reset 任一变化立即取消；永久 warming、换身份、离页继续 fail-closed，不重复 mount/bind/started 或 evidence POST。
- **候选与集成**：来源 `aef50c1eb623` 精确父为 `aa24fbf91d68`，仅修改 `shared/js/module-selector.js` 与 Physics binding 合同；主开发无冲突重放为 `18b5e05af77e`，来源/集成业务字节保持等价。
- **验证证据**：丢失 catalogue-ready、provider 晚于 120ms 稳定、永久 warming、route/identity 取消等定向合同通过；候选完整 `npm test` exit 0、150.9 秒，235 个 tracked JS 与 56/56 前端合同通过。真实 canonical 首载仍交 QA-022 在 V820 统一 revision 复验，不把确定性合同冒充 Browser PASS。

---

## 1.0.170 V8.0.20 PM chore(showcase): 接入力学首次冷加载修复并重开终验（PM-022）

- **提交日期**：2026-08-12
- **任务编号**：`PM-022`
- **提交标题**：`V8.0.20 PM chore(showcase): 接入力学首次冷加载修复并重开终验（PM-022）`
- **资源接线**：ModuleSelector 更新为 `20260812v819PhysicsFirstLoadP0`；学生/教师工作台与 Physics/Future 继续使用已验 V817/V813/V814。`index.html`、app-session、experiment/page registry、Router、main、Service Worker cache/APP_SHELL 统一为 `20260812v820ShowcaseFirstLoadP0`，确保普通 reload 不会命中 V818/V816 旧字节；`/api` 继续完全旁路缓存。
- **集成边界**：本提交只接线 V819、更新精确静态合同与 01/02/03/08；不改 FE-031 业务算法、UI、API、后端、数据库或 evidence owner。后端产品字节自 V8.0.12 独立 639 项 PASS 后仍无变化。
- **门禁与终验边界**：FE-031、cache generation、session shell、page/experiment registry 定向全部通过；完整 `npm test` exit 0、141.4 秒，235 个 tracked JS 与 56/56 前端合同通过；BE-018 定向 14 passed，仅有 2 条既有 Python 3.12 SQLite datetime adapter 弃用 warning；文档 compatibility 为 0 error/1 个既有 99 号归档命名 warning，项目对接与 `git diff --check` 通过。QA-022 必须从本提交重启 9001 运行快照、核对 HTTP 字节，并以多个新 context 证明首次 canonical load、普通 reload、ledger/started/POST、scope、console/pageerror/Network；返回前 Goal 保持 active，不能称最终 Browser PASS。
- **QA 结果**：QA-022 在精确 `582a636`、V820/V819 HTTP 字节一致的环境以 5 个每次重登的全新 context 直接打开 canonical `#physics/mechanics`：cold 仅 2/5 PASS（1.331–1.561s），3/5 在 10 秒后仍 `evidenceReady=false` 并输出 `publication_context_unavailable`；5/5 普通 reload 均于 0.898–1.243s PASS。失败 run 的 provider 已稳定到 identity/class/generation，但 `/courses/1/units` 后直接 warning、没有 recovery GET；ledger 保持 total=6/`started=1`、evidence POST=0、pageerror=0。V820 结论 P0=0、P1=1、P2=0，RETURN。

---

## 1.0.171 V8.0.21 FE fix(publication-singleflight): 合并同权威发布上下文并发读取（FE-032）

- **提交日期**：2026-08-12
- **任务编号**：`FE-032`
- **提交标题**：`V8.0.21 FE fix(publication-singleflight): 合并同权威发布上下文并发读取（FE-032）`
- **根因证据**：生产 provider 与 ModuleSelector 的确定性 seed 证明 evidence `resolveContext` 发出 `resolve/scope#1` 后，同 identity/class/activity 的并发 publication reader 在 1–2ms 内用共享 `state.controller` 的 `begin/scope#2` 取消前者；外围唯一 retry 的 scope#3 又被 scope#4 取消。全程 authority generation 未变化，原轨迹为 `readyCalls=2/bound=0/warning`，说明稳定快照或延长超时均不能保护下一次 cancel-last。
- **实现边界**：provider 拆为 `prepareFlight` 与 `resolveFlight`。prepare key 为 identity；resolve key 为 identity/class/activity/exact hash/authority generation。每 caller 持有独立 waiter/Abort，单 waiter 取消只 detach，最后 waiter 微任务确认仍为 0 才 abort 底层；identity/class/hash/close/switch 变化整体失效旧 flight，旧结果必须通过 key/generation/route 校验才能返回。同 identity prepare 可与有效 resolve 并行，不再互相取消。ModuleSelector、UI、Physics、API、后端和 evidence 事件字节不变。
- **候选与集成**：来源 `cda35711e674` 精确父为 `582a63698953`，只修改 provider、Physics binding 合同和 A03a 合同；主开发无冲突重放为 `1b497577223c`，三文件来源/集成 blob 保持等价。
- **验证证据**：seed 3/11/29/47/83 均转为 `ready=1/bind=1/mount=1/POST=0`；同 identity prepare 两 waiter、同 authority resolve 两 waiter、单/全 waiter abort、prepare↔resolve 并行、换 identity/hash/route 等合同全部通过。A03a PASS；完整 `npm test` exit 0、101.1 秒，235 个 JS 与 56/56 前端合同通过。真实 5× canonical cold/reload 交 QA-022 在 V822 统一 revision 复验。

---

## 1.0.172 V8.0.22 PM chore(showcase): 接入发布上下文单飞并重开终验（PM-022）

- **提交日期**：2026-08-12
- **任务编号**：`PM-022`
- **提交标题**：`V8.0.22 PM chore(showcase): 接入发布上下文单飞并重开终验（PM-022）`
- **资源接线**：ModuleSelector 继续使用 `20260812v819PhysicsFirstLoadP0`，学生/教师/旗舰课继续 V817/V813/V814；`index.html`、app-session、experiment/page registry、Router、main、Service Worker cache/APP_SHELL 统一为 `20260812v822PublicationSingleflightP0`，完成 V820→V822，并由 app-session→loader 把 V822 查询代际传播到新 publication provider 字节。`/api` 继续完全旁路缓存。
- **集成边界**：本提交只接线 V822、更新精确静态合同与 01/02/03/08；不改 FE-032 单飞算法、ModuleSelector、UI、Physics、API、后端、数据库或 evidence owner。后端产品字节自 V8.0.12 独立 639 项 PASS 后仍无变化。
- **门禁与终验边界**：FE-032/provider/Physics binding/A03a、cache generation、session shell、page registry 与 Future publication 定向全部通过；完整 `npm test` exit 0、103.9 秒，235 个 tracked JS 与 56/56 前端合同通过；BE-018 定向 14 passed，仅有 2 条既有 Python 3.12 SQLite datetime adapter 弃用 warning；文档 compatibility 为 0 error/1 个既有 99 号归档命名 warning，项目对接与 `git diff --check` 通过。QA-022 必须从本提交重启 9001、核对 HTTP 字节，以至少 5 个全新 context 证明首次 canonical load 与 reload、请求共享、ledger/started/POST、scope、console/pageerror/Network，并抽查 Engineering/busy/教师回读/390/键盘/reduced-motion；返回前 Goal 保持 active，不能称最终 Browser PASS。
- **QA 结果**：QA-022 在精确 `156324d`、V822 provider HTTP 字节一致的环境完成 10 个独立新 context：cold 5/10 PASS（0.882–1.003s），5/10 在 10 秒后仍失败；reload 5/5 PASS。三路同 authority 并发 resolve 探针 5/5 仅产生一组 course/unit 请求，证明单飞修复有效；失败 snapshot 的 catalogue ready/allow、provider class/generation、active mechanics、transition 与 exact hash 均正确，但 mount 随 warning 清除。ledger 保持 Physics 6/`started=1`、Engineering 7/`started=1`，POST/requestfailed/pageerror 为 0。V822 结论 P0=0、P1=1、P2=0，RETURN。

---

## 1.0.173 V8.0.23 FE fix(publication-route): 忽略同路由迟到哈希事件（FE-033）

- **提交日期**：2026-08-12
- **任务编号**：`FE-033`
- **提交标题**：`V8.0.23 FE fix(publication-route): 忽略同路由迟到哈希事件（FE-033）`
- **根因证据**：确定性生产源码红灯在真实 provider.resolve 的 courses 请求 in-flight 时投递迟到事件 oldURL `#physics`→newURL `#physics/mechanics`；当前 `window.location.hash` 已是 exact mechanics，但基线无条件 hashchange listener 仍把 request signal 置为 aborted。该轨迹对应 V822 失败 run 的 courses 多于 units、无 recovery GET，最终 snapshot 稳定会掩盖瞬时取消。
- **实现边界**：resolve flight 在发出任何请求前保存 `owner.route = resolveRoute`；hashchange handler 只在当前 `location.hash !== flight.route` 或 route 缺失时 abort。同路由迟到事件不取消，真实切换 gas-laws、离页 planets、identity/class/close/caller detach/all-waiter abort 继续失败关闭。不改 ModuleSelector、catalogue、UI、Physics、API、后端或 evidence。
- **候选与集成**：来源 `812e119e9f84` 精确父为 `156324d6d1ab`，只修改 provider 与 Physics binding 合同；主开发无冲突重放为 `23c30de9363b`，两文件来源/集成 blob 保持等价。
- **验证证据**：同路由迟到事件从 signal aborted 转为继续完成；真实换实验/离页仍 abort，Physics binding、A03a、frontier publication 定向通过。最终完整 `npm test` exit 0、100.8 秒，235 个 JS 与 56/56 前端合同通过；真实 10× canonical cold/reload 交 QA-022 在 V824 统一 revision 复验。

---

## 1.0.174 V8.0.24 PM chore(showcase): 接入同路由迟到事件修复并重开终验（PM-022）

- **提交日期**：2026-08-12
- **任务编号**：`PM-022`
- **提交标题**：`V8.0.24 PM chore(showcase): 接入同路由迟到事件修复并重开终验（PM-022）`
- **资源接线**：ModuleSelector 继续 V819，学生/教师/旗舰课继续 V817/V813/V814；`index.html`、app-session、experiment/page registry、Router、main、Service Worker cache/APP_SHELL 统一为 `20260812v824PublicationRouteP0`，完成 V822→V824，并由 app-session→loader 把 V824 传播到 provider 新字节。`/api` 继续完全旁路缓存。
- **集成边界**：本提交只接线 V824、更新精确静态合同与 01/02/03/08；不改 FE-033 route 算法、ModuleSelector、UI、Physics、API、后端、数据库或 evidence owner。后端产品字节自 V8.0.12 独立 639 项 PASS 后仍无变化。
- **门禁与终验边界**：FE-033/Physics binding/A03a/Future publication、cache generation 与 session shell 定向全部通过；完整 `npm test` exit 0、107.9 秒，235 个 tracked JS 与 56/56 前端合同通过；BE-018 定向 14 passed，仅有 2 条既有 Python 3.12 SQLite datetime adapter 弃用 warning；文档 compatibility 为 0 error/1 个既有 99 号归档命名 warning，项目对接与 `git diff --check` 通过。QA-022 必须从本提交重启 9001、核对 HTTP 字节，以至少 10 个全新 context 证明首次 canonical load/reload、ledger/started/POST、scope、console/pageerror/Network，并抽查 Engineering/busy/教师回读/390/键盘/reduced-motion；返回前 Goal 保持 active，不能称最终 Browser PASS。
- **QA 结果**：QA-022 在精确 `3622981`、V824/V819 HTTP 字节一致的同一 fresh 数据环境跑满 20 个独立 canonical cold context：仅 7/20 PASS（0.908—1.427 秒），13/20 在 10.014—10.085 秒后仍失败；前 10 个 ordinary full reload 为 9/10 PASS，唯一失败曾短暂 `evidenceReady=true`，约 20ms 后回落 false。失败 cold 固定为 classes/courses/units/recovery=3/3/2/0，成功为 3/5/4/1；全轮 requestfailed/pageerror/evidence POST=0，教师账本仍为 Physics 6/`started=1`、Engineering 7/`started=1`。V824 结论 P0=0、P1=1、RETURN。

---

## 1.0.175 V8.0.25 FE fix(physics-bind-order): 在课程初始化后挂载学习证据（FE-034）

- **提交日期**：2026-08-12
- **任务编号**：`FE-034`
- **提交标题**：`V8.0.25 FE fix(physics-bind-order): 在课程初始化后挂载学习证据（FE-034）`
- **根因证据**：`openModule()` 调用 `_initModule()` 时只排队资源加载、50ms init 与最多 20 次重试，却在下一行立即调用 `_mountEvidenceRuntime()`。cold 时 evidence 可早于 `window.PhysicsSim` owner 出现而直接 `publication_context_unavailable`；reload 时缓存 owner 可先 bind 并写 `evidenceReady=true`，随后迟到的 `PhysicsSim.init() → destroy() → bindCourse() → _resetCourseOwnerState()` 清空 binding 并写回 false，精确对应 V824 两种失败轨迹。
- **实现边界**：`_initModule()` 增加 current-generation completion callback；仅当前 owner 已初始化或 registry init 成功后回调，`openModule()` 继续同步返回 boolean，但 evidence mount 只从 completion 执行。dirty、stale、asset load failure、init failure 均不挂载；重复 open 不重复 init/mount。不改 Physics 数值/owner、publication provider、UI、API、后端、数据库或 evidence 事件。
- **候选与集成**：来源 `622e3739d610` 精确父为 `362298107c41`，只修改 ModuleSelector、module lifecycle 合同和 A03a fixture；主开发无冲突重放为 `4624f5d`。A03a 仅让测试桩履行 completion callback，原 owner 次数与业务断言不变。
- **验证证据**：delayed-assets cold 在 loading 期间 mount=0，owner init=1 后 mount=1；cached-owner reload 的 init/reset=1 严格先于 bind=1，最终 ready=true；initialized 同步完成，dirty/stale completion=0。A03a、module lifecycle、Physics binding/evidence、experiment registry 与 cleanup 定向通过；候选完整 `npm test` exit 0、102.5 秒，235 个 tracked JS 与 56/56 前端合同通过。真实 cold/reload 仍交 QA-022 在 V826 统一 revision 复验。

---

## 1.0.176 V8.0.26 PM chore(showcase): 接入初始化后证据挂载并重开终验（PM-022）

- **提交日期**：2026-08-12
- **任务编号**：`PM-022`
- **提交标题**：`V8.0.26 PM chore(showcase): 接入初始化后证据挂载并重开终验（PM-022）`
- **资源接线**：ModuleSelector 更新为 `20260812v825PhysicsBindOrderP0`；学生、教师与旗舰课继续使用已验 V817/V813/V814。`index.html`、app-session、experiment/page registry、Router、main 与 Service Worker cache/APP_SHELL 统一为 `20260812v826PhysicsBindOrderP0`，完成 V824→V826；app-session→loader 继续把 V826 查询传播到 queue/client/status/catalog/publication context/activity，`/api` 继续完全旁路缓存。
- **集成边界**：本提交只接线 V825/V826、更新精确静态合同与共享 01/02/03/08；FE-034 运行时字节已在前一独立提交 `4624f5d` 集成。本提交不改 UI、Physics 数值、publication provider、API、后端、数据库或 evidence owner。后端产品字节自 V8.0.12 独立 639 项 PASS 后仍无变化。
- **门禁与终验边界**：module lifecycle、A03a、Physics binding/evidence、cache generation、session shell、experiment/page registry 与 workspace hierarchy 定向全部通过；完整 `npm test` exit 0、167.9 秒，235 个 tracked JS 与 56/56 前端合同通过；BE-018 定向 14 passed、37.2 秒，仅有 2 条既有 Python 3.12 SQLite datetime adapter 弃用 warning。文档 compatibility 为 0 error/1 个既有 99 号归档命名 warning，项目对接与 `git diff --check` 通过。QA-022 必须从同一 clean revision 重启 9001、核对 V825/V826 HTTP 字节，复验至少 20× canonical cold、10× ordinary reload，显式确认全过程不发生 ready→false，并复核 ledger/started/POST、scope、console/pageerror/Network 与 Engineering/busy/390/键盘/reduced-motion/教师回读；返回前 Goal 保持 active，不能称最终 Browser PASS。
- **QA 结果**：QA-022 在精确 `2a34976e3343`、同一 fresh data、无重新初始化的环境核对 14 个 HTTP 资源全部 200 且与本地 blob 逐字节一致；V826 主壳、V825 ModuleSelector、V817/V813/V814 角色与旗舰代际正确。20/20 canonical cold 与前 10/10 ordinary reload 全部 PASS，cold 1.945—2.346 秒、reload 1.459—1.679 秒；10ms 持续采样中 ready→false=0，warning/error/requestfailed/pageerror/HTTP≥400/evidence POST=0。Physics 6/started1、Engineering 7/started1 账本及 scope/type 不漂移；Engineering 唯一入口、师生 busy、390、键盘、reduced-motion、教师双课回读通过。独立 clean `npm test` 235 JS/56 contracts、FE-034 5/5、BE-018 14/14 通过；P0/P1/P2=0/0/0，PASS。External extension 不可用，Browser 后端为 Playwright 1.60 + system Edge 151 headless，不声明 external Edge PASS。

---

## 1.0.177 V8.0.27 PM docs(showcase): 重开效果表达与内容充实阶段（PM-023）

- **提交日期**：2026-08-12
- **任务编号**：`PM-023`
- **提交标题**：`V8.0.27 PM docs(showcase): 重开效果表达与内容充实阶段（PM-023）`
- **具体完成**：保留并写回 V826 精确 SHA、HTTP 字节、20/20 cold、10/10 reload、0 次 ready→false、双课账本、Engineering/师生 busy/390/键盘/reduced-motion/教师回读、独立自动化和 Browser caveat；根据用户恢复指令撤销未提交的“关闭 Goal”草稿，把新周期收束为师生工作台展示叙事与双旗舰课内容表达，建立 `PM-023 / UI-006 / FE-035 / QA-023`、V8.0.27—V8.0.31 版本路线和新树边界。
- **边界**：本提交只修改共享 01/02/03/08，不改产品代码或资源代际；V8.0.26 的 API、后端、数据库、权限、作用域、证据、状态机、solver、physics loop、完成投影和教师回读作为保护基线。内容充实只发生在既有双旗舰课与师生工作台，不恢复六课迁移、复杂分析、深层重构、实证或生产发布。
- **验证要求**：文档 compatibility、项目对接、资源代际合同、`git diff --check` 与 Git clean 必须通过后才允许创建 `ui06/fe35` 新树并派发 V8.0.28/V8.0.29；任何失败均保持 PM-023 进行中。

---

## 1.0.178 V8.0.28 UI ui(role-story): 深化师生工作台教学叙事（UI-006）

- **提交日期**：2026-08-12
- **任务编号**：`UI-006`
- **来源与集成**：来源 `57a78dfac476`（父 `d549a2226b33`），主线字节等价集成 `968fc6972729`；精确 6 文件，stable patch-id 一致。
- **具体完成**：不新建工作台 owner，而是在既有 student/teacher focus stage 内补齐“当前班课—本节目标—预计产出—下一动作—完成回执”。学生只读 selected class/course、exact-open unit 或 assignment 与权威 recovery progress；教师只读当前班课、release plan、开放单元、待处理提交和写锁。预计产出使用保守的“将形成记录/待发布/待反馈”语言，不伪造掌握、误区、统计或服务端完成。
- **边界与验证**：API/fetch/data-* 集合、scope gate、publication authority、写锁与键盘 owner 不变；教师文件保持 2883 行冻结上限。定向和完整 `npm test` 235 JS/57 contracts 通过。Playwright 1.60 + system Edge headless 在真实本地页面覆盖学生/教师 1440×1000、390×844、键盘、focus-visible 与 reduced-motion；学生 390 的唯一 CTA 为 46px，目标和 CTA 均在固定 dock 上方、无横溢出。External Browser 入口不可用，未冒充 external PASS。

---

## 1.0.179 V8.0.29 FE feat(flagship-story): 深化双课教学内容表达（FE-035）

- **提交日期**：2026-08-12
- **任务编号**：`FE-035`
- **来源与集成**：来源 `4acc6792e966`（父 `d549a2226b33`），主线字节等价集成 `db1b8acd6d2b`；精确 6 文件，stable patch-id 一致。
- **具体完成**：Engineering 增加 `data-load-path-brief/coach/conclusion`，阶段讲解明确固定 60kN、完整 15 杆与唯一 B→C→D 变量，结论只在 exact B/C/D observation 全部有效后显示；Physics 增加 `data-mechanics-brief/coach/comparison/conclusion`，比较只消费真实 `.40/.80` measurement，把页面实测 h/hH 与理想 e² `.16/.64` 分层呈现且不互相覆盖，结论只在 P7/P8 和真实双快照齐备后显示。
- **边界与验证**：两个 story helper 均为无状态、无 DOM、无 solve/RAF/record 的只读投影；solver、Canvas 单 writer/唯一 RAF、Physics 真实 loop/首峰、H/g/r/vx/damping、e 唯一变量、authority/permit/receipt/evidence 与服务端 `completed` owner 均未改。定向和完整 `npm test` 235 JS/57 contracts、diff-check 通过；独立 Edge 服务覆盖两课 1440×1000、390×844、键盘和 reduced-motion。合成学生的 publication authority 在结果阶段按设计失败关闭，因此该隔离证据不冒充真实结果 E2E，统一结果旅程留 QA-023。

---

## 1.0.180 V8.0.30 PM chore(showcase): 接入角色与旗舰课展示代际并启动终验（PM-023）

- **提交日期**：2026-08-13
- **任务编号**：`PM-023`
- **资源接线**：学生/教师使用 `20260812v828RoleStoryP0`，Physics/Engineering/Future 使用 `20260812v829FlagshipStoryP0`，ModuleSelector 保持已验 `20260812v825PhysicsBindOrderP0`；`index.html`、app-session、experiment/page registry、Router、main 与 Service Worker 统一为 `20260812v830ShowcaseStoryP0`。app-session → loader → queue/client → catalog/activity 继续传播同一 V830 查询，角色资源继续 network-only，`/api` 完全旁路缓存。
- **集成边界**：本提交只完成 V828/V829/V830 静态代际、精确资源合同和共享 01/02/03/08；UI-006 与 FE-035 运行时字节已分别在 V8.0.28/V8.0.29 独立提交集成。不改 API、后端、数据库、课程事实、状态机、solver、physics loop、evidence 或服务端完成 owner；ARCH-004 所述页面步骤级精确恢复仍未扩大声明。
- **验收状态**：V8.0.30 是交给 QA-023 的统一候选，不是提前宣告的终局 PASS。主线完整 `npm test` 已实测 237 JS/58 contracts、BE-018 定向 14 passed，文档兼容为 0 error/1 个既有 99 号归档命名 warning，项目对接、资源代际与 diff-check 通过；提交后仍必须从同一 clean revision 核对 HTTP blob，并完成师生及双课桌面/390、键盘、reduced-motion、刷新恢复、教师回读、console/pageerror/Network 与账本无漂移验收。

---

## 1.0.181 V8.0.31 PM docs(showcase): 登记终验展示退回项（PM-023）

- **提交日期**：2026-08-13
- **任务编号**：`PM-023`
- **QA-023 阶段结果**：精确 V830 `39b58d6` 的 fresh data、20 项 HTTP 与新 Edge headless evidence 前置通过。A 批真实学生/教师 desktop+390 证明目标、产出、唯一动作、≥46px、无横溢出和零 console/page/network 错误，但两个 390 首屏的完成回执均位于 viewport 下方；B 批干净 Engineering 单轮以 7 个预期事件跑通 prediction→B→C→判断→D→corrected→explained，exact 数值、结论门禁、模型边界和零错误均正确，但 1440×1000 所有阶段 Canvas、数值表和 status 都无法同帧核对。
- **处置**：按用户“效果表达与呈现优先”的当前 Goal，将两项从普通间距线索提升为赛事展示 P1 退回，建立 UI-007 与 FE-036 两个互斥窄修；只允许移动 focus CSS 与 Engineering 舞台表现层，不改 API、后端、课程事实、状态机、solver、physics loop、evidence 或完成 owner。V830 保留为首个统一候选，不覆盖其自动化与阶段证据；后续 V832/V833 修复、V834 同 revision 重验、V835 文档/Git 闭环。

---

## 1.0.182 V8.0.32 UI fix(role-mobile): 让师生首屏回执进入可视范围（UI-007）

- **提交日期**：2026-08-13
- **任务编号**：`UI-007`
- **来源与集成**：UI 长期工作树 `ui06` 在共同父线 `10eb17d` 形成来源 `fde573f`；主线串行等价集成为 `b730a62`，5 文件 allowlist 与 stable patch-id `742398f5deda` 一致。
- **具体完成**：只重排 390×844 的 student/teacher focus stage：上下文跨列，学生下一动作与完成回执共享紧凑状态行，教师唯一 action 与 receipt 共享第二行，教学轨道仍保留在后续文档流。没有删除五段语义、隐藏回执或使用 fixed/absolute 假层。
- **边界与验证**：未改 JS、DOM、API、data owner、scope、共享运行时、课程、后端或缓存；teacher.js 保持 2883 行。Edge 151 等价生产 DOM 的学生、教师 empty/success 均满足 action/receipt bottom 小于 dock top 778，primary=1、CTA≥46px、无横溢出；完整 `npm test` 237 JS/59 contracts、diff-check 通过，真实统一 revision 仍交 QA-023。

---

## 1.0.183 V8.0.33 FE fix(load-path-layout): 让工程观察与数值在桌面同帧呈现（FE-036）

- **提交日期**：2026-08-13
- **任务编号**：`FE-036`
- **来源与集成**：FE 长期工作树 `fe35` 在共同父线 `10eb17d` 形成来源 `476cdf6`；主线在 V832 后串行等价集成为 `fc49324`，2 文件 allowlist 与 stable patch-id `b6ea08372a99` 一致。
- **具体完成**：仅在 desktop `engineering.load-path` 让真实 `.fg-stage` sticky，以紧凑双列组织原 brief/coach/步骤，并保持真实 B/C/D 表为全宽证据行；Canvas 高度在 265—315px，移动端继续使用原单列。没有复制 Canvas/数值、缩放页面或建立第二个状态 owner。
- **边界与验证**：未改 BridgeTruss、solver、state transition、Canvas writer/RAF、evidence、authority、API、后端、入口或缓存。评审后把进度、表格、步骤文字收回 10—11px 可读下限，动作仍≥44px；prediction 1440×1000 实测 Canvas/table/status/coach 同帧且无横溢出。C/D 未在组内冒充 PASS，明确留给 QA-023/V834；完整 `npm test` 237 JS/59 contracts、diff-check 通过。

---

## 1.0.184 V8.0.34 PM chore(showcase): 接入移动回执与工程同帧布局并重开终验（PM-023）

- **提交日期**：2026-08-13
- **任务编号**：`PM-023`
- **资源接线**：学生/教师使用 `20260813v832RoleMobileReceiptP0`，Future/Engineering 使用 `20260813v833LoadPathSameFrameP0`，Physics 保持 `20260812v829FlagshipStoryP0`，ModuleSelector 保持 `20260812v825PhysicsBindOrderP0`；`index.html`、app-session、experiment/page registry、Router、main 与 Service Worker 统一为 `20260813v834ShowcaseLayoutP0`。app-session → loader → queue/client → catalog/activity 继续传播同一 V834 查询，角色资源继续 network-only，`/api` 完全旁路缓存。
- **协作与集成边界**：主控只发版本令牌、核对来源提交并以 patch-id 等价串行集成，未代写 UI/FE 专业字节；`ui06/fe35/qa22` 继续作为 UI、FE、QA 的长期责任树。本文和 01/02/08 已同步真实任务、来源 SHA、工作树与复验边界，不把聊天记录当第二份任务看板。
- **候选门禁**：主线完整 `npm test` exit 0、139.9 秒，239 个受跟踪 JavaScript 与 60/60 前端合同通过；BE-018 定向 14 passed，仅保留 2 条既有 Python 3.12 SQLite datetime adapter 弃用 warning。文档 compatibility 为 0 error/1 个既有 99 号归档命名 warning，项目对接通过；后端产品字节未变化，不重复完整 639 项。
- **验收状态**：V8.0.34 是交给原 QA-023 的再验候选，不是提前宣告终局 PASS。提交后由 `qa22` fast-forward 到精确 SHA，核对 HTTP blob/资源代际，复验真实移动回执和 Engineering prediction/C/D 三态，并保护 Physics、busy/reload、双课账本与教师回读。

---

## 1.0.185 V8.0.35 PM chore(repository): 归并原始文档并收束工作树（PM-024）

- **提交日期**：2026-08-22
- **任务编号**：`PM-024`
- **提交标题**：`V8.0.35 PM chore(repository): 归并原始文档并收束工作树（PM-024）`
- **用户决策**：全部工作组、子工作组和后台推进继续解散；产品开发与 QA 暂停。文档治理改以项目原有的 `01-开发者手册.md`、`02-更新规划.md`、`03-发布历史.md` 为唯一三份主文档，不因通用编号规范另建重复入口，也不大规模简化原始正文。
- **工作树收束**：对账 24 棵非根工作树。`6f2a/be13/c04e/da03/f27a/fe13/fe29/fe30/fe35/ft87/fz30/qa22/u356/ui04/ui05/ui06` 共 16 棵 clean 且已集成、补丁等价或主线已有对应完成事实的工作树，以普通 `git worktree remove` 安全退出并执行 prune，全程未使用 `--force`。
- **保护资产**：`5779`、`f017/f017a/f017b/f017c`、`fr89`、`ux64` 共 7 棵含未提交修改；`u358@3734a2f` clean 但含 1 条主线外提交。8 棵均保留原目录和分支，未 reset、clean、rebase、merge、cherry-pick 或强制删除。66 条本地分支数量保持不变。
- **文档归并**：此前独立的当前实现卷、后期开发记录和规划副本，分别完整并入原始开发者手册、发布历史与更新规划。删除三个重复文件，修正 README、项目指引、后端/前端说明、子文档和静态合同中的旧文件名链接，并逐处消除合并后出现的自链接和重复目录行。
- **架构与状态同步**：开发者手册补入当前运行/数据架构、学生—教师旗舰课闭环、模块完成度和暂停恢复顺序；更新规划登记 V834 未验边界、全部工作组停止态、8 棵保全工作树与恢复门禁。V8.0.35 不修改产品运行逻辑、API、数据库、课程事实或历史分支。
- **验证证据**：重复文件 0、旧文件名文本/链接 0、三份主文档齐全、66 条本地分支和 9 个登记 worktree（根工作区 + 8 个保全现场）对账一致；20 份改动 Markdown 的相对链接、锚点和围栏为 0 error，规划任务结构为 0 error，`git diff --check` 为 0 error。239 个跟踪 JavaScript 语法、FE-024 文档/缓存合同及排除基线异常后的 59/59 前端合同通过。完整 `npm test` 在未修改的 `local-preview-python-selection-contract` 外置 venv 断言处停止；启动器与该合同的工作区 blob 均和 HEAD 一致，不冒充全量 PASS。通用文档校验器 compatibility 为 0 error / 4 warning，其中 3 项及项目对接校验器 2 项均来自工具硬编码的重复新文件名；遵照用户原始文档优先规则不重建副本。

---

## 1.0.186 V8.0.36 PM chore(showcase): 解散遗留工作树并冻结展示优先路线（PM-025）

- **提交日期**：2026-08-22
- **任务编号**：`PM-025`
- **提交标题**：`V8.0.36 PM chore(showcase): 解散遗留工作树并冻结展示优先路线（PM-025）`
- **用户决策**：当前优先由用户直接与主开发对接，不再保留工作组、子工作组、后台任务或长期工作树；未来开发以师生前端表现、课程内容与动画、演示素材和竞赛包装为首要价值，不为了生产级安全、额外加密、极限性能、真实 MySQL、通用 OJ、全量统一内核或架构洁癖消耗赛前主体时间。
- **协作与工作树结果**：用户进一步明确“工作组”首先指侧栏的长期对接对话。后端组、架构组、前端组、内容组、QA 组五个 Codex 任务均已归档，列表复核后“星序 Astra”只保留当前“主开发｜总控”；归档可恢复，不删除历史。协作代理树核对后也仅有 `/root`。PM-024 保全的 7 棵脏工作树分别先以 `worktree-archive:<标识>:2026-08-22:before-dissolve` 建立 `--include-untracked` stash，并在逐树确认 clean 后用普通 `git worktree remove` 退出；`u358` 原本 clean，独有提交继续由 `codex/ui-v7.9-admin-resource-generation@3734a2f` 分支保存后普通退出。最后执行 prune，Git 只登记根工作树，全程未使用 `--force`、`reset`、`clean`、`rebase`、递归删除或分支删除，66 条本地分支数量不变。
- **可恢复锚点**：`5779=637637791b3f77eab741fa47c48a0faa50eff71c`、`f017=e0781961adbcb8738b8e3bd010d5f2d06c715553`、`f017a=0c30b32b003aefe2ddbad8063f5702f87c780522`、`f017b=c8af9a71183dd55eba5772549fdf457d5a8933d9`、`f017c=53f62a9a2f4294615acbdc77577aa2379d84dffb`、`fr89=5adf446e37cb17755cc41e0e8a45d9ed5ff3b7f7`、`ux64=ff0c24dcf5f7a8b7c40debc8cde4145fda5dd4fc`。这些 SHA、原分支和 02 第 9 节共同构成恢复索引；退出工作树不等于采纳、丢弃或删除候选。
- **全局功能盘点**：从当前代码而非旧任务摘要重新核对三角色、三星系和业务 API。当前主线包含学生入班/选课/作业/进度/证据，教师课程挂接/发布节奏/成员/作业/评分/代码与证据回读，管理员组织/用户/课程/内容/审计治理；工科试验室 5 学科 88 实验、代码空间 6 组 18 活动、未来星系 6 方向 18 活动，以及 Engineering/Physics 两条服务端完成闭环。仓库当前约 640 个受跟踪文件、239 个受跟踪 JavaScript、266 个 Python 文件和 179 个 FastAPI 路由装饰器，证明工程不是简单空壳；同时明确普通实验/活动的教学深度不能由两门旗舰课外推。
- **竞赛完成度裁决**：功能广度约 90%、关键演示闭环约 86%、师生可见完成度约 78%、内容与动画表达约 72%、竞赛包装约 55%，优秀毕设/多媒体竞赛综合约 80%—82%，剩余约 18%—20% 是高价值尾差。原长期产品蓝图仍约 65%—70%，但全量六课精确续学、生产 OJ、真实课堂与正式部署不再作为当前竞赛退出条件。
- **未来路线与工作量**：冻结 S0 候选终验、S1 演示导航、S2 师生平台、S3 双旗舰、S4 内容补面、S5 竞赛包装、S6 最终浏览器七阶段，完整优秀候选预计 22—36 个专注人日、单开发者约 4—7 周；可参赛最小包可压缩为 10—15 人日。预计 65% 工作量直接投入课程动画和师生前端，15% 投入包装、15% 投入 QA，必要文档/后端接线不超过 5%。
- **运行边界**：本提交不修改产品运行逻辑、API、后端、数据库、课程事实、状态机、求解器、物理积分器、Service Worker 资源代际或历史分支。V8.0.34 的 QA-023 同 revision 终验继续保持未完成，不因文档和 Git 收束自动升级为 PASS。
- **验证证据**：Codex 任务列表中“星序 Astra”仅剩当前“主开发｜总控”，五个专业长期对接任务均返回 `archived=true`；协作代理树仅剩 `/root`，Git 仅登记根工作树。66 条本地分支、7 个具名 stash SHA 与 `u358@3734a2f` 独有提交均可解析。重复主文档文件与历史重复文件名残留引用均为 0；通用文档校验为 0 error / 4 个预期 compatibility warning，项目对接校验器仍仅报告其硬编码的两个已删除副本文档名，遵照用户决定不重建。239 个受跟踪 JavaScript 语法、FE-024 合同及排除未修改基线异常后的 59/59 前端合同通过；完整 `npm run test:contracts` 仍停在未修改的 `local-preview-python-selection-contract` 外置 venv Python 选择断言，启动器与该合同的工作区 blob 均和 HEAD 一致，不冒充全量 PASS。`git diff --check` 为 0 error。

---

## 1.0.187 V8.0.37 DOC docs(core-docs): 分层重组开发手册规划与发布历史（DOC-002）

- **提交日期**：2026-08-22
- **任务编号**：`DOC-002`
- **提交标题**：`V8.0.37 DOC docs(core-docs): 分层重组开发手册规划与发布历史（DOC-002）`
- **用户决策**：继续以原始 `01-开发者手册.md`、`02-更新规划.md`、`03-发布历史.md` 为三份主文档；保留原有内容与命名，不建立重复主文档。01 适度减负，02 超大规模重写，03 按大版本分卷；历史迁移不能被当作删除。
- **01 分层结果**：根手册重写为产品、架构、角色/作用域、班级业务、三星系、前后端、关键旅程、启动、完成边界和技术导航。原第 2—20 章按职责完整迁入 11—19：架构目录、前端生命周期、后端 API、课程证据、学科内容、界面规范、环境部署、测试维护、当前业务闭环。迁移前 SHA-256 为 `36c6cb6f2cfeae30553f00b3740c150a4ad9e6beac2f70c1d208d136a9659076`。
- **02 重写结果**：基于当前代码量、179 个 FastAPI 路由、三角色、124 个实验/活动、大型前后端 owner 和现有两课证据闭环，重新审查前端深度、课程内容、学生/教师/管理员体验、后端重叠领域、页面拼接、跨端边界和竞赛交付。路线拆为 V8.1—V8.6，共 39 个唯一小版本任务：T0=19、T1=14、T2=6；每个任务只保留一条直白目标，专业细节进入目标解释。只做 T0 估计 25—36 人日，完整路线估计 35—53 人日。O2O、微信小程序、正式服务器/OJ/AI/课堂实证/企业级工程和全量内核统一全部记录在长期方向，不占当前完成度。
- **03 分卷结果**：迁移前原 03 SHA-256 为 `cdee560a7144340e8d7ec762c394692d7323a84eb7d6a6dd11fa365070b980e4`，共 9028 行。481 个可归属版本记录块进入 31—35：V4=41、V5=3、V6=196、V7=205、V8 原记录=36；650 行跨版本说明和混合表格原文进入 39。重写前 02 的完整控制面快照也进入 39，回算 SHA-256 为 `56b3209e7ef4d8f11f9936cf1fad437fb7b79710777fdde169e380e001184271`。
- **导航同步**：README、00 总纲、项目指引、21—29 历史设计子文档、51 管理员契约和 99 历史审查入口同步到新结构；删除 01/03 子目录占位文件，未创建 `01-开发者文档.md`、`02-项目规划.md`、`03-开发历史.md` 等重复主文档。
- **运行边界**：本版本只修改文档、导航和项目指引，不修改产品 JavaScript/Python/C++、API、数据库、迁移、课程事实、状态机、求解器或缓存代际。V8.0.34 仍是未完成 QA-023 同 revision 终验的产品候选。
- **验证证据**：专项文档校验覆盖 38 份 Markdown，文件、单一 H1（39 迁移附录例外）、相对链接、目标锚点和代码围栏为 0 error/0 warning；01 迁移章节映射、V4—V8 标题大版本归属和三份迁移源哈希全部对账；02 的 39 个小版本与 39 个任务编号唯一且字段完整。`fe024-cache-generation-contract` 通过，V834 资源代际、ARCH-004 和 QA-023 待同 revision 复验语义仍可定位；`git diff --check` 通过。通用技能校验器仍会因用户明确保留的旧主文档名、历史章节编号和原始长卷格式报告结构不兼容，本版本遵照“原始文档优先”不为追求通用规则而重命名或批量改号。

---

## 1.0.188 V8.1.6 PM revert(experiments): 恢复力学与桥梁直接实验体验（RESTORE-01）

- **提交日期**：2026-08-24
- **任务编号**：`RESTORE-01`
- **提交标题**：`V8.1.6 PM revert(experiments): 恢复力学与桥梁直接实验体验（RESTORE-01）`
- **用户裁决**：否决在完成度较高的实验页内堆叠课程目标、预测/修正表单、证据工作流和大量说明文本。既有 124 项实验从本版本起默认冻结；除明确知识性错误外，不改原交互、动画、布局和求解器，后续只允许新增实验以及实验外的平台协同与竞赛包装。
- **Physics 还原**：`physics.mechanics` 删除被否决的课程包装和专属样式/状态机，恢复“参数控制 + Canvas + 直接操作”的原实验体验；ModuleSelector 对该 owner 建立反注入边界，课程系统可以链接实验，但不得把证据流程挂进实验首屏。
- **Engineering 还原**：`#engineering` 恢复静态桥梁桁架页面和 `BridgeTruss` 独立 owner；从 Future runtime 与 publication context 管理集合移除，Router/PageRegistry 直接加载工程实验资源，避免再次被课程 DOM 动态替换。
- **缓存与防回归**：公共缓存代际提升为 `20260824v816ExperimentRestoreP2`；Physics、Engineering、owner、路由、缓存和历史课程层均有拒绝式合同，确保被否决包装不能从旧入口、迟到生命周期或离线缓存复活。
- **验证工具闭环**：本地预览锁文件摘要改用 .NET `SHA256` 只读流，消除收紧 `PATH` 时 PowerShell 模块自动加载导致的基线合同异常；该调整只修复验证入口，不改变前后端产品业务。
- **验证证据**：从 Git 暂存区导出独立快照后，239 个 JavaScript/MJS/CJS 文件语法通过，60/60 个该快照内受跟踪前端合同通过，覆盖架构行数边界、缓存代际、Physics/Engineering 直接 owner、Future runtime 隔离、离线资源、本地预览 Python 选择和角色/班课既有回归；`git diff --cached --check` 为 0 error。此前恢复工作区的真实 Edge/Chrome 桌面与 390×844 代表旅程只作为恢复预览证据，不替代后续竞赛终验。
- **诚实边界**：旧 V8.1.0—V8.1.5 内容平台与公共引导仍是未提交、待项目负责人裁决的工作区候选，本提交不包含、不发布，也不以其未跟踪合同扩大全绿数量；新增实验模板属于后续 V8.2 `EXT-01`，另行提交。

---

## 1.0.189 V8.2.0 EXT feat(scaffold): 建立只增不改的新实验模板（EXT-01）

- **提交日期**：2026-08-24
- **任务编号**：`EXT-01`
- **提交标题**：`V8.2.0 EXT feat(scaffold): 建立只增不改的新实验模板（EXT-01）`
- **任务目标**：让开发者可以复制一套最小结构新增实验，同时保证模板本身不会注册空壳入口，也不会要求任何既有实验迁移。
- **模板组成**：`tools/templates/new-experiment/` 提供 `manifest.json.tpl`、`module.html.tpl`、`index.js.tpl`、`styles.css.tpl` 和 `model-notes.md.tpl`；`template.contract.json` 冻结占位符、复制目标、回退方式和验证命令。
- **运行边界**：单 owner 运行时包含 AbortController、ResizeObserver、DPR 上限、requestAnimationFrame、暂停/重置、参数快照、reduced-motion 静态初始化和完整 destroy；失败初始化主动清理半挂载状态。CSS 所有选择器同时受 page、module 和 `.astra-exp--namespace` 限制。
- **内容边界**：模型说明要求变量与单位、公式、适用条件、简化假设、数值方法、误差、参考依据、已知限制、画面映射和可复核样例；说明进入开发/论文文档，不堆进实验首屏。
- **自动验证**：`new-experiment-template-contract.cjs` 把模板渲染为真实 Physics 探针，解析 manifest，编译运行时，真实执行 init、参数变化和 destroy，确认事件 signal 已 abort、ResizeObserver 已断开、CSS 未越界、模型字段齐全，并检查生产 `config/registry/index` 不含模板身份或占位符。
- **验证证据**：从 Git 暂存区导出的独立 V8.2.0 快照通过 240 个受跟踪 JavaScript/MJS/CJS 文件语法检查与 61/61 前端合同；专项输出为 `new-experiment-template-contract: EXT-01 copyable template PASS`，`git diff --cached --check` 为 0 error。
- **诚实边界**：本版本没有新增用户可见实验，没有修改 124 项既有活动，也没有接入目录、路由、Service Worker 或运行时注册。旧 V8.1.0—V8.1.5 候选继续原位保留但不在本提交中；下一任务为 V8.2 `EXT-02` 新增实验注册检查。

---

## 1.0.190 V8.2.1 EXT feat(registry-gate): 建立新增实验注册检查（EXT-02）

- **提交日期**：2026-08-24
- **任务编号**：`EXT-02`
- **提交标题**：`V8.2.1 EXT feat(registry-gate): 建立新增实验注册检查（EXT-02）`
- **任务目标**：在新增工科实验进入生产目录和注册表之前，阻止它覆盖既有身份、复用 owner/脚本或以缺失资源和无 cleanup 的状态形成空壳入口。
- **只读基线**：`check-new-experiment-registration.cjs` 在 VM 中加载 `CONFIG.experiments` 与 `AstraExperimentRegistry`，核对两者 88 项身份一一对应，然后建立 key、activity key、route、同学科 title、cleanup owner 和 script 集合；验证过程只复制集合，不重写生产源文件。
- **候选合同**：manifest 必须使用 schema 1、现有五学科 subject、kebab-case id、`subject.id` activity key、`#subject/id` 路由、独立标题/PascalCase owner、`init<Owner>`、私有目录内带 `?v=` 的 JS/CSS，以及 `owner.destroy / verified=true` cleanup。运行时在隔离 VM 中解析和加载，owner、init、destroy 必须存在，destroy 在未初始化状态调用不得抛错。
- **失败关闭**：重复 key/activity key/route/同学科标题/owner/脚本、未知学科、未替换占位符、缺版本、资源 404、目录越界、错误扩展名、缺 owner/init/destroy 或 destroy 抛错均返回明确错误码并使 CLI exit 1；生产树没有新 manifest 时 exit 0 并输出“protected baseline unchanged”。
- **防副作用证据**：合同在系统临时目录生成一个真实模板候选，覆盖正例和全部主要反例；执行前后 `shared/js/config.js`、`shared/js/experiment-registry.js`、`index.html` 的 SHA-256 完全一致，证明检查器没有借验证写入注册项。
- **验证证据**：从 Git 暂存区导出的独立 V8.2.1 快照通过 242 个受跟踪 JavaScript/MJS/CJS 文件语法检查与 62/62 前端合同；专项输出为 `new-experiment-registration-contract: conflicts, resources, owner and cleanup PASS`，`git diff --cached --check` 为 0 error。
- **诚实边界**：本版本只覆盖五学科 88 项工科实验，因为新增模板当前面向 `pages/<subject>/<experiment-id>/`；代码空间 18 项与未来星系 18 项有独立清单/运行时，完整 124 项旧活动变更告警属于下一任务 `EXT-03`。本版本没有新增可见实验，也没有包含旧 V8.1.0—V8.1.5 候选。

---

## 1.0.191 V8.2.2 EXT feat(protection): 建立124项旧活动变更告警（EXT-03）

- **提交日期**：2026-08-24
- **任务编号**：`EXT-03`
- **提交标题**：`V8.2.2 EXT feat(protection): 建立124项旧活动变更告警（EXT-03）`
- **任务目标**：让任何误删、改名、改路由、改 owner 或改动既有活动实际消费资源的行为，在提交前明确指出活动与文件，同时允许完全独立的新增活动继续开发。
- **跨架构投影**：`check-protected-activity-changes.cjs` 保留三套既有架构，不做迁移；它从 `CONFIG.experiments + AstraExperimentRegistry` 投影 88 项工科实验，从 Code Space course manifest 投影 18 项课程，从 Future manifest 与学习 owner 配置投影 18 项活动，再与 `AstraLearningActivityCatalog` 的 `88/18/18` 身份做双向核对。
- **保护粒度**：`protected-activities-v822.json` 以 `0744205 / V8.2.1` 为来源修订，确定性记录 key、route、title、owner、init/cleanup 以及活动实际消费的 JS、CSS、manifest、catalog、challenge 或精确 `[data-module]` DOM 片段 SHA-256；基线从纯 Git index/HEAD 投影重建后字节一致。
- **新增与差异边界**：当前投影中不在旧基线的身份只进入 additions 并被忽略，不会阻止只增不改；旧身份缺失或任一受保护字段变化则 exit 1，并输出 galaxy、activity key、field/file、before/after 与 64 位 issue signature。
- **一次性例外**：`protected-activity-exceptions.json` 默认为空。例外必须同时具备精确 signature、任务号、批准人、日期、理由和 `one_time: true`；signature 绑定完全相同的 before/after 指纹，格式缺失、重复或泛化均失败关闭。未经负责人批准不得重写基线消除告警。
- **自动验证**：专项合同证明基线确定性与 `88/18/18` 计数，普通新增不报警，Physics mechanics 资源差异和 Code Space route 差异能定位活动/文件，精确例外只抑制对应差异，畸形例外失败。CLI 当前输出 `124 protected activities PASS; 0 unprotected addition(s) ignored`。
- **验证证据**：V8.2.1 已独立证明原 242 个受跟踪 JavaScript/MJS/CJS；本版本新增的质量工具与专项合同 2 个 CJS 分别通过 `node --check`。同一 Git 暂存快照首段通过排序前 47 项，随后 sqlite 合同因隔离目录未复制 `.venv` 诚实失败；确认项目解释器可 `import sqlite3` 后，通过测试原生 `PYTHON` 入口显式复用该解释器，sqlite 及排序后续 16/16 全部通过，合计 63/63。该修正不复制、不修改虚拟环境；`git diff --cached --check` 为 0 error。
- **诚实边界**：本版本没有修改 124 项既有活动，没有新增用户可见实验，也没有把生成 JSON 加入生产运行时或 Service Worker。旧 V8.1.0—V8.1.5 候选继续原位保留但不在本提交中；下一任务为 `EXT-04` 新实验视觉基础件。

---

## 1.0.192 V8.2.3 EXT feat(visual-kit): 建立新实验视觉基础件（EXT-04）

- **提交日期**：2026-08-24
- **任务编号**：`EXT-04`
- **提交标题**：`V8.2.3 EXT feat(visual-kit): 建立新实验视觉基础件（EXT-04）`
- **任务目标**：让今后新增实验可以直接获得清晰、可操作、响应式的视觉与状态骨架，同时避免把课程正文、说明性长文或统一外壳重新塞进实验内部。
- **视觉基础件**：复制模板新增运行状态、舞台读数、双图例、带单位主参数、播放/暂停/重置、短状态反馈和原生 `<details>` 控制抽屉；新增占位符只描述参数、单位、图例和学科主题色，不包含课程目标或章节说明。
- **状态与适配**：同一 owner 输出 `running`、`paused`、`reduced-motion`；减少动态效果停止连续 rAF，但直接参数输入继续渲染静态结果。桌面 1440×900 使用舞台/控制双栏，834×1112 保持收窄双栏，390×844 改为单列抽屉；触达区至少 44px且键盘焦点可见。
- **作用域与内容边界**：所有视觉选择器继续由 `#page-<subject> + [data-module=<id>] + .astra-exp--<namespace>` 三重限定。`visual-primitives.contract.json` 明确 `new-experiment-only` 和 `production_registration: false`；首屏只保留模型标签、可观察数值和一句操作反馈，长篇原理继续进入模型说明或实验外内容。
- **真实问题与修复**：首次桌面 Edge 验收发现舞台/抽屉高度会从约 522px 持续增长到 1600px 以上。根因是 ResizeObserver 把含边框的父级 rect 高度写回 Canvas，Canvas 又把自动高度父级撑大。模板现使用 `height: clamp(380px, 58vh, 650px)`（移动为 `clamp(340px, 54vh, 520px)`），运行时读取父级 `clientWidth/clientHeight` 并在零尺寸返回；复测舞台 522px、Canvas 520px，2.1 秒后保持稳定。
- **自动验证**：V8.2.2 已独立证明 242 个脚本和 63/63 合同。本版本独立暂存快照盘点到 64 项受跟踪合同，3 个受影响/新增 CJS 通过 `node --check` 和实际执行，124 项旧活动保护 CLI 与 FE024 缓存合同通过；未重复执行的 61 项沿用上一基线，因此不把本轮描述为新的 64/64 全跑。
- **浏览器证据**：真实外部 Edge/Chrome 完成 1440×900、834×1112、390×844 与 reduced-motion。参数 72m 会同步读数，暂停/继续和重置有效；移动抽屉可关闭并重新展开；reduced-motion 下时间读数静止但参数 64m 可更新；各视口根级无新增横向溢出，console warning/error 为 0。
- **诚实边界**：本版本只修改复制模板、模板合同和测试，没有写入 `CONFIG.experiments`、`AstraExperimentRegistry`、`index.html`、Service Worker 或任一既有实验。旧 V8.1.0—V8.1.5 候选继续原位保留但不在本提交中；下一任务为 `EXT-05` 新实验模型说明模板。

---

## 1.0.193 V8.2.4 EXT feat(model-doc): 建立新实验模型说明与科学复核门禁（EXT-05）

- **提交日期**：2026-08-24
- **任务编号**：`EXT-05`
- **提交标题**：`V8.2.4 EXT feat(model-doc): 建立新实验模型说明与科学复核门禁（EXT-05）`
- **任务目标**：让每个新增实验的公式、单位、适用条件、限制、误差和验证证据都能被开发者、毕业论文与答辩复核，同时保证这些长说明不会再次挤入实验首屏。
- **模型记录结构**：`model-notes.md.tpl` 从项目符号清单扩展为 11 节、7 张结构表，覆盖现象/结论边界、变量与 SI 单位、核心公式或算法不变量、条件/假设/限制、数值方法、画面映射、典型值/边界值、误差预算、实现对应关系、参考依据和发布前复核。
- **身份与样例**：manifest 与说明统一从实验 id 生成 `model-<experiment-id>` 显式锚点，删除可手填错位的独立锚点占位符。`model-notes.example.md` 用匀速运动给出三变量、量纲核对、解析方法、两组可手算样例、误差、实现位置和两条结构化依据的完整示范。
- **机器合同**：`model-document.contract.json` 冻结新实验专用、文档专用、不注册生产页面，以及章节、表头、最少行数、变量角色、公式块、引用字段和五项复核要求。它不规定具体学科结论，具体公式与来源仍需内容复核者负责。
- **检查器**：`check-new-experiment-model-document.cjs` 从 manifest 解析仓库内 `doc/` Markdown 与精确锚点，验证身份、标题、复核状态/日期、未填标记、课程章节、各表、SI 单位、公式、结构化引用和勾选项；CLI 成功时报告章节、验证样例和引用数量，失败时给出稳定错误码。
- **注册联动**：`check-new-experiment-registration.cjs` 在既有 key/route/title/owner/resource/cleanup 检查之外调用同一模型验证器。合同证明缺文档会输出 `model_document_missing`，完整候选会显示 `model documents reviewed`，检查过程仍不写生产注册文件。
- **负例证据**：专项合同覆盖缺/错锚点、标题/身份错配、草稿状态、未填复核人、缺自变量或 SI 单位、缺公式、缺已知限制、缺边界样例、未使用或字段不足的引用、未勾选复核和课程化章节；完整示例和独立 CLI 正例通过。
- **完整验证**：纯 Git 暂存快照通过 247 个受跟踪 JavaScript/MJS/CJS 语法检查、65/65 前端合同和 `124 protected activities PASS; 0 unprotected addition(s) ignored`。首个临时快照因新仓库没有 `HEAD`，在 QA-016 解析修订时停止；生成只存在于临时目录的快照提交后从头完整重跑通过，真实 `主开发` 历史没有被改写。
- **诚实边界**：本版本没有新增用户可见实验，没有向任何实验首屏注入模型记录，没有修改 `CONFIG.experiments`、`AstraExperimentRegistry`、`index.html`、Service Worker 或 124 项既有活动。旧 V8.1.0—V8.1.5 候选继续原位保留但不在本提交中；下一任务为 `EXT-06` 实验缩略预览素材流程。

---

## 1.0.194 V8.2.5 EXT feat(preview-assets): 建立新增实验预览素材流程（EXT-06）

- **提交日期**：2026-08-24
- **任务编号**：`EXT-06`
- **提交标题**：`V8.2.5 EXT feat(preview-assets): 建立新增实验预览素材流程（EXT-06）`
- **任务目标**：让未来新增实验的目录卡片能够用一张轻量画面直接说明“实验会发生什么”，同时让比例、体积、替代文本、来源和减少动态效果降级可复核。
- **静态 poster**：每个候选在自己的目录提供 `preview.webp`，固定 1600×900、非动画、1—120 KiB；manifest 使用版本化 URL，记录使用无查询路径并保存实际宽高、字节和小写 SHA-256。
- **来源与内容记录**：`preview-record.json.tpl` 绑定 subject/id、alt、路由、舞台 selector、1600×900 捕获视口、责任人、日期、权利状态和新实验源文件；四项内容声明要求展示交互结果、非纯装饰、未使用旧活动素材、未把标题烘焙进图片。
- **替代文本与降级**：alt 长度为 12—120 字符，需描述主体、变化和结果，拒绝只写标题或“图片/封面/截图/preview/image/webp”。manifest 与记录均使用 `reduced_motion: poster`，因此可选短循环不能成为理解实验的唯一方式。
- **可选短循环**：contract 允许另加 960×540 animated WebP，约束 1.5—5 秒、最多 60 帧和 768 KiB。当前版本只证明静态 poster 正例与“静态文件不能冒充 motion”负例，没有提交正式动图，不能写成产品已有动态卡片。
- **纯 Node 检查**：`check-new-experiment-preview.cjs` 解析 RIFF/WEBP 的 VP8、VP8L、VP8X、ANIM 和 ANMF 元数据；`--inspect` 输出宽高、字节、哈希、动画、帧数、时长，`--manifest` 验证 manifest、记录和文件三方一致。
- **注册联动**：新增注册检查在身份、资源、模型、owner 和 cleanup 之外调用预览检查；完整候选输出 `preview assets and model documents reviewed`，缺记录则产生 `preview_record_missing`，过程不修改生产注册表面。
- **专项证据**：合同覆盖 WebP 正/反解析、身份错配、通用 alt、错误 reduced-motion、未填来源、越界源文件、旧活动复用、元数据/哈希错配、超预算、静态 motion 冒充、inspect 与 CLI。测试只把 `UI/future-galaxy/orbit-observatory.webp` 临时复制到系统临时目录作为确定性二进制夹具，没有将其复制或登记为新实验素材。
- **完整验证**：纯 Git 暂存快照通过 249 个受跟踪 JavaScript/MJS/CJS 语法检查、66/66 前端合同和 `124 protected activities PASS; 0 unprotected addition(s) ignored`；工作区 V8.1 待裁决候选未进入快照。
- **诚实边界**：本版本没有创建用户可见目录卡片、正式 poster 或短循环，没有抓取、裁剪、重排或修改 124 项既有活动，也没有改变生产路由、缓存和注册。下一任务为 `EXT-07` 新增实验验收脚本。

---

## 1.0.195 V8.2.6 EXT feat(browser-journey): 建立新增实验隔离浏览器验收（EXT-07）

- **提交日期**：2026-08-24
- **任务编号**：`EXT-07`
- **提交标题**：`V8.2.6 EXT feat(browser-journey): 建立新增实验隔离浏览器验收（EXT-07）`
- **任务目标**：让每个未来新增实验在接入生产目录前，都用同一真实浏览器流程证明能绘制、操作、重置、释放、重入并适配手机与减少动态效果。
- **候选资源入口**：manifest 新增版本化 `module.html` 路径；注册检查把 module 与脚本、样式一并约束在 `pages/<subject>/<id>/`，缺文件、越界路径、扩展名或资源代际都会失败。模板合同和注册正反夹具同步更新。
- **机器合同**：`browser-journey.contract.json` 固定 `new-experiment-only`、`production_registration: false`、1440×900 桌面、390×844 手机、1440×900 reduced-motion，以及九项通用 check、两项手机 check、参数 72/重入 64/重置 50、44px 和资源归零要求。
- **隔离运行器**：`check-new-experiment-journey.cjs` 读取候选 manifest/module/CSS/JS，启动仅监听 `127.0.0.1` 的临时页面并直接挂载 module；候选必须保持 `candidate-unregistered`，运行器不读取或修改 `CONFIG.experiments`、`AstraExperimentRegistry`、`index.html` 和 Service Worker。
- **旅程语义**：首次进入检查非零 Canvas、2D context、像素指纹与读数；参数 72 后要求 snapshot/控件/读数/画面同步；普通环境验证暂停时 elapsed 不增长、继续后恢复，reduced-motion 验证 rAF 为 0 但参数仍更新；重置回到 50 且 elapsed 接近 0。
- **释放与重入**：隔离页包装 rAF 与 ResizeObserver 计数；离开时保留旧 AbortSignal，调用真实 owner.destroy，移除 module，并要求 owner 的 root/canvas/context/controls/observer 等引用全部为 null、frame/活跃 rAF/活跃 observer 为 0。重新挂载后参数 64 与读数仍有效，随后再次释放。
- **移动与错误门禁**：手机真实关闭并重新展开 `<details>`，滑杆、暂停、重置和 summary 高度均至少 44px；三组根级无横向溢出，严重 console、pageerror、资源 4xx 与 requestfailed 必须为空。临时服务器只开放候选目录，既支持同目录素材/模块，又拒绝借验收页读取仓库其他路径；报告验证器拒绝缺 viewport、缺 check、false check、身份不符和截图丢失。
- **真实浏览器证据**：Playwright 1.60 调用系统 Edge 151.0.4129.101 headless，三组报告均为 PASS；桌面截图同时显示舞台和控制，手机使用整页截图显示舞台、抽屉、两个动作和状态，静态截图显示“静态模式”。JSON 和截图位于 Git 忽略的 `test-screenshots/new-experiment/v826-ext07-selftest/`。
- **专项与完整验证**：专项合同会渲染临时探针、真实启动三组浏览器、逐项核对报告、反转 desktop reset 证明失败关闭，再用独立 CLI 复核原报告。纯 Git 暂存快照通过 251 个受跟踪 JavaScript/MJS/CJS 语法检查、67/67 前端合同和 `124 protected activities PASS; 0 unprotected addition(s) ignored`；V8.1 待裁决候选未进入快照。
- **诚实边界**：本版本没有新增生产实验、目录卡片、路由、缓存或正式预览，没有运行、截图或改造 124 项既有活动；旧活动只通过保护性 smoke。下一任务为 `EXT-08` 新实验开发态调试面板。

---

## 1.0.196 V8.2.7 EXT feat(debug-panel): 建立新增实验开发态调试面板（EXT-08）

- **提交日期**：2026-08-24
- **任务编号**：`EXT-08`
- **提交标题**：`V8.2.7 EXT feat(debug-panel): 建立新增实验开发态调试面板（EXT-08）`
- **任务目标**：让开发者在不污染正式实验和竞赛画面的前提下，更快核对新候选的模型变量、模拟步长、帧率和运行资源。
- **默认关闭边界**：`check-new-experiment-journey.cjs` 只有收到 `--debug` 才在隔离 HTML 加入专用脚本，并只在该模式由 loopback 服务器开放 `/__astra_new_experiment_debug_panel.js?v=V8.2.7`。普通模式三视口均证明脚本请求 0、全局导出 false、面板节点 0、面板定时器 0；生产 `index.html`、Service Worker、配置和实验注册表不含脚本路径、面板 id 或导出对象。
- **只读数据提供者**：新增模板 owner 提供冻结的 `debugSnapshot()`，返回运行状态、模型快照、最近/最大步长、rAF/ResizeObserver/控制作用域和 Canvas bitmap 尺寸。该方法不轮询、不建 DOM、不发请求、不暴露可修改 owner 引用；普通模拟只记录最近一步，调试 UI 仍是独立开发脚本。
- **开发面板**：`new-experiment-debug-panel.js` 使用独立 Shadow DOM，每 250ms 最多 4Hz 更新“运行状态、模型变量、模拟步长、帧率、资源数量、画布尺寸”六个分区。桌面为固定右侧检查器，760px 以下为视口底部有界面板并内部滚动；关闭按钮至少 44px，减少动态效果禁用装饰动画。
- **浏览器计量**：隔离层包装候选使用的 rAF 与 ResizeObserver，记录活跃/创建/执行数量、最近帧间隔和 fps；参数 72 后普通桌面/手机要求正步长、正 fps 与活跃 rAF，reduced-motion 要求状态正确且步长/fps/rAF 均为 0。截图前强制刷新一次面板，避免 250ms 周期留下与参数 50 重置状态不一致的旧读数。
- **释放与重入**：候选离开前先销毁调试面板，要求 DOM 为 0、timer inactive；候选重新挂载后调试面板重新挂载并回读参数 64，最终离开再次检查面板、rAF 与 ResizeObserver 全部释放。手机同时验证面板不越过视口、内部可滚动且关闭动作达到 44px。
- **开发命令**：普通自动验收保持原命令；人工排查使用 `node tools/browser/check-new-experiment-journey.cjs --manifest <pages/subject/experiment/manifest.json> --debug --headed --debug-hold <milliseconds>`。带 `-debug.png` 的截图和 `test-screenshots/new-experiment/v827-ext08-selftest/` 只作 Git 忽略的本机开发证据，不是正式封面或竞赛截图。
- **专项与完整验证**：`new-experiment-debug-panel-contract.cjs` 先跑三视口关闭模式，再跑三视口开启模式，核对脚本、六个分区、模型 72、重置 50、重入 64、普通/reduced-motion 计量、手机布局、两轮释放、截图和生产零引用；Playwright 1.60 + 系统 Edge 151 通过。纯 Git 暂存快照通过 253 个受跟踪 JavaScript/MJS/CJS 语法检查、68/68 前端合同和 `124 protected activities PASS; 0 unprotected addition(s) ignored`；V8.1 待裁决候选未进入快照。
- **诚实边界**：本版本没有新增用户可见实验、目录卡片、路由、缓存、正式 poster 或竞赛浮层，没有运行、截图或改造 124 项既有活动。V8.2 `EXT-01—EXT-08` 至此完成；V8.3 的首批数量与题目必须由项目负责人裁决后再启动。

---

## 1.0.197 V8.3.0 docs(planning): 确定首批三项新增展示实验（SHOW-01）

- **提交日期**：2026-08-24
- **任务编号**：`SHOW-01`
- **提交标题**：`V8.3.0 docs(planning): 确定首批三项新增展示实验（SHOW-01）`
- **负责人授权**：项目负责人在收到“先做三项、PN 结保留第四候选”的推荐后回复“刚才不小心终止，请继续”。本版本据此解除 V8.3 数量与题目门禁，不把模糊恢复指令扩张到第四项。
- **定题结果**：首批固定为 `physics.double-pendulum-chaos`、`chemistry.chromatography-separation`、`engineering.robot-arm-ik`，顺序为双摆混沌、色谱分离、机械臂逆运动学；PN 结只保留为未启动 T1。
- **画面与模型边界**：三项分别采用近初值双摆轨迹分离、色谱柱/色谱峰联动、三连杆目标拖拽与残差回读；每项已锁定主要变量、可见读数、教学模型适用范围和不得宣称的结果。长模型说明继续只写开发指南，不进入实验首屏。
- **去重证据**：只读核对 88 项工科配置/注册、18 项 Code Space、18 项 Future manifest 与 124 项保护基线，四个替代候选 ID/route 均未发现直接身份冲突；原候选中五项直接或高度重复，继续排除。
- **后续顺序**：`SHOW-02 / V8.3.1` 实现双摆混沌，`SHOW-03 / V8.3.2` 实现色谱分离，`SHOW-04 / V8.3.3` 实现机械臂逆运动学；每项独立提交并经过 manifest、模型、预览、浏览器旅程和旧活动保护门禁。
- **诚实边界**：本版本只更新规划与发布记录，没有创建生产实验、目录卡片、路由、缓存、图片或动画，没有修改任何既有实验，也没有纳入旧 V8.1.0—V8.1.5 工作区候选。

---

## 1.0.198 V8.3.1 SHOW feat(physics): 交付双摆混沌展示实验（SHOW-02）

- **提交日期**：2026-08-24
- **任务编号**：`SHOW-02`
- **提交标题**：`V8.3.1 SHOW feat(physics): 交付双摆混沌展示实验（SHOW-02）`
- **用户可见成果**：新增 `physics.double-pendulum-chaos` 与 `#physics/double-pendulum-chaos`。舞台同屏绘制两组只差极小第二杆初角的平面双摆，以青色/洋红尾迹、相空间、末端分离、模型时间和能量相对漂移表达初值敏感性；用户可调初始角差、阻尼、时间倍率，切换低能/翻转/混沌姿态，暂停、重置或直接拖动末端摆球重新释放。
- **模型与数值**：采用等长等质量平面双摆、240Hz 固定步长 RK4 和可选线性教学阻尼。15 号模型记录完成 11 节、变量与 SI 单位、运动方程、适用条件/假设/限制、数值方法、画面映射、误差预算、实现对应、两组验证样例和两条结构化依据；默认无阻尼样例在 10 秒的末端分离为 1.655606826m，相对能量漂移为 `1.843836e-6`。实验页不复制长模型说明，不宣称预测真实装置的长期轨迹。
- **视觉与素材**：新增实验私有三重作用域 CSS、响应式舞台/控制抽屉、44px 触达区、键盘焦点、暂停态与 reduced-motion 静态态。原创 `preview.webp` 为 1600×900、52,396 字节、SHA-256 `260044d6ac0e863a8450b1f56f2277d511880d6b402bb35e07a502c828892db9`；`preview.json` 记录真实浏览器捕获路由、选择器、源文件、替代文本、权利和静止降级，不复用旧活动素材。
- **生产接线**：`CONFIG.experiments`、`AstraExperimentRegistry`、Physics DOM、样式入口、路由加载与 `20260824v831Show02P0` 资源代际完成接线；可读结构保留在独立 `module.html`，`index.html` 只保存一行生成镜像，使冻结壳层维持 3,752 行并通过 3,758 行架构上限。候选 `manifest.json` 在生产注册和真实浏览器 smoke 后退役，注册发现器恢复为“no candidate manifest found”。
- **反课程化边界**：页面首屏只保留模型标签、实时读数、直接操作和一句短反馈，不增加课程目标、章节导语、预测/修正表单、证据工作台或实现说明。新目录条目以 `guide: false` 单项关闭会遮挡首操作且含旧课程措辞的通用物理指南；其他 124 项旧活动的引导、页面、动画、参数和 owner 行为保持原样。
- **生命周期与生产浏览器自验**：主开发在隔离 demo 数据和同源生产路由完成桌面参数 72、暂停/继续、重置、实验 A→B、实验导航复开、刷新直达与 390×844 旅程。离开快照为 `animation_frames=0 / resize_observers=0 / control_scopes=0 / canvas=0×0`，重入各资源恢复为 1；移动端根级 `scrollWidth=clientWidth=384`，三姿态按钮高 44px、两动作按钮高 46px，生产 reload 的 console warning/error 与 runtime exception 均为 0。
- **自动验证**：模型、poster、模板、视觉基础件、调试面板、页面/实验注册、缓存代际、架构边界和候选退役合同均通过；最终暂存快照使用认可的工作区 Playwright 依赖执行 254 个受跟踪 JavaScript/MJS/CJS 语法检查与 68/68 个提交内前端合同。工作区额外存在的 V8.1 内容平台候选合同未计入本版本。旧活动保护输出 `124 protected activities PASS; 1 unprotected addition(s) ignored`，证明保护基线未被重写，当前总投影只增加这一项。
- **诚实边界**：本版本新增一项独立实验，没有修改既有 124 项活动的源码、DOM、样式、参数、求解器或路由；没有新增课程壳、后端接口、数据库表、真实装置标定、3D、随机初值或长期轨迹预测。V8.3 尚未达到退出条件，下一任务为 `SHOW-03 / V8.3.2` 色谱分离。

---

## 1.0.199 V8.3.2 SHOW feat(chemistry): 交付色谱分离展示实验（SHOW-03）

- **提交日期**：2026-08-24
- **任务编号**：`SHOW-03`
- **提交标题**：`V8.3.2 SHOW feat(chemistry): 交付色谱分离展示实验（SHOW-03）`
- **用户可见成果**：新增 `chemistry.chromatography-separation` 与 `#chemistry/chromatography-separation`。舞台左侧显示溶剂前沿与 A/B/C 三色带沿柱推进，右侧同步显示完整预测峰、扫描线之前的已检测合成曲线、峰顶保留时间和下一峰提示；用户可调保留因子差、流量，切换峰重叠/均衡/高分离，暂停、重新进样或拖动检测扫描线定位时刻。
- **模型与边界**：采用无反应恒流教学模型，固定 `V_M=10 mL`、`N=900`，使用 `t_M=V_M/F`、`t_R=t_M(1+k)`、`v_i/u=1/(1+k_i)`、对称高斯峰和 IUPAC 基线峰宽分离度。默认 `p=50 / F=1.4 mL/min` 得到 `t_R=(11.428571,15.892857,20.357143) min`、`min R_s=1.847291`；`p=0` 得到 `min R_s=0.949367`。15 号文档完成 11 节、变量与 SI 单位、三类边界、两组样例、误差预算、实现对应和 7 条 IUPAC/NIST 依据；页面不映射具体物质、仪器、线下步骤或真实定量信号。
- **视觉与素材**：新增三重作用域私有 CSS、桌面双栏、390×844 单列控制抽屉、44px 触达区、键盘焦点、运行/暂停/reduced-motion 三态和窄屏横向柱布局。原创 `preview.webp` 为 1600×900、35,790 字节、SHA-256 `2eca2a7a8781b8857da54ded000ba73fa566e3dcc4cab6aed845875cddc9aa03`；`preview.json` 记录真实浏览器捕获视口、舞台选择器、源文件、替代文本、权利和静止降级，未复用旧活动素材。
- **生产接线**：`CONFIG.experiments`、`AstraExperimentRegistry`、Chemistry DOM、样式入口、路由懒加载和 `20260824v832Show03P0` 资源代际完成接线；新条目以 `guide: false` 避开会遮挡首次操作的通用化学课程指南。权威可读结构保留在 `module.html`，生产 `index.html` 仅增加一行镜像并保持 3,756 行，低于 3,758 行架构上限；候选 `manifest.json` 在注册、预览、模型和浏览器门禁通过后退役。
- **候选与生产浏览器自验**：普通模式和显式调试模式均在系统 Edge 151 完成 1440×900、390×844、reduced-motion 三视口旅程。主开发另以授权教师会话验证正式 hash 直达、参数 72 对应 `Δk=0.79`、流量 2.0 对应 `t_M=5.0 min`、重叠预设、暂停/恢复、扫描线拖动、重置、色谱→双摆→色谱、刷新与手机抽屉；离页为 `animation_frames=0 / resize_observers=0 / control_scopes=0 / canvas=0×0`，重入各资源恢复为 1。390×844 下根宽 358px、舞台宽 334px、文档宽 `390=390`，主要控件高 44—46.6px；应用 console、pageerror、请求失败和 HTTP error 均为 0。
- **身份与测试边界**：学生无班级数据时，当前工作区内未提交的课程目录候选会按其既有守卫返回星球页；本版本不修改或提交该候选，因此生产自验使用教师身份，隔离候选旅程仍无需登录。模型、预览、注册、实验注册表、缓存代际、页面层级和旧活动保护定向合同均通过；最终提交快照的完整脚本/合同数量以提交前暂存复验为准。
- **诚实边界**：本版本只新增一项独立化学展示实验，没有修改既有 124 项活动的源码、DOM、样式、参数、求解器或路由，也没有新增课程壳、后端接口、数据库表、真实仪器标定、具体药品、实验处方或定量分析能力。保护输出为 `124 protected activities PASS; 2 unprotected addition(s) ignored`；V8.3 尚未达到退出条件，下一任务为 `SHOW-04 / V8.3.3` 机械臂逆运动学。

---

## 1.0.200 V8.3.3 DOC docs(planning): 重构课程闭环路线与目标 UML（DOC-003）

- **提交日期**：2026-08-25
- **任务编号**：`DOC-003`
- **提交标题**：`V8.3.3 DOC docs(planning): 重构课程闭环路线与目标 UML（DOC-003）`
- **负责人验收**：项目负责人明确回复“验收通过”，同意从继续扩充实验转向通用课程与三角色闭环，并要求后续严格按规划执行、遇到未明确决策时停止汇报。
- **规划主线**：执行顺序固定为 `V8.3.4` 收尾机械臂并冻结其他实验，随后以 `V8.4.0—V8.4.6` 依次完成课程身份与教师申请、教师创建课程、管理员审核、学生加入课程、课程编辑与不可变发布、三角色工作台和完整演示数据验收。
- **目标架构**：2 号文档新增 P-01—P-13 共 13 幅 Mermaid 目标图，覆盖系统分层、身份组织、课程关系、内容发布、教师申请、课程审核、学生加入、课程创建、多教师发布、学习完成、三角色数据流、Web/小程序边界和版本依赖；每幅图均写明目标版本、当前缺口、复用对象、新增对象和迁移门禁。
- **业务裁决**：星系与学科只承担分类和默认筛选，授课课程是教师和学生实际协作的学习容器，行政班只承担组织身份与准入；课程信息审核与教学内容发布分离，不再建立精品/普通课程等级，也不在既有实验内部增加课程正文或证据面板。
- **兼容策略**：现有课程、共同教师、课程单元、发布版本、作业、提交和学习证据继续复用；规划新增教师申请、课程信息修改、准入班级、课程选课和加入申请，并以隐藏课程群组兼容既有班级作用域。目标态不冒充当前稳定实现。
- **文档边界**：V7.5—V7.9 子规划补入现行覆盖规则，明确历史课程壳、代表课程和班级固定版本设计不能反向覆盖新路线；1 号文档没有提前迁入目标 UML，3 号文档只记录本次已验收规划事实。
- **诚实边界**：本版本只重构规划和历史控制面，没有新增或修改业务模型、接口、页面、实验、缓存或数据库迁移；`engineering.robot-arm-ik` 仍由下一独立版本 `V8.3.4 / SHOW-04` 完成生产接线。

---

## 1.0.201 V8.3.4 SHOW feat(engineering): 交付机械臂逆运动学实验（SHOW-04）

- **提交日期**：2026-08-25
- **任务编号**：`SHOW-04`
- **提交标题**：`V8.3.4 SHOW feat(engineering): 交付机械臂逆运动学实验（SHOW-04）`
- **实现提交**：`0699940`
- **用户可见成果**：新增 `engineering.robot-arm-ik` 与 `#engineering/robot-arm-ik`。舞台显示三连杆机械臂、目标点、可达工作空间、关节限制、末端坐标和残差；用户可拖动目标，切换自由、边界与关节限制预设，并执行暂停、继续和重置。超出臂长时明确显示“超出臂长”，不伪造可达解。
- **模型与交互边界**：求解器以二维教学机械臂的解析/迭代约束为边界，保留连杆长度、目标位置、三关节角、可达状态和残差等直接读数。页面没有加入课程目标、章节说明、预测表单或学习证据面板，也不宣称代表真实工业机械臂标定、动力学、碰撞规划或控制器性能。
- **生产接线**：Future manifest、统一活动目录、正式 hash、工程页 owner、独立 module/CSS/runtime、首页计数、页面注册、HTTP fallback 和 Service Worker 代际完成接线。工程页负责 `engineering` 与 `engineering/robot-arm-ik` 两条路由的互斥显示、延迟加载和销毁，既有 `bridge-truss.js` 未修改。
- **视觉与素材**：新增 1600×900 原创 `preview.webp`，文件为 27,740 字节、SHA-256 `6baca743c89c1465347445482bbd75b857d3eeae234d5eaceccc31de574a90fa`；同目录 `preview.json` 保存来源、路由、替代文本、权利和静止降级说明。桌面舞台和控制同屏，390×844 使用可展开控制区，主要动作达到 44—46px 且根级横向溢出为 0。
- **主开发浏览器自验**：在读取真实生产文件的只读浏览器夹具中完成桌面与手机检查。桌面验证拖动目标、不可达预设、滑杆、暂停/继续、重置、机械臂→桥梁→机械臂同页切换和刷新直达；手机验证首屏舞台、控制抽屉、关节限制预设、触达区和横向边界。两视口 console warning/error 均为空，离页销毁后旧 owner 不再保留活动资源。
- **自动验证**：纯 Git 暂存快照通过 258 个受跟踪 JavaScript 文件语法检查和 69/69 项前端合同；新增专项合同覆盖注册、求解器、路由切换和 cleanup。旧活动保护输出为 `124 protected activities PASS; 3 unprotected addition(s) ignored`，三个 addition 正好对应 V8.3 已批准的双摆、色谱和机械臂。
- **诚实边界**：生产站点现有会话守卫仍要求有效身份，浏览器自验不等于匿名公开直达；本版本没有修改 124 项既有实验、没有继续扩充第四项实验、没有接入课程候选后端、公共指南候选或三角色页面候选。V8.3 至此达到退出条件，后续严格进入 `V8.4.0 / ARCH-010`。

---

## 1.0.202 V8.4.0 ARCH feat(course): 统一课程层级与目录语义（ARCH-010）

- **提交日期**：2026-08-25
- **任务编号**：`ARCH-010`
- **提交标题**：`V8.4.0 ARCH feat(course): 统一课程层级与目录语义（ARCH-010）`
- **实现提交**：`87d9f60`
- **数据层级**：Alembic `20260825_0054` 为 `Course` 新增非空 `subject_key`，旧数据由 `course_key` 回填；为 `ClassGroup` 新增 `kind`，旧班级回填为 `homeroom`，并只允许 `homeroom / course_cohort`。模型、索引、DTO 和迁移往返保持一致。
- **组织边界**：班级创建、班级列表、管理员组织列表和组织统计只把 `homeroom` 当作行政班，避免未来内部课程群组混入教师和管理员的行政班界面。V8.4.1 只建立课程关系模型；`course_cohort` 实例的创建留给 `V8.4.2 / BE-025` 首次审核事务。
- **课程与目录语义**：授课课程使用 `galaxy_key / subject_key / course_key`；课程单元继续引用稳定 `activity_key`。统一活动目录默认返回完整 127 项活动，历史 `representative` 只保留为演示选择，不再成为精品/普通课程等级。
- **兼容与验证**：既有课程可升级并回滚；旧目录调用、学习证据活动映射、ModuleSelector 和 QA019 同步使用统一分类。`test_course_taxonomy.py` 的接口/可见性与 SQLite 迁移往返两项测试进入主线。
- **诚实边界**：本提交只建立课程分类和组织类型，不建立课程选课、课程码、内部课程群组实例或课程信息审核，也不修改任何既有实验本体。

---

## 1.0.203 V8.4.0 BE feat(identity): 建立教师申请审核与待审只读门（BE-023）

- **提交日期**：2026-08-25
- **任务编号**：`BE-023`
- **提交标题**：`V8.4.0 BE feat(identity): 建立教师申请审核与待审只读门（BE-023）`
- **实现提交**：`eb47948`
- **模型与迁移**：Alembic `20260825_0055` 新增 `teacher_applications`，保存申请人、`pending / approved / rejected` 状态、申请说明、审核人、审核时间和审核说明；旧申请不会因重新申请或后续审核被覆盖。
- **接口**：新增申请、本人状态、管理员分页列表和管理员审核四个接口族。只有活动学生账号可以申请；正式教师不能重复申请；待审和已批准申请都有明确冲突结果。
- **审核事务**：管理员在同一锁和事务内审核申请与申请人；批准时把 `User.role` 从 `student` 提升为 `teacher`，拒绝时角色保持学生。重复审核返回 `409`，批准/拒绝及创建动作写入审计。
- **待审权限**：认证依赖在服务端识别待审申请，除读取、申请状态、退出和撤销本人会话外，其他写请求统一返回 `403`。前端即使被绕过，也不能加入班级、写课程或产生学习进度。
- **验证**：`test_teacher_applications.py` 覆盖批准、待审读写边界、拒绝后重申、正式教师拒绝申请和 SQLite 迁移往返四项；与 ARCH-010 合并的隔离提交快照共 6 项后端专项通过。
- **诚实边界**：本提交没有教师建课页面、课程审核、课程选课或小程序接口，也没有删除 `/api/auth/register` 的受控兼容角色参数；公开页面路径由 FE-037 收束。

---

## 1.0.204 V8.4.0 FE feat(identity): 完成教师申请与待审只读交互（FE-037）

- **提交日期**：2026-08-25
- **任务编号**：`FE-037`
- **提交标题**：`V8.4.0 FE feat(identity): 完成教师申请与待审只读交互（FE-037）`
- **实现提交**：`8a22773`
- **公开账号入口**：注册表单新增“学生账号 / 申请教师身份”用途选择。两种选择都先注册学生；申请教师时再调用教师申请接口，可填写说明，避免公开 UI 直接提交 `role=teacher`。
- **申请者体验**：会话层读取本人最新申请，显示待审横幅和状态弹窗；拒绝后允许再次申请，批准后主动刷新身份并进入教师角色。待审用户不能进入学生班课工作台，星球页使用 `teacher-applicant` 只读模式展示正式活动目录。
- **管理员体验**：管理首页新增待审数量和身份治理入口；申请卡片显示申请人、说明、状态和时间，批准/拒绝需要明确审核说明与二次确认，操作后刷新列表和总览。
- **客户端边界**：学习证据加载器在待审身份下清除并禁用写入权威；这只作为体验和本地状态保护，最终拒绝仍由 BE-023 服务端门禁负责。缓存代际、页面注册、角色资源裁剪和离线壳同步更新。
- **自动验证**：纯 Git 暂存快照排除内容平台、公共导览和其他角色候选后，259 个受控 JavaScript 文件通过语法检查，70/70 前端契约通过；专项契约覆盖公开注册、本人状态、待审守卫、管理员治理和资源代际。
- **主开发浏览器自验**：使用临时 SQLite 与本地预览验证注册申请、待审横幅/弹窗、`403` 写拒绝、管理员总数/列表、批准二次确认、批准后身份刷新，以及桌面和 `390×844` 的触达区、无横向溢出和零新增 console warning/error。该浏览器运行发生在保留其他未提交候选的工作区，最终提交隔离性由随后纯暂存快照的自动化证明；本记录不把公共导览候选算入 FE-037。
- **文档迁移**：原规划 P-02、P-05 已按真实类名、状态、接口和页面修正，迁入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v840-身份组织与教师申请当前稳定实现)。V8.4.0 至此完成，下一任务为 `V8.4.1 / DATA-009`。

---

## 1.0.205 V8.4.1 DATA feat(course): 建立课程关系与成员模型（DATA-009）

- **提交日期**：2026-08-25
- **任务编号**：`DATA-009`
- **提交标题**：`V8.4.1 DATA feat(course): 建立课程关系与成员模型（DATA-009）`
- **实现提交**：`5596112`
- **迁移链**：项目负责人选择 Q-001 方案 A 后，未提交内容平台迁移候选完整移入 `backend/alembic/candidates/`；稳定迁移 `20260825_0056` 单线继承 `20260825_0055`，当前 Alembic 只有一个 head。内容平台候选将在 V8.4.4 按届时稳定 head 重新连接，本任务没有提前提交或改写它。
- **课程扩展**：`Course` 新增可空课程码、学年、上课时间、总课时、准入方式和可空当前信息修订；课程码唯一、总课时必须为正，准入只允许 `open / class_restricted`。正式课程码和当前批准修订继续由后续审核事务生成。
- **关系模型**：新增 `CourseInformationRevision`、`CourseAdmissionClass`、`CourseJoinRequest` 和 `CourseEnrollment`，分别保存完整课程信息/教师快照、允许申请的行政班、可重复申请的审核历史和课程成员唯一真值，并建立外键、状态约束、查询索引和业务唯一约束。
- **旧数据兼容**：有活动 `CourseClass` 的旧课程回填为指定班级准入，没有活动挂班的旧课程回填为公开申请；其他新字段保持为空。SQLite 迁移通过升级、关系写入、唯一约束、回滚和再次升级，旧课程与旧挂班数量保持不变。
- **自动验证**：纯 Git 暂存快照排除内容平台、公共导览和角色页面候选后，59 项课程与身份回归全部通过；范围包含 DATA-009 专项、课程分类、教师申请、课程学习循环、发布计划、课程状态、单元访问、学校班级和学生作业中心。迁移显式标识长度符合 MySQL 上限。
- **文档边界**：已实现数据事实写入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v841-课程关系数据底座data-009-当前稳定实现)。P-03 仍是 V8.4.1—V8.4.3 的未完成整体目标，不提前迁移到 1 号文档。
- **诚实边界**：本任务没有新增教师建课、管理员审核、学生加入课程或名单接口，没有创建 `course_cohort` 实例、课程码、初始发布版本或用户页面，也没有修改 127 项现有活动。下一任务严格为 `V8.4.1 / BE-024`。

---

## 1.0.206 V8.4.1 BE feat(course): 完成教师创建课程与共同教师（BE-024）

- **提交日期**：2026-08-25
- **任务编号**：`BE-024`
- **提交标题**：`V8.4.1 BE feat(course): 完成教师创建课程与共同教师（BE-024）`
- **实现提交**：`4fa147c`
- **接口**：新增 `GET /api/v1/courses/authoring-options`、`POST/GET /api/v1/courses`、`GET /api/v1/courses/{course_id}` 和 `POST /api/v1/courses/{course_id}/information-revisions/{revision_id}/submit`。旧 `/api/courses` 继续服务既有测试、初始化和学习闭环，没有被破坏性重写。
- **创建事务**：正式教师一次提交名称、说明、学年、上课时间、课时、星系、学科、准入方式、共同教师和允许行政班；服务在同一事务写入 `Course(status=draft)`、有效 `CourseCollaborator(editor)`、`CourseAdmissionClass` 和 `CourseInformationRevision(revision_number=1,status=draft)`，并记录审计事实。
- **共同教师**：创建者可管理多门课程，同一课程可选择多名同校正式教师。创建者与有效共同教师读取同一草稿，任一方都能把当前信息修订从 `draft` 提交为 `submitted`；重复提交冲突，不覆盖原提交时间。跨校教师、失效身份、重复 ID 和创建者自选失败关闭。
- **准入与分类**：公开课程不接受允许班级；指定班级课程必须至少选择一个同校有效行政班，跨校班级和内部 `course_cohort` 不能成为选项。星系与学科只负责分类，测试证明共同教师仍可引用其他分类的稳定 `activity_key`。
- **未批准边界**：创建和提交后课程仍为 `draft`，`course_code`、`current_information_revision_id`、`CourseClass` 内部范围和 `course_cohort` 实例均为空。教师调用旧课程状态接口也不能自行发布；管理员必须在 BE-025 的专用审核事务中批准。
- **自动验证**：纯 Git 暂存快照排除内容平台、公共导览和角色页面候选后，70 项课程、权限与身份回归全部通过；覆盖 BE-024、DATA-009、课程分类、教师申请、访问控制、课程学习循环、发布计划、课程状态、单元访问、学校班级和学生作业中心。
- **文档边界**：已实现接口写入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#be-024-当前稳定接口与事务)。P-03 仍等待学生准入，P-08 仍等待管理员批准，因此两图继续留在 2 号规划文档。
- **诚实边界**：本任务没有教师向导页面、管理员审核、课程码、内部课程群组、初始发布版本、学生加入或名单管理，也没有改动 127 项现有活动。下一任务严格为 `V8.4.1 / FE-038`。

---

## 1.0.207 V8.4.1 FE feat(teacher): 完成教师创建课程向导（FE-038）

- **提交日期**：2026-08-25
- **任务编号**：`FE-038`
- **提交标题**：`feat(teacher): complete V8.4.1 course creation wizard`
- **实现提交**：`b27b886`
- **页面入口**：教师工作台“组织与课程”把旧的单页课程表单替换为按角色加载的 `teacher-course-authoring` 模块，使用独立 JavaScript、CSS 和缓存代际，教师主文件继续受冻结行数门禁保护。
- **四步向导**：依次填写基本信息、选择共同教师、选择星系/学科与公开或限班准入、预览并提交。页面不允许教师填写内部 `course_key`、课程码或 `published` 状态；分类只用于归类和默认筛选。
- **权威写入与恢复**：先调用 `POST /api/v1/courses` 创建草稿，再提交当前信息修订。若创建已成功但提交失败，页面保留服务端草稿并提供再次提交，不通过自动重试制造重复课程。
- **共同教师回读**：创建者和共同教师通过 `/api/v1/courses` 读取同一条课程、教师名单和修订状态；批准前显示“待管理员审核”和“审核后生成课程码”。共同教师不需要先属于某个行政班。
- **浏览器验收**：临时 SQLite 实测完成创建者四步填写、创建 `201`、提交 `200`、创建者待审回读、共同教师待审回读以及 `390×844` 移动端课程卡/向导；控制台无新增 warning/error。
- **自动验证**：纯 Git 暂存快照排除内容平台、公开导览和其他候选后，261 个受跟踪 JavaScript 文件语法检查、71/71 前端契约、4 项课程创建/关系后端专项全部通过；Alembic 唯一 head 为 `20260825_0056`。
- **架构校准**：教师课程创建逻辑进入独立 owner；旧课程编排契约改为分别校验教师主文件中的活动键和新 owner 中的星系分类，课程键继续由服务端生成。BE-024 新接口去除仅用于类型标注的持久层直接导入，架构边界契约恢复通过。
- **文档边界**：当前可见流程已写入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#fe-038-当前可见工作流)。P-03 仍等待学生准入，P-08 仍等待管理员批准、课程码、内部课程群组和初始版本，因此不提前迁移。
- **诚实边界**：本任务没有实现管理员课程审核、课程码、内部课程群组、初始发布版本、学生加入或名单管理，也没有修改 127 项现有活动。V8.4.1 至此完成，下一任务严格为 `V8.4.2 / BE-025`。

---

## 1.0.208 V8.4.2 BE feat(course): 完成课程信息审核后端（BE-025）

- **提交日期**：2026-08-25
- **任务编号**：`BE-025`
- **提交标题**：`feat: complete course information review backend`
- **实现提交**：`3fde9af`
- **负责人裁决**：项目负责人为 Q-002 选择方案 B。首次课程信息批准只生成正式课程信息、课程码和内部课程群组；不提前接入 V8.1 内容平台候选，不伪造占位章节、完成规则或空发布版本。首个不可变版本继续留给 V8.4.4 的教师显式发布。
- **接口**：在现有 `/api/v1` 课程创作路由中新增管理员课程信息修订分页队列和一次性批准/退回动作，并新增教师 `POST /courses/{course_id}/information-revisions`。管理员队列默认读取 `submitted`，可按状态、学校和分页过滤，并返回拟议信息、当前批准信息、差异字段、拟议/当前教师和准入班级。
- **首次批准事务**：锁定待审修订和课程，重新确认教师与行政班资格，应用名称、学年、时间、课时、星系、学科和准入方式，同步共同教师与准入班级，生成 8 位不可预测短课程码，创建隐藏 `ClassGroup(kind=course_cohort)` 与唯一 `CourseClass`，更新当前批准修订和课程状态，最后记录管理员审计。任一步失败都不提交半成品。
- **修订历史**：拒绝不会覆盖修订；教师在上一修订被拒或批准后创建下一条完整快照，再沿用显式提交动作进入审核。课程已有 `draft / submitted` 修订时拒绝并行创建。运行中修订待审或被拒时，旧批准信息、共同教师、准入班级、课程码和内部群组继续生效。
- **内容状态**：课程创作和审核读取增加 `has_published_content=false`、`content_status=not_published` 和“暂无已发布内容”。服务没有导入 `CourseRelease`、`CourseClassReleaseBinding` 或候选 release service；若工作区候选表存在，专项测试确认审批后对应行数仍为 0。
- **回滚与可见性**：模拟内部群组创建失败后，请求失败，课程保持 `draft`、课程码为空、当前批准修订为空、待审修订仍为 `submitted`，且没有残留内部群组或 `CourseClass`。内部课程群组不会进入现有行政班列表。
- **自动验证**：纯暂存隔离快照排除内容平台、公共导览和角色页面候选后，17 项课程审核、教师建课、课程关系、课程分类、教师申请和课程状态回归全部通过，架构边界契约通过；Alembic 唯一稳定 head 保持 `20260825_0056`，本任务没有新增迁移。
- **文档迁移**：原规划 P-06 已按真实状态、接口和失败边界迁入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#当前课程信息审核状态图原-p-06)。P-08 继续留在 2 号文档，待 `FE-039` 管理员可见审核页面完成后再迁移。
- **诚实边界**：本任务只完成课程信息审核后端，没有实现 FE-039 管理员页面、学生申请/名单、课程内容编辑器、真实发布版本、小程序、服务器部署或竞赛材料，也没有修改 127 项现有活动。下一任务严格为 `V8.4.2 / FE-039`。

---

## 1.0.209 V8.4.2 FE feat(admin): 完成管理员课程信息审核页面（FE-039）

- **提交日期**：2026-08-25
- **任务编号**：`FE-039`
- **提交标题**：`feat: complete admin course information review`
- **实现提交**：`67a597f`
- **可见主线**：管理员课程治理页新增“信息审核 / 状态治理”视图，默认进入信息审核。页面并行读取课程与待审修订，显示首次批准/运行中修改、当前值、拟议值、差异字段、教师名单、准入班级和空状态。
- **审核安全交互**：批准与退回均需要二次点击确认；审核说明变更会使上一次确认失效。写入期间复用管理页共享事务锁，防止视图切换和重复写入；结果不明时不自动重试。
- **回执与边界**：首次批准回执显示 8 位课程码、内部课程群组和“暂无已发布内容”，并明确首个真实版本仍由教师显式发布。退回回执区分首次草稿重提和旧批准信息继续运行。
- **响应式呈现**：桌面端使用页内差异面板；`<=820px` 使用全屏审核对话框，`<=520px` 进一步收紧排版。真实浏览器首轮发现桌面差异网格溢出，收紧最小列宽后复测容器 `clientWidth` 与 `scrollWidth` 一致。
- **自动验证**：纯 Git 暂存快照排除内容平台、公开导览和角色页候选后，完整 `npm test` 通过：262 个受跟踪 JavaScript 文件语法正常，72/72 前端契约通过。`test_course_information_reviews.py + test_course_authoring.py` 后端专项 6 项通过。
- **浏览器验收**：使用临时 SQLite 与正式 API 构建两条待审记录，完成桌面首次批准、运行中退回、队列 `2→1→0` 和 `390×844` 全屏对话框审核。两个视口都无横向滚动，无新增 console warning/error。
- **Q-002 复核**：首次批准后数据库中 `course_releases=0`、`course_release_units=0`、`course_class_release_bindings=0`，证明页面没有绕过方案 B 提前发布空内容。
- **文档迁移**：原规划 P-08 已按真实时序、页面和失败边界迁入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#当前课程创建与信息审核时序图原-p-08)；2 号文档删除重复详图，只保留完成结论和链接。
- **诚实边界**：本任务没有实现学生加入、课程名单、课程内容编辑器、真实发布版本、小程序、服务器部署或竞赛材料，也没有修改 127 项活动。V8.4.2 至此完成，下一任务严格为 `V8.4.3 / BE-026`。

---

## 1.0.210 V8.4.3 BE feat(course): 完成课程准入与成员后端（BE-026）

- **提交日期**：2026-08-25
- **任务编号**：`BE-026`
- **提交标题**：`feat: complete course enrollment backend`
- **实现提交**：`19e4e5c`
- **课程发现与申请**：普通学生可用不区分大小写的短课程码查找已批准课程，读取分类、教师、准入方式、资格、最新申请和成员状态。公开课程允许无行政班学生申请；指定班级课程在申请和批准两个时点都重新校验有效行政班成员关系。
- **审批与历史**：课程创建者、有效共同教师或管理员可分页读取并一次性批准或退回申请；非课程教师无权操作。重复待审申请返回原记录，被拒后可以用新的 `request_number` 再次申请，旧申请、作业、提交和学习事实不被覆盖。
- **成员与内部群组**：`CourseEnrollment` 继续作为课程成员唯一真值。批准或批量加入时，在同一事务内创建或激活隐藏 `course_cohort` 学生成员；学生退出或教师移除只把两处成员状态转为非活动，不删除历史。名单在没有来源行政班时明确显示“未关联班级”。
- **批量加入**：课程教师或管理员可按允许行政班批量加入当前有效学生，每名学生返回新增、已加入、曾退出或不符合资格等独立结果；系统不自动恢复曾退出/被移除成员。已有待审申请的学生被批量加入后，对应申请同步结束为已批准。
- **接口范围**：在稳定课程创作路由中新增课程码发现、申请创建/分页/审核、成员分页、行政班批量加入和成员退出/移除接口；没有新增迁移，也没有接入内容发布候选。
- **自动验证**：纯 Git 暂存快照排除内容平台、公开导览和学生页面候选后，架构边界契约通过；课程成员、信息审核、教师建课、关系模型和课程分类共 14 项后端测试通过，Ruff 通过。专项覆盖开放/限班、无班级、重复申请、共同教师、管理员兜底、越权拒绝、资格复核、再次申请、批量逐项结果、退出、移除、历史、一人多课和内部成员同步。
- **文档边界**：当前稳定后端事实已写入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v843-课程准入与成员后端be-026-当前稳定实现)。P-03 与 P-07 仍留在 2 号文档，必须等 FE-040 的学生、教师可见旅程及双视口验收完成后再迁移。
- **诚实边界**：本任务没有实现学生加入页面、教师审批与名单页面、课程内容编辑器、真实发布版本、小程序、服务器部署或竞赛材料，也没有修改 127 项活动。下一任务严格为 `V8.4.3 / FE-040`。

---

## 1.0.211 V8.4.3 FE feat(course): 完成学生选课与教师课程名单（FE-040）

- **提交日期**：2026-08-25
- **任务编号**：`FE-040`
- **提交标题**：`feat: complete course enrollment workspaces`
- **实现提交**：`81763a3`
- **学生可见流程**：学生工作台新增独立课程加入模块。学生输入 4—16 位课程码后读取课程摘要、教师、准入方式、当前资格和可用行政班，再提交公开或限班申请；页面区分可以申请、待审核、已退回可重申和已加入，并以二次确认执行退出。
- **教师可见流程**：教师工作台“组织与课程 → 创建授课课程”下新增课程成员中心，只显示已批准且已有课程码的本人课程。教师可筛选待审/历史申请和在读/历史名单，查看来源行政班，以二次确认批准、退回或移除。
- **行政班批量加入**：公开课程可选择任一本人行政班，限班课程只显示允许行政班。批量完成后分别显示新增、已在课、曾退出和不符合资格数量，不自动恢复曾退出或被移除学生。
- **写入交互**：学生申请、退出，教师审批、移除和批量加入都沿用正式 API 和共享写事务约束；不可确认的写结果不会自动重投。页面刷新从服务端重新读取权威状态。
- **真实浏览器验收**：本地 SQLite 演示数据走通“学生课程码申请 → 教师二次确认批准 → 名单显示高一（3）班 → 教师批量加入第二名学生 → 学生回读已加入”。批量回执为“新增 1、已在课 1、曾退出 0、不符合 0”，名单来源分别为“学生申请”和“行政班批量加入”。
- **响应式修正**：桌面与 `390×844` 均无横向溢出。移动端首轮发现申请/名单筛选框和移除按钮为 38px，在同一提交中统一为 44px 后复验全部可见输入、筛选和操作控件不低于 44px。
- **自动验证**：纯 Git 暂存快照通过 265 个受跟踪 JavaScript 文件语法检查和 73 项前端契约；新增 `course-enrollment-fe040-contract`，架构边界、角色移动回执、旧活动保护和既有课程契约继续通过。BE-026 已通过的 14 项后端课程专项继续作为本闭环后端证据。
- **文档迁移**：原规划 P-03 与 P-07 已按真实模型、页面状态和批量路径改写为当前稳定图，迁入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v843-学生选课与教师课程名单fe-040-当前稳定实现)；2 号文档只保留完成结论和链接。
- **诚实边界**：本任务没有实现共享课程草稿、结构化编辑器、不可变发布版本、完成规则、小程序、服务器部署或竞赛材料，也没有修改 127 项活动。V8.4.3 至此完成，下一任务严格为 `V8.4.4 / BE-027`。

---

## 1.0.212 V8.4.4 BE feat(course): 完成多教师共享草稿与不可变发布后端（BE-027）

- **提交日期**：2026-08-25
- **任务编号**：`BE-027`
- **提交标题**：`feat: add shared course draft releases`
- **实现提交**：`5a2d5f1`
- **共享草稿**：`Course.content_draft_revision` 成为整门课程的整数并发代际；每个有效 `CourseUnit` 只保留一条 active `ContentDraft`。课程创建者、有效共同教师和管理员读取同一份草稿，`PATCH /api/v1/courses/{course_id}/draft` 以 `expected_revision` 原子替换、增删和重排完整单元列表；旧修订返回 `409 / course_draft_revision_conflict`，不会静默覆盖另一位教师的修改。
- **不可变发布模型**：稳定迁移 `20260825_0057_course_content_releases` 接续 `20260825_0056`，为草稿补课程/单元作用域与 revision，并新增 `CourseRelease`、`CourseReleaseUnit`、`CourseClassReleaseBinding` 三类事实。三类发布对象由 SQLAlchemy 事件拒绝 update/delete；release number、package hash、单元位置、来源单元和 binding 链均有唯一约束。
- **显式发布事务**：`POST /api/v1/courses/{course_id}/releases` 锁定课程并复核 revision，为每个单元生成精确 `ContentPageVersion`，冻结顺序、活动 key、媒体摘要和完成规则快照，建立 release 与 release units，为隐藏 `course_cohort` 对应 `CourseClass` 追加 binding，记录审计并把共享草稿滚动到下一 revision。任何阶段失败都会整体回滚，相同 package 不会重复发布。
- **读取与学生边界**：新增 release 列表、当前版本和指定历史版本读取。创建者、有效共同教师和管理员可读完整内容；active `CourseEnrollment` 学生在下次读取时沿最新内部群组 binding 自动取得新版本，也可回读有权访问的旧版本。学生 DTO 会递归移除检查点的 `correctChoiceIds`、`numericAnswer`、`tolerance` 和 `acceptedAnswers`。
- **完成规则兼容**：BE-027 只冻结发布时规则快照，不执行 BE-028。课程尚无有效规则时写入确定性的 `not_configured / version 0` 快照和哈希，明确表示“未配置”，不伪造实验、检查点或作业完成结果。
- **课程状态回读**：教师与管理员课程列表的 `has_published_content`、`content_status` 和 `content_status_label` 改为查询真实 `CourseRelease`；首发前显示“暂无已发布内容”，首发后显示“已发布第 N 版”。
- **候选取舍**：删除冲突的旧 `20260823_0056_content_platform_storage` 迁移候选、旧内容创作 service、旧架构 JSON 与契约。旧候选中的行政班手动选版、管理员逐内容审批、检查点进度和冲突迁移编号没有进入稳定主线；七类结构化 schema 和与新版课程发布一致的底层对象被复用。
- **自动验证**：共享草稿、发布 API、不可变存储和七类内容 schema 的 25 项专项通过；课程创建、课程信息审核、课程成员与发布的组合回归 34 项通过；`backend/tests` 全量通过。Alembic 完成升级、回滚和再次升级，唯一 head 为 `20260825_0057`。Ruff 的导入、语法、升级和格式检查通过。
- **隔离前端门禁**：纯 Git 暂存快照通过 265 个受跟踪 JavaScript 文件语法检查、73 项前端契约和架构边界。主工作区架构边界单独受到用户原有 `shared/js/main.js` 行数改动影响；该文件未进入本提交，隔离快照证明 BE-027 没有突破冻结边界。
- **文档迁移**：原规划 P-04、P-09 已按真实类名、binding 作用域、revision 冲突、发布事务和学生脱敏修正，迁入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v844-多教师共享草稿与不可变发布be-027-当前稳定实现)；2 号文档删除重复详图，只保留完成结论和链接。
- **诚实边界**：本任务没有实现 FE-041 教师可见结构化编辑器、BE-028 三种完成预设、自由拖拽页面搭建、任意 HTML/脚本、管理员逐内容审核、小程序、服务器部署或竞赛材料，也没有修改 127 项活动。下一任务严格为 `V8.4.4 / FE-041`。

---

## 1.0.213 V8.4.4 FE feat(course): 完成结构化课程编辑器（FE-041）

- **提交日期**：2026-08-25
- **任务编号**：`FE-041`
- **提交标题**：`feat: add structured course content editor`
- **实现提交**：`54fe180`
- **教师可见入口**：教师工作台新增按需加载的课程内容中心，只显示本人创建或共同授课、已批准且已经生成课程码的课程。课程列表、共享草稿、release 和历史全部读取 BE-027 稳定接口，没有建立前端私有发布状态。
- **结构化编排**：教师可建立、删除和重排章节/单元，组合封面、学习任务、富文本、媒体、正式实验引用、检查点和来源七类内容块。后端新增正式活动 allowlist，与前端目录精确统一为 127 项；未知 key、单元与活动块 key 不一致、重复活动引用、危险 Markdown、无替代文字媒体和非法检查点均拒绝保存。
- **预览与发布**：教师可在保存前预览七类内容；显式发布必须经过预览和二次确认。发布成功后页面显示真实第 N 版、读取不可变历史并取得滚动后的下一草稿 revision；页面没有直接写入完成结果。
- **并发冲突**：旧 revision 保存收到 `409 / course_draft_revision_conflict` 时，同时保留本地草稿和服务器草稿，提供“导出本地副本、采用服务器草稿、以最新 revision 保留本地内容”三种明确动作。系统不会静默覆盖、自动合并或自动重投结果不明的写入。
- **页面与生命周期修正**：课程整行命中不再拦截内部控件；工作台刷新、保存和发布后保持“组织与课程 / 课程内容”展开状态；只修改已有文本也会启用保存；移动端两个新增按钮由 36px 修正到 44px。
- **真实浏览器验收**：用临时课程建立包含七类内容块和机械臂引用的草稿，验证危险脚本拒绝、保存、预览、发布第 1 版、刷新历史、外部 revision 2→3 冲突和显式保留本地至 revision 4；随后恢复标题并保存至 revision 5。桌面 `1440×900` 与移动 `390×844` 均无横向溢出，主要课程按钮不低于 44px，console warning/error 为 0。
- **自动验证**：33 项课程内容、课程创建、课程审核和课程成员定向后端回归通过；纯 Git 暂存快照通过 267 个受跟踪 JavaScript 文件语法检查、74 项前端契约和架构边界；后端全量到 100% 无失败，仅保留既有 skip/xfail 和两条 SQLAlchemy 弃用警告。
- **文档迁移**：P-09 已按教师页面、冲突选择、预览、两步发布和历史回读校正；结构化编辑器能力、边界和真实浏览器证据写入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v844-结构化课程编辑器fe-041-当前稳定实现)。P-04 继续由同一章节维护唯一权威实现。
- **诚实边界**：本任务没有修改 127 项活动，没有实现 BE-028 的实验操作、检查点和作业批改三种完成预设，也没有实现自由拖拽、管理员逐内容审核、小程序、服务器部署或竞赛材料。下一任务严格为 `V8.4.4 / BE-028`。

---

## 1.0.214 V8.4.4 BE feat(course): 完成三种课程完成预设（BE-028）

- **提交日期**：2026-08-25
- **任务编号**：`BE-028`
- **提交标题**：`feat: add course completion presets`
- **实现提交**：`c9fb4dc`
- **负责人裁决**：按 Q-003 方案 A 实施。每个课程单元只选择一种预设；实验操作绑定该单元正式活动，检查点绑定一个明确 inline `checkpointKey`，作业绑定一个明确 `assignmentId`。客户端不能声明完成，question-set 在没有受信题库判分器前不能作为完成目标。
- **教师配置**：`CourseUnitRefV2.completion` 使用 `CourseUnitCompletionV1` 保存 `experiment_operation / checkpoint_passed / assignment_reviewed` 和条件目标。草稿允许暂时未配置，发布必须逐单元校验。教师课程内容中心只新增紧凑的完成方式卡和目标选择，没有重做七类内容编辑器或实验页面。
- **规则发布事务**：发布根据全部单元配置生成规范化 `LearningCompletionRule`。定义和哈希相同则复用既有规则；不同则递增版本，并同步 `LearningRuleActivation` 与隐藏 `course_cohort` 的 `LearningRuleClassBinding`。规则、内容页版本、release、release unit、内容 binding 与规则 binding 处于同一事务，任一步失败全部回滚。
- **迁移与历史**：新增迁移 `20260825_0058_course_completion_presets` 和 append-only `CheckpointAttempt`，并把已经被 release 引用的旧 `CourseUnit` 校正为 `published`，避免教师继续编辑草稿时使学生当前版本失效。旧 release、规则、binding、尝试、提交和学习事实继续保留；配置不变的新内容发布不制造无意义规则版本。
- **实验操作完成**：`experiment_operation` 只接受正式 `LearningActivityRuntime` 作用域下由服务端接收的 learner `attempted` 见证，再由规则派生 `rule / completed`。普通无 runtime 的通用事件不能完成，客户端写 `completed` 继续被证据合同拒绝。
- **检查点完成**：新增 `POST /api/v1/courses/{course_id}/units/{unit_id}/checkpoints/{checkpoint_key}/attempts`。服务端锁定学生、课程成员、当前 release、规则和发布时 `ContentPageVersion`，自行校验结构化答案；`CheckpointAttempt` 记录 request hash、release/page/rule 坐标、次数和判分。稳定 attempt id 可安全重放，不同请求复用 id、旧 release、错误目标和超限尝试被拒绝，响应不返回标准答案。
- **作业批改完成**：既有批改入口允许课程创建者和有效共同教师处理隐藏课程群组内的提交；指定作业被批改为 `graded` 或 `returned` 且已有评分或反馈时，在同一事务追加第一次 `trusted_assessment / completed`。重复批改不重复产事实，越权或模拟完成写入失败会让批改与完成事实一起回滚。
- **统一投影**：实验规则派生事实、正确检查点和有效作业批改最终都进入既有 `LearningEvidenceEvent`，由 `LearningActivityProjection` 与恢复投影汇总。投影删除后可以仅依靠 append-only 事实重建，没有新增第二套课程进度表。
- **自动验证**：`backend/tests` 全量到 100% 且无失败，仅保留两条 SQLite datetime 弃用警告；新增覆盖三种规则、错误/正确检查点、无答案泄露、幂等重放、请求冲突、旧 release、次数上限、错误目标、规则复用与换版、发布事务回滚、共同教师批改、重复批改、批改回滚和投影重建。267 个受跟踪 JavaScript 文件语法检查通过；课程内容、authoring、样式分层、角色资源裁剪和缓存代际相关契约通过。
- **真实浏览器验收**：在隔离的本地演示数据中建立并批准临时课程，验证内容中心读取 127 项活动、三种完成方式、无作业时的明确提示、检查点目标选择、草稿保存、首个 release 发布和历史中的规则编号。移动 `390×844` 可见宽度无根级横向溢出；首轮发现异步重绘后单元操作图标未再次执行 Lucide hydration，同一提交补 `refreshIcons()` 并复验三个操作按钮均有 SVG，console warning/error 为 0。
- **文档迁移**：原规划 P-10 已按真实发布门禁、三种事实来源和统一投影修正，迁入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#当前学习完成流程图原-p-10)；2 号文档删除重复详图，只保留完成结论和链接。
- **诚实边界**：本任务没有修改 127 项活动本体，没有增加第四种完成预设、question-set 判分器、自由拖拽、管理员逐内容审核、小程序、服务器部署或竞赛材料。V8.4.4 至此完成，下一任务严格为 `V8.4.5 / BE-029`。

---

## 1.0.215 V8.4.5 BE feat(course): 完成三角色工作台聚合读取（BE-029）

- **提交日期**：2026-08-25
- **任务编号**：`BE-029`
- **提交标题**：`feat: add role workbench aggregates`
- **实现提交**：`3dc21bb`
- **统一读取入口**：新增只读 `GET /api/v1/workbench`。服务端只依据当前已认证用户的真实角色选择响应 DTO，学生、教师和管理员分别得到 `StudentWorkbenchDTO`、`TeacherWorkbenchDTO` 或 `AdminWorkbenchDTO`；客户端不能用参数越权切换角色。
- **学生聚合**：首屏数据包含在读课程、最近作业、继续学习位置、提交/反馈数量和行政班身份。课程成员以 active `CourseEnrollment` 为真值；作业可见性继续核对隐藏 `course_cohort`、成员关系、教学计划和作业发布策略，不另建工作台课程或作业状态。
- **教师聚合**：首屏数据包含本人创建或共同授课的课程、待审批学生、未发布草稿和待批改提交。作用域同时要求当前有效学校成员关系和课程创建者/有效共同教师身份，停用学校成员关系后不再展示原学校课程。
- **管理员聚合**：首屏数据包含待审教师申请、待审课程信息修订、组织治理提醒和目录查询总数。两类审核队列继续引用原申请与修订事实，组织提醒只表达当前缺口，不产生新的治理状态。
- **下一动作与分页**：三种 DTO 都返回一个结构化 `primary_action`，按角色业务优先级明确首个可执行动作。预览列表统一支持 `limit` 与 `offset`，`limit` 限制为 1—20，并返回各区块自己的分页元数据；全部详情仍通过原领域接口读取。
- **分区降级**：聚合服务逐区块容错。单一区块读取失败时，该区块以同结构空结果返回并写入 `section_errors`，其余健康区块继续可见；接口不会把失败静默伪装成完整成功，也不缓存第二份业务真值。
- **实现边界**：本任务只组合既有 `Course`、`CourseEnrollment`、作业、提交、教师申请、课程信息修订、组织和目录事实，没有新增数据库表、迁移、写接口、缓存表或后台任务；所有业务写入仍由原课程、申请、作业和审核接口处理。
- **自动验证**：5 项工作台专项覆盖鉴权与角色分派、空状态、分页边界和两页数据、学生课程/作业/反馈对账、教师有效学校作用域、管理员审核队列与组织提醒，以及单区块故障降级；课程创建、课程审核和课程成员定向回归通过，`backend/tests` 全量运行到 100% 无失败，Ruff 与 `compileall` 通过。
- **文档边界**：当前稳定聚合接口已经写入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v845-三角色工作台聚合读取be-029-当前稳定实现)。P-11 仍在 2 号文档标记为“BE 已完成、FE 尚未完成”，必须等 FE-042 三角色可见页面和双视口验收通过后再校正迁移。
- **诚实边界**：本任务没有修改学生、教师或管理员页面，没有改动 127 项活动，也没有实现小程序、服务器部署、竞赛材料或生产级安全加固。下一任务严格为 `V8.4.5 / FE-042`。

---

## 1.0.216 V8.4.5 FE feat(workbench): 完成三角色可见首屏与主要业务跳转（FE-042）

- **提交日期**：2026-08-25
- **任务编号**：`FE-042`
- **提交标题**：`feat: add role workbench overviews`
- **实现提交**：`506f4ea`
- **共享首屏 owner**：新增 `shared/js/role-workbench-overview.js`。学生、教师和管理员复用同一个只读渲染 owner，各自只读取 `GET /api/v1/workbench?limit=6&offset=0`；owner 校验服务端角色、分页、基础总数和分区错误，负责加载、空态、失败重试、唯一下一动作与次要入口，不建立前端业务真值。
- **三角色信息层次**：学生按“课程—最近任务—续学—提交反馈—加入课程”显示；教师按“授课课程—学生申请—未发布草稿—待批改”显示；管理员按“教师申请—课程信息审核—组织提醒—查询总数”显示。三种首屏都只突出一个当前下一动作，避免和原工作台多个并列入口争抢注意力。
- **共享导航桥**：新增 `shared/js/role-workbench-bridge.js`，只把结构化 action 派发到现有 DOM owner。学生进入课程、作业、反馈或课程码入口；教师进入建课、成员、内容、批改，V8.4 内容中心没有旧课程时退回原教师课程范围；管理员进入既有教师审核、课程治理或组织治理。相同下拉值不会重复派发 `change`。
- **生命周期与角色裁剪**：三个角色主文件分别增加一个摘要宿主和一条 refresh/reset/destroy 桥接，首次角色确认后加载，刷新前清空，离开或身份失效时销毁并中止请求。`AstraPageRegistry` 只向对应角色分发共享 JS/CSS，并用集合去重；匿名资源边界保持不变。
- **可见样式**：新增共享首屏样式，桌面保留标题、唯一主动作、指标和四段队列的清晰层次；容器查询依据角色页面真实宽度在 760px 和 560px 重排，移动端指标为 2×2、队列为单列。交互控件最小高度 44px，支持键盘焦点和 reduced motion，不使用 `100vw`。
- **缺陷收束**：真实浏览器首轮发现初次登录时 reset 抢先懒加载导致首屏需手动刷新，已改为只有 refresh 才首次加载桥；教师建课入口改为点击嵌套向导控件；旧课程增加原工作台回退；`published` 状态显示为“已发布”。管理员窄内容列的指标再由容器查询修正为 2×2。
- **自动验证**：`role-workbench-fe042-contract`、角色资源裁剪、教师样式层、管理员治理、课程成员和课程内容定向合同通过；新增合同同时锁定首屏读取唯一性、只读边界、三角色信息顺序、嵌套建课动作、旧课程回退、重复范围事件保护、容器查询、44px 与 reduced motion。267 个受跟踪 JavaScript 文件语法门禁在本任务实现快照通过，`git diff --check` 通过。
- **真实浏览器验收**：外部 Edge/Chrome 在隔离临时数据中复核学生、教师、管理员首次进入，唯一下一动作和主要跳转；教师主动作能选中 `Engineering Systems`，管理员课程治理能切入对应分区，学生无 V8.4 选课时如实显示加入课程入口。桌面和 `390×844` 无可见横向溢出，console warning/error 为 0。固定 10 秒窗口只有两轮教师学情读取，与既有 4 秒轮询一致，没有新增事件循环。
- **文档迁移**：原 P-11 已按真实 DTO、共享 owner、路由桥、角色宿主和领域接口边界迁入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#当前三角色工作台数据流原-p-11)；2 号文档删除重复详图，只保留完成结论和链接。
- **诚实边界**：本任务没有数据库迁移、写接口、缓存表、后台任务、课程领域重构或实验改动，没有处理公开导览候选，也没有实现小程序、服务器部署、竞赛材料或生产级安全加固。下一任务严格为 `V8.4.6 / DATA-010`。

---

## 1.0.217 V8.4.6 DATA feat(demo): 建立完整课程闭环演示数据（DATA-010）

- **提交日期**：2026-08-25
- **任务编号**：`DATA-010`
- **提交标题**：`feat: seed complete V8.4 course loop demo`
- **实现提交**：`abfc9be`
- **单一初始化入口**：继续扩展 `backend/scripts/initialize_demo_data.py` 和无凭据 manifest，只通过正式 ASGI HTTP API 编排；没有新增 seed 系统，没有导入 ORM、SQLAlchemy、Alembic、model 或 service，也没有修改数据库结构。
- **身份与组织样例**：保留原管理员、教师和行政班学生，新增一名待审教师申请者、一名经管理员批准的共同教师和一名无行政班学生。共同教师再经正式班级 teacher 加入申请取得同校资格，这是 Q-004 方案 A 的最终落地；待审者保持 student，只读边界不被演示数据绕开。
- **课程治理样例**：建立一门公开课程和一门指定行政班课程，两门均有创建者与共同教师、独立八位课程码和内部 `course_cohort`。限班课程第 1 次信息提交被管理员退回，第 2 次由共同教师重提并批准；公开学生走申请/教师批准，行政班学生走批量加入。
- **内容与发布样例**：公开课程包含实验操作、inline 检查点和作业批改三个单元，两名教师共同维护共享草稿并显式发布两个不可变 release。第 2 版自动成为学生当前版本，第 1 版和 binding 历史保留；学生读取检查点时不包含标准答案。
- **学习结果样例**：正式活动 runtime 产生实验操作见证，服务器判定的正确检查点产生完成事实，92 分教师批改产生作业完成事实；恢复接口对三个活动均回读 `completed`。同时保留另一份未评分提交，供教师待批改队列展示。
- **幂等与漂移门禁**：自然键、角色、课程字段、审核次数、教师集合、准入班级、成员来源、课程码格式、release 数量、完成规则、作业和提交结果均逐项回读。重复运行不增殖；相同客户端事实安全重放，字段漂移、重复对象或超过两个 release 会失败关闭。
- **验证证据**：`test_demo_data_initializer.py` 全部 12 项通过，其中主旅程覆盖全新初始化和连续两次重放，三次 secret-free 报告完全一致；`demo-data-initializer-contract.cjs` 通过并锁定新增正式接口族、三类完成预设和 manifest 无凭据边界；QA-016 定向回归为 `1 passed, 1 xfailed`，既有现场学生预制证据问题继续诚实标注。
- **诚实边界**：本任务没有修改实验页面、三角色页面、领域模型或接口，没有开发小程序、服务器部署、竞赛材料或生产级安全加固。下一任务严格为 `V8.4.6 / QA-032` 全量与真实浏览器验收。

---

## 1.0.218 V8.4.6 QA/DOC fix(course-loop): 完成课程闭环终验与架构迁移（QA-032、DOC-004）

- **提交日期**：2026-08-26
- **任务编号**：`QA-032`、`DOC-004`
- **实现提交**：`c46199a`、`5b84424`、`3cfd38f`、`dc6b5f0`、`85a6427`、`78b98a0`、`b968dff`、`35c3ec5`、`c3be367`、`9fb73f8`、`7e18ab5`、`11b47b6`、`16d8923`
- **验收收据对齐**：`c46199a—dc6b5f0` 只校正已经改变的角色资源清单、工作台架构上限和可见收据，没有借 QA 新增产品功能。教师课程批改适配器随后由 `9fb73f8` 独立为 `teacher-course-grading.js`，教师主 owner 回到既定 2883 行上限；`7e18ab5` 把课程作业作用域集中到访问控制服务，避免继续在路由中复制课程/行政班判断。
- **无行政班学生作用域**：`85a6427` 将 active `CourseEnrollment` 固定为课程访问真值。学生没有行政班时不再被全局班级遮罩阻断，内部 `course_cohort` 继续承载兼容作业与学习证据的隐藏范围；用户只看到“未关联行政班”，不需要理解内部群组。
- **课程直属教师闭环**：`78b98a0` 从教师首屏待批改项传递课程、内部群组和提交坐标，进入原“批改与学情”页面；`b968dff`、`35c3ec5`、`7e18ab5` 和 `11b47b6` 统一授权课程创建者或有效共同教师读取作业、作业策略、提交、聚合证据和逐项进度，同时行政班课程仍要求班级教师/管理员作用域。
- **冻结实验收口**：`c3be367` 只修复新增机械臂独立 hash 下根容器被通用 `.content-section[data-module]` 规则隐藏的问题，并更新 SHOW-04 合同；没有修改机械能等 124 项保护活动，也没有为实验重新加入课程说明壳。
- **真实浏览器验收**：外部 Edge/Chrome 使用 API-only 演示数据库覆盖三角色。无行政班学生可见当前第 2 版、3/3 完成、作业 92 分和教师反馈，刷新直达及 `390×844` 通过；教师可从唯一下一动作进入课程直属名单，看到待批改提交、评分表、学习概况与逐单元进度，相关接口全部 200；管理员教师审核抽屉、批准/退回入口和刷新直达通过。机械能旧实验保持原控件；机械臂覆盖正式路由、可达/限位/越界预设、暂停、重置、离开重进及双视口。页面未见新增 warning/error。
- **自动验证**：704 项后端测试收集并全量运行至 100% 无失败，只保留既有 skip/xfail 与依赖弃用警告；干净 Git 提交态 272 个受跟踪 JavaScript 文件语法检查和 76 项前端合同全部通过。合同覆盖真实 Playwright 浏览器旅程、架构边界、角色资源裁剪、旧实验保护和机械臂生命周期；变更文档相对文件链接与 `git diff --check` 通过。
- **文档迁移**：`DOC-004@16d8923` 将 P-01 按真实用户可见 Web 层、前端页面 owner、FastAPI 接口、领域服务、关系数据库、隐藏课程群组、活动 manifest 与不可变历史校正后迁入 [`01-开发者手册.md`](../01-开发者手册.md#22-分层结构)。完整闭环和作用域解释写入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v846-完整课程闭环与验收收口当前稳定实现)；2 号文档删除 P-01 详细副本。
- **诚实边界**：本版本证明的是本地同源 Web 作品闭环，不代表微信小程序、公网服务器、正式 MySQL、隔离 OJ、真实课堂实证、竞赛申报材料或生产级安全加固已经完成。上述方向继续暂缓，必须由项目负责人另开版本；本任务没有创建工作组或额外 worktree。

---

## 1.0.219 V8.4.6 补充 UI ui(public-guide): 整合登录前作品导览（历史 UI-015）

- **提交日期**：2026-08-28
- **历史任务编号**：`V8.1.5 / UI-015`；该编号用于追溯早期候选，不表示当前产品从 V8.4.6 回退。
- **提交标题**：`V8.1.5 UI ui(public-guide): 交付登录前作品导览（UI-015）`
- **实现提交**：`bace1b4`
- **公开发现路径**：未登录首页新增作品定位、教学闭环、三星系目录、两种代表性互动和真实账号入口。用户可以先理解作品，再进入登录、注册或密码重置；导览不新增 hash 业务页面。
- **目录单一来源**：公开统计读取 `AstraExperimentRegistry`、代码空间 manifest 与未来星系 manifest，当前显示 `90 / 18 / 19 = 127`；文案不建立精品、重点或普通课程等级。
- **会话与权限边界**：导览只在未认证会话中显示，沿用原 Cookie Session、登录注册和三角色资源裁剪。它不创建课程、班级、学习进度或发布状态，也不能绕过待审教师只读门。
- **工程接线**：新增 `shared/js/public-guide.js`，并在原会话样式、入口装配、页面注册和 service worker 缓存代际中接线；`shared/js/main.js` 保持既定 489 行架构上限。
- **自动验证**：当前整合态 `npm test` 全量通过 274 个受跟踪 JavaScript 文件语法检查和 77 项前端合同；`public-guide-ui015-contract` 同时锁定无课程分级措辞，架构、实验注册、页面注册、真实 Playwright 旅程和旧活动保护合同继续通过。
- **浏览器证据**：候选阶段已在同源本地入口覆盖 `1440×900`、`390×844`、键盘焦点、44px 触控、横向溢出、登录/注册/重置和 reduced-motion。本次收束尝试启动新的隔离同源服务时，Windows 进程创建在执行前被平台策略拒绝；未产生数据库、日志或后台进程，因此不把旧证据写成“本轮重新截图”。
- **诚实边界**：本提交没有修改 127 项活动本体、课程后端、数据库迁移、学生/教师/管理员权限、小程序、服务器部署、竞赛材料或生产级安全；它只是把已经审查过的公开导览能力与当前主线完成兼容整合。

---

## 1.0.220 V8.4.6 DOC docs(control): 对齐当前基线并登记后续方向（DOC-004）

- **提交日期**：2026-08-28
- **任务编号**：`DOC-004` 延续收束；不新建产品版本或业务任务。
- **提交标题**：`V8.4.6 DOC docs(control): 对齐当前基线并登记后续方向（DOC-004）`
- **实现提交**：本记录与文档变更位于同一提交，以以上唯一标题在 Git 中定位。
- **版本对账**：统一把 `bace1b4` 标记为当前产品代码基线，明确 `V8.1.5 / UI-015` 是历史任务号而不是版本回退；正式活动、Alembic head、后端终验和当前前端门禁分别对齐为 `90 / 18 / 19 = 127`、`20260825_0058`、704 项全量无失败、274 个 JavaScript 与 77 项合同通过。
- **规划收束**：2 号文档把当前执行入口压缩为十项直白候选，按 T0/T1/T2 列出技术延伸、可见收益和工时；未获负责人批准前不分配版本、不建立任务、不修改业务代码。已完成 V8.3.4—V8.4.6 章节只作验收对账。
- **历史边界**：V8.1 旧内容平台片段继续保留用于追溯，但统一标记为非当前实现；退役 JSON、合同与冲突迁移不再作为可点击权威入口，当前课程实现只指向 1 号文档和 19 号子文档。
- **文档门禁**：11 份本轮变更文档的相对文件目标均存在，`git diff --check` 无空白错误；通用文档规范验证器与项目保留的原始三主文档命名、历史编号和章节规则不兼容，因此只作为诊断，不以大规模改名或删减历史来换取通过。
- **诚实边界**：本提交不修改产品代码、数据库、接口、角色权限或实验内容，也不启动下一周期候选；本地 `main` 与 `主开发` 的快进同步属于提交后的 Git 整理，不删除历史分支或远端引用。

---

## 1.0.221 V8.5.0 DEV feat(workbench): 建立教学管理驾驶舱（DEV-003）

- **提交日期**：2026-08-28
- **任务编号**：`DEV-003`
- **实现提交**：`e057550`
- **后端聚合**：`AdminWorkbenchDTO` 新增可选 `teaching_snapshot`，只读汇总已发布/草稿课程、active 选课、不可变 release、完成投影、待批改、星系分布和近期课程脉搏；分区异常仍按原工作台规则降级。
- **可见页面**：管理员首屏新增“教学运行驾驶舱”，以教学信号、星系分布、课程运行卡和原业务入口代替原始实体堆叠；桌面和窄屏均采用同一只读 owner。
- **验证与边界**：工作台专项、角色资源裁剪和真实管理员页面通过。本任务没有新增数据库迁移、写接口、缓存表、治理状态或实验改动。

---

## 1.0.222 V8.5.1—V8.5.2 DEV feat(course): 建立版本时光机与发布影响预演（FE-043、DEV-004）

- **提交日期**：2026-08-28
- **任务编号**：`FE-043`、`DEV-004`
- **实现提交**：`5e76419`
- **版本时光机**：教师可选择任一不可变 release，查看相对上一版的新增、修改、删除和稳定单元；首版以基线方式解释，旧版只读且不覆盖当前草稿。
- **影响预演**：课程草稿响应复用 active 课程成员数量；前端在发布确认前显示下一版本号、受影响学生与单元变化，内容一致时禁用重复发布。
- **差异口径**：比较只包含学生可见的 schema、星系、学科、标题、摘要、布局、完成规则和内容块；内部 `slug/courseId/status`、对象键顺序及空可选字段不再被误报为教学内容变化。
- **验证与边界**：后端内容平台专项、纯差异合同以及教师端桌面/`390×844` 页面通过。本任务不开发自由拖拽，不改变发布事务，不修改 release 历史和 127 项活动。

---

## 1.0.223 V8.5.3 DEV feat(evidence): 建立师生学习证据链（DEV-005）

- **提交日期**：2026-08-28
- **任务编号**：`DEV-005`
- **实现提交**：`d9b302d`
- **学生视图**：把现有活动投影整理为“操作—规则—结果—来源”时间链，显示尝试、纠正、说明、状态、时间和原活动返回入口。
- **教师视图**：逐事件证据对话框采用同一四阶段表达，并允许按来源活动定位完整事件链；原纠正、事实和 release 状态继续保留。
- **验证与边界**：新增证据链合同并在师生桌面/移动页面通过。普通浏览仍不等于完成，客户端不能直接写完成状态，也没有建立第二套学习进度表。

---

## 1.0.224 V8.5.4 QA test(showcase): 完成展示验收与文档迁移（QA-033）

- **提交日期**：2026-08-28
- **任务编号**：`QA-033`
- **验收提交**：`3c28562`；文档同步以 `docs(QA-033): 收束 V8.5 展示周期` 标题在 Git 中定位。
- **自动验证**：后端全量测试运行至 100% 无失败，只保留既有 skip/xfail 与两条 SQLite datetime 弃用警告；275 个受跟踪 JavaScript 文件语法检查和 78 项前端合同全部通过。
- **真实页面**：管理员驾驶舱、教师版本时光机与发布预演、教师证据事件链、学生证据投影链均覆盖默认桌面和 `390×844`，没有根级横向溢出；重复发布误判已在本轮修正并复验。
- **文档迁移**：当前实现图、数据来源和展示边界迁入 [`19-当前功能业务闭环与展示边界.md`](../01-子文档/19-当前功能业务闭环与展示边界.md#v850v854-管理平台展示增强当前稳定实现)；2 号文档只保留完成任务和后续冻结能力池。
- **诚实边界**：本周期没有新增 Alembic 迁移，没有修改 127 项活动，没有开发微信小程序、正式服务器、竞赛材料、生产级安全加固或大规模预留架构。

---

## 1.0.225 V8.6.0 DOC docs(planning): 收束历史规划并激活阶段收口（DOC-005）

- **提交日期**：2026-08-28
- **任务编号**：`DOC-005`
- **提交标题**：`V8.6.0 DOC docs(planning): 收束历史规划并激活阶段收口（DOC-005）`
- **实现提交**：本记录与文档变更位于同一提交，以以上唯一标题在 Git 中定位。
- **规划减负**：2 号文档由约 98 KB、1184 行收束为约 26 KB、374 行，删除的是已完成任务、验收流水和目标态 UML 的重复副本；当前稳定实现继续由 1 号与 19 号子文档解释，V8.3—V8.5 提交继续由 3 号与本分卷保存。
- **兼容更新**：21—29 号历史规划和 51 号治理契约中指向原 2 号章节的链接改向当前稳定实现或长期冻结表；没有批量重命名、移动或简化旧文档。
- **启动确认**：项目负责人于 2026-08-28 启动 `V8.6 / 课程运营表现增强`，总上限 20 个有效开发小时，固定执行 `UI-016 → BE-030 → FE-044 → UI-017 → QA-034`，全部由主开发单线负责。
- **文档验证**：188 个相对 Markdown 文件引用存在，103 个章节锚点可解析，已删除的旧 2 号章节没有遗留引用，`git diff --check` 通过。
- **诚实边界**：本提交只修改 Markdown 文档和任务状态，没有修改业务代码、数据库、接口、角色权限、实验页面或 127 项活动；V8.6 功能实现从下一独立任务 `UI-016` 开始。

---

## 1.0.226 V8.6.1 UI ui(showcase): 清理管理平台演示技术残留（UI-016）

- **提交日期**：2026-08-28
- **任务编号**：`UI-016`
- **提交标题**：`V8.6.1 UI ui(showcase): 清理管理平台演示技术残留（UI-016）`
- **实现提交**：本记录与实现位于同一提交，以以上唯一标题在 Git 中定位。
- **用户可见清理**：账号区、角色工作台、名单、发布者和错误空态只显示中文业务身份与可执行说明；内部恢复码、裸数据库编号和原始 JSON 不再进入主演示路径。
- **演示数据**：三门课程的任务、代码题、结构化作答和批改反馈改为中文；初始化器继续只调用正式 API，并可按稳定课程单元兼容识别、幂等升级既有演示库，不直接改数据库。
- **发布预演**：差异计算把学生可见内容与完成规则拆成两个语义令牌；预演分别显示受影响学生、新增单元、内容调整、完成规则和移除单元，历史记录使用中文草稿代次和业务发布者称呼。
- **缓存代际**：入口、页面注册、三角色资源和服务工作线程统一为 `20260828v861PresentationCleanupP3`，专项合同防止资源版本漂移。
- **验证证据**：`backend/tests/test_demo_data_initializer.py` 12 项通过；前端全量完成 275 个受跟踪 JavaScript 文件语法检查和 79 项合同；学生真实作答/反馈与教师发布预演页面专项复验通过。
- **诚实边界**：本版本不新增迁移、数据库状态表、角色权限或写接口，不修改 127 项活动，也不启动小程序、部署、安全或新实验任务；V8.6 下一任务为 `BE-030`。

---

## 1.0.227 V8.6.2 BE feat(workbench): 生成可解释课程健康提示（BE-030）

- **提交日期**：2026-08-28
- **任务编号**：`BE-030`
- **提交标题**：`V8.6.2 BE feat(workbench): 生成可解释课程健康提示（BE-030）`
- **实现提交**：本记录与实现位于同一提交，以以上唯一标题在 Git 中定位。
- **DTO 扩展**：`AdminCoursePulseSummary.health` 新增 `state / label / reason / facts / next_action`；每条提示至少携带两项非负数值事实，动作继续复用 `WorkbenchPrimaryAction` 并绑定原课程。
- **确定性规则**：依次选择待首发、待批改、待发布、尚未形成结果和运行正常；顺序只用于同时命中时选择最直接动作，不产生课程评分或教师评价。
- **事实来源**：只读取 `Course`、`CourseEnrollment`、`CourseRelease`、`content_draft_revision`、`LearningActivityProjection` 和 `Submission`；没有新增模型、迁移、缓存、写接口或第二套状态。
- **验证证据**：`backend/tests/test_workbench.py` 7 项通过，覆盖五种组合、原因/数值/动作完整性和真实 API 序列化；隔离演示数据库真实读取得到待批改与待首发状态。
- **诚实边界**：本版本只完成后端解释层，管理员可见矩阵与跳转留给 `FE-044`；不修改前端、权限、127 项活动、小程序、部署或安全专项。

---

## 1.0.228 V8.6.3 FE feat(admin): 展示课程健康矩阵（FE-044）

- **提交日期**：2026-08-28
- **任务编号**：`FE-044`
- **提交标题**：`V8.6.3 FE feat(admin): 展示课程健康矩阵（FE-044）`
- **实现提交**：本记录与实现位于同一提交，以以上唯一标题在 Git 中定位。
- **可见矩阵**：管理员可以按星系和待首发、待批改、待发布、尚未形成结果、运行正常五类状态筛选近期课程；卡片显示后端中文原因、至少两项数字事实、完成信号和教师责任动作。
- **事实边界**：前端删除完成率阈值推断，并对健康 DTO 的状态、原因、事实和动作做完整校验；不完整响应不会被包装成看似可信的课程判断。
- **安全跳转**：所有健康动作进入管理员原课程治理的状态视图并选中同一课程，窄屏复用治理检查器；管理员不会被导向教师草稿、发布或批改写入页。
- **资源与验证**：共享工作台和全局缓存代际更新为 `20260828v863CourseHealthMatrixP0`；276 个 JavaScript 与 80 项前端合同通过，真实管理员桌面和 `390×844` 的筛选、课程定位、44px 动作及无横向溢出通过。
- **诚实边界**：本版本不新增 API、数据库模型、迁移、健康分、预测、角色权限或实验改动；127 项活动保持不变，V8.6 下一任务为 `UI-017`。
