# 星序 Astra — 前端页面实现索引

> **首次建立**: 2026-07-04 | **最后更新**: 2026-08-13 | **维护**: ARCH组 / FE组 / UI组 / PM组
> **适用读者**: 前端视觉、交互、动效与页面重绘维护人员
> **文档边界**: 本文只说明“页面由哪些文件实现、改哪里最直接”。设计规范仍看 [`05-UI规范模板.md`](05-UI规范模板.md)，长期架构仍看 [`01-开发者手册.md`](01-开发者手册.md)。
> **V6.6.59 隐私/隔离更新**：收藏、评分、测验、导览和访问进度只保留页面生命周期内存，旧普通 storage 键由 API client 清除；backend sandbox 任何初次加载后的导航都会终止并恢复静态实验。不要重新引入学生行为 localStorage/sessionStorage，也不要放宽 iframe `allow-scripts` 单一权限。
> **V6.6.60 浏览器验收更新**：opaque sandbox 中带 SRI 的脚本资产必须由后端 hash-bound asset 端点返回匿名资源级 CORS；不要给全局凭据型 API 增加 wildcard。`tools/browser/script-sandbox-isolation-proof.cjs` 已覆盖桌面、真实 390×844、交互、重挂载、SRI/CSP、异常导航恢复、console/network 和水平溢出，外部 Edge 27/27。
> **V7.4.0 身份外壳更新**：主站在 Router 启动前确认 `/api/users/me`；未登录只呈现统一账号入口。`#student/#teacher/#admin` 的导航、section 和懒加载脚本按权威角色裁剪，越权 hash 先重定向再加载页面脚本。
> **V7.4.13 页面注册更新**：19 个主站页面的星域、分组、脚本、ready、enter 与 leave 元数据统一登记在 `shared/js/page-registry.js`；角色权限仍以 `app-session.js` 为唯一权威，88 个实验注册属于 FE-001 后续切片。
> **V7.4.18 实验清理更新**：88 项实验当前为 `65 legacy-callback + 23 validated-callback + 0 missing`；排序与细胞结构已补齐真实 owner/取消协议，23 项只在整页 `leavePage` 执行。实验切换/返回画廊仍不执行 cleanup，65 项 legacy 尚未据此升级为模块切换已验证。
> **V7.4.19 生物模式挂载更新**：DNA 与遗传的模式 wrapper 必须从 Canvas 最近的 `.demo-section` 查找 sibling controls；QA-003 证明从 `.canvas-container` 内查找会在真实页面静默得到 0。合同夹具必须保持真实兄弟 DOM，三项生物 wrapper 的按钮数固定为 DNA 4、光合作用 3、遗传 3。
> **V7.4.20 首页视口裁剪更新**：活动首页必须由 `.page.home-page.active` 显式覆盖通用 `.page.active`，固定到动态视口并裁剪星空、极光和轨道装饰；390×844 的 HTML/body `scrollWidth` 不得超过 390，活动首页边界必须为 `0..390` 且 computed style 为 fixed/hidden。装饰层可以在被裁剪容器内部保留更大的布局溢出。
> **V7.4.23 角色资源裁剪更新**：三角色 CSS/JS 不再由 `index.html`、APP_SHELL 或星系 HTTP fallback 静态加载。`app-session.js` 在 `/api/users/me` 成功后按 `AstraPageRegistry` 的角色矩阵加载样式，Router 只在越权 hash 回落后加载允许页面脚本；角色资源在 Service Worker 中固定 network-only，登出、401 或跨角色切换通过清理后整页重载隔离旧脚本执行域。QA-008 已对精确提交完成 163 个 JS、17/17 合同、三角色桌面/390×844、强制 SW 与真实 CacheStorage 独立验收，结论通过。
> **V7.4.24 管理组织契约更新**：R4 管理 UI 只允许调用 `GET/PATCH /api/admin/schools/{id}` 与 `GET/PATCH /api/admin/classes/{id}`，提交白名单字段、非空原因和当前 `expected_version`。409、超时或响应未知时必须锁定本次操作并重新 GET 权威资源对账，不得自动重发；界面不得暴露任意 SQL、表名或原始 JSON 编辑器。归档资源继续显示历史统计，但相关教学写按钮必须呈现只读/不可用状态。
> **V7.4.25 管理组织界面更新**：管理数据地图展示学校/班级 active/archived 统计和筛选；原生 dialog 只编辑白名单元数据，归档/恢复为独立动作，双确认失效、no-op、单次 PATCH、2xx 权威不一致、409、发送后歧义、显式对账和跨路由 lifecycle owner 均有门禁。归档学校/班级使教师成员、成绩、积分和策略写入只读，学生提交显示 `class_archived`；资源代际统一为 `20260716v7425OrganizationGovernanceP0`。
> **V7.4.26 实验模块切换更新**：QA-005 已对 V7.4.19 精确提交独立通过；88 项仍为 `65 legacy-callback + 23 validated-callback + 0 missing`。`cleanupModule(subject, id)` 只执行精确 `verified:true` owner，A→B 与 B→画廊先关闭 Zoom 再 cleanup，失败在 DOM/hash/active state 变化前中止；65 项 legacy 不执行模块级 callback。transition generation 会失效旧资源、init、resize、焦点和相关推荐任务，整页离开继续走兼容 cleanup 且不双执行。当前统一代际为 `20260716v7426ModuleSwitchLifecycleP4`。
> **V7.4.32 统一界面更新**：`#planets` 改为代码原生的开放式三星系资源索引，保留 `initPlanets/destroyPlanets` 页面生命周期并增加身份/角色入口；`#admin` 仍是唯一管理员页，新增全部星系范围说明和 `overview/identity/organizations/content/operations` 本地分区，所有原面板/API/写锁继续复用。视觉资源代际为 `20260718v7432UnifiedAtlasP0`，新增 `unified-atlas-layout-contract.cjs` 冻结结构、响应式和缓存引用。
>
> **UI-003 / V7.9.4 管理员治理更新**：当前 `#admin` 已收束为 `overview/organizations/identity/classes/courses/audit` 六领域；治理总览以正式业务 API 依次呈现待审关系、停用账号、归档组织、课程状态分布、最近业务审计，再呈现经过全部消费字段与受控角色键校验的统计/数据地图，畸形 2xx、空态和失败态不伪造计数。角色首页的必需 ID 只接受原始有限正整数，审计可显示标识只接受 string/number/null；角色首页和治理总览按 25 条分页、最多 4 页 / 100 条有界寻找精确 action/resource 业务审计，可覆盖第 26 条真实 `class_membership` / `class_membership_batch` 记录并受 generation/Abort 保护。`admin.js` 只作不超过 2485 行的 orchestrator，课程状态和次级能力分别由两个 admin-only owner 按权威角色与进入时机加载；任一脚本失败会清理整批节点、监听和共享 Promise，再次调用创建全新节点，并发加载共享有效批次。课程可信 2xx 在精确审计前固化为跨离页 checkpoint；任一锁阻断其他课程和新 PATCH，但当前锁行保留移动端只读重开/对账入口。API Base 变化先失效 active course read，并清空 secondary 旧 authority；迟到响应不能回写，高级诊断首次进入前仍为零请求。资源代际为 `20260729v794AdminGovernanceP0`；真实浏览器验收与 DATA-001 演示联调仍待后续精确 QA。
> **V7.4.33 角色落点更新**：`ROLE_LANDING` 对 student/teacher/admin 均为 `planets`，`ROLE_WORKSPACE` 独立映射 `student/teacher/admin`；空 hash 统一显示星序，合法显式深链保留，越权深链在角色脚本加载前回落。星序顶栏提供 Session 安全退出；app-session/page-registry/main/planets/SW 使用 `20260718v7433RoleLandingP0`，三角色 workspace 资源继续 network-only。
> **V7.4.34 本机同源更新**：`app.local_preview` 返回的首页在任何业务脚本前设置 `ASTRA_LOCAL_PREVIEW_SAME_ORIGIN=true` 并清除旧 `astra-api-base`；统一 API client 在该模式只接受当前 origin，旧 `apiBase` 查询由服务器移除；正式拆分部署仍按受控 `CONFIG.backend.apiBaseUrl` 规则运行。
> **V7.4.35/36 集中验收更新**：api-client/app-session/page-registry/main/planets/SW 的公共验收缓存代际统一为 `20260718v7435QaCloseoutP0`。三角色证明的越权深链按当前外壳回落 `#planets`，随后再显式进入有权 workspace；管理员交互先选择 `overview/identity/organizations/operations` 本地分区再定位面板，用户治理使用服务端查询定位目标，不依赖首屏分页。归档和恢复按钮继续通过语义色区分。QA-009 已对精确 V7.4.35 提交使用真实 Edge 150 完成 12/12 语义、桌面/390×844、7 张截图与五项概念—实装签收，P0/P1/P2 为 0；V7.4.36 已关闭 R5c。
> **V7.4.37 角色层级更新**：`student/teacher/admin` 的 `galaxy` 统一为 `astra`，启动预判不再把角色 hash 归入 englab；Router 对全部 astra 页面隐藏工科 navbar，仅对真实 englab 非首页显示工科页脚。三个角色 section 共享星序 rail、移动三单元条和角色 dock。教师端以 `overview/structure/assignments/grading` 四分区按需渲染，创建操作使用 disclosure；公共代际为 `20260719v7437AstraWorkspaceP0`，`astra-workspace-hierarchy-contract.cjs` 固定层级、壳显隐和移动 dock 边界。
>
> **V8.0.13—V8.0.26 参赛展示版更新**：学生与教师工作台采用“当前班课 → 唯一主动作 → 状态回执”；工程载荷路径和力学恢复系数课程使用 `20260812v814FlagshipMotionP0`，分别提供 B→C→判断→D 受力过程和 `e=0.40→0.80` 真实首峰对照。QA-022 在 V8.0.15 发现 Physics canonical 冷加载竞态、Engineering 工作台入口误判和师生 busy 反馈缺口；V8.0.17 关闭后两项。V818/V820/V822/V824 四轮 Physics 首载均诚实 RETURN；V824 的 20 个 cold context 仅 7/20 PASS，前 10 个 reload 为 9/10 PASS，且失败 reload 明确出现 evidence 先 ready、再被迟到的 Physics init/reset 清空。V8.0.25 将 ModuleSelector 的 evidence mount 排在 current-generation owner init 成功之后，dirty/stale/load/init failure 不挂载；V8.0.26 以 `20260812v825PhysicsBindOrderP0` 加载 ModuleSelector，并以 `20260812v826PhysicsBindOrderP0` 统一 app-session、experiment/page registry、Router、main 与 Service Worker，完成 V824→V826；app-session → loader → queue/client/status/catalog/publication context/activity 继续传播 V826。共享学生进度面板不展示 0051/recovery/projection 等内部术语。QA-022 在精确 `2a34976e3343` 上完成 20/20 cold、10/10 reload、0 次 ready→false，并通过 Engineering 入口、师生 busy、390×844、键盘、reduced-motion、教师双课回读及 console/pageerror/Network 门禁，P0/P1/P2=0/0/0；Browser 后端为 Playwright 1.60 + system Edge 151 headless，external extension 不可用，不把该结论扩写为 external Edge 或生产发布 PASS。

> **V8.0.28—V8.0.30 展示叙事深化候选**：V828 角色资源在既有真实 scope 与唯一主动作上补齐“当前班课—本节目标—预计产出—下一动作—完成回执”；V829 旗舰资源为工程 B/C/判断/D 与力学 `.40/.80` 路径增加 brief、阶段 coach、真实数值比较、模型边界和结论，展示层只消费 exact observation/measurement，不写 evidence 或 `completed`。V8.0.30 用 `20260812v828RoleStoryP0`（V828）加载学生/教师、用 `20260812v829FlagshipStoryP0`（V829）加载 Physics/Engineering/Future，并以 `20260812v830ShowcaseStoryP0`（V830）统一 app-session、experiment/page registry、Router、main 与 Service Worker；app-session → loader → queue/client → catalog/activity 传播及 ARCH-004 边界不变。QA-023 待同 revision 验收，当前只称统一候选，不提前声明 Browser PASS。
> **V8.0.32—V8.0.34 展示构图返修候选**：V832 仅重排 390×844 的学生/教师 focus stage，使班课、目标、产出、唯一动作与回执在可见 dock 上方闭合；V833 仅为 `engineering.load-path` 增加 desktop sticky 真实舞台与紧凑 evidence 栅格，Canvas、表格、coach/status 仍由原 owner 写入。V8.0.34 用 `20260813v832RoleMobileReceiptP0`（V832）加载学生/教师、用 `20260813v833LoadPathSameFrameP0`（V833）加载 Future/Engineering，并以 `20260813v834ShowcaseLayoutP0`（V834）统一 app-session、experiment/page registry、Router、main 与 Service Worker；app-session → loader → queue/client → catalog/activity 继续传播 V834，ARCH-004 边界不变；Physics 保持 V829、ModuleSelector 保持 V825。QA-023 待同 revision 复验真实师生 success/empty 与 Engineering prediction/C/D，当前不提前声明最终 PASS。
> **V7.5.7 教师编排与学生上下文更新**：教师端新增 `curriculum` 第五分区，统一消费三星系 release plan、学生进度和代码提交/源码/判题接口；代码空间新增 Cookie Session 学生上下文与正式提交 adapter，未来星系新增 Router 前共享发布上下文，目录、直达子课和学生工作台执行同一 hidden/locked/open 裁剪。主壳、teacher、page registry、Router、main 与 SW 代际为 `20260719v757TeacherCurriculumP0`，未来星系上下文代际为 `20260719v757FuturePublicationP0`。
>
> **V7.5.8 综合收束更新**：未来星系课程运行时从根 HTML 静态引用改为 page registry/Router 按路由加载，两张背景改为总计 136,676 字节 WebP 并退出 APP_SHELL；总览自适应高度、椭圆焦点、Canvas 减少动效/DPR 和离页监听均进入合同。教师进度/代码提交使用服务端分页，代码空间入口资源统一为 `758r1`。主壳/page registry/Router/main/SW 为 `20260719v758ReleaseAuditP0`，teacher 为 `20260719v758TeacherPaginationP0`；共享发布上下文继续保持 `20260719v757FuturePublicationP0`。
>
> **V7.5.9/10 可访问性返修与终验更新**：未来星系课程滑杆、预设、判断行、提交和返回入口提供不小于 44px 的真实触达区；教师五分区 tablist 使用 roving `tabindex` 并支持方向键、Home/End。代码空间移动品牌/主导航、星序身份入口和教师 API 输入同步达到 44px。主壳、teacher、Future 与 app-session 样式代际为 `20260719v759A11yP0`，代码空间入口为 `759r1`；QA-010 已用真实 Edge 150 在精确 390×844 复验 PASS，P0-P3 均为 0。
>
> **Review re2 离线依赖更新**：主壳不再注入远程字体样式；协议页通过 `pages/about/about.js` 按需加载同源 [`marked@12.0.0`](../shared/vendor/marked-v12/SOURCE.md)。版本、MIT 许可、npm integrity 与 SHA-256 均在 vendor 目录及 `offline-runtime-assets-contract.cjs` 固定，禁止恢复字体或 Markdown 运行时 CDN。
>
> **Review re3—re14 审查更新**：代码挑战保留完成结果焦点并轮询正式提交终态，孤儿 Home 资源已删除；未来发布上下文先同步进入 unavailable，manifest 声明只对应真实互动 owner。教师端已迁移高基数分页消费，原单体样式按原级联顺序拆为 foundation/workbench/curriculum 三层，旧 `teacher.css` 不再发布。主壳、注册表、Router、main、SW 与教师资源统一为 `20260719v75ReviewTeacherLayersP0`；student/teacher/admin 顶层 JS/CSS 全部保持 network-only，三层教师样式加载顺序由合同和浏览器证明固定。
>
> **V7.6.3/4 架构门禁更新**：新增 `shared/js/product-capabilities.js`，冻结 13 项能力的规范名称、角色、状态、路由和声明边界，其中“账号级上次学习位置”保持 `planned`；该文件尚未由已提交页面消费，当前 `#planets` 仍是目录优先实现。`AstraRoleHomeClient`、`AstraLearningEvidenceClient` 与 IndexedDB 队列被登记为后续唯一 owner；门禁扫描 `.js/.mjs/.cjs` 及直接赋值、`Object.assign`、`Object.defineProperty/defineProperties`、`Reflect.*` 等全局注册方式，页面不得自行直连学习证据 API、复制完成算法或新增资源版本表。QA-014 A01 已对 V7.6.4 独立 PASS；完整规则见 [`24-V7.6全栈模块边界契约.md`](02-子文档/24-V7.6全栈模块边界契约.md)。
>
> **V7.6.5/9 学习证据与仓库瘦身更新**：BE-006 已通过 QA-014 A02；共享学习证据的首批前端实现见下方 V7.7.2 条目。全仓入口和引用图谱确认 [`pages/planets/`](../pages/planets/) 是唯一 `#planets` 星序总览，[`pages/home/`](../pages/home/) 是 `#home` 工科试验室首页，两者不是重复页面。qianduan 的旧页面只保存在 `archive/qianduan-wip-before-main-sync-20260727` 标签；V6 `muban/` 模板、未加载的 `cards.js/common.js`、空 `bento-grid` / 旧 `.card` 样式链和已被 favicon / WebP 替代的旧图片均已删除。当前课程目录由 `ModuleSelector` 生成 `.module-gallery/.module-card`，不要恢复旧卡片栈。
>
> **V7.7.2 / V7.7.5 FE-012 首批集成与 QA 更新**：`主开发` 已集成共享 catalog / client / queue / status 与角色回读 owner，只为工科 `physics.mechanics` 和代码空间 `control-flow.loop-boundary` 接入真实学习事实。QA-014 A03a 已对真实 9001、390px、多标签、SQLite 与三角色执行验收；自动化、两条闭环、离线重放、撤权和账本通过，但 locked 工科单元仍从学生工作台暴露可执行深链、进入后 Canvas 可操作，且工科清空 / 暂停 / 放大 / range 触达低于 44px，因此结论为 FAIL。Future 18 个 activity key 仍仅登记目录，没有 Future 页面 / Bridge / owner 运行接线；不能写成 FE-012 或三星系稳定交付。
>
> **V7.8.5 教师自然工作流更新**：教师顶层只保留教学总览、课程节奏、批改与学情三个入口，组织与课程、作业发布降为各自主入口内的二级 disclosure。课程节奏以明确 `class + course` 读取学生 × 分块进度、班级概况与逐学生安全证据；桌面语义表和移动详情互斥，hidden 只显示数量。发布先预览再单次 PATCH，评分按同一作业 / 班级 / offset 权威回读；409、畸形 2xx 与发送后歧义都不盲重发。教师资源代际为 `20260730v785TeacherNaturalWorkflowP0`，三层 CSS 顺序和 admin 复用矩阵不变。
>
> **V7.8.7 教师课程挂接竞态返修**：`shared/js/teacher-learning-evidence.js` 在既有 scope key 之外记录最近一次刷新实际消费的挂接布尔值，令同一 `base + class + course` 下的 false→true 通过原单 timer 恢复恰一组 progress + aggregate，true→false 则零新增请求并清空桌面表格、移动 disclosure、证据 / 纠错 / 反馈及全部旧私有引用。没有新增 owner、listener、Observer、资源文件或 API；独立运行时验收状态仍以 `02/03` 为准。
>
> **V8.0.2 教师课程作用域返修**：[`pages/teacher/teacher.js`](../pages/teacher/teacher.js) 为 pending submission、代码提交页、源码和 attempts 捕获不可变 `generation + class + course`，请求显式带班课范围，响应在落 state 前核对条目范围、父 submission 与精确连续分页。任一异课程行、提前 `next_offset=null`、重叠游标或未完成空页都会整页失败关闭；切课会 abort/隔离迟到响应。班级 roster、活跃学生和无课程知识汇总仍按班级读取；真实浏览器完成状态见 `02/03`。

> **V8.0.4 Future 代表活动接线**：18 个 Future activity key 与 route 不变，只有 `engineering.load-path` 被登记为 representative。该页使用固定 60 kN 理想二维桁架和 B/C/D 数值表，按预测 → B → C → 判断 → D → 纠正 → 解释 → 等待服务端投影推进；`domainOnly` 证据面板不暴露通用快捷写入。真实 `course_unit_id` 只来自发布上下文保留的 BE-004 响应，每次事件前重新核对班级、课程、单元、活动、身份、generation 与 open；撤权、离页、迟到响应和人工介入冲突均只读零写。客户端不产生 `completed`，合成模型不表示现实安全结论。`index.html` 的 app-session/page-registry/router/main、Router 的 Future runtime、main fallback / SW 注册与 `sw.js` APP_SHELL 使用同一 `20260809v804FutureEvidenceP0`；app-session 把该 `v` 传给 loader，loader 再传给 queue/client/status/catalog/activity。动态 URL 已由 Node 合同验证；集成后独立 QA 已在 external Edge 的桌面 / 390×844 完成代表活动、SQLite 六类事实和旧 SW→V804 普通 reload/旧 cache 删除/新资源接管，浏览器门禁通过。

> **V8.0.5 力学受控顺序与缓存接线**：`physics.mechanics` 现在由 P0—P8 状态机与持久化结果共同控制真实控件：预测成功后只开放固定 `e=0.40`，其完整快照和 `attempted` 成功后才开放 `e=0.80`，第二次快照后才开放比较、模型边界修正和结构化解释，最后只等待服务端投影。每次 record 前后重查 identity、route、class/course/unit/activity、generation 与 open；manual、provider/controller 缺失、撤权和迟到返回均只读零写。Mechanics opt-in 拒绝覆盖同 ID/type 未终结操作，record result 与 peer terminal 都按请求身份及一致终态矩阵校验；矛盾终态失败关闭。无身份 `removed/expired-pruned` 对正式 queued waiter 立即释放，对首次授权 / 初始传输只锁存 loss 并保留 identity fence，待初始结果安全返回后再决定释放，避免旧请求仍存活时同 ID 重入。每次 record 在首次授权前合并 session、authority invalidation 与 caller signal，并传入授权回调、publication provider 与 client；Mechanics 在 held authorize、initial transport 和 queued waiter 三段都可立即取消，终结后移除监听。即使 pending accessor、授权或底层传输忽略 signal，外层也会收束；迟到授权不能进入 record，迟到 confirmed/queued/manual 不能回调或签发 receipt。Physics 在消费一次性私有回执时再次核对 result 内部 ID/type。失败重试复用 ID，显式重做产生新 ID；自由探索、改控制量、拖拽、暂停、清除、resize、重复/越序档位不写课程尝试。无法证明完整受控组时页面安全回到 P0，完整跨刷新恢复仍依赖 ARCH-004；服务端仍不证明组合顺序或页面量规，客户端不写 completed。主壳、ModuleSelector、experiment registry、Physics、main fallback 与 SW 使用 `20260809v805MechanicsSequenceP0`，app-session → loader → queue/client/status/catalog/engineering context/activity 的实际动态 URL 同代际；未改的 Future runtime 仍保留 V804。独立 QA 已在 external Edge 桌面 / 390×844 跑通完整 P0→P8、网络失败同 ID 恢复、SQLite 唯一事实、Future/CodeVis 回归、应用日志及 V804→V805 单次普通 reload/旧缓存删除/新字节接管，P0—P3 无候选阻断项。
>
> **V7.9.68 学生主路径收束**：`pages/planets/planets.js` 现在是学生授权课程目录与入班可用性的单一页面 owner：零班级 student 的 `#student` 深链归一到 `#planets`，全部“我的学习”入口隐藏并 inert，`shared/js/role-home-client.js` 的入班 dialog 不提供取消且只有正式 API 写入后再次权威回读确认才关闭。学生可加入多个班级；目录按每个班级的 published 课程和 exact-open units 生成，工作台按当前班级隔离课程并按规范课程身份去重。活动学生页脚本迁为 [`pages/student/student-workbench.js`](../pages/student/student-workbench.js)，旧 [`pages/student/student.js`](../pages/student/student.js) 保持冻结且不再由 PageRegistry 加载。`#student/<subject>` 保留星序侧栏，提供进度、比例、分数和仅内存临时笔记；五个工科目录移除方法 / 说明并把来源放在课程目录之后。`#frontier` 增量改为单视口六节点星图，但不代表 FE-017 的四层 18 课程重构完成。资源代际统一为 `20260731v7968StudentFlowP2`。
>
> **V7.9.69 学生工作台与授权发现返修**：[`pages/student/student.css`](../pages/student/student.css) 以可收缩 rail + `minmax(0, 1fr)` stage 取代 `100vw` 二次占宽，主内容默认纵向并只在 stage 至少 1180px 时切双栏；[`pages/student/student-workbench.js`](../pages/student/student-workbench.js) 管理 44px rail toggle、精确 listener cleanup 和班课选择恢复。[`pages/planets/planets.js`](../pages/planets/planets.js) 提供 identity-scoped 内存选择，`role-home-client.js` 与学生页自动恢复首门可用课程和权威进度。工科顶栏、画廊与 [`shared/js/global-search.js`](../shared/js/global-search.js) 共同消费学生授权目录，未开放 page / activity 不进入可见或可激活结果。五学科 ledger 只保留实验数量；Englab 按需加载 Hero owner，普通模式绘制动画、减少动效保留静态首帧。资源代际统一为 `20260731v7969StudentUiP0`。
>
> **V7.9.71 实验目录授权生命周期返修**：[`shared/js/module-selector.js`](../shared/js/module-selector.js) 以单例 `astra:student-catalogue-ready` listener 替换旧身份生成的 sidebar、ledger、gallery 与 sources，四类目录都只消费 `_visibleExperiments()` 的当前 exact-open、非 upcoming 集合；零活动不生成目录节点。主启动与 Router 延迟 support 重复 init 幂等，backdrop、keydown 和 catalogue listener 各一份；身份替换 / 撤权先走既有 module close，同身份仍授权活动保留运行态。ModuleSelector 入口代际为 `20260731v7971CatalogueCountP0`。
>
> **V7.7.6 FE-012 定向返修**：学生课程面板只为权威 `effective_release_state=open` 生成链接，locked 无 `href`，hidden / 未知状态不渲染；班级下拉通过 `switchClass()` 同步工科发布上下文，空班立即清空发布 scope，切班以 generation / `AbortSignal` 取消旧导航与在途发布查询。学生直达 `#physics/mechanics` 时，`ModuleSelector` 在脚本与 Canvas owner 初始化前解析工科发布状态，locked 显示说明与安全返回，hidden 返回列表，open 保留完整教学路径。状态复核会先释放既有 owner 再销毁 evidence；pending 期间切换模块、关闭或 reset 会移除旧门禁且忽略迟到结果。力学清空、暂停、放大与四个 range 输入以 44px 为元素几何下限并保持键盘焦点。该实现已进入主开发集成线；独立复验结果与剩余限制见下一条。
>
> **V7.7.7 QA-014 A03a 复验**：精确集成提交 `c6dc2cb` 的 210 个 JavaScript 语法检查、39 个前端合同、真实 A-open→B-locked / 空班 / 迟到响应、locked 零 owner、hidden 归一、open completed、权限与只读 SQLite 0051 账本均通过；两视口核心控件几何均不低于 44px，应用 console error/warn 为 0。唯一 P2 是精确 390×844 下评分卡会截获放大按钮中心，关闭评分卡后固定 `.fab-trigger` 仍约重叠 831px²，使按钮不存在无遮挡的完整 44×44px 命中区。A03a 因此仍为 FAIL；V7.7.8 只允许原 FE-012 调整该固定浮层避让 / 层级，不得改写已通过的发布 authority、证据或 Future 运行边界。
>
> **V7.7.8 FE-012 浮层返修已集成**：[`shared/js/experiment-rating.js`](../shared/js/experiment-rating.js) 只对力学活动把评分卡插到当前 `.demo-section` 的 `.demo-layout` 前，[`shared/css/experiment-rating.css`](../shared/css/experiment-rating.css) 让该卡片随文档流排版；[`shared/css/fab-trigger.css`](../shared/css/fab-trigger.css) 将导览、收藏、返回顶部、导出和主 FAB 组成 safe-area 感知的移动端底部横向 dock，并让力学页 scrim 不再覆盖实验区。模块侧栏打开时 dock / 导出菜单低于 backdrop / sidebar / toggle，减少动效同时禁用主 FAB、导览、收藏与返回顶部 ripple；五个 dock 控件在收起、展开和动画中均保持至少 44px。外部 Edge 在 390×844 与 1015×898 对评分卡可见 / 关闭、滚动、侧栏 toggle / 首末项、清空、暂停、放大、四个 range、五个 dock 及导出菜单执行矩形交集和 `elementFromPoint` 网格，本地结果均为 0 遮挡 / 可命中，应用来源 console error/warn 为 0。该实现已进入 `主开发` 集成线，仍待独立 QA，不代表 FE-012 或 A03a 已完成。
>
> **V7.7.9 QA-014 A03a 复验退回**：精确集成提交 `1bc5af0` 的评分卡文档流、两视口核心遮挡、44px、侧栏层级、locked 深链、权限、SQLite 0051、根级溢出和应用 console 均通过；原 V7.7.7 浮层遮挡已关闭。A03a 仍为 FAIL：真实点击“记录预测”并调整 range 后，隔离账本为 5 条 `started`、3 条 `attempted`、0 条 `predicted/completed`，投影保持 `in_progress`（P1）；dock 折叠时 `.favorite-fab` 虽为 `opacity:0/pointer-events:none`，仍保留在 Tab 顺序且移动 / 桌面均可聚焦（P2）。V7.7.10 只允许原 FE-012 修复预测动作的权威确认 / 离页边界及折叠控件焦点管理，不得触碰 Future、Router、入口、后端或已通过的发布态与浮层几何。
>
> **V7.7.10 FE-012 返修已集成**：[`learning-evidence-activity.js`](../shared/js/learning-evidence-activity.js) 在 `started` 获得 `confirmed/reconciled/queued` 耐久结果前保持控件不可操作，以本次 `client_event_id + event_type` 绑定 tracked / untracked 命令，并用独立 domain 状态槽、16 条页面缓冲和单个在途请求串行处理真实操作。queued / local-pending 身份受 authority 代际、scope 与 256 条上限约束；exact peer confirmed 单调升级，错误 ID / 类型 / scope 不释放或冒充结果。authority 清除、身份重配与 destroy 会失效旧 record / 初始化 / projection recovery 并立即清空旧投影；任何 recovery 写 state / DOM 前都校验本次实际 `AbortSignal` 与上下文。[`learning-evidence-queue.js`](../shared/js/learning-evidence-queue.js) 的严格 peer projection 保留 `event_type`；[`learning-evidence-client.js`](../shared/js/learning-evidence-client.js) 继续负责本地队列自动重放；[`fab-trigger.js`](../shared/js/fab-trigger.js) 统一管理四个次级 FAB 与导出菜单项的 Tab / ARIA / inert、动态 owner、焦点回主 FAB、Observer 和 timer 生命周期。动态合同见 [`learning-evidence-v7710-contract.cjs`](../tools/tests/learning-evidence-v7710-contract.cjs)。两条独立提交审查均为 P0—P3=0；真实 390×844 / 约 1015×898、Tab、console 与 SQLite 仍待 QA-014 A03a / V7.7.11，不代表 FE-012 或 A03a 已完成。
>
> **共享队列背压补充**：Activity 的 256 条 peer identity 与共享 IndexedDB 队列容量是两个独立门禁。`queue_limit_reached` 会保留页面队首及原 `client_event_id`，停止后续 drain 且不 warn / 自旋；[`learning-evidence-client.js`](../shared/js/learning-evidence-client.js) 只在物理队列 `confirmed`、`removed`、`expired-pruned` 后发送无身份的 `queue-capacity-released`，每个信号只触发一次重试。authority 清除、身份重配或 destroy 会清除阻塞；[`learning-evidence-queue.js`](../shared/js/learning-evidence-queue.js) 的 schema 与容量合同不变。

> **V7.7.12 / V7.7.16 移动力学复验边界**：[`physics.css`](../pages/physics/physics.css) 在共享单列断点让 mechanics grid、控制区、可视区和 Canvas 真实收缩，阻止桌面 inline Canvas 宽度成为移动端 min-content 反馈；[`physics.js`](../pages/physics/physics.js) 与 [`physics-zoom.js`](../pages/physics/physics-zoom.js) 在窗口跨断点和 Zoom 归位后按父容器宽度与 DPR 立即重建 Canvas 显示 / 位图尺寸。Zoom dialog 打开后聚焦关闭按钮并约束 Tab，close / Escape / destroy 归还可用触发点并移除 modal、document 与 window 监听，重进只挂一组。QA-014 / V7.7.16 已在 390×844 与 1015×898 确认上述裁切、命中和 Zoom 生命周期通过；A03a 仍因次级 FAB Escape 继续进入模块关闭 owner 而 RETURN。
>
> **V7.7.17 / V7.7.18 Escape 集成与复验**：[`fab-trigger.js`](../shared/js/fab-trigger.js) 在 document capture 阶段仲裁展开 dock 的 Escape，Zoom modal、实验指南 overlay 或导出菜单打开时让其既有 owner 优先；其余情况下执行 `preventDefault()`、`stopImmediatePropagation()` 与既有 `collapse()`，归还主 FAB 焦点且不依赖 `ModuleSelector` 的监听注册顺序。keydown 使用相同 capture 参数挂载 / 释放，鼠标、布局、动画和模块 owner 不变。[`learning-evidence-v7710-contract.cjs`](../tools/tests/learning-evidence-v7710-contract.cjs) 以 show → 模块 owner 初始化 → hide → show 的顺序同时运行真实 `FabTrigger`、`ModuleSelector` 与 Zoom key owner，证明首次 Escape 只折叠 dock、保持 `#physics/mechanics` 与 active module，后续独立 Escape 才关闭模块；同时覆盖导出 / 指南 / Zoom 优先级及重复 show / hide 无 listener 泄漏。V7.7.18 已独立确认这条路径及既有双视口、发布态、证据和资源门禁通过，A03a 仅因 hidden 冷启动的预期拒绝 warning 而 RETURN。
>
> **V7.7.19 / V7.7.20 FE-012 hidden 诊断真实边界**：[`module-selector.js`](../shared/js/module-selector.js) 只在学生、物理页、当前无活动 owner、hash 与目标精确一致、slug 符合稳定键，且 registry 服务存在、`get` API 健康、一次成功 lookup 确认目标缺键、DOM 也不存在时，以 `physics.<slug>` 请求工科权威发布态；明确 `activity_hidden/activity_locked` 才无 warning 归一到 `#physics`。V7.7.20 在真实 9001 / SQLite / 外部 Edge 上证明学生 `GET /api/courses/{course_id}/units` 会省略 hidden，publication context 因而把 hidden 与不存在项同归 `course_unit_missing`，实际冷启动仍输出一次含目标键的 unknown-module warning 后归一；A03a RETURN。registry 基础设施固定摘要、异步 generation、拒绝后恢复和非目标零 owner 回归均通过。后续必须由 BE-011 提供不返回单元内容或标识的精确访问处置，再由原 FE-012 接线并把合同夹具改为与真实学生过滤语义一致；不得把 hidden 元数据重新塞回学生单元列表。
>
> **V7.7.21 QA-014 最终回执补录**：V7.7.20 的最终结论为 P0=0、P1=0、P2=2、P3=1。hidden 深链 2/2 冷启动均输出一次含 `physics:hidden-qa-v7718` 的应用 warning；普通未知深链虽失败关闭至 `#physics`，warning 仍回显 `physics:ordinary-unknown`，不满足固定安全诊断门槛。Zoom 打开态从桌面切到移动视口会短暂沿用旧位图尺寸，关闭后才立即对齐。原 FE-012 后续范围因此固定为：接入 BE-011 精确处置、让 hidden/locked 静默归一、让 ordinary missing 使用不含 slug 的固定诊断，并让 Zoom 打开态 resize 立即按当前容器与 DPR 重建；不得拆出重复修复任务。
>
> **V7.7.22 FE-012 已集成接线**：[`engineering-lab-publication-context.js`](../shared/js/engineering-lab-publication-context.js) 限定 student、当前 class 与唯一 physics course，先读既有 `/units`；exact open / locked 直接沿用列表，多匹配失败关闭且不探针，只有零匹配才用同一 `AbortSignal` 请求 BE-011 `/unit-access`。响应严格只接受二字段 DTO 与 hidden / locked / missing allowlist；hidden / locked 静默归一，missing / ordinary unknown 使用不含 slug 或异常正文的单参数固定 warning，畸形、未知或缺少 `course_unit_id` 的 available 结果不启动 owner。known / unknown pending 各持有独立 controller；切换、close / reset / leave 由 `_cancelPublicationGate()` 同步 abort 后再清 gate，context 把外部 signal 桥接到 prepare 与 `/courses → /units → /unit-access` 单一内部 scope。通过 pending 去重后新建 unknown 分类会取得新 transition generation；迟到回调先匹配 exact controller，再由 generation 隔离忽略取消的结果；动态 ABA 合同证明 reset 后同 slug 重开的新 pending 不会被旧 promise 删除或归一。[`physics.js`](../pages/physics/physics.js) 保持唯一 Canvas writer，[`physics-zoom.js`](../pages/physics/physics-zoom.js) 复用既有 resize owner，在 Zoom 打开态按 original parent 当前宽度与 DPR 同步 CSS、backing store、`W/H`、即时 render 和缩放基准，不新增全局监听。专业组提交 `5db1112e` 经三轮独立复审 P0—P3=0 后已进入主开发；真实 QA-014 A03a 仍待执行，不能写成完整 FE-012 或 A03a 已完成。
>
> **V7.7.27 FE-012 四项收束实现**：[`module-selector.js`](../shared/js/module-selector.js) 将 student publication gate 扩到 registry + DOM 已知的全部稳定 Physics module；open 才初始化 owner，listed / backend hidden 或 locked 均静默归一 `#physics`，known missing 与普通 unknown 仅保留固定无 slug 诊断，teacher / admin / 非 Physics 路由不变。[`physics.js`](../pages/physics/physics.js) 与 [`physics.css`](../pages/physics/physics.css) 按冻结课程契约实现 `e=0.40 / 0.80` 固定落高对照、预测、两次第一峰值测量、等价表、分层反馈、修正和解释移交；自由滑块 / 发射 / 暂停 / 清空 / 重看 / Zoom 不写 evidence，客户端不写 completed。课程 owner 重开会把课程 DOM、球体、暂停 / 拖拽和受控状态一起复位且不增加 evidence；受控运行遇逻辑 Canvas resize 会取消当前轨迹、保持零 attempted 并开放重试，超过固定落高的测量直接拒绝。[`learning-evidence-activity.js`](../shared/js/learning-evidence-activity.js) 使用 recovery 中当前活动合法 `first_started_at` 跳过同 scope 重复 started，fresh scope 与 identity / authority replacement 各只写一次。[`experiment-guide.js`](../shared/js/experiment-guide.js) 在 document capture 层持有首个 Escape，遵守 `Zoom > Guide > export/dock/module` 并对称释放、返焦。对应动态合同为 [`physics-mechanics-evidence-contract.cjs`](../tools/tests/physics-mechanics-evidence-contract.cjs)、[`learning-evidence-v7710-contract.cjs`](../tools/tests/learning-evidence-v7710-contract.cjs)、[`module-switch-lifecycle-contract.cjs`](../tools/tests/module-switch-lifecycle-contract.cjs) 与 [`learning-evidence-a03a-contract.cjs`](../tools/tests/learning-evidence-a03a-contract.cjs)；当前只表示实现与合同通过，真实 9001 / SQLite / 外部浏览器仍待独立 QA，不能记为 A03a PASS。

> **V7.7.30 FE-012 Zoom 跨断点候选**：[`physics.js`](../pages/physics/physics.js) 复用既有原父容器 ResizeObserver；同一 Canvas 被 Zoom 持有时，观察回调以 Canvas + parent 双 identity 调用 [`physics-zoom.js`](../pages/physics/physics-zoom.js) 的窄 `syncOriginalParentResize()`，再由 Zoom 协调唯一 `PhysicsSim.resizeForZoom()` writer 更新 CSS、backing store、DPR、`W/H` 和即时 render。自动 base scale 为 `min(1, sx, sy)`，保留下缩、禁止过渡态低分辨率位图自动放大，主动 pinch 语义不变；没有新增 Observer、RAF、window / document listener 或 Canvas owner。[`learning-evidence-a03a-contract.cjs`](../tools/tests/learning-evidence-a03a-contract.cjs) 模拟 390px 打开、window resize 先采样 863px、原父容器随后在无第二次 window resize 时稳定到 943px，断言最终 943×528.08 逻辑尺寸、943×528 DPR1 位图、缩放基准、关闭重开 / 直接桌面等价及完整释放。该候选仍须独立复审与真实浏览器续验，不能提前关闭 A03a。

---

## 0. 先读这几条

1. 主站是单页应用，入口只有 [`index.html`](../index.html)。所有主站页面的 DOM 骨架都在这个文件里，用 `section#page-xxx` 区分页面。
2. 主站页面身份与普通页面脚本在 [`shared/js/page-registry.js`](../shared/js/page-registry.js) 登记，路由消费端在 [`shared/js/router.js`](../shared/js/router.js)；工科试验室五大学科页的实验脚本仍由 [`shared/js/module-selector.js`](../shared/js/module-selector.js) 按 `data-module` 懒加载。
3. 工科试验室五大学科页不是“一个实验一个 HTML”。页面骨架在 `index.html`，实验入口由 `data-module="<实验ID>"` 标记，实验逻辑在 `pages/<学科>/<实验>.js`，公共视觉通常在 `pages/<学科>/<学科>.css`。
4. 代码空间是独立子站，入口是 [`codevis/index.html`](../codevis/index.html)，不要和主站的 `shared/`、`pages/` 混用。
5. 如果当前 URL 像 `#physics/energy-conservation`：先找 `section#page-physics`，再找 `data-module="energy-conservation"`，再改 [`pages/physics/energy-conservation.js`](../pages/physics/energy-conservation.js) 和 [`pages/physics/physics.css`](../pages/physics/physics.css)。

---

## 1. 全局入口与运行时

| 层级 | 负责内容 | 主要文件 |
| --- | --- | --- |
| 主站 HTML 外壳 | 顶栏、加载屏、全部 `section#page-*`、页脚、全局弹层 | [`index.html`](../index.html) |
| 主站启动 | 初始化 Router、加载核心功能、Service Worker / HTTP cache fallback | [`shared/js/main.js`](../shared/js/main.js) |
| 页面注册表 | 19 个主站页面的 galaxy、course/frontier 分组、角色/样式/懒加载脚本和页面级 ready/enter/leave 钩子 | [`shared/js/page-registry.js`](../shared/js/page-registry.js) |
| 主站路由 | hash 解析、页面切换、按星系加载支持脚本、页面进入/离开清理 | [`shared/js/router.js`](../shared/js/router.js) |
| 主站配置 | 星系配置、页面元数据、实验卡片数据、学习设计元数据 | [`shared/js/config.js`](../shared/js/config.js) |
| 统一 API client | cookie-only、严格 API origin、no-store、超时/错误分类、写入结果未知语义，以及历史 token/学习行为 storage 清理 | [`shared/js/api-client.js`](../shared/js/api-client.js) |
| 应用会话与星序角色外壳 | Router 前会话确认、统一登录/注册/重置入口、角色能力矩阵与资源裁剪；显式登录/注册在建立 Session 前先完成学习证据 authority 清理。app-session 把自身 `v` 传给 loader，loader 继续把同一代际传给 queue/client/status/catalog/activity；loader 以固定 transient marker 加非敏感持久 epoch 保护跨重载状态，BroadcastChannel / storage set 即时撤销活动标签，`pagehide` 释放通道、`pageshow` 比较 epoch 补回错过的清理；持久化或物理清理失败时保持锁定、不重载并提供重试 | [`shared/js/app-session.js`](../shared/js/app-session.js), [`shared/css/app-session.css`](../shared/css/app-session.css), [`shared/js/learning-evidence-loader.js`](../shared/js/learning-evidence-loader.js) |
| V7.6 产品能力与前端边界 | 不可变能力账本与机器门禁；V7.7.2 由共享 loader / client / queue/status、稳定活动 catalog 和角色 owner 消费首批能力，页面不得自行调用学习证据 API | [`shared/js/product-capabilities.js`](../shared/js/product-capabilities.js), [`shared/js/learning-evidence-loader.js`](../shared/js/learning-evidence-loader.js), [`shared/js/learning-evidence-client.js`](../shared/js/learning-evidence-client.js), [`shared/js/learning-evidence-queue.js`](../shared/js/learning-evidence-queue.js), [`shared/js/learning-activity-catalog.js`](../shared/js/learning-activity-catalog.js), [`tools/architecture/v76-module-boundaries.json`](../tools/architecture/v76-module-boundaries.json) |
| 学习证据页面 owner 与状态 | 工科发布上下文、按班级 / 课程 / 活动隔离的代表活动控制器、学生 recovery 主刷新、教师约 4 秒发起 progress + aggregate 轮询、逐学生证据筛选 / 分页 / 纠错、发布预览 dialog、星序身份配置竞态回读，以及 pending / failed / conflict / 动态资源失败重试状态。工科 publication provider 的 `prepare` 按 identity 单飞，`resolve` 按 identity/class/activity/exact hash/authority generation 单飞；每 caller 独立取消，最后 waiter 才 abort 底层，identity/class/close/switch 变化整体失效旧 flight。同路由迟到 hashchange 只有在当前 hash 真与 flight 创建时 exact route 不同才取消，真实换实验/离页仍严格 abort。教师 owner 同时识别 scope key 与同 scope 挂接双向边沿：挂接恢复合并为一组读取，失去挂接则零新增请求并清空旧私有状态；切 scope 同步 abort 并清空旧证据、源码与对话框。dialog 打开时暂停普通轮询，但失挂接仍优先 fail-closed；关闭 / 销毁释放 capture listener 并返焦 | [`engineering-lab-publication-context.js`](../shared/js/engineering-lab-publication-context.js), [`learning-evidence-activity.js`](../shared/js/learning-evidence-activity.js), [`role-home-client.js`](../shared/js/role-home-client.js), [`student-learning-evidence.js`](../shared/js/student-learning-evidence.js), [`teacher-learning-evidence.js`](../shared/js/teacher-learning-evidence.js), [`learning-evidence-status.js`](../shared/js/learning-evidence-status.js), [`learning-evidence.css`](../shared/css/learning-evidence.css) |
| 第一方账号组件 | 三角色登录/退出、学生与教师固定角色注册、管理员公开注册边界、密码重置、活动会话与错角色切换；只消费 HttpOnly Cookie | [`shared/js/auth-ui.js`](../shared/js/auth-ui.js), [`shared/css/auth-ui.css`](../shared/css/auth-ui.css) |
| 三角色业务证明 | 仅对一次性本地数据库、通过正式 API/bootstrap 建立夹具；覆盖教师建课、学生提交、教师批改、管理员首页任务、加入审批四类回读、六领域真实业务总览、课程状态 1/2/1 结构回执、精确审计筛选、越权 403、写入歧义、390×844 锁课程重入/关闭/键盘重开/返焦，以及 1015×898；要求显式隔离环境确认 | [`tools/browser/role-workflows-proof.cjs`](../tools/browser/role-workflows-proof.cjs) |
| 实验选择器 | 工科五大学科的画廊、侧栏、实验统计、深链 `#subject/module`、实验 JS 懒加载、经验证 owner 的模块级精确 cleanup、generation 防迟到任务，以及 evidence owner 与实验 owner 同事务释放；sidebar / ledger / gallery / sources 在 student catalogue 变化时按当前 exact-open 集合原子替换，重复 init 与全局 listener 保持单例，身份替换 / 撤权先 close 旧 module。学生 `physics.mechanics` 先通过工科发布门禁，open 才初始化 owner；canonical 冷加载若仅遇到 `publication_context_unavailable` 暂态，会只读观察 publication provider 的 `class_id/classes/identity_id/authority_generation`，同身份、同班级和同代际连续稳定 120ms 后复用同一 controller 一次。40ms 采样与 2.5 秒严格上限受精确 hash、transition generation、AbortSignal、catalogue allowlist 和 route/reset 围栏约束；等待期间不调用 `prepare/resolve/controller.ready`，换页、换身份、hidden/locked、永久 warming 仍失败关闭且不重复 `started`。学生冷启动的合法未知物理 slug 仍只在精确门禁下分类；切换 / 关闭 / reset 会清除 pending 门禁和恢复监听 | [`shared/js/module-selector.js`](../shared/js/module-selector.js) |
| 后端 schema 试点 | `?backendSchema=1` 时插入学习任务卡，严格消费 embed/template/document contract；iframe 只允许首次加载，后续导航 fail closed 并恢复 static fallback | [`shared/js/backend-content.js`](../shared/js/backend-content.js), [`shared/css/backend-content.css`](../shared/css/backend-content.css) |
| 管理端治理页 | `#admin` 六领域治理；orchestrator 承载角色门禁、组织/人员/班级/审计与加入审批，治理总览先读真实待审/停用/归档组织/课程分布/业务审计；课程 owner 承载 active-read 失效、可信 2xx checkpoint、全课程写门禁、当前锁只读重开及精确审计确认；secondary owner 承载总览 generation、按进入加载、切回重绘与 API Base authority reset；活动会话操作由本页 CSS 限定为至少 44px，不改变共享认证/撤销实现；不开放任意表/SQL | [`pages/admin/admin.js`](../pages/admin/admin.js), [`pages/admin/admin-course-governance.js`](../pages/admin/admin-course-governance.js), [`pages/admin/admin-secondary-governance.js`](../pages/admin/admin-secondary-governance.js), [`pages/admin/admin.css`](../pages/admin/admin.css) |
| 教师端工作台 | `#teacher` 星序内教师门禁；教学总览、课程节奏、批改与学情为三个主入口，组织与课程、作业发布是二级流程。首屏 focus stage 以真实当前班课、开放单元、发布计划和待处理提交呈现“本节目标—预计产出—唯一下一教学动作—同步回执”，不把预计产出冒充学生已达成事实。课程节奏承载发布计划、学生 × 分块进度、安全证据和代码审阅；pending/code/source/attempt 以同一 `class + course + generation` 校验请求、父级和连续分页，异常整页拒绝，切课迟到响应不得写入；写入不重试并按同范围权威对账。刷新 busy 时稳定根容器设置 `aria-busy=true`，结束或失败后移除，不改变既有控件 owner | [`pages/teacher/teacher.js`](../pages/teacher/teacher.js), [`pages/teacher/teacher-foundation.css`](../pages/teacher/teacher-foundation.css), [`pages/teacher/teacher-workbench.css`](../pages/teacher/teacher-workbench.css), [`pages/teacher/teacher-curriculum.css`](../pages/teacher/teacher-curriculum.css), [`shared/js/teacher-learning-evidence.js`](../shared/js/teacher-learning-evidence.js) |
| 学生端学习页 | `#student[/<subject>]` 本人多班级、授权课程、进度 / 比例 / 分数 / 作业 / 反馈 / 积分与内存笔记；首屏 focus stage 以真实当前班课、开放单元/作业和权威恢复进度呈现“本节目标—预计产出—唯一下一动作—完成回执”，不声明未经服务端证明的掌握或完成。零班级被 Router 与角色首页硬门禁阻止，课程单元只信任 published + exact-open 权威范围。Future 的无 `content_slug` 单元只有在当前 course、class directory、activity catalog 与 `allowsActivity` 全部精确匹配时才用 `recoveryHref` 生成真实入口；刷新解析期设置 `aria-busy` 并显示明确 loading，不渲染旧 scope 链接或伪空态 | [`pages/student/student-workbench.js`](../pages/student/student-workbench.js), [`pages/student/student.css`](../pages/student/student.css), [`pages/planets/planets.js`](../pages/planets/planets.js), [`shared/js/engineering-lab-publication-context.js`](../shared/js/engineering-lab-publication-context.js), [`shared/js/frontier-publication-context.js`](../shared/js/frontier-publication-context.js) |
| Service Worker | 静态离线缓存；V8.0.34 的 cache generation 与 app-session/experiment-registry/page-registry/router/main APP_SHELL 使用统一 V834 代际，main 以同代际 URL 注册；app-session 与 loader 把该查询值继续传播到学习证据子资源及 publication provider。角色页继续 network-only，学生/教师使用 V832；ModuleSelector 保持已验 V825；Physics 使用 V829，Future/Engineering 使用 V833。精确 `/api` 与 `/api/*` 完全旁路 | [`sw.js`](../sw.js) |
| 本地静态服务 | 开发预览静态站 | [`server/dev-static-server.mjs`](../server/dev-static-server.mjs) |

主站全局样式加载顺序大致是：

| 类别 | 文件 |
| --- | --- |
| 设计变量与基础层 | [`shared/css/tokens.css`](../shared/css/tokens.css), [`shared/css/base.css`](../shared/css/base.css), [`shared/css/typography.css`](../shared/css/typography.css) |
| 导航与布局 | [`shared/css/navbar.css`](../shared/css/navbar.css), [`shared/css/page-layout.css`](../shared/css/page-layout.css) |
| 工科实验公共 UI | [`shared/css/module-selector.css`](../shared/css/module-selector.css), [`shared/css/experiment-guide.css`](../shared/css/experiment-guide.css), [`shared/css/experiment-export.css`](../shared/css/experiment-export.css), [`shared/css/experiment-quiz.css`](../shared/css/experiment-quiz.css), [`shared/css/experiment-favorites.css`](../shared/css/experiment-favorites.css), [`shared/css/experiment-rating.css`](../shared/css/experiment-rating.css), [`shared/css/experiment-polish.css`](../shared/css/experiment-polish.css) |
| 全局工具 | [`shared/css/global-search.css`](../shared/css/global-search.css), [`shared/css/related-experiments.css`](../shared/css/related-experiments.css), [`shared/css/back-to-top.css`](../shared/css/back-to-top.css), [`shared/css/fab-trigger.css`](../shared/css/fab-trigger.css) |
| 账号与响应式兜底 | [`shared/css/auth-ui.css`](../shared/css/auth-ui.css), [`shared/css/app-session.css`](../shared/css/app-session.css), [`shared/css/responsive.css`](../shared/css/responsive.css)，`responsive.css` 最后加载 |

---

## 2. 主站页面索引

### 2.1 星序总览、角色工作区与工科试验室

| URL / hash | 页面 | DOM 位置 | 页面 JS | 页面 CSS | 备注 |
| --- | --- | --- | --- | --- | --- |
| `#planets` | 星序总览 / 授权资源索引 | `index.html` 的 `section#page-planets` | [`pages/planets/planets.js`](../pages/planets/planets.js) | [`pages/planets/planets.css`](../pages/planets/planets.css) | 非学生仍可发现三星系；student 只看到本人各班 published + exact-open 课程，类目标题直达首个可用课程；零班级时隐藏个人页入口并由不可取消 dialog 要求正式入班 |
| `#home` | 工科试验室首页 | `section#page-home` | [`pages/home/home.js`](../pages/home/home.js) | [`pages/home/home.css`](../pages/home/home.css) | 星空、HUD、卫星系统、首页动效；活动态固定视口并内部裁剪 |
| `#mathematics` | 数学学科页 | `section#page-mathematics` | 实验由 `module-selector` 懒加载 | [`pages/mathematics/mathematics.css`](../pages/mathematics/mathematics.css) | 20 个实验模块 |
| `#physics` | 物理学科页 | `section#page-physics` | 实验由 `module-selector` 懒加载 | [`pages/physics/physics.css`](../pages/physics/physics.css) | 20 个实验模块；学生访问任一 registry + DOM 已知的稳定 Physics module 都先过权威发布门禁，open 才加载 owner。`physics.mechanics` 以固定 `H=200/g=980/r=16/vx=0/damping=0` 只比较 `e=0.40→0.80`，真实 loop 产生下降、首次碰撞、回弹上升与第一峰值，测量表和双峰对照只在 authority 确认后推进；自由探索不计课程证据。`/units` 精确零匹配时调用最小 `/unit-access`；hidden / locked 静默归一、missing / ordinary unknown 使用固定安全诊断，所有拒绝继续零 owner。核心操作命中盒至少 44px，单列断点真实收缩；Zoom、ResizeObserver 与 Canvas writer 边界不变 |
| `#chemistry` | 化学学科页 | `section#page-chemistry` | 实验由 `module-selector` 懒加载 | [`pages/chemistry/chemistry.css`](../pages/chemistry/chemistry.css) | 17 个实验模块 |
| `#algorithms` | 算法学科页 | `section#page-algorithms` | 实验由 `module-selector` 懒加载 | [`pages/algorithms/algorithms.css`](../pages/algorithms/algorithms.css) | 12 个实验模块 |
| `#biology` | 生物学科页 | `section#page-biology` | 实验由 `module-selector` 懒加载 | [`pages/biology/biology.css`](../pages/biology/biology.css) | 19 个实验模块；额外增强脚本 [`pages/biology/biology.js`](../pages/biology/biology.js)、[`pages/biology/biology-zoom.js`](../pages/biology/biology-zoom.js) |
| `#student[/<subject>]` | 星序学生学习工作台 | `section#page-student[data-astra-workspace="student"]` | [`pages/student/student-workbench.js`](../pages/student/student-workbench.js) | [`pages/student/student.css`](../pages/student/student.css) | `galaxy=astra`；仅已加入至少一个班级的 student 可进入；首屏按真实发布态、作业和当前班课形成唯一“继续学习/开始学习/处理作业”主动作及进度回执，保留星序侧栏、授权课程、作业、反馈、积分与临时笔记；旧 `student.js` 不加载 |
| `#teacher` | 星序教师教学工作台 | `section#page-teacher[data-astra-workspace="teacher"]` | [`pages/teacher/teacher.js`](../pages/teacher/teacher.js) | [`teacher-foundation.css`](../pages/teacher/teacher-foundation.css) → [`teacher-workbench.css`](../pages/teacher/teacher-workbench.css) → [`teacher-curriculum.css`](../pages/teacher/teacher-curriculum.css) | `galaxy=astra`；teacher/admin 可见/可加载；首屏用当前班课、发布计划、待批改页和单元状态形成唯一下一教学动作，三主入口 + 二级流程不变。课程敏感读取固定 `class + course + generation` 并在 state/DOM 前拒绝异范围或不连续分页；progress / aggregate / events 不从客户端 ledger 推算完成 |
| `#admin` | 星序全局治理 | `section#page-admin[data-astra-workspace="admin"]` | [`pages/admin/admin.js`](../pages/admin/admin.js) → 权威 admin 后动态加载两个窄 owner | [`pages/admin/admin.css`](../pages/admin/admin.css) | `galaxy=astra`；仅 admin 可见/可加载；六领域为 `overview/organizations/identity/classes/courses/audit`，普通领域按进入读取，高级诊断首次进入前零预取 |
| `#license` | 开源协议页 | `section#page-license` | [`pages/about/about.js`](../pages/about/about.js) | [`pages/about/about.css`](../pages/about/about.css) | 渲染 LICENSE 内容 |
| `#changelog` | 更新日志页 | `section#page-changelog` | [`pages/about/about.js`](../pages/about/about.js) | [`pages/about/about.css`](../pages/about/about.css) | 渲染更新日志内容 |

页面层级判断以 `AstraPageRegistry.galaxyFor(page)` 为准：三个角色页面属于 `astra`，工科 navbar、工科页脚、`coursePages`、ModuleSelector 和实验工具不得在这些页面启用。`index.html` 中三个角色骨架可以共享 `.astra-workspace-*` 结构，但角色权限仍只由 `app-session.js` 的 `ROLE_PAGE_ACCESS` 和后端 API 授权决定。

三角色资源矩阵由 `AstraPageRegistry` 唯一登记：

| 权威角色 | 登录后允许加载的角色样式/脚本 | 禁止预热或缓存 |
| --- | --- | --- |
| student | `student.css`、通过零班级门禁后进入 `#student` 时的 `student-workbench.js` | teacher、admin 全部资源；冻结 legacy `student.js` 不加载 |
| teacher | `teacher-foundation.css` → `teacher-workbench.css` → `teacher-curriculum.css`；进入 `#teacher` 时的 `teacher.js` | student、admin 全部资源 |
| admin | 按上述顺序复用教师三层样式，再加载 `admin.css`；Router 先加载 `admin.js`，由它再次确认 `/api/users/me` 为 admin 后才动态加载 `admin-course-governance.js`、`admin-secondary-governance.js` | student 全部资源；两个 admin owner 对匿名/student/teacher 禁止预热或缓存 |

样式必须在会话确认后、应用解锁前完成；页面脚本仍由 Router 在角色守卫通过后懒加载。退出、运行中 401 与跨角色重新认证必须整页重载，因为从 DOM 删除 `<link>` 不能卸载已经执行的 JavaScript。静态文件可被公开 URL 请求并不代表获得业务权限，后端仍需对每个 API 做角色与作用域授权。

### 2.2 未来星系

未来星系页面仍登记在主站 `index.html`，7 个 section 都只保留 `.frontier-runtime-mount`。课程清单与样式集中在 [`pages/frontier/frontier-manifest.js`](../pages/frontier/frontier-manifest.js) 和 [`pages/frontier/frontier.css`](../pages/frontier/frontier.css)；[`shared/js/frontier-publication-context.js`](../shared/js/frontier-publication-context.js) 在 Router 前消费 Cookie Session、班级和课程映射，为目录与直达子课配置权威发布状态；[`shared/js/frontier-learning.js`](../shared/js/frontier-learning.js) 只在当前路由创建课程 DOM、按需 Canvas/Three owner，并由统一 destroy 释放。

| URL / hash | 页面 | DOM 位置 | 页面 JS | 页面 CSS | 备注 |
| --- | --- | --- | --- | --- | --- |
| `#frontier` | 六方向单视口星图 | `section#page-frontier > [data-frontier-runtime-mount="frontier"]` | `frontier-manifest.js` + `frontier-learning.js` | `pages/frontier/frontier.css` | 只负责发现与直达当前 open 方向；六个链接保持 DOM / 键盘语义，主页无页面纵向滚动并响应减少动效；方向 / 子课仍沿用现行 owner，不声称 FE-017 已完成 |
| `#cosmos/<route_slug>` | 地球与宇宙子课 | `section#page-cosmos` 最小挂载点 | `frontier-learning.js`；Canvas owner `pages/cosmos/earth-sun.js`；轨道代表课动态导入 Three | 同上 | `day-season/orbital-scale/evidence-log`；无 WebGL/减少动效可完成降级 |
| `#engineering/<route_slug>` | 工程系统子课 | `section#page-engineering` 最小挂载点 | `frontier-learning.js` + 按需 `pages/engineering/bridge-truss.js` | 同上 | `load-path/member-choice/safety-check`；仅 `load-path` 是 Future representative。预测前只显示中性 15 杆与固定 60 kN 工况；B→C 的载荷、反力与杆力用 exact solve 副本插值，B/C/D 表格、差值摘要和判断—纠正步骤轨仍以最终精确结果为证据；controller、RAF 和 Canvas writer 进入同一 runtime cleanup 栈 |
| `#datascience/<route_slug>` | 数据科学子课 | `section#page-datascience` 最小挂载点 | `frontier-learning.js` + 按需 `pages/datascience/linear-regression.js` | 同上 | `model-fit/outlier-test/evidence-claim` |
| `#infotech/<route_slug>` | 信息技术子课 | `section#page-infotech` 最小挂载点 | `frontier-learning.js` + 按需 `pages/infotech/network-layers.js` | 同上 | `packet-route/layer-contract/fault-trace` |
| `#materials/<route_slug>` | 材料微观子课 | `section#page-materials` 最小挂载点 | `frontier-learning.js` + 按需 `pages/materials/materials-lab.js` | 同上 | `grain-boundary/defect-path/process-window` |
| `#humanities/<route_slug>` | 人文与未来子课 | `section#page-humanities` 最小挂载点 | `frontier-learning.js` + 按需 `pages/humanities/text-lab.js` | 同上 | `context-map/voice-shift/claim-review` |

未来星系共享资产：生成主视觉为 [`UI/future-galaxy/orbit-observatory.webp`](../UI/future-galaxy/orbit-observatory.webp)；Three 来源、MIT 许可、npm integrity 与 SHA-256 在 [`shared/vendor/three-r185/SOURCE.md`](../shared/vendor/three-r185/SOURCE.md)。`three.module.js` 显式依赖同目录 `three.core.js`，两者必须作为一组锁定，禁止 CDN 或单文件复制。

`engineering.load-path` 的刷新恢复只消费共享 client 导出的同 identity/authority/scope、有界且去敏的 pending 记录，并逐事件核对冻结类型、8.6 叶子和顺序；`manual-intervention` 不属于恢复前缀，会使整项活动保持只读。现有 `/me/recovery` 不能重建完整 B→C→D 分组历史，已同步的事件也不会继续留在离线队列，因此无法证明完整链时必须退回较早安全状态。完整跨刷新恢复登记为 ARCH-004 依赖；“服务端状态”只表示现有投影，不代表后端已增加组合顺序或量规校验。外部桌面 / 390×844、网络 / DOM / console 与旧 Service Worker 激活本轮均为 Browser NOT-RUN。

---

## 3. 工科试验室实验模块索引

定位一个实验时按这个顺序查：

1. `shared/js/config.js` 的 `CONFIG.experiments[subject]`：决定画廊卡片标题、描述、图标和排序。
2. `index.html` 中 `data-module="<module-id>"` 的 `content-section`：决定该实验的静态 DOM、按钮、canvas、信息面板。
3. `shared/js/experiment-registry.js`：决定打开实验时加载哪个 JS、调用哪个初始化函数，登记 cleanup 状态，并只为 `verified:true` 条目提供冻结的模块级 cleanup 报告。
4. `shared/js/module-selector.js`：消费注册表，负责按需加载、初始化、模块切换事务、generation 防迟到任务、公共实验工具关闭与唯一整页离开入口。
5. `pages/<subject>/<subject>.css`：大部分实验视觉样式集中在学科 CSS 内；个别实验的局部 class 也在这里。

当前核对结果：`CONFIG.experiments`、`AstraExperimentRegistry` 与 DOM 均覆盖 88 个唯一实验 ID，学科数量为数学/物理/化学/算法/生物 `20/20/17/12/19`；DOM 共 90 个节点，其中 `searching` 使用 3 个 section。注册表 cleanup 状态保持 `65 legacy-callback / verified:false + 23 validated-callback / verified:true + 0 missing`。模块切换与返回画廊只对 23 项已验证 owner 执行精确 cleanup；65 项 legacy 全部 fail closed。物理和生物仍通过 `_pageEnhancementScripts` 追加学科级 Zoom/页面增强脚本，模块转场和整页离开都会先关闭 Zoom；模块 cleanup 成功后删除 init 标记以允许重新 init，失败则保留旧 DOM/hash/state。整页离开由 `cleanupPage()` 兼容处理，并避免与已执行的精确 cleanup 双调用。`sorting` 的 `SortingLab` 负责取消并 settle 排序计时任务，`cell-structure` 的 `CellStructure` 负责 RAF、Observer、DOM/Canvas 监听器和长按 timeout。

### 3.1 数学 `#mathematics`

公共样式：[`pages/mathematics/mathematics.css`](../pages/mathematics/mathematics.css)

| 模块 ID / URL | 实验 | 逻辑文件 | DOM 定位 |
| --- | --- | --- | --- |
| `function-graph` / `#mathematics/function-graph` | 函数图像 | [`pages/mathematics/mathematics.js`](../pages/mathematics/mathematics.js) | `data-module="function-graph"` |
| `calculus` | 微积分 | [`pages/mathematics/calculus.js`](../pages/mathematics/calculus.js) | `data-module="calculus"` |
| `geometry` | 几何变换 | [`pages/mathematics/geometry.js`](../pages/mathematics/geometry.js) | `data-module="geometry"` |
| `complex` | 复数运算 | [`pages/mathematics/complex-numbers.js`](../pages/mathematics/complex-numbers.js) | `data-module="complex"` |
| `trigonometry` | 三角函数 | [`pages/mathematics/trigonometry.js`](../pages/mathematics/trigonometry.js) | `data-module="trigonometry"` |
| `probability` | 概率统计 | [`pages/mathematics/probability.js`](../pages/mathematics/probability.js) | `data-module="probability"` |
| `set-operations` | 集合运算 | [`pages/mathematics/set-operations.js`](../pages/mathematics/set-operations.js) | `data-module="set-operations"` |
| `vector-ops` | 向量运算 | [`pages/mathematics/vector-ops.js`](../pages/mathematics/vector-ops.js) | `data-module="vector-ops"` |
| `inequality` | 不等式 | [`pages/mathematics/inequality.js`](../pages/mathematics/inequality.js) | `data-module="inequality"` |
| `conic-sections` | 圆锥曲线 | [`pages/mathematics/conic-sections.js`](../pages/mathematics/conic-sections.js) | `data-module="conic-sections"` |
| `solid-geometry` | 立体几何 | [`pages/mathematics/solid-geometry.js`](../pages/mathematics/solid-geometry.js) | `data-module="solid-geometry"` |
| `permutation-combination` | 排列组合 | [`pages/mathematics/permutation-combination.js`](../pages/mathematics/permutation-combination.js) | `data-module="permutation-combination"` |
| `sequences` | 数列可视化 | [`pages/mathematics/sequences.js`](../pages/mathematics/sequences.js) | `data-module="sequences"` |
| `function-properties` | 函数性质 | [`pages/mathematics/function-properties.js`](../pages/mathematics/function-properties.js) | `data-module="function-properties"` |
| `exp-log` | 指数与对数 | [`pages/mathematics/exp-log.js`](../pages/mathematics/exp-log.js) | `data-module="exp-log"` |
| `binomial-theorem` | 二项式定理 | [`pages/mathematics/binomial-theorem.js`](../pages/mathematics/binomial-theorem.js) | `data-module="binomial-theorem"` |
| `statistics-regression` | 统计与回归 | [`pages/mathematics/statistics-regression.js`](../pages/mathematics/statistics-regression.js) | `data-module="statistics-regression"` |
| `modeling-numerical` | 建模与数值方法 | [`pages/mathematics/modeling-numerical.js`](../pages/mathematics/modeling-numerical.js) | `data-module="modeling-numerical"` |
| `spatial-vector` | 空间向量 | [`pages/mathematics/spatial-vector.js`](../pages/mathematics/spatial-vector.js) | `data-module="spatial-vector"` |
| `derivative-application` | 导数应用 | [`pages/mathematics/derivative-application.js`](../pages/mathematics/derivative-application.js) | `data-module="derivative-application"` |

### 3.2 物理 `#physics`

公共样式：[`pages/physics/physics.css`](../pages/physics/physics.css)
额外增强：[`pages/physics/physics-zoom.js`](../pages/physics/physics-zoom.js)

| 模块 ID / URL | 实验 | 逻辑文件 | DOM 定位 |
| --- | --- | --- | --- |
| `mechanics` / `#physics/mechanics` | 恢复系数与第一次反弹高度 | [`pages/physics/physics.js`](../pages/physics/physics.js) | `data-module="mechanics"`；学生深链先解析发布态，exact open 且证据 authority 成功绑定后才启用课程控件。P0—P8 固定 `H=200 px、g=980、r=16、vx=0、damping=0`，严格按预测权威确认 → `e=0.40` 完整快照 / `attempted` 权威确认 → `e=0.80` 完整快照 / `attempted` 权威确认 → 比较与模型边界修正 → 结构化解释同一次真实 record 的严格 DTO、终态与一次性回执 → 等待服务端投影推进；`queued/local-pending` 只以原 `client_event_id` 等待同步，不写页面测量或推进，同 ID/type 并发、manual、矛盾/未知终态、错 ID/type/证据/代际、重复或迟到回调均失败关闭。首次授权前即建立 identity fence 与合并 signal；destroy、撤权或 caller abort 会结束 held authorize / transport 的外层命令并释放 owner/UI，迟到授权与结果均保持 stale。初始传输期间的无身份 queue-loss 只锁存、不提前删除 fence；queued 到达后才安全释放并允许原 ID 重试。该等待、取消与回执能力由 Physics 显式 opt-in，Future / CodeVis / generic 保持既有默认记录时序。自由探索、越序 / 重复档位和中断零课程 evidence；owner 重开或无法证明完整历史时复位到安全预测态。移动端一列收缩，Zoom 打开态通过既有父容器 Observer 收敛到稳定宽度，归位继续按当前父宽 / DPR 重测。自动合同已覆盖负向顺序、held authorize / transport、终态回传、回执完整性、队列移除交错与权限竞态；独立 external Edge 已通过桌面 / 390×844 完整 P0→P8、同 ID 队列恢复、SQLite 唯一性和 V804→V805 缓存升级 |
| `gas-laws` | 气体实验定律 | [`pages/physics/gas-laws.js`](../pages/physics/gas-laws.js) | `data-module="gas-laws"` |
| `thermodynamics` | 热力学基础 | [`pages/physics/thermodynamics.js`](../pages/physics/thermodynamics.js) | `data-module="thermodynamics"` |
| `electromagnetism` | 电磁场 | [`pages/physics/electromagnetic.js`](../pages/physics/electromagnetic.js) | `data-module="electromagnetism"` |
| `waves` | 波动演示 | [`pages/physics/waves.js`](../pages/physics/waves.js) | `data-module="waves"` |
| `relativity` | 相对论 | [`pages/physics/relativity.js`](../pages/physics/relativity.js) | `data-module="relativity"` |
| `fluid-dynamics` | 流体力学 | [`pages/physics/fluid-dynamics.js`](../pages/physics/fluid-dynamics.js) | `data-module="fluid-dynamics"` |
| `optics` | 光学 | [`pages/physics/optics.js`](../pages/physics/optics.js) | `data-module="optics"` |
| `kinematics` | 匀变速运动 | [`pages/physics/kinematics.js`](../pages/physics/kinematics.js) | `data-module="kinematics"` |
| `projectile` | 抛体运动 | [`pages/physics/projectile.js`](../pages/physics/projectile.js) | `data-module="projectile"` |
| `circular-motion` | 圆周运动 | [`pages/physics/circular-motion.js`](../pages/physics/circular-motion.js) | `data-module="circular-motion"` |
| `energy-conservation` | 机械能守恒 | [`pages/physics/energy-conservation.js`](../pages/physics/energy-conservation.js) | `data-module="energy-conservation"` |
| `circuit-analysis` | 电路分析 | [`pages/physics/circuit-analysis.js`](../pages/physics/circuit-analysis.js) | `data-module="circuit-analysis"` |
| `em-induction` | 电磁感应 | [`pages/physics/electromagnetic-induction.js`](../pages/physics/electromagnetic-induction.js) | `data-module="em-induction"` |
| `alternating-current` | 交流电 | [`pages/physics/alternating-current.js`](../pages/physics/alternating-current.js) | `data-module="alternating-current"` |
| `gravitation` | 万有引力 | [`pages/physics/gravitation.js`](../pages/physics/gravitation.js) | `data-module="gravitation"` |
| `force-composition` | 力的合成与分解 | [`pages/physics/force-composition.js`](../pages/physics/force-composition.js) | `data-module="force-composition"` |
| `momentum-conservation` | 动量守恒 | [`pages/physics/momentum-conservation.js`](../pages/physics/momentum-conservation.js) | `data-module="momentum-conservation"` |
| `charged-particle` | 带电粒子运动 | [`pages/physics/charged-particle.js`](../pages/physics/charged-particle.js) | `data-module="charged-particle"` |
| `atomic-physics` | 原子物理 | [`pages/physics/atomic-physics.js`](../pages/physics/atomic-physics.js) | `data-module="atomic-physics"` |

### 3.3 化学 `#chemistry`

公共样式：[`pages/chemistry/chemistry.css`](../pages/chemistry/chemistry.css)

| 模块 ID / URL | 实验 | 逻辑文件 | DOM 定位 |
| --- | --- | --- | --- |
| `periodic-table` / `#chemistry/periodic-table` | 元素周期表 | [`pages/chemistry/periodic-table.js`](../pages/chemistry/periodic-table.js) | `data-module="periodic-table"` |
| `molecular-structure` | 分子结构 | [`pages/chemistry/molecular-structure.js`](../pages/chemistry/molecular-structure.js) | `data-module="molecular-structure"` |
| `hybrid-orbitals` | 杂化轨道理论 | [`pages/chemistry/hybrid-orbitals.js`](../pages/chemistry/hybrid-orbitals.js) | `data-module="hybrid-orbitals"` |
| `crystal-structures` | 晶体结构 | [`pages/chemistry/crystal-structures.js`](../pages/chemistry/crystal-structures.js) | `data-module="crystal-structures"` |
| `reactions` | 化学反应 | [`pages/chemistry/chemical-reactions.js`](../pages/chemistry/chemical-reactions.js) | `data-module="reactions"` |
| `chemical-equilibrium` | 化学平衡 | [`pages/chemistry/chemical-equilibrium.js`](../pages/chemistry/chemical-equilibrium.js) | `data-module="chemical-equilibrium"` |
| `electrochemistry` | 电化学 | [`pages/chemistry/electrochemistry.js`](../pages/chemistry/electrochemistry.js) | `data-module="electrochemistry"` |
| `chemical-bond` | 化学键 | [`pages/chemistry/chemical-bond.js`](../pages/chemistry/chemical-bond.js) | `data-module="chemical-bond"` |
| `organic-chemistry` | 有机化学 | [`pages/chemistry/organic-chemistry.js`](../pages/chemistry/organic-chemistry.js) | `data-module="organic-chemistry"` |
| `reaction-rate` | 反应速率 | [`pages/chemistry/reaction-rate.js`](../pages/chemistry/reaction-rate.js) | `data-module="reaction-rate"` |
| `solution-ionization` | 溶液与电离 | [`pages/chemistry/solution-ionization.js`](../pages/chemistry/solution-ionization.js) | `data-module="solution-ionization"` |
| `ionic-reaction` | 离子反应 | [`pages/chemistry/ionic-reaction.js`](../pages/chemistry/ionic-reaction.js) | `data-module="ionic-reaction"` |
| `redox` | 氧化还原 | [`pages/chemistry/redox.js`](../pages/chemistry/redox.js) | `data-module="redox"` |
| `atomic-structure` | 原子结构与电子排布 | [`pages/chemistry/atomic-structure.js`](../pages/chemistry/atomic-structure.js) | `data-module="atomic-structure"` |
| `element-compounds` | 元素化合物 | [`pages/chemistry/element-compounds.js`](../pages/chemistry/element-compounds.js) | `data-module="element-compounds"` |
| `intermolecular-forces` | 分子间力与氢键 | [`pages/chemistry/intermolecular-forces.js`](../pages/chemistry/intermolecular-forces.js) | `data-module="intermolecular-forces"` |
| `experiments` | 虚拟实验 | [`pages/chemistry/virtual-experiments.js`](../pages/chemistry/virtual-experiments.js) | `data-module="experiments"` |

### 3.4 算法 `#algorithms`

公共样式：[`pages/algorithms/algorithms.css`](../pages/algorithms/algorithms.css)

| 模块 ID / URL | 实验 | 逻辑文件 | DOM 定位 |
| --- | --- | --- | --- |
| `sorting` / `#algorithms/sorting` | 排序算法 | [`pages/algorithms/algorithms.js`](../pages/algorithms/algorithms.js) | `data-module="sorting"`；`SortingLab` validated cleanup |
| `searching` | 搜索算法 | [`pages/algorithms/search-algorithms.js`](../pages/algorithms/search-algorithms.js) | `data-module="searching"`，含多个 section |
| `hash-tables` | 哈希表 | [`pages/algorithms/hash-tables.js`](../pages/algorithms/hash-tables.js) | `data-module="hash-tables"` |
| `bst-avl` | 二叉搜索树 / AVL | [`pages/algorithms/bst-avl.js`](../pages/algorithms/bst-avl.js) | `data-module="bst-avl"` |
| `graph` | 图算法 | [`pages/algorithms/graph-algo.js`](../pages/algorithms/graph-algo.js) | `data-module="graph"` |
| `mst-compare` | 最小生成树对比 | [`pages/algorithms/mst-compare.js`](../pages/algorithms/mst-compare.js) | `data-module="mst-compare"` |
| `greedy-scheduling` | 贪心算法 | [`pages/algorithms/greedy-scheduling.js`](../pages/algorithms/greedy-scheduling.js) | `data-module="greedy-scheduling"` |
| `data-structures` | 数据结构 | [`pages/algorithms/data-structures.js`](../pages/algorithms/data-structures.js) | `data-module="data-structures"` |
| `sorting-compare` | 排序对比 | [`pages/algorithms/sorting-compare.js`](../pages/algorithms/sorting-compare.js) | `data-module="sorting-compare"` |
| `recursion-vis` | 递归可视化 | [`pages/algorithms/recursion-vis.js`](../pages/algorithms/recursion-vis.js) | `data-module="recursion-vis"` |
| `dynamic-programming` | 动态规划 | [`pages/algorithms/dynamic-programming.js`](../pages/algorithms/dynamic-programming.js) | `data-module="dynamic-programming"` |
| `string-matching` | 字符串匹配 | [`pages/algorithms/string-matching.js`](../pages/algorithms/string-matching.js) | `data-module="string-matching"` |

### 3.5 生物 `#biology`

公共样式：[`pages/biology/biology.css`](../pages/biology/biology.css)
额外增强：[`pages/biology/biology.js`](../pages/biology/biology.js)、[`pages/biology/biology-zoom.js`](../pages/biology/biology-zoom.js)

| 模块 ID / URL | 实验 | 逻辑文件 | DOM 定位 |
| --- | --- | --- | --- |
| `cell-structure` / `#biology/cell-structure` | 细胞结构 | [`pages/biology/cell-structure.js`](../pages/biology/cell-structure.js) | `data-module="cell-structure"`；`CellStructure` validated cleanup |
| `dna` | DNA 结构 | [`pages/biology/dna-helix.js`](../pages/biology/dna-helix.js) | `data-module="dna"`；`#bio-dna-modes` 挂载于 controls 前 |
| `photosynthesis` | 光合作用 | [`pages/biology/photosynthesis.js`](../pages/biology/photosynthesis.js) | `data-module="photosynthesis"`；`#bio-photo-modes` 挂载于 controls 前 |
| `enzyme-properties` | 酶的特性 | [`pages/biology/enzyme-properties.js`](../pages/biology/enzyme-properties.js) | `data-module="enzyme-properties"` |
| `homeostasis` | 内环境稳态 | [`pages/biology/homeostasis.js`](../pages/biology/homeostasis.js) | `data-module="homeostasis"` |
| `humoral-regulation` | 体液调节 | [`pages/biology/humoral-regulation.js`](../pages/biology/humoral-regulation.js) | `data-module="humoral-regulation"` |
| `genetics` | 遗传学 | [`pages/biology/genetics.js`](../pages/biology/genetics.js) | `data-module="genetics"`；`#bio-genetics-modes` 挂载于 controls 前 |
| `mitosis` | 有丝分裂 | [`pages/biology/mitosis.js`](../pages/biology/mitosis.js) | `data-module="mitosis"` |
| `neural-regulation` | 神经调节 | [`pages/biology/neural-regulation.js`](../pages/biology/neural-regulation.js) | `data-module="neural-regulation"` |
| `immune-system` | 免疫系统 | [`pages/biology/immune-system.js`](../pages/biology/immune-system.js) | `data-module="immune-system"` |
| `population-community` | 种群与群落 | [`pages/biology/population-community.js`](../pages/biology/population-community.js) | `data-module="population-community"` |
| `material-cycles` | 生态系统物质循环 | [`pages/biology/material-cycles.js`](../pages/biology/material-cycles.js) | `data-module="material-cycles"` |
| `ecosystem` | 生态系统 | [`pages/biology/ecosystem.js`](../pages/biology/ecosystem.js) | `data-module="ecosystem"` |
| `meiosis` | 减数分裂 | [`pages/biology/meiosis.js`](../pages/biology/meiosis.js) | `data-module="meiosis"` |
| `gene-expression` | 基因表达 | [`pages/biology/gene-expression.js`](../pages/biology/gene-expression.js) | `data-module="gene-expression"` |
| `gene-engineering` | 基因工程 | [`pages/biology/gene-engineering.js`](../pages/biology/gene-engineering.js) | `data-module="gene-engineering"` |
| `cellular-respiration` | 细胞呼吸 | [`pages/biology/cellular-respiration.js`](../pages/biology/cellular-respiration.js) | `data-module="cellular-respiration"` |
| `substance-transport` | 物质运输 | [`pages/biology/substance-transport.js`](../pages/biology/substance-transport.js) | `data-module="substance-transport"` |
| `gene-mutation` | 基因突变 | [`pages/biology/gene-mutation.js`](../pages/biology/gene-mutation.js) | `data-module="gene-mutation"` |

---

## 4. 代码空间子站索引

代码空间使用独立目录和 `cv-` 命名空间。V7.5.7 以课程目录进入子课和挑战，并在 Cookie Session 学生上下文就绪后执行发布裁剪与正式提交；旧追踪页只作兼容工具。V7.5.9 将 `codevis/index.html` 内 19 个本地 CSS/JS 查询代际统一为 `759r1`，移动品牌和主导航保持不小于 44px；以后修改入口资产必须整组核对，不能只更新一部分。不要改主站 `shared/css/*.css`，除非明确要影响主站。

| URL / hash | 页面 | DOM 位置 | JS | CSS | 备注 |
| --- | --- | --- | --- | --- | --- |
| `/codevis/#catalog` | 代码空间课程目录 | [`codevis/index.html`](../codevis/index.html) 的 `section#cv-page-catalog` | [`codevis/pages/course-catalog/course-catalog.js`](../codevis/pages/course-catalog/course-catalog.js) | [`codevis/pages/course-catalog/course-space.css`](../codevis/pages/course-catalog/course-space.css) | 6 组课程、发布状态与代表挑战 |
| `/codevis/#lesson?activity=<activity_key>` | 当前子课程 | `section#cv-page-lesson` | [`codevis/pages/course-catalog/course-catalog.js`](../codevis/pages/course-catalog/course-catalog.js) | [`codevis/pages/course-catalog/course-space.css`](../codevis/pages/course-catalog/course-space.css) | 参数支持刷新恢复并同步 `CvCourseSession`，不作为授权来源 |
| `/codevis/#challenge?activity=<activity_key>` | 可执行挑战 | `section#cv-page-challenge` | [`codevis/pages/course-challenge/course-challenge.js`](../codevis/pages/course-challenge/course-challenge.js) | [`codevis/pages/course-catalog/course-space.css`](../codevis/pages/course-catalog/course-space.css) | 预测—运行—追踪—修正、公开预检与权威正式提交；本地过程态按权威班级 + 课程 + 活动隔离，切 scope 重挂证据 controller；`control-flow.loop-boundary` 发送最小 predicted / attempted / corrected 事实且不发送源码 |
| `/codevis/#trace` | 兼容代码执行追踪 | `section#cv-page-trace` | [`codevis/pages/code-trace/code-trace.js`](../codevis/pages/code-trace/code-trace.js) | [`codevis/pages/code-trace/code-trace.css`](../codevis/pages/code-trace/code-trace.css) | 保留编辑、播放、变量/数组面板，不再是默认入口 |

代码空间共享运行时：

| 层级 | 文件 |
| --- | --- |
| 子站路由 | [`codevis/shared/js/router.js`](../codevis/shared/js/router.js) |
| 子站启动与路由注册 | [`codevis/shared/js/main.js`](../codevis/shared/js/main.js) |
| 6 组/18 活动稳定清单与发布状态 adapter | [`codevis/shared/js/course-manifest.js`](../codevis/shared/js/course-manifest.js) |
| Cookie Session、班级 / 课程 / 单元与共享证据 scope；401 清理失败进入当前页不可逆终态，禁用选班、解析与活动动作 | [`codevis/shared/js/student-context.js`](../codevis/shared/js/student-context.js) |
| 稳定活动题目解析与正式提交 adapter | [`codevis/shared/js/submission-adapter.js`](../codevis/shared/js/submission-adapter.js) |
| 首批共享学习证据 catalog / loader / client / queue | [`shared/js/learning-activity-catalog.js`](../shared/js/learning-activity-catalog.js)、[`shared/js/learning-evidence-loader.js`](../shared/js/learning-evidence-loader.js)、[`shared/js/learning-evidence-client.js`](../shared/js/learning-evidence-client.js)、[`shared/js/learning-evidence-queue.js`](../shared/js/learning-evidence-queue.js) |
| 子站 runtime 抽象 | [`codevis/shared/js/runtime.js`](../codevis/shared/js/runtime.js) |
| 本地审计运行时按需加载 | [`codevis/shared/js/runtime-loader.js`](../codevis/shared/js/runtime-loader.js)、[`codevis/vendor/manifest.json`](../codevis/vendor/manifest.json) |
| JS runtime | [`codevis/shared/js/runtimes/runtime-js.js`](../codevis/shared/js/runtimes/runtime-js.js) |
| Python runtime | [`codevis/shared/js/runtimes/runtime-py.js`](../codevis/shared/js/runtimes/runtime-py.js) |
| C/C++ runtime | [`codevis/shared/js/runtimes/runtime-cpp.js`](../codevis/shared/js/runtimes/runtime-cpp.js) |
| 子站设计变量与布局 | [`codevis/shared/css/tokens.css`](../codevis/shared/css/tokens.css), [`codevis/shared/css/base.css`](../codevis/shared/css/base.css), [`codevis/shared/css/navbar.css`](../codevis/shared/css/navbar.css), [`codevis/shared/css/layout.css`](../codevis/shared/css/layout.css) |

代码空间运行时只从 [`codevis/vendor/`](../codevis/vendor/) 按需同源加载固定文件，来源、许可证和 SHA-256 见 [`codevis/THIRD_PARTY_NOTICES.md`](../codevis/THIRD_PARTY_NOTICES.md) 与 manifest；禁止恢复 CDN 或分支 URL。C/C++ 运行位于 Worker 并由主线程硬终止，学生页面不显示这些实现细节。

---

## 5. 常见修改目标怎么找

| 目标 | 优先查看 | 说明 |
| --- | --- | --- |
| 改主站顶栏 | [`index.html`](../index.html), [`shared/css/navbar.css`](../shared/css/navbar.css), [`shared/js/router.js`](../shared/js/router.js) | 顶栏显隐和星系切换由 Router 控制 |
| 改主站加载屏 | [`index.html`](../index.html) 内联 loading styles, [`shared/js/main.js`](../shared/js/main.js) | 首屏 loading 大部分写在 HTML 内 |
| 改星序总览与学生授权目录 | [`pages/planets/planets.js`](../pages/planets/planets.js), [`pages/planets/planets.css`](../pages/planets/planets.css), [`shared/js/role-home-client.js`](../shared/js/role-home-client.js) | 非学生静态星系身份与 student 的班级 / published / exact-open 目录在同一 owner 收束；零班级门禁不能只改 CSS |
| 改工科首页视觉 | [`pages/home/home.css`](../pages/home/home.css), [`pages/home/home.js`](../pages/home/home.js), `section#page-home` | 星空、HUD、卫星和眼球追踪在 JS |
| 改学科页画廊卡片 | [`shared/js/module-selector.js`](../shared/js/module-selector.js), [`shared/css/module-selector.css`](../shared/css/module-selector.css), [`shared/js/config.js`](../shared/js/config.js) | 卡片内容来自 `CONFIG.experiments` |
| 改某个实验的按钮/面板布局 | `index.html` 的 `data-module="<id>"` 段 + 对应学科 CSS | 控件通常在 HTML 预置，部分由 JS 动态生成 |
| 改某个实验的 Canvas 绘制 | 对应 `pages/<subject>/<module>.js` | 搜 `init<ModuleName>` 或模块对象名 |
| 改实验帮助、导出、小测、收藏、评分 | `shared/js/experiment-*.js` 与 `shared/css/experiment-*.css`；折叠与导出菜单焦点生命周期另看 [`shared/js/fab-trigger.js`](../shared/js/fab-trigger.js) | 这些是所有工科实验的公共工具；不可见次级操作必须退出 Tab 顺序，展开后精确恢复 |
| 改全局搜索 | [`shared/js/global-search.js`](../shared/js/global-search.js), [`shared/css/global-search.css`](../shared/css/global-search.css) | 搜索浮层 DOM 在 `index.html` 底部 |
| 改相关实验推荐 | [`shared/js/related-experiments.js`](../shared/js/related-experiments.js), [`shared/css/related-experiments.css`](../shared/css/related-experiments.css) | 实验打开后动态注入 |
| 改共享学习证据 | [`shared/js/learning-evidence-loader.js`](../shared/js/learning-evidence-loader.js), [`shared/js/learning-evidence-client.js`](../shared/js/learning-evidence-client.js), [`shared/js/learning-evidence-queue.js`](../shared/js/learning-evidence-queue.js), [`shared/js/learning-activity-catalog.js`](../shared/js/learning-activity-catalog.js), 对应页面 owner | loader 管 authority marker / epoch 与跨标签撤权；client 是唯一 API owner，在线写入受统一 AbortController 约束；页面只发领域命令，恢复只用 catalog 深链，队列保持有界且不含敏感内容 |
| 改未来星系课程 | [`pages/frontier/frontier-manifest.js`](../pages/frontier/frontier-manifest.js)、[`pages/frontier/frontier.css`](../pages/frontier/frontier.css)、[`shared/js/frontier-publication-context.js`](../shared/js/frontier-publication-context.js)、[`shared/js/frontier-learning.js`](../shared/js/frontier-learning.js) | 保持目录—子课、直达发布裁剪、稳定键/route slug 分离、按需 owner 和完整 destroy |
| 改统一 API 请求/错误语义 | [`shared/js/api-client.js`](../shared/js/api-client.js) | 三端和 schema adapter 共用；不得在页面内恢复 direct fetch/Bearer/retry |
| 改后端 schema 学习任务卡 / iframe sandbox | [`shared/js/backend-content.js`](../shared/js/backend-content.js), [`shared/css/backend-content.css`](../shared/css/backend-content.css), [`backend/app/services/content_script_sandbox_templates.py`](../backend/app/services/content_script_sandbox_templates.py) | 仅 opt-in；template/document contract 与静态回退必须同步 |
| 改学生端学习闭环 | [`pages/student/student-workbench.js`](../pages/student/student-workbench.js), [`pages/student/student.css`](../pages/student/student.css), [`pages/planets/planets.js`](../pages/planets/planets.js), `section#page-student` | 本人多班级、授权课程、作业、反馈、积分、学科概览和临时笔记；始终携带班级上下文。旧 `student.js` 是冻结遗留文件，不得重新登记为活动 owner |
| 改教师端工作台 | [`pages/teacher/teacher.js`](../pages/teacher/teacher.js), [`teacher-foundation.css`](../pages/teacher/teacher-foundation.css), [`teacher-workbench.css`](../pages/teacher/teacher-workbench.css), [`teacher-curriculum.css`](../pages/teacher/teacher-curriculum.css), [`shared/js/teacher-learning-evidence.js`](../shared/js/teacher-learning-evidence.js), `section#page-teacher` | 三主入口、二级组织 / 作业流程、发布预览、学生 × 分块进度、安全证据、代码审阅、评分和学情；保持三层 CSS 的注册顺序 |
| 改管理端治理页 | [`pages/admin/admin.js`](../pages/admin/admin.js), [`pages/admin/admin-course-governance.js`](../pages/admin/admin-course-governance.js), [`pages/admin/admin-secondary-governance.js`](../pages/admin/admin-secondary-governance.js), [`pages/admin/admin.css`](../pages/admin/admin.css), `section#page-admin` | orchestrator 只管门禁/六领域/组织与加入审批；课程状态与 active-read/锁恢复改课程 owner；业务总览、内容草稿、更多治理、高级诊断及跨 Base generation 改 secondary owner；保持单次写入、权威对账、诚实空/错态和离页释放 |
| 改代码空间导航/外壳 | [`codevis/index.html`](../codevis/index.html), [`codevis/shared/css/navbar.css`](../codevis/shared/css/navbar.css), [`codevis/shared/js/student-context.js`](../codevis/shared/js/student-context.js), [`codevis/shared/js/submission-adapter.js`](../codevis/shared/js/submission-adapter.js) | 与主站隔离；学生上下文和提交只用 same-origin Cookie |

---

## 6. 新增或重绘页面的登记规则

1. 改页面 DOM：在本文件对应表格补 `section#page-*` 或 `section#cv-page-*`。
2. 改主站页面身份、脚本或页面钩子：同步更新 `shared/js/page-registry.js`；代码空间仍按 `codevis/shared/js/main.js` 的 `CvRouter.register` 维护。
3. 改工科实验模块：同步更新 `shared/js/config.js` 的 `CONFIG.experiments`、`shared/js/experiment-registry.js` 和 `data-module` DOM，并在本文第 3 节登记；禁止恢复 ModuleSelector/Router 私有脚本或 destroy 清单。
4. 新增实验只有完成真实 owner、Zoom/取消资源和切换/离开验证后才能登记 `validated-callback / verified:true` 并进入 `cleanupModule()`；owner 或 Zoom 失败必须阻止转场，legacy callback 不得在模块级执行。新增异步加载、重试、resize、焦点或相关推荐 timer 必须绑定当前 transition generation。
5. 新增页面 CSS：普通主站或代码空间页面确认 `index.html` / `codevis/index.html` 已加载；三角色页面 CSS 只能登记到 `AstraPageRegistry.styles`，不得恢复静态首屏加载。
6. 视觉重绘完成后，至少检查桌面和移动端 hash 直达路径，例如 `#physics/energy-conservation`、`#frontier`、`#cosmos/orbital-scale`、`/codevis/#catalog`；代码空间还要从目录真实进入 `#lesson/#challenge`，未来星系必须分别验证 Three 桌面路径、无 WebGL/减少动效降级和离页资源清理。
7. `#admin`、`#teacher`、`#student` 是星序 `astra` 身份工作区的非课程页：在 `AstraPageRegistry` 登记 `galaxy: 'astra'` 且不加 `course` 标签；角色可见性与越权回落只改 `app-session.js`，不要复用工科 navbar/页脚或课程页的 `[data-module]` / `.content-section` 语义。
8. 三端页面新增或更新静态资源时，只在 `shared/js/page-registry.js` 同步角色矩阵与版本，并确认 `index.html`、`shared/js/main.js`、`sw.js` 没有把角色资源重新放入公共清单；同时复查 hash 直达、越权回落、退出/401/跨角色清理与移动端导航可达性。当前 UI-003 的 admin 资源代际为 `20260729v794AdminGovernanceP0`，教师资源代际为 `20260730v785TeacherNaturalWorkflowP0`；page registry 不登记两个 admin owner，它们只能由通过权威门禁后的 `admin.js` 加载。其他未改资源保持各自既有代际；教师样式必须保持 foundation → workbench → curriculum 顺序，禁止恢复旧 `teacher.css`。
9. 三端和后端 schema 请求统一使用 `AstraApiClient`：只使用 HttpOnly Cookie，禁止 `Authorization`、敏感 localStorage/sessionStorage 和写请求自动重试；401/离线/网络失败必须清空旧业务 DOM，恢复只做权威读取。
10. Service Worker 的 API 判断必须位于 navigation、扩展名和静态 cache 逻辑之前；任何 `/api` 动态响应都不得进入 CacheStorage 或离线 fallback。三角色 CSS/JS 的精确 network-only 判断同样必须先于通用静态 stale-while-revalidate 分支。
11. 学习证据只能由 `shared/js/learning-evidence-client.js` 调用服务端；页面只发稳定领域命令，不得提交 `completed=true`、自行计算权威完成或增加 `LearningProgress.markVisited` 调用者。当前 producer 是 `physics.mechanics`、`control-flow.loop-boundary` 与 Future 唯一代表活动 `engineering.load-path`；其余 Future key 只登记在 catalog，不能据此获得 producer 能力。
12. 离线学习事件只允许进入 `AstraLearningEvidenceQueue` 的有界 IndexedDB：7 天、256 条、1 MiB、单 evidence 16 KiB、单批最多 50 条；不保存账号、凭据、姓名、成绩、源码、自由文本解释或完整答案。退出、401、账号/角色变化立即清除，Service Worker 不参与后台同步。需要敏感字段的动作必须联网完成。
13. 不再新增页面私有资源版本常量。历史版本表只允许删除；资源变更应使用登记 owner 和现有统一代际机制，并保持 `npm test` 的架构门禁通过。
