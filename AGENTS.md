# 星序 Astra — 项目协作约定

## 产品方向

星序是一站式可交互学习平台。Web 实验体验与教学管理后端同等重要：前者承担探索、设计与展示，后者承接课程、协作、评价及可解释的学习结果，也需要支撑毕业设计的架构论证。当前优先打磨 Web 与后端，暂不进入微信小程序设计。

用户已于 2026-09-05 授权审查并重构不合理的前后端设计、重写过时指引、审查清理历史残留。对已授权范围内的可回退改动直接执行，完成后说明原因、结果和验证。旧版本的阶段冻结、文件 allowlist、固定行数和旧模型审批记录不构成永久产品约束。

## 阅读与事实来源

1. 当前用户指令优先；不要从历史文档推断用户仍要求暂停开发。
2. 开工先读 `git status --short`、当前分支和最近提交。提交基线与未提交工作区改动分开描述。
3. 项目定位：[00-项目总纲](doc/00-项目总纲.md)。
4. 当前实现：[01-开发者手册](doc/01-开发者手册.md)，按需进入 11—19 子文档。
5. 当前任务与阶段：[02-更新规划](doc/02-更新规划.md)。
6. 目标架构：[01 第 13 章](doc/01-开发者手册.md#13-目标架构与设计待实现)；完整规则与任务只认 02。71 号保留首轮审查背景，不作为当前执行原件。
7. 发布事实：[03-发布历史](doc/03-发布历史.md)。V4—V8 分卷及旧设计只用于追溯；发生冲突时核对代码，不能用历史约束覆盖当前需求。

核心文档保留现有文件名，不新建同职责副本；本文件是唯一协作规则原件，`.codex/AGENTS.md` 和 Copilot 指引只提供入口。

## 架构与实现

- 保持 FastAPI 为业务权威入口；Node/C++ 静态服务不承担教学业务。
- 正式门户在 `qianduan/`，使用 TypeScript/Vite/Three.js；真实 API 与静态演示适配分开。原三个学习空间的权威注册表和求解/渲染代码保留，构建时打包至 `labs/`；不在旧角色页面继续建立第二套正式门户。
- 区分实验资源、课程内学习单元、课程成员、不可变发布、操作事实与完成评价；不要把同名字段当成同一对象。
- 优先按业务职责拆分模块。前端视图、请求与状态派生，实验求解与渲染，后端 HTTP 适配、用例服务与查询应有明确边界。
- 已有框架和模块可以重构；先用一个完整业务切片证明收益，再决定推广。不因模型更换批量换技术栈。
- 实验可改进，先明确可观察收益、科学模型、交互与回退方式。既有保护检查仍运行；确需改变其规则时说明原因并同步有效回归，不能为变绿而重写哈希或压缩源码。
- 科学求解结果与视觉表现分离。3D 美术资产不替代数值模型；探索过程不等于学习掌握，学习完成只由服务端规则或经授权的评价产生。
- 页面和实验有成对生命周期，初始化幂等；离页释放监听器、定时器、观察器、动画帧和 GPU 资源。支持触控、键盘、容器尺寸变化和减少动态效果。
- 使用现有 Cookie 会话与资源授权；不持久化敏感学习正文或凭据到普通浏览器存储，不缓存业务 API。页面隐藏不替代服务端权限。
- 保持共享草稿的并发冲突处理、发布和批改事务、幂等写入、历史保留及组织/课程权限。结构变更带 Alembic 迁移与适用数据库验证。
- 外部投递、生产发布及学生源码执行只在相应任务范围内启用；不要把本地模拟包装成真实外部能力。

## 工作方式

- 根工作区的 `main` 是完整源码基线，`qianduan` 分支只保存 GitHub Pages 静态演示制品，发布使用 `qianduan/scripts/publish-demo.mjs`，不在制品分支手工改业务代码。默认单线推进；独立开发分支按任务建立，用户明确要求并行时再组织代理或工作树。
- 先登记任务，再修改对应代码与文档。设计建议、已实现行为和历史证据分开写。
- 用户已有修改与当前数据需要保留。清理前核对用途、Git 可达性和实际路径，不删除尚有价值的未合入成果。
- 不把旧失败现场、stash 内容和历史候选自动应用到当前工作区。
- 版本号以已明确安排为准。用户要求 Git 提交而没有安排产品发布号时，按任务做普通开发提交，在 03 单列真实标题、提交和验证；未提交与已提交未发布应分清，不虚构版本。一次提交只处理一个任务。
- 按实际变更选择验证：Python 业务回归、前端契约、实验模型与生命周期、浏览器操作。不要让固定源码行数代替架构判断，也不要删掉能发现真实回归的测试。
- 浏览器任务优先使用当前机器可用的 Browser 插件；需要用户现有 Edge/Chrome 状态时走外部扩展后端，不把没有选中标签页误判为连接失败。
- 汇报说明实际改动、验证结果、未覆盖部分和重要取舍。旧模型的历史工具限制不作为当前环境事实；当前真实的审批/工具拒绝如实说明，不绕过。

## 常用入口

```powershell
powershell -ExecutionPolicy Bypass -File .\astra-local.ps1
npm ci --ignore-scripts
npm test
npm --prefix qianduan test
npm --prefix qianduan run build
npm --prefix qianduan run verify:package
.\.venv\Scripts\python.exe -m pytest backend
git diff --check
```

Node 工具版本取 `.node-version` / `package.json`；新 Python 环境按 `backend/requirements.lock` 安装。本机 SQLite、真实 MySQL、浏览器实测和外部部署是不同验证范围，不能互相替代。只修改文档时以职责、链接和差异检查为主。
